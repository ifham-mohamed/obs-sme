# MVP Feature Tracker

Status legend: 🔲 not started · 🟡 in progress · 🟢 done · 🔴 blocked · ⚪ dropped

## Foundation

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-01 | Monorepo scaffold (`backend/ frontend/ ml/ infra/ docs/tracker/ scripts/`) + root configs | 🟢 | `BUILD_02 §1–§6` | `tree -L 2` matches §1 layout |
| F-02 | Dev infrastructure: `docker-compose.dev.yml` + `infra/postgres/init.sql` | 🟢 | `BUILD_04 §2` | `make up` brings Postgres healthy |
| F-03 | `.gitignore`, `.env.example`, `Makefile`, root `README.md`, pre-commit | 🟢 | `BUILD_01 §6` | `pre-commit install` succeeds |
| F-04 | Tracker bootstrap (`docs/tracker/{README,FEATURES,SESSIONS,CHANGES}.md`) | 🟢 | this plan | All four files exist |

## Backend foundation

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-05 | FastAPI app: `main.py`, `settings.py`, `logging_config.py`, `exceptions.py`, `deps.py` | 🟢 | `BUILD_03 §3–§7` | `GET /health` returns 200 (verified once `make up && make migrate` runs) |
| F-06 | Pydantic v2 settings with `DATABASE_URL` (asyncpg) + `JWT_SECRET` + `CORS_ORIGINS` | 🟢 | `BUILD_03 §3` | `app/settings.py` boots with `.env` |
| F-07 | SQLAlchemy 2.0 async session + `DeclarativeBase` + `TimestampMixin` | 🟢 | `BUILD_04 §3` | `db_session` fixture in `conftest.py` validates round-trip |
| F-08 | ORM models: `User`, `SMEProfile`, `SurveyResponse`, `AuditLog` | 🟢 | `BUILD_04 §5` + `research/06 §4` | `python -m compileall app` clean; models registered in metadata |
| F-09 | Alembic init + initial migration (`202605080001_initial_schema.py`) | 🟢 | `BUILD_04 §4` | `alembic upgrade head` succeeds on fresh DB (verified at runtime) |
| F-10 | Auth: `core/security.py` (bcrypt + HS256 JWT access/refresh) | 🟢 | `BUILD_06 §2` | `tests/unit/test_security.py` round-trips access + refresh |
| F-11 | Auth service + schemas + router (`/auth/register`, `/auth/login`, `/auth/refresh`) | 🟢 | `BUILD_06 §3–§5` | Integration test passes register + login |
| F-12 | RBAC: `get_current_user`, `require_admin`, `require_annotator` deps | 🟢 | `BUILD_03 §6` + `BUILD_06 §6` | Integration test asserts SME → 403 on admin endpoint |
| F-13 | Inbound rate limit on `/auth/login` and `/auth/register` (slowapi) | 🟢 | `BUILD_06 §9` | `core/rate_limit.py` wired in `main.py`; auth router decorated |
| F-14 | `users.py` router: `GET /api/v1/users/me`, admin-only `GET /api/v1/users` | 🟢 | `BUILD_03 §13` | `api/v1/users.py` returns user + profile |
| F-15 | `surveys.py` router: `POST /surveys/{instrument}/submit`, `GET /surveys/{instrument}/responses` (admin) | 🟢 | this plan | Integration test exercises both ends |
| F-16 | Audit-log writes on auth events | 🟢 | `BUILD_06 §10` | `auth_service` writes `auth.register`, `auth.login.success/failure`, `auth.refresh` rows |
| F-17 | Module-router 501 stubs (`regulations`, `qa`, `risk`, `verify`) | 🟢 | this plan | Integration test asserts each returns 501 + `code: not_implemented` |
| F-18 | `seed_dev.py`: admin + annotator + sample SME | 🟢 | `BUILD_04 §8` | `make seed` is idempotent |

## Frontend foundation

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-19 | Next.js 14 app (App Router, TS, Tailwind) | 🟢 | `BUILD_05 §1` | `package.json` + `next.config.mjs` + `app/layout.tsx` present |
| F-20 | shadcn-pattern primitives: button, input, label, card, badge, alert, skeleton, dropdown-menu, select, radio-group, checkbox, table, toaster, textarea | 🟢 | this plan | `components/ui/*.tsx` present and HSL-token aware |
| F-21 | Domain-tuned theme tokens (trust-blue 217 91% 50% + amber 38 92% 50%) for light + dark | 🟢 | this plan + `BUILD_05 §2` | `app/globals.css` defines all shadcn tokens for both themes |
| F-22 | `next-themes` light/dark/system toggle in topbar | 🟢 | this plan | `components/layout/theme-toggle.tsx` mounted in `Topbar` |
| F-23 | Multilingual fonts via `next/font` (Inter + Noto Sans Sinhala + Noto Sans Tamil) | 🟢 | `BUILD_05 §3` | `app/layout.tsx` registers all three font CSS variables |
| F-24 | `next-intl` with `en/si/ta`; locale switcher in topbar | 🟢 | `BUILD_05 §4` | `i18n.ts` + 3 messages files; `LocaleSwitcher` writes `NEXT_LOCALE` cookie |
| F-25 | API client (`lib/api/client.ts`) with `ApiError` + bearer token plumb | 🟢 | `BUILD_05 §5` | `lib/api/{client,auth,surveys,users}.ts` |
| F-26 | `lib/auth/{session,roles}.ts` — server-side `requireUser()` / `requireRole()` | 🟢 | `BUILD_06 §8` | `(app)` and `(admin)` layouts call them |
| F-27 | TanStack Query + theme provider in `components/providers.tsx` | 🟢 | `BUILD_05 §9` | Query client mounted under `ThemeProvider` |

## Auth flow

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-28 | `/login` page (zod + react-hook-form + shadcn) | 🟢 | `BUILD_06 §8` | Posts to backend, calls `/api/auth/establish`, redirects to `next` |
| F-29 | `/register` page (email + password + SME profile fields) | 🟢 | `BUILD_06 §3` | Creates user + profile, then auto-logs-in |
| F-30 | Cookie route handlers: `/api/auth/establish`, `/api/auth/token`, `/api/auth/logout` | 🟢 | `BUILD_06 §8` | HTTP-only cookies set on login, cleared on logout |
| F-31 | Auth-guarded `(app)` layout + admin-guarded `(admin)` layout + middleware fast-path | 🟢 | `BUILD_05 §7` | `middleware.ts` redirects unauth'd users; `requireRole` redirects non-admins |
| F-32 | Topbar with role badge + email + theme + locale + logout | 🟢 | this plan | `components/layout/topbar.tsx` |

## Survey flow

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-33 | Awareness question bank `lib/surveys/awareness.ts` (12 items) | 🟢 | `research/08` + plan | All 6 question kinds covered (single, multi, likert, date, numeric, short_text); April 2026 thresholds (36M / 18%) used in literal references |
| F-34 | Reusable `SurveyForm` + `QuestionRenderer` for all 6 kinds | 🟢 | `BUILD_05 §11` | Each kind validates + serialises to backend `SurveyAnswerIn` |
| F-35 | `/surveys/awareness` page submits to `POST /surveys/awareness/submit` | 🟢 | this plan | On success, router pushes to `/surveys/awareness/thank-you` |
| F-36 | `/surveys/awareness/thank-you` confirmation | 🟢 | this plan | Renders w/ "Back to dashboard" CTA |
| F-37 | Admin response list `/admin/surveys/awareness` (table + total + pagination params) | 🟢 | this plan | shadcn `Table` lists submissions grouped by (sme, submitted_at) |
| F-38 | Admin user list `/admin/users` (read-only, role badges) | 🟢 | this plan | shadcn `Table` shows email + role badge + sector + region |
| F-39 | "Coming soon" stubs for `/regulations`, `/qa`, `/verify`, `/risk` | 🟢 | `BUILD_05 §10` | Each renders `<ComingSoon>` with the relevant `BUILD_NN` reference |

## Smoke tests

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-40 | Backend integration test `test_survey_flow.py` (httpx + Postgres testcontainer) | 🟢 | plan + `BUILD_15 §6` | Two test cases: full register/login/submit/admin-list flow + 501-stub assertions |
| F-41 | Backend unit tests for `core/security` (bcrypt + JWT round-trip) | 🟢 | this plan | `tests/unit/test_security.py` |
| F-42 | Playwright E2E `auth_survey_admin.spec.ts` | 🟢 (script ready) | plan | Runs on `make dev-frontend` + seeded backend; needs runtime verification once `pnpm install` runs |

---

## Runtime-verified items

These features are written but only fully verified once the dev stack runs (`make up && make migrate && make seed && make dev-backend && make dev-frontend`). The plan's "Verification" section §1–§14 covers the runtime smoke; tracker entries reflect "code complete + statically validated" until then.

| Item | Static check passed | Runtime check |
|------|--------------------|---------------|
| `python -m compileall backend/app backend/alembic` | ✅ no syntax errors | — |
| FastAPI app boots | ⚠ requires Postgres running | `make up && make dev-backend` then `curl localhost:8000/health` |
| Alembic migration | ⚠ DB-dependent | `make up && make migrate` |
| Frontend build | ⚠ requires `pnpm install` | `cd frontend && pnpm install && pnpm build` |
| Theme tokens render | ⚠ visual | `pnpm dev` and inspect light/dark via toolbar |
| Sinhala / Tamil rendering | ⚠ visual | Locale switcher in topbar; verify Noto fonts active |
| Playwright E2E | ⚠ live | `pnpm e2e` with both servers up |

## Onboarding documentation

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-43 | `docs/SETUP/` track — onboarding/setup guide. **Now 12 files** (00–10 from session 3 + new `11_Survey_System.md` from session 4). | 🟢 | this plan | All files exist; root `README.md` slimmed to one-screen front door; back-references added in `BUILD_00` and `research/00`; cross-references resolve |

## Survey system + cross-module linkage (M2 Knowledge + M3 Vulnerability slice)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-44 | Backend schema: `regulatory_domains`, `sectors`, `m2_questions`, `m2_knowledge_scores`, `m3_compliance_history`, `m3_behavioural_signals` + `survey_responses` extension columns | 🟢 | this plan + `module_2_and_3 §PART D` | Migration `202605090001` applies cleanly; `python -m compileall` clean |
| F-45 | M2 question bank seed (`seed_m23_questions.py`) — 9 domains + 12 sectors + 40 canonical questions verbatim from `module_2_and_3` PART A | 🟢 | this plan | `make seed` is idempotent; spot-check 3 questions match the architecture doc |
| F-46 | Cross-module-linkage rules (`m2_linkage_rules.py`) — awareness → M2 question selection | 🟢 | this plan | Rule fires on `awareness.v1.q04 = no` forcing `VAT_FACT_002`; documented inline |
| F-47 | M2 scoring engine (`m2_scoring.py`) — mcq, scenario, numeric, ordered_steps with partial-credit, open with element-matching | 🟢 | this plan | `test_m2_scoring.py` covers every format incl. partial-credit + unknown-format fallback |
| F-48 | M2 service: `questions_for_sme`, scoring on submit, `recompute_knowledge_score` cache write | 🟢 | this plan | `test_m2_flow.py` asserts `is_correct` populated and cache row written |
| F-49 | M3 service: history submit, behavioural submit, combined `risk-signals` view (BUILD_00 contract) | 🟢 | this plan | `test_m3_flow.py` asserts the combined payload includes M2 score + M3 history + behaviour |
| F-50 | M2 + M3 routers (`/api/v1/m2/*`, `/api/v1/m3/*`); `SupportedInstrument` extended to `["awareness","knowledge","vulnerability"]` | 🟢 | this plan | OpenAPI lists new endpoints; SME-only endpoints reject other-SME reads with 403 |
| F-51 | Frontend conditional questions: shared `types.ts`, `dependsOn` evaluator, renderer `useWatch`, submit-time stripping | 🟢 | this plan | M3 vulnerability "if-yes-how-many?" follow-up shows/hides correctly; new `ordered_steps` kind renders |
| F-52 | Frontend SME pages: Surveys hub `/regulations`, Knowledge survey + thank-you, Vulnerability survey + thank-you, Risk read-only `/risk` | 🟢 | this plan | Clicking through hub → knowledge → score on thank-you works end-to-end |
| F-53 | Frontend admin pages: M2 question bank with inline Verify, M2 scores, M3 risk-signals | 🟢 | this plan | Admin `/admin/m2/questions` Verify action sets `verified_by` + `verified_at` |
| F-54 | UX polish: sticky `SurveyProgress` (visible-only denominator), `useAutosave` to localStorage every 10s with Resume banner, `SurveyErrorSummary` with anchor links, `RegulationContextCard`, per-module accent (`module-m1/m2/m3`) for light + dark | 🟢 | this plan | Refresh mid-form preserves answers; submit toast shows count; theme tokens swap correctly per module |
| F-55 | i18n parity: en/si/ta all carry the new `surveys.hub`, `surveys.knowledgeTitle`, `risk`, `admin` keys (116 keys each) | 🟢 | this plan | JSON key count matches across all three locales |
| F-56 | Backend integration tests (`test_m2_flow.py`, `test_m3_flow.py`) + unit (`test_m2_scoring.py`) + Playwright (`surveys_knowledge_vulnerability.spec.ts`) | 🟢 | this plan | Unit tests pass on `pytest -q`; integration + E2E pass after `make up && make migrate && make seed` |

## First-runtime hardening (Session 5 — runtime verification + fixes)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-57 | bcrypt pin `>=4.0,<4.1` in `backend/pyproject.toml` — stops the recurring passlib `__about__` AttributeError + 72-byte ValueError on hash. | 🟢 | session 5 (issue surfaced during `make seed`) | `make seed` runs cleanly; `pytest unit/test_security.py` still green |
| F-58 | Defensive 72-byte UTF-8 truncation in `app/core/security.py` — both `hash_password` and `verify_password` now route every secret through `_truncate()` before bcrypt. | 🟢 | session 5 | A 200-character password no longer crashes auth; round-trip unit test green |
| F-59 | `.env.example` `CORS_ORIGINS` ships in JSON-array form (`["http://localhost:3000"]`) with an inline comment. | 🟢 | session 5 (Pydantic Settings v2.1+ JSON-decodes complex types) | Fresh `cp .env.example .env` boots the backend without the `JSONDecodeError: Expecting value` crash |
| F-60 | `docs/SETUP/09_Troubleshooting.md` updated: entry 9 rewritten with the actual root cause + actual fix; new 9b (long unicode passwords), 25b (missing `enigmatrix` Postgres role / Homebrew collision), 25c (CORS JSON-array gotcha). | 🟢 | session 5 | Every error message a contributor hit during first runtime now maps to a fix-by-grep entry |
| F-61 | CORS preflight 400 fix in `app/main.py` — strip trailing slash from every entry before passing to `CORSMiddleware`. Pydantic v2's `AnyHttpUrl.__str__` had been turning `http://localhost:3000` into `http://localhost:3000/`, which never matches a browser `Origin` header. Logs now emit `cors_origins_configured`. Frontend login + register pages also surface the underlying `Error.message` in the on-screen error. Troubleshooting entry #26 documents the symptom → fix. | 🟢 | session 5 | `OPTIONS /api/v1/auth/register` returns 200; registration succeeds |
| F-62 | Auth diagnostics + DB-inspection toolkit. New `make doctor` (`backend/app/scripts/doctor.py`) checks bcrypt version, hash round-trip, and the seeded users-table rows in one shot. New `make db-shell`, `make db-users`, `make reseed-users` Make targets cover the recurring "stale bcrypt hash / wrong DB" failure mode. Troubleshooting entry #27 documents the `Invalid credentials` after a CORS-only fix as the venv-not-resynced symptom. | 🟢 | session 5 | `make doctor` exits 0 on a healthy stack and exits 1 with the named broken check on a sick one |
| F-63 | App-Router parallel-pages collision fix. The admin awareness response list was at `/admin/surveys/awareness`, but Next.js strips route groups from URLs — so both `(admin)/surveys/awareness/page.tsx` and `(app)/surveys/awareness/page.tsx` resolved to `/surveys/awareness` and the dev server returned 500 on every page. Moved the admin page to `/admin/surveys/awareness/responses/page.tsx` (semantically clearer too); updated sidebar, Playwright spec, and four SETUP docs. New troubleshooting entry #22. | 🟢 | session 5 | All routes compile; `/surveys/awareness` is the SME survey, `/admin/surveys/awareness/responses` is the admin list |
| F-64 | `useAutosave` no longer reaches into `useFormContext()` — the call ran before `<FormProvider>` mounted, returning `null` and crashing every survey page with `TypeError: Cannot destructure property 'control' of … as it is null`. The hook now takes a `methods` arg (the `useForm()` return value) so the data dependency is explicit and works irrespective of provider ordering. `SurveyForm` passes `methods` through. New troubleshooting entry #23 documents the symptom + the "pass methods, don't context-fetch" pattern. | 🟢 | session 5 | `/surveys/awareness`, `/surveys/knowledge`, `/surveys/vulnerability` all render without runtime errors; autosave + Resume banner still work |

## Session 6 — Unified survey wizard + admin-managed regulations

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-65 | Unified `survey_questions` schema (rename of `m2_questions` + `module_number`/`linked_regulation_id`/`next_question_rules`/`is_required`/`instrument_section`/`is_branching_root`) + new `m1_regulations` + `m1_regulation_sectors` M2M + `survey_responses.linked_regulation_id` | 🟢 | session 6 | Migration `202605100001` applies; rename preserves the 40 existing M2 rows; FK SET NULL on regulation delete |
| F-66 | Idempotent seed scripts: `seed_regulations` (5 canonical regs), `seed_awareness_questions` (12 M0 with cross-module rules), `seed_vulnerability_questions` (M3 history/behaviour/stress with rule-encoded `dependsOn`); `seed_m23_questions` updated for the new fields incl. cross-module link constants | 🟢 | session 6 | `make seed` → 12 M0 + 40 M2 + ~25 M3 + 5 regulations; reruns are no-ops |
| F-67 | Flow engine (`survey_question_service`): `start_flow`, `next_question`, `_resolve_rule` (predicates `answer_eq` / `answer_in` / `answer_lt` / `answer_gt`); resume by replaying existing `survey_responses` rows — no new progress table needed | 🟢 | session 6 | OQ10 resolved; missing `goto_question_code` falls back to linear next |
| F-68 | M1 regulation service: `list_regulations`, `get_regulation`, `get_regulation_sectors`, `create_regulation`, `update_regulation`, `verify_regulation`. Every mutation writes a row to `audit_log` (`regulation_create` / `regulation_update` / `regulation_verify`). | 🟢 | session 6 | All admin mutations land in `audit_log`; verify flips `expert_verified` + `expert_verified_at` |
| F-69 | Backend routers + schemas: `/api/v1/survey-flow/{start,answer}` (SME) and `/api/v1/m1/regulations/*` (admin CRUD + `/{id}/public` SME-safe read). Mounted in `api/v1/router.py` next to existing m2/m3. New schemas: `RegulationPublicOut`, `RegulationAdminOut`, `RegulationsPage`, `FlowQuestionOut`, `FlowAnswerIn`, `FlowProgressOut`, `FlowStateOut`. | 🟢 | session 6 | OpenAPI lists the new endpoints; SME-side public read returns the 5-field subset only |
| F-70 | M3 projection on `survey_service.submit`: `_project_m3_snapshots` writes `module_number = 3` answers into `m3_compliance_history` / `m3_behavioural_signals` via `M3_HISTORY_FIELDS` + `M3_BEHAVIOURAL_FIELDS` field maps. Single source of truth (`survey_responses`) preserved. | 🟢 | session 6 | OQ11 resolved; `/risk` page works unchanged when answers come in via the unified wizard |
| F-71 | Unified `/surveys` page (server) + `SurveyWizard` client + `flow-question-to-ui.ts` adapter (single/multi/likert/date/numeric/short_text/ordered_steps + `valueToAnswer` helper). One question at a time, sticky progress, regulation-context card inline when `next_question.regulation` present, cross-module hint, module-accent class swap (`module-m0/m2/m3`). New typed clients `SurveyFlowApi` + `RegulationsApi`; `client.ts` extended with `patch` + `delete`. | 🟢 | session 6 | `/surveys` page compiles; "No" on awareness Q4 routes to `VAT_FACT_002`; thank-you renders on `flow_status === "completed"` |
| F-72 | i18n parity: new `surveys.flowTitle / flowSubtitle / questionOfTotal / crossModuleHint` keys added to `en/si/ta`. | 🟢 | session 6 | All three locales carry the new keys; existing `nav.adminRegulations` / `nav.adminQuestions` keys still pending with F-74 |
| F-73 | Follow-up migration `202605100002_relax_survey_question_columns.py` — drops `NOT NULL` from `survey_questions.domain_code` and `knowledge_type` so M0 (awareness) and M3 (vulnerability) rows can insert. The legacy `m2_questions` table created those columns `NOT NULL`; the original `202605100001` only relaxed `correct_answer_json`. | 🟢 | session 6 fix | `make migrate && make seed` runs cleanly post-fix; previously crashed on `M3_HIST_001` insert |

### Pending — Session 7 carry-over

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-74 | **Phase A landed (session 7).** Admin regulations CRUD: `/admin/regulations` list (filters + pagination + inline Verify), `/admin/regulations/new` + `/admin/regulations/[id]/edit` (RegulationForm with EN/SI/TA Tabs, sectors multi, severity 1–5, dates, source URL, live "Preview as SME"), zod validators (`regulationCreateSchema`/`regulationUpdateSchema`), sidebar `nav.adminRegulations`. Phase B (generalised `/admin/questions` UI + branching-rules editor + redirect) is now tracked separately as F-77. | 🟡 | session 6 plan §Step 6, refined in session 7 | Admin can create/edit/verify a regulation end-to-end; SME context card on `/surveys` reflects the change |
| F-75 | Backend tests: extend `test_m2_scoring.py` (M3 answers don't get scored); new `test_survey_flow.py` (start → answer → branch → null, incl. resume); new `test_m1_regulations.py` (admin CRUD + verify path + SME-safe-fields read) | 🔲 | session 6 plan §Step 7 | `pytest -q` green incl. all three new/extended files |
| F-76 | Playwright spec: `surveys_unified_flow.spec.ts` walking the VAT-no branch into `VAT_FACT_002` and verifying the module-accent swap | 🔲 | session 6 plan §Step 7 | `pnpm e2e` green incl. the new spec |
| F-77 | **Phase B (deferred).** Generalised `/admin/questions` UI with `?module=` filter, linked-regulation picker, branching-rules JSON editor; backend admin endpoints `GET/POST/PATCH /api/v1/admin/survey-questions` with `?module_number=&domain=&sector=&unverified=&page=&size=`; sidebar `nav.adminQuestions` rename + `/admin/m2/questions` redirect (resolves OQ12). Requires the backend addition first. | 🔲 | session 7 plan §Phase B | Admin can list / create / edit any-module question incl. `next_question_rules` and `linked_regulation_id`; M2 admin URL redirects |

## Session 7 — Admin UI for the unified wizard + admin-managed regulations

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-78 | New `Tabs` primitive (`components/ui/tabs.tsx`) on `@radix-ui/react-tabs` (already in deps). Reused by the regulation form's EN/SI/TA panels; available for any future bilingual or section-grouped admin form. | 🟢 | session 7 | Tabs component renders + switches state; visual matches existing primitives (focus-visible ring, dark-mode safe) |
| F-79 | Per-module accent extended into admin shells: `module-m2` wraps `/admin/m2/questions` and `/admin/m2/scores`; `module-m3` wraps `/admin/m3/risk-signals`. Buttons / focus rings now match the unified-wizard accent for the same module. | 🟢 | session 7 | Visiting the three pages shows the matching accent; SME wizard answering an M2 question now visually matches the admin M2 area |
| F-80 | i18n keys for the regulations admin surface (en/si/ta parity at 185 keys each): `nav.adminRegulations`, `regulations.{listTitle, listSubtitle, newTitle, newSubtitle, newAction, editAction, empty, previewAsSme, verifiedMeta, verifierPlaceholder, pagination}`, `regulations.section.{identity, dates, sectors, localised}`, `regulations.filters.{allDomains, allSectors}`, `regulations.columns.{shortCode, title, documentType, severity, effectiveDate}`, `regulations.tabs.{en, si, ta}`, full `regulations.fields.*` set, `regulations.documentTypes.*`, `regulations.errors.*`, `regulations.toast.*` | 🟢 | session 7 | JSON-key parity check returns identical deep-key counts across the three locale files |
| F-81 | Constants extracted to single source of truth: `lib/constants/domains.ts` (DOMAINS) and `lib/constants/sectors.ts` (SECTORS + DOCUMENT_TYPES) — used by the regulations list filter, the regulation form, and (later) by the generalised questions admin UI. | 🟢 | session 7 | New regulations pages import from `lib/constants/*`; older `/admin/m2/questions` page left untouched (still has its own inline copy) |

## Session 8 — App-shell redesign + admin route hotfix

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-82 | **Hotfix: parallel-pages route collision** (recurrence of F-63 class). The new `/(admin)/regulations/page.tsx` collided with the SME `/(app)/regulations/page.tsx` (both resolved to `/regulations`). All eight `(admin)/*` pages moved into `(admin)/admin/*` so URLs match the sidebar (`/admin/m2/questions`, `/admin/regulations`, etc.) and the SME hub keeps `/regulations`. Latent broken-link bug across every admin sidebar entry resolved at the same time. | 🟢 | session 8 (issue surfaced when running `make dev-frontend` after F-74) | Dev server compiles; `/regulations` shows the SME hub; every admin sidebar link works |
| F-83 | **App-shell redesign.** Floating-card layout: rounded sidebar with brand mark + gradient logo, collapsible (persists to `localStorage`), active-item left-border accent, tooltip on icon-only mode. Topbar gains breadcrumb display (via `BreadcrumbContext`), sidebar-collapse trigger, refresh button, and an avatar dropdown menu (email + role + sign-out). Both SME and admin shells use the same primitives. | 🟢 | session 8 | Visual matches reference screenshots; per-module accent (Sessions 6 & 7) still works inside the new shell |
| F-84 | New shell primitives + glue: `components/ui/avatar.tsx` (initials fallback), `components/ui/breadcrumb.tsx`, `components/ui/tooltip.tsx` (radix-tooltip already in deps). `components/layout/{breadcrumb-context,sidebar-state,page-header,avatar-menu}.tsx`. `<PageHeader>` injects breadcrumb into the topbar via context so every page declares its breadcrumb once. | 🟢 | session 8 | 18 primitives total in `components/ui/` (was 15 in Session 7); zero new top-level deps |
| F-85 | **Dashboard upgrade.** Welcome banner (gradient Card + CTAs into `/surveys`, `/risk`, and `/admin/regulations` for admins). Four stat cards (Knowledge / Risk signals / Survey progress / Regulations or Last update) with circular icon badges in tones (`primary` / `emerald` / `amber` / `violet`). Two-column "Pending tasks" + "Recent activity" panels. Server component, parallel `Promise.all` fetches with graceful fallback when `sme_id` is absent. | 🟢 | session 8 | Server-rendered; works for SME (no `regulations` card) and admin (with it); zero hard-coded colours |
| F-86 | **List-page polish.** `/admin/regulations` gets a vertical filter rail (Verification / Domain / Sector groups, click-to-toggle), a search input, polished table (avatar+code cells, hover row highlight, severity dots, responsive column hiding via Tailwind `md:` / `lg:` breakpoints), URL-driven pagination. `/admin/m2/questions` gets `PageHeader` + breadcrumb + table-row hover state + tinted header row (kept the existing horizontal filter row to minimise blast radius — vertical rail can follow with F-77 Phase B). | 🟢 | session 8 | Filter rail toggles update the URL; table responsive at all viewports; per-module accent (`module-m2`) preserved on the m2 page |
| F-87 | **i18n: `dashboard.*` (welcome + stats + tasks + activity) + `topbar.*` (refresh / avatarLabel / signOut / sidebarToggle) added across en/si/ta. Parity preserved at 211 deep keys per file. | 🟢 | session 8 | JSON-key parity check returns 0 diff |
| F-88 | **Form-page polish.** Regulation form gains numbered section chips ("01" – "04") on each Card header (`SectionStep` helper + `Badge`-style pill), and a sticky save bar at the bottom (`sticky bottom-0` + `backdrop-blur`). New + edit pages adopt `<PageHeader>` with full breadcrumb (`Dashboard > Regulations > <code>`). | 🟢 | session 8 | Save bar stays pinned during scroll; section chips visible at every viewport |

## Session 9 — Survey-wizard bug fix + Module 4 data-collection scaffolding

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-89 | **Survey-wizard dotted-field-name bug fix.** Awareness `question_code` values use dotted IDs (`awareness.v1.q01`); react-hook-form interpreted the dots as object-path notation, so form values lived at `values.awareness.v1.q01` (nested) while the wizard read `values["awareness.v1.q01"]` (flat) and got `undefined`. Answers were sent as `null`, the backend's `_validate_answer` rejected them, the wizard caught the error silently, and the user saw "no save + counter stuck at 1". Fix: new `lib/surveys/safe-field-id.ts` (`toFieldId(code)` replaces `.` with `__DOT__`); `flow-question-to-ui.ts` returns the safe id as `Question.id`; the wizard's submit reads `values[toFieldId(current.question_code)]`. Backend untouched — `current.question_code` is still the original dotted code. | 🟢 | session 9 (issue surfaced during runtime test of `/surveys`) | DB row written for awareness Q01–Q12; "Question N of M" counter increments after each submit; Playwright spec for the unified flow (when F-76 ships) will not regress |
| F-92 | **`/admin/users` redesign** — list page mirrors `/admin/regulations` style. 240 px vertical filter rail with four searchable multi-select filters (Role, Language, Status, Sector) using a new Combobox primitive; client-side text search across email + region; four stat pills at the top (Total / SMEs / Annotators / Admins) with tonal icon badges; polished table with avatar+email cells, sub-sector hint, role badges, language code, sector capitalisation, region with pin icon, joined date, active dot, responsive column hiding (`md:`/`lg:`); `<PageHeader>` with breadcrumb + total badge + Create-user CTA. | 🟢 | session 10 (user request: "for the user's screen … like in the regression screen") | Filters narrow the table client-side; create dialog opens from the header; new user appears in the list after submit |
| F-93 | **Searchable multi-select Combobox primitive** (`components/ui/combobox.tsx`) — single + multi modes, type-to-search, click-to-toggle. Selected values render as removable chips in the trigger; multi mode keeps the panel open after each pick (rapid selection); single mode closes after pick. Click-outside + Escape close the panel. Built without a popover/cmdk dep — absolute positioning + a click-outside listener (~250 LOC). Reused by the users-page filter rail (4 instances) and the Create-user dialog (4 instances). | 🟢 | session 10 | Reusable across any future admin filter / form-field surface |
| F-99 | **`<SurveyForm>` dotted-id leak (per-instrument awareness page).** F-89 redux: same root cause as the unified-wizard fix, different surface. The per-instrument `<SurveyForm>` (used by `/surveys/awareness`, `/knowledge`, `/vulnerability`) read form values via flat `values[q.id]` lookups in three places — `<SurveyProgress>` (counter stuck at 0/12), `valuesToAnswers` (silent zero-row submit triggering "this question is required"), and the `contextByQuestionId` lookup for regulation cards. Awareness ids contain `.`; RHF interprets that as object-path notation. Fixed by sanitising every id at the SurveyForm boundary using the existing `toFieldId()` helper (no new file): builds `safeQuestions` (with rewritten `dependsOn.questionId`), `idMap` for submit-time translation, and `safeContextById` for the regulation card lookup. Backend untouched — `survey_responses.question_id` still gets the original dotted format because the submit handler translates safe id → original via `idMap` before POST. | 🟢 | session 10 (issue surfaced on `/surveys/awareness` with the "0 / 12 · 0%" progress strip stuck) | Progress counter increments live; awareness submit POSTs all answered questions; TypeScript clean (no new errors); knowledge + vulnerability paths unaffected (no dots in their codes) |
| F-95 | **Two runtime bug fixes.** (A) `<SelectItem value="">` crash on `/admin/regulations/new` — Radix Select v2 forbids empty-value items; replaced with a `__none__` sentinel (regulation form) and `__all__` for the m2-questions filter. (B) `/surveys/knowledge` 500: `m2_service.questions_for_sme` queried `survey_questions` without a `module_number` filter, so awareness/M3 rows leaked in and failed `M2QuestionOut` validation (their `domain_code` / `knowledge_type` are NULL post-Session-6). Added `module_number == 2` filter to both the SME-side and admin-list queries. | 🟢 | session 10 (continuation) | `/admin/regulations/new` and `/admin/m2/questions` render without crash; `/surveys/knowledge` returns the M2 bank only |
| F-96 | **Backend full CRUD for users.** New schemas `AdminUpdateUserIn` + `AdminResetPasswordIn`. New `auth_service` functions: `update_user` (role + profile patch with last-admin demote-guard), `set_active` (toggle `is_active`, refuses to deactivate the last active admin), `reset_password` (hash + audit, no password ever logged), `delete_user` (soft-delete via `set_active(False)` with distinct `user.delete` audit event). New endpoints: `PATCH /api/v1/users/{id}`, `POST /{id}/activate`, `POST /{id}/deactivate`, `POST /{id}/reset-password`, `DELETE /{id}`. All admin-only, all audit-logged. | 🟢 | session 10 (continuation) | `last-admin` guard fires on backend; soft-deactivate makes login refuse; password reset rotates the hash |
| F-97 | **Backend full CRUD for regulations + soft-archive migration.** New migration `202605110001` adds `m1_regulations.is_active` (+ index) defaulting TRUE for existing rows. Service: `archive_regulation` / `restore_regulation` / `bulk_verify` (atomic update of N rows, idempotent, single audit row) / `duplicate_regulation` (`secrets.token_hex(3)` suffix, strips existing `_COPY_*` so duplicates of duplicates don't pile up; M2M sectors copied; `expert_verified` reset to FALSE). `list_regulations()` accepts `include_archived` (default FALSE → admin list hides archived rows by default). SME public read `/m1/regulations/{id}/public` 404s archived rows so context cards stop appearing in the unified wizard. Endpoints: `DELETE /{id}`, `POST /{id}/restore`, `POST /{id}/duplicate`, `POST /bulk-verify`. New `RegulationBulkVerifyIn` schema. | 🟢 | session 10 (continuation) | Archived row hidden by default + 404 on SME read; bulk-verify returns count of *newly* verified; duplicate redirects to new edit page |
| F-98 | **Frontend full CRUD UI** — row actions menu + confirm/edit/reset/bulk dialogs. New primitives: `components/ui/confirm-dialog.tsx` (generic destructive-confirm with inline async-error), `components/admin/row-actions.tsx` (3-dot DropdownMenu with `danger` slot below a separator). New form components: `components/forms/edit-user-dialog.tsx`, `components/forms/reset-password-dialog.tsx`. Users page gets a trailing actions column (Edit / Reset password / Activate-Deactivate / Delete) wired to TanStack mutations + i18n toasts. Regulations page gets a leading checkbox column + bulk-verify action bar (visible when ≥ 1 selected) + a "Show archived" filter row + row actions (Edit / Duplicate / Archive ↔ Restore). New `RegulationsApi.{bulkVerify, archive, restore, duplicate}` and `UsersApi.{update, activate, deactivate, resetPassword, delete}` methods + `adminUpdateUserSchema` + `adminResetPasswordSchema` zod validators. New i18n `users.actions.*` / `users.edit.*` / `users.resetPassword.*` / `regulations.actions.*` namespaces (en/si/ta parity at 294 keys). | 🟢 | session 10 (continuation) | Every action surfaces from the row's 3-dot menu; backend last-admin error renders inline in the confirm dialog; bulk-verify clears selection on success |
| F-94 | **Admin-create-user endpoint + dialog.** New `POST /api/v1/users` (admin-only) backed by `auth_service.admin_create_user` — distinct from self-registration because the role is caller-supplied (sme/annotator/admin) and the audit row records `created_by` + `role`. Frontend `<CreateUserDialog>` (Radix Dialog primitive — `components/ui/dialog.tsx` new) wires email + password + role + language + optional profile (sector/region/employee band) with zod validation and a TanStack mutation that invalidates the users query on success. | 🟢 | session 10 | Admin can seed annotators / other admins from the UI without dropping to SQL; audit log captures the actor |
| F-91 | **Sticky desktop sidebar + mobile drawer (hamburger trigger).** Two layout bugs fixed in one slice. (1) Desktop: the `<aside>` was a flex sibling of the scrolling main column without `sticky` / `h-screen` containment, so on long pages the sidebar slid up out of view as the user scrolled. Fixed by adding `md:sticky md:top-0 md:h-screen md:self-start` to the aside. (2) Mobile: the sidebar was `hidden md:flex` and the `PanelLeft` toggle was `hidden md:inline-flex` — a phone user had no nav whatsoever. Fixed by extracting `<SidebarContent>` from the existing `<Sidebar>`, adding a new `<Sheet>` primitive (Radix Dialog styled as a left-edge slide-in panel), and a new `<MobileSidebar>` client component that opens the drawer via a `Menu` (hamburger) icon button placed at the topbar's left edge (visible only below `md`). Drawer auto-closes on nav-link tap (`onItemClick` callback wired through `<NavLink>`), tap-outside, and Escape. | 🟢 | session 10 (issue surfaced during runtime test of `/admin/regulations`) | Sidebar stays visible during scroll on desktop; mobile drawer opens via hamburger and closes on nav; existing F-83 collapse toggle preserved on desktop |
| F-100 | **Backend admin survey-questions CRUD.** New `app/schemas/survey_question.py` (`SurveyQuestionAdminOut`, `SurveyQuestionCreateIn`, `SurveyQuestionUpdateIn`, `BranchingPredicate` with one-key model_validator, `BranchingRule`, `SurveyQuestionsPage`, `SurveyQuestionsByRegulationOut`, `SurveyQuestionVerifyIn`, `SurveyQuestionBulkVerifyIn`). Service `survey_question_service` extended with `list_questions / get_question / create_question / update_question / archive_question / restore_question / duplicate_question / verify_question / bulk_verify_questions / questions_for_regulation`, all audit-logged (`survey_question.{created,updated,archived,restored,duplicated,verified,bulk_verified}`). New router `app/api/v1/admin_survey_questions.py` with 10 endpoints under `/api/v1/admin/survey-questions` (list, get, create, patch, archive, restore, duplicate, verify, bulk-verify, by-regulation). Legacy `/m2/questions` keeps emitting `Deprecation: true` + `Link: …successor-version` headers. | 🟢 | session 11 (user request: "the systematic, regulation-driven authoring workflow that interlinks M1/M2/M3 question banks") | Admin endpoints work behind `require_admin`; SME-side flow engine unchanged; audit log carries every mutation |
| F-101 | **`<QuestionForm>` + `/admin/questions/{list,new,[code]/edit}` pages.** Five-card form (Identity / Linkage / Localised / Answers / Branching) mirroring `<RegulationForm>`'s layout — sticky save bar, numbered SectionStep chips, EN/SI/TA Tabs, regulation Combobox lookup, format-adaptive Answers card via the new `<OptionsBuilder>`. List page mirrors `/admin/regulations` styling: filters (Module / Domain / Sector / Format / unverified-only / archived-toggle), search, bulk-verify action bar, row actions (Duplicate / Archive ↔ Restore), URL-driven pagination. New + edit pages are server components hydrating an access token from the session. | 🟢 | session 11 | New question authored end-to-end in the UI without touching seed scripts |
| F-102 | **`<BranchingRulesEditor>` (Visual + JSON tabs).** Single source of truth on the form's `next_question_rules` array. Visual mode: one templated row per rule with predicate Select (`answer_eq` / `answer_in` / `answer_lt` / `answer_gt`), value input that adapts to the question's `question_format` (Combobox of options for categorical / multi / yes_no, numeric input for likert / numeric, free text fallback), goto-question Combobox scoped to the linked regulation by default with a "Search all questions" toggle, up/down reorder + delete. JSON mode: prettified textarea with apply / reset buttons and inline error surface. Both views read/write the same form-state field. | 🟢 | session 11 | Switching between Visual and JSON preserves edits; engine `_resolve_rule` accepts the produced predicates verbatim |
| F-103 | **Regulation-anchored authoring wizard + LinkedQuestionsPanel.** New route `/admin/regulations/[id]/authoring` walks Step 1 (M1 awareness root, pre-filled `module_number=0` + `is_branching_root=true` + `instrument_section=awareness`) → Step 2 (M2 knowledge follow-ups for yes/no answers) → Step 3 (M3 vulnerability follow-ups for M1=No and M2-wrong paths). Each step is a thin orchestration layer over `<QuestionForm>` with the regulation locked. The regulation edit page now carries a `<LinkedQuestionsPanel>` Card grouping questions by module with deep-links into `/admin/questions/[code]/edit`. | 🟢 | session 11 | Admin authors a full M1→M2→M3 chain for one regulation without leaving the page |
| F-104 | **i18n: `questions.*`, `regulations.linkedQuestionsPanel.*`, `regulations.authoring.*`, `nav.adminQuestions`, `common.previous`.** ~50 new keys added to en/si/ta (411 keys per locale, parity preserved). Most-visible labels (nav entry, list title, common.previous) are natively translated in si/ta; long-form bodies fall back to English copies until a translator passes through (consistent with how Session-7/8/10 staged i18n rollout). | 🟢 | session 11 | `pnpm typecheck` clean for new files; key counts identical across locales |
| F-105 | **Sidebar entry `nav.adminQuestions` + deprecation note on the legacy M2 list.** New ListChecks-icon entry between `/admin/regulations` and `/admin/m2/questions`. The legacy admin M2 list stays live for one slice; OQ12 (the hard `/admin/m2/questions` → `/admin/questions?module=2` redirect) deferred to a follow-up. | 🟢 | session 11 | New "Question bank (unified)" entry visible in admin sidebar |
## Session 12 — Universal regulation-anchored survey system (M:N + visual flow + DB-driven SME pages)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-106 | **Branching validators (forward-ref / cycle / archived-target).** New `validate_branching` service helper detects three soft-warn issues via DFS over `next_question_rules`. New schema `BranchingValidationWarning` + endpoint `GET /admin/survey-questions/{code}/validate-flow`. `create_question` / `update_question` re-run on every write and stash results on the ORM instance for `_to_admin` to surface as `validation_warnings`. Never blocks. | 🟢 | session 12 | Manual cycle in seeds → admin response includes amber-banner warnings; engine still serves the SME via linear fallback |
| F-107 | **Regulation-scoped flow.** `start_flow` / `next_question` accept optional `regulation_id`; root selection + linear fallback + `goto_question_code` resolution all scope to junction-linked questions when set. Out-of-scope rule targets fall back to linear progression within scope. `/api/v1/survey-flow/{start,answer}?regulation_id={uuid}` + `flow.started_for_regulation` audit event. `<SurveyWizard>` threads the param. | 🟢 | session 12 | SME at `/surveys/regulation/<vat>` walks only VAT-linked questions |
| F-108 | **M:N junction `survey_question_regulations`.** Two new migrations: junction (composite PK + weight + is_primary + partial-unique idx) with backfill from `linked_regulation_id` (FK constraint dropped, column kept as cached primary pointer), and `survey_questions.is_baseline`. Service helpers `link_regulation` / `unlink_regulation` / `set_primary_regulation` / `regulations_for_question` keep the cached column in sync via `_sync_primary_junction`. `SurveyQuestionAdminOut` gains `linked_regulations: list[LinkedRegulationRef]`. Endpoints `POST/DELETE /admin/survey-questions/{code}/regulations[/{reg}/primary]`. Audit events `survey_question.{linked_to_regulation,unlinked_from_regulation,primary_regulation_changed}`. Seeds write junction rows on insert. | 🟢 | session 12 | One question primary-linked to reg A and secondary-linked to reg B; archive of A drops `linked_regulations` row |
| F-109 | **Backend i18n fallback.** `app/services/i18n_utils.py` with `localised(record, field_base, locale) -> tuple[value, fallback_used]`. Wired into `survey_flow._to_flow_question` and the new instrument-questions endpoint. `FlowQuestionOut` + `SurveyQuestionPublicOut` carry `prompt`, `prompt_locale`, `language_fallback_used`. `Accept-Language` header overrides `user.preferred_language`. | 🟢 | session 12 | SI request on a question with only `prompt_en` returns English + flag=true; admin fills SI → flag=false |
| F-110 | **DB-driven per-instrument question fetch.** `GET /api/v1/surveys/{instrument}/questions?sector?=&regulation_id?=&include_baseline=true` merges baselines + regulation-linked rows ordered by `is_baseline DESC, effective_date ASC`. New SME-safe `SurveyQuestionPublicOut` (no correct_answer / scoring / ground_truth) and `SurveysInstrumentQuestionsOut`. | 🟢 | session 12 | Adding a regulation in admin grows SME's `/surveys/awareness` without code changes |
| F-111 | **Translation queue admin.** New router `/admin/translations`: list (UNION across questions+regulations needing SI/TA), PATCH for question / regulation, POST bulk-mark-translated (questions only). Audit `translation.completed`. | 🟢 | session 12 | Admin fills SI for one question, bulk-marks 5 others — flags clear, queue shrinks |
| F-112 | **Dashboard pending-regulations endpoint.** `GET /api/v1/dashboard/pending-regulations?limit=N` — active sector-relevant regulations with no SME responses linked yet. Server-side join. | 🟢 | session 12 | Fresh SME with one active reg: count=1; submits one answer → drops to 0 |
| F-113 | **Visual flow canvas.** New `/admin/regulations/[id]/flow` page with `<FlowCanvas>` (CSS grid M0/M2/M3 columns), `<FlowQuestionDrawer>` (slim form via Radix Sheet — creates child question + auto-upserts parent's `next_question_rules`), `<FlowValidationBanner>` (forward-ref/cycle/archived warnings with click-to-jump). Replaces rigid 3-step wizard for advanced flows; wizard kept as a "Quick start" entry. Edit page gains "Open flow canvas" CTA. CSS-grid chosen over ReactFlow to avoid the ~60 KB dep. | 🟢 | session 12 | Admin clicks any answer chip → drawer pre-fills regulation + parent context; saving creates child + edge label; cycles surface in red banner |
| F-114 | **DB-driven per-module SME surveys.** Awareness, knowledge, vulnerability pages fetch via `SurveysApi.questionsForInstrument` and convert via new `lib/surveys/db-question-adapter.ts`. Knowledge page builds the regulation-context map dynamically from each row's `linked_regulation`. New `<VulnerabilityForm>` client shell owns the partition-on-submit logic. Hardcoded `lib/surveys/{awareness,m3-vulnerability}.ts` no longer imported. | 🟢 | session 12 | Adding a regulation grows the standalone surveys with no code changes; `/risk` still updates from M3 history+behavioural |
| F-115 | **Two-tab surveys hub + regulation-scoped flow page.** `/surveys` is `<SurveysHubTabs>` with `?view=regulation\|module`. By-regulation tab lists `<RegulationCard>` per pending sector-relevant regulation → `/surveys/regulation/[id]` (scoped flow). By-module tab keeps three abstract instrument cards routing to standalone surveys. | 🟢 | session 12 | SME with two pending regs sees two cards on by-regulation tab; clicking opens scoped wizard |
| F-116 | **Dashboard widget — Regulations awaiting your assessment.** Up to 3 cards from `/api/v1/dashboard/pending-regulations` on the SME dashboard, "View all" → `/surveys?view=regulation`. | 🟢 | session 12 | Widget visible when count>0; hides itself when empty |
| F-117 | **Translations admin page + sidebar nav.** `/admin/translations` page + `<TranslationsQueue>` component (filters, inline SI/TA edit, bulk-mark for questions). New `Languages` admin sidebar entry. New `lib/api/admin-translations.ts` typed client. | 🟢 | session 12 | Admin paginates queue, edits SI/TA inline, bulk-marks 5 — needs_translation=false on those rows |
| F-118 | **Frontend types + API extensions.** `SurveyQuestionAdmin` gains `is_baseline`, `linked_regulations`, `validation_warnings`. New types `LinkedRegulationRef`, `BranchingValidationWarning`, `SurveyQuestionPublic`, `SurveysInstrumentQuestions`, translation-queue types. `AdminSurveyQuestionsApi` gains link/unlink/setPrimary/validateFlow. `SurveysApi` gains `questionsForInstrument`. `SurveyFlowApi` accepts `regulation_id`. New `DashboardApi`. | 🟢 | session 12 | `pnpm typecheck` clean; new endpoints reachable from typed clients |

## Session 13 — UI documentation + animated loading skeleton

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-119 | **`<AnimatedLoadingSkeleton>` (`components/ui/`) — theme-adapted shadcn-pattern component.** Framer-motion search-icon sweep over a shuffling responsive card grid (3/2/1 cols by breakpoint). Ported from the supplied component with hardcoded light colours swapped for theme tokens (`bg-card`, `from-muted/40 to-muted`, `bg-primary/15` + `text-primary` so it follows `.module-m{1,2,3}`, `hsl(var(--primary))` glow keyframes) and the inner placeholders rebuilt on the existing `<Skeleton>` primitive. `"use client"`; named export + default alias; optional `className`; `aria-busy` + `sr-only` "Loading…". Demo at `components/ui/animated-loading-skeleton-demo.tsx`. New dep `framer-motion@^11`. | 🟢 | session 13 (user: "integrate this component … install framer-motion") | Component renders correctly in light + dark; glow recolours to emerald inside `.module-m2`; `pnpm typecheck` clean; `framer-motion` resolves |
| F-120 | **Streaming `loading.tsx` segments + skeleton wiring across the app.** New `loading.tsx` for the server-rendered surfaces (`/surveys`, `/surveys/regulation/[id]`, `/dashboard`, `/admin/regulations/[id]/{flow,edit}`, `/admin/translations`) — each renders `<AnimatedLoadingSkeleton>` in the page's width container so nothing jumps when content streams in. Replaced the plain `"Loading…"` text in the client-rendered admin tables (`/admin/regulations`, `/admin/questions`, `/admin/m2/questions`, `/admin/users`) and in `<LinkedQuestionsPanel>` + `<TranslationsQueue>` with `<AnimatedLoadingSkeleton>` (chrome stripped via `className`). Also fixed three pre-existing TS errors that were blocking `pnpm typecheck` (`useWatch` `name` type in `question-renderer.tsx`, `valuesToAnswers` filter predicate in `survey-form.tsx`, missing `domain_code`/`change_category`/`severity_level` on the `RegulationPublic` TS type). | 🟢 | session 13 | Throttled-network: server pages stream the skeleton then swap to content with no layout shift; client tables show the skeleton during `isLoading`; `pnpm typecheck` clean; existing Playwright/Vitest specs still pass |
| F-121 | **New SETUP doc `12_UI_Screens_and_Loading.md`.** Screen-by-screen map of every SME + admin page (route · purpose · key components · why · what-you-see), the reusable-component catalog (24 `components/ui/` primitives + the `components/forms\|surveys\|admin\|layout` composites), the design-system recap (HSL tokens, dark-first, per-module accents, trilingual, radius), the loading-state strategy (§6) and a when-to-use-which decision table (§7). Linked from `00_INDEX.md`; `11_Survey_System.md` footer now points to `12`; `05_Frontend_Development.md` gains a `§10 Loading states` stub pointing at `12 §4/§6-§7`. | 🟢 | session 13 (user: "write the documentation … what are the UIs and screens developed up to now and why") | Doc exists, listed in `00_INDEX.md`, no broken relative links; three spot-checked screen entries match the actual `page.tsx` |
| F-122 | **App-themed `<Pagination>` (`components/ui/`), wired into all eight admin tables.** Dependency-free replacement for the Ant Design `<Pagination>` reference (declined `antd` — it's a full UI framework with a separate CSS-in-JS + token system that can't read the project's HSL contract, so "use app colors" would be impossible to honour). Built on `<Button>`/`<Select>`/`<Input>` + theme tokens: page numbers with `…` collapses, prev/next, optional page-size changer, optional quick-jumper, "Total N items". Controlled mode (`onPageChange`/`onPageSizeChange`) for client tables; link mode (`buildHref` → `<Link>`s) for server-component tables. Wired into `/admin/regulations`, `/admin/questions` (URL `?page=&size=`), `/admin/m2/questions`, `/admin/users` (client-side slice), `/admin/translations` (`<TranslationsQueue>` state refactor), `/admin/surveys/awareness/responses`, `/admin/m2/scores`, `/admin/m3/risk-signals` (link mode). New `pagination.*` i18n namespace; demo at `components/ui/pagination-demo.tsx`. | 🟢 | session 13 (user: "use app colors and take following as reference and add that type of pagination for all tables") | All eight admin tables paginate with the same component; active page = `bg-primary`, hover = `bg-accent`, focus ring = `ring`; recolours in dark mode + module accents; `pnpm typecheck` + `pnpm build` clean |
| F-123 | **QA bug-fix batch.** (a) **Missing i18n keys** — Session-12 surveys-hub + dashboard-widget code referenced `surveys.hub.{tabRegulation,tabModule,regulationEmpty,regulationLead,moreRegulations,moduleLead,module*Title,module*Body,pageTitle,pageSubtitle}`, `surveys.empty`, and `dashboard.{pendingRegulationsTitle,viewAllRegulations,morePendingRegulations}` — none existed, so the UI showed raw key strings. Added to en/si/ta; `/surveys` header now uses `surveys.hub.pageTitle/pageSubtitle`. (b) **"By regulation" tab / dashboard widget always empty** — `pending-regulations` treated the `"universal"` sentinel as a specific sector, so the five seeded (all-`universal`) regulations matched no SME; fixed to `sector_code IN (sme.sector, "universal")`. (c) **Question-form regulation combobox empty** — added a `regulationsLoaded` flag + an empty-state hint + a normal helper hint. (d) **Auto `question_code`** — `<QuestionForm>` (create) + `<FlowQuestionDrawer>` auto-suggest `<instrument>.v<N>.qNN` from the chosen module (0→awareness/2→knowledge/3→vulnerability) + version, NN incremented past the highest existing match; admin can override. New helper `lib/surveys/question-code.ts`; create-validator regex relaxed `[A-Z0-9_.]+` → `[A-Za-z0-9_.]+`; drawer code input no longer force-uppercases. New `questions.fields.{questionCodeHint,linkedRegulationHint,noRegulationsHint}`. | 🟢 | session 13 (user QA report: raw `surveys.hub.*` keys on screen; regulation link not showing; want `awareness.v1.qNN`-format codes; unified survey not showing) | Hub renders proper labels in all 3 locales; a fresh SME sees the 5 seeded regulations under "By regulation" + on the dashboard widget; creating a question pre-fills `awareness.v1.q13` / `knowledge.v1.q01` / `vulnerability.v1.q01` from the module; `pnpm typecheck` + `pnpm build` clean |

## Session 14 — Audit logging + authorship tracking + admin Activity Log

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-124 | **Audit-write consolidation + coverage closure.** Every open-coded `insert(AuditLog).values(...)` migrated onto a single `audit_service.record(db, *, event_type, actor, table_name?, record_id?, record_key?, data?)` (behaviour-preserving — no `event_type` string or `event_data_json` shape changed; only `record_key` added). `audit_service` also exposes `list_events(...)` (conditional `.where`, `actor` = case-insensitive email substring, `q` = `event_data_json::text ILIKE`, ordered `occurred_at DESC, log_id DESC`, size clamped 1–100) and `distinct_event_types(...)`. New indexed `audit_log.record_key` column (natural key for `survey_questions.question_code`). Coverage gap closed: `survey_service.submit` writes one `survey.submitted` row (`{ instrument, answered_count, regulation_ids, m2_scored, m3_snapshots_projected }`) — the M2 score recompute + M3 snapshot projection are folded into it, not separate rows. `flow.started_for_regulation` (a read) was never emitted in code → dropped from the docs, not added. | 🟢 | session 14 (user: "for the whole application … for the relevant each actions should be saved in the db wherever the changes or the actions happened") | A knowledge-batch submit → exactly one new `audit_log` row, `event_type == "survey.submitted"`, `m2_scored is True`, `answered_count == N`; a vulnerability submit → `m3_snapshots_projected is True`; creating a question → `audit_log.record_key == question_code`; the migrated auth/regulation/question/translation calls produce byte-identical `event_type` / `event_data_json` |
| F-125 | **`AuthorshipMixin` — `created_by` / `updated_by` everywhere it matters.** New `app/db/mixins.py::AuthorshipMixin` (plain nullable `String`, not FKs — denormalised acting-user email, survives user deletion, matches `user_name` / `*_verified_by` convention) on `M1Regulation`, `SurveyQuestion`, `SurveyQuestionRegulation`, `User`, `SMEProfile`. **Not** on the static lookups, the append-only `audit_log` (has `user_name`), or the append-only response/score/snapshot tables (have `sme_id`). Stamped by the service layer on **every** create / update / verify / archive / restore / link / translation-patch — *including soft-deletes* (`is_active=false` is an update → `updated_by` moves). `_sync_primary_junction` gained a required `actor_email` param; `admin_update_user` uses a `profile_touched` flag so the profile's `updated_by` only moves when a profile field actually changed; `register` self-stamps own email on `User` + the auto-created `SMEProfile`. Surfaced in `RegulationAdminOut` / `SurveyQuestionAdminOut` / `UserOut` (+ `updated_at` on `UserOut`). Migration `202605140001` adds the 10 columns + `audit_log.record_key` + `ix_audit_log_record_key`; no backfill (historical authorship genuinely unknown). | 🟢 | session 14 (user: "include the created by and edited by where the related data got changes even the status changes by … even for the soft delete the who made the changes its") | Create a regulation → `created_by == admin.email`; PATCH it → `updated_by` changes; `DELETE` (archive) it → `updated_by` changes + an `m1_regulation.archived` row; register a new SME → `GET /users/me` → `created_by == own email`; `alembic upgrade head` → `alembic downgrade -1` → `alembic upgrade head` round-trips, `alembic check` clean |
| F-126 | **Admin Activity Log API.** New router `app/api/v1/admin_audit.py` (behind `require_admin`) at `/api/v1/admin/activity-log`: `GET ""` — query `event_type`, `table_name`, `actor`, `record_id`, `record_key`, `since`, `until`, `q`, `page` (≥1), `size` (1–100) → `AuditLogPage` (ordered `occurred_at DESC`); `GET "/event-types"` → `AuditEventTypesOut` (distinct types + tables) for the filter dropdowns. New schemas `app/schemas/audit.py`. Typed frontend client `lib/api/admin-audit.ts` (`AdminAuditApi.list/eventTypes`, `AuditLogParams`); new TS types `AuditLogEntry` / `AuditLogPage` / `AuditEventTypes`. | 🟢 | session 14 | `GET /admin/activity-log` as admin → 200, `total` correct, each filter narrows correctly, pagination edges work; as non-admin → 403; `…/event-types` returns the distinct types seen |
| F-127 | **Admin Activity Log UI.** New server-rendered `/admin/activity-log` page (`force-dynamic`) — `<PageHeader>` + `<ActivityFilters>` (client: event-type + table `<Select>`, actor `<Input>`, since/until date inputs, Apply/Clear → `router.push`) + a `<Table>` (When / Event / Target / User / Details) + a link-mode `<Pagination>` preserving the active filters. Event = colour-coded `<Badge>` via `eventBadgeVariant()` + `eventLabel()` (both in new `lib/audit-events.ts` — a hardcoded label map kept out of i18n because next-intl treats `.` as a nesting separator; admin chrome stays English). Details = `renderDetail(entry)` — a type-aware switch (`survey.submitted` → "instrument · N answers · scored", `*.updated`/`user.admin_update` → `fields_changed` chips, `translation.completed` → bulk msg or chips, `*.bulk_verified` → "N verified by X", `auth.login.failure` → email/reason, `*.duplicated` → new code, fallback → `<details>` raw-JSON). `loading.tsx` renders `<AnimatedLoadingSkeleton>`. Sidebar gains a `ScrollText` "Activity log" admin entry. New `activity.*` i18n namespace at en/si/ta parity. | 🟢 | session 14 (user: "for that related logs UI can be added in the UI under the permission of the admin as beautiful with the existing style … when the logs should be shown as per the type of action correctly make them more modern and stylish") | Log in as admin → sidebar "Activity log" → table renders with colour-coded badges; apply an event-type filter → URL gains `?event_type=`, rows narrow; pagination link navigates; an event row expands "view raw"; `pnpm typecheck` + `pnpm build` clean; en/si/ta have the new `activity.*` + `nav.adminActivityLog` keys at parity |
| F-128 | **Authorship footer on the admin edit pages.** New `frontend/components/admin/authorship-meta.tsx` (async server component) — a muted dashed-border line "Created by … · {date} · Last edited by … · {date}" + a "View this record's history" link. Wired into `/admin/regulations/[id]/edit` (→ `/admin/activity-log?record_id=${id}`) and `/admin/questions/[code]/edit` (→ `?record_key=${code}`). New `activity.authorship.{createdBy,editedBy,viewHistory}` i18n keys (ICU `{name}` interpolation). | 🟢 | session 14 | The regulation edit page shows "Created by … · Last edited by …" and the link lands on `/admin/activity-log?record_id=…`; the question edit page links with `?record_key=…` |

## Session 15 — Unified survey flow plumbing + worked VAT/SSCL-merge scenario + data-driven M3 mapping

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-129 | **`survey_responses.linked_regulation_id` populated end-to-end.** `survey_service.submit` / `_build_row` take an optional `regulation_id` (threaded from `POST /survey-flow/answer?regulation_id=…`); when set it's authoritative, else `_build_row` falls back to the answered question's cached `survey_questions.linked_regulation_id`. New helpers `_question_code_candidates` / `_question_linked_regulation`. Knock-on: the `survey.submitted` audit `regulation_ids` is non-empty for scoped flows; `dashboard/pending-regulations` marks a regulation answered once any linked question is answered; the Activity-Log `survey.submitted` detail shows "N regulation(s)". | 🟢 | session 15 (user: the unified survey "should be saved … wherever the changes happened" + interlink anchored on a regulation) | A regulation-scoped answer → that row's `linked_regulation_id` == the scoped uuid; a standalone-page answer to a reg-linked question → `linked_regulation_id` == the question's cached value; an un-scoped answer to a generic question → NULL; the `survey.submitted` audit row's `regulation_ids` == `[<uuid>]` |
| F-130 | **`meta.from_question_code` / `meta.from_rule` flow breadcrumb + `start_flow` fallback.** `survey_flow.py::_flow_breadcrumb`: before persisting, checks whether the SME's prior answered row had a `next_question_rules` entry resolving to the question being answered, and if so records `{from_question_code, from_rule}` into `meta` (absent on the branching root, on linearly-reached rows, and on standalone batch submits). `survey_question_service.start_flow` falls back to the first in-scope question by `(module_number, sort_order, question_code)` when a reg-scoped flow has no in-scope `is_branching_root` (fixes the latent "scoped flow returns None" bug). | 🟢 | session 15 | Answer q13 with `no` → next is `VAT_SSCL_MERGE_FACT_001`; that row's `meta.from_question_code == "awareness.v1.q13"` and `meta.from_rule.goto_question_code == "VAT_SSCL_MERGE_FACT_001"`; the q13 row (the root) has no `from_question_code`; a reg with no in-scope branching root still starts a flow |
| F-131 | **Worked end-to-end scenario `VAT_SSCL_MERGE_2026` seeded.** New M1 regulation ("VAT and SSCL merged into a single 20% VAT, April 2026 — a restructuring, not a real 2-point increase"), M0 awareness `awareness.v1.q13` (`is_branching_root`, `is_baseline=false`, 4-way nuanced options routing the non-ideal three → M2), M2 `VAT_SSCL_MERGE_FACT_001` (+ application sibling `VAT_SSCL_MERGE_APP_001`; the factual routes wrong → M3), M3 `M3_VAT_SSCL_MERGE_PRACTICE` (→ `M3_VAT_SSCL_MERGE_PENALTY`) — all four junction-linked to the regulation so the reg-scoped flow traverses the whole chain. `seed_vulnerability_questions._q`/`main()` gained `linked_regulation_short` support; `seed_awareness_questions._q` gained an `is_baseline` kwarg. Demo SMEs answer q13 + the M2 questions automatically. | 🟢 | session 15 (user: model "the regulation of value-added tax increased to 20% … combined to one … that's the scenario") | `make seed` on a fresh DB → the 4 new questions + their junction rows exist; `GET /survey-flow/start?regulation_id=<VAT_SSCL_MERGE_2026>` → `awareness.v1.q13`; answering wrong all the way walks q13 → `VAT_SSCL_MERGE_FACT_001` → `M3_VAT_SSCL_MERGE_PRACTICE` → `M3_VAT_SSCL_MERGE_PENALTY` → `flow_status: "completed"`; `yes_restructure` on q13 → linear progress |
| F-132 | **OQ32-class follow-up resolved — data-driven M3 question→risk-column mapping.** New nullable `survey_questions.m3_field_mapping` JSONB (migration `202605150001`); `{target, column, coerce}` shape. `survey_service._project_m3_snapshots` rewritten to resolve per-question mapping (custom → `_M3_DEFAULT_MAPPINGS[code]` → skip) — byte-identical for the canonical `M3_HIST_*`/`M3_BEH_*`/`M3_STR_*` codes; the `M3_HIST_003` CSV special-case is now `coerce: "csv_list"`. `M3_MAPPING_COLUMNS` is derived from the M3 models at import; `validate_m3_mapping` rejects bad ones. New `M3FieldMapping` schema on `SurveyQuestionCreateIn`/`UpdateIn`/`AdminOut`; CRUD persists + validates it. Frontend `QuestionForm` gains an "M3 risk-signal mapping" card (module-3 only); new TS type, validator constants mirroring the backend, i18n. | 🟢 | session 15 (user: "Include OQ32 (m3_field_mapping)") | An M3 question with a custom `m3_field_mapping` projects into the chosen column; a canonical `M3_HIST_001` still projects to `missed_deadline_24mo`; the M3-mapping card appears only for module-3 questions; migration round-trips |
| F-133 | **Docs reconciliation + sidebar `/surveys` consolidation.** `SETUP/11` (M0-vs-"M1 Regulations" naming callout, M2 per-sector partition, `VAT_SSCL_MERGE_2026` example in §10.3, `m3_field_mapping`-driven §10.4, per-regulation-chain recipe in §10.9, new §10.10 on `linked_regulation_id`+`meta` breadcrumb, fixed stale §1 table), `SETUP/03` §5 ERD (M:N junction box, `is_baseline`/`m3_field_mapping`/cached-primary on `survey_questions`, populated `linked_regulation_id`/`meta`, `audit_log` columns, authorship columns, updated DDL-sources line), `BUILD_07/08/09` "Cross-module linkage" subsections, `research/module_{2_and_3,1_and_4}` VAT/SSCL notes. Sidebar SME nav: first survey entry now `/surveys` (the unified hub) with `nav.surveys` instead of `/regulations`+`nav.regulations`="Surveys hub" (Session-13 follow-up); `ClipboardList` icon; the three standalone `/surveys/{instrument}` entries kept, demoted. | 🟢 | session 15 (user: "update into this … the documentation … survey architecture") | No broken relative links; `SETUP/03` §5 shows the junction + `is_baseline` + `m3_field_mapping`; `SETUP/11` has the naming callout + the worked example + the §10.10 notes; `BUILD_08` has a "Cross-module linkage" subsection linking SETUP/11; the sidebar "Surveys" link goes to `/surveys`; `pnpm typecheck` clean |

| F-90 | **Module 4 data-collection research scaffolding.** Three new docs under `docs/research/`: `module_4_data_collection.md` (methodology — volumes, language balance, annotation workforce + budget, deduplication, time-normalised virality, PII, ethics), `module_4_perplexity_prompt.md` (a 7-task ready-to-paste research prompt that returns a structured registry of Sri Lankan FB/YT/X/Reddit sources + scam-SMS patterns + EN/SI/TA keywords), `module_4_sri_lankan_sources.md` (the registry skeleton — pre-filled with FactCheck.lk + canonical subreddits, TODO rows for Perplexity to populate). Resolves architecture-doc §7 open questions (annotation workforce, class balance, language balance, dedup, virality time-normalisation, ethics) without writing any runtime code. | 🟢 | session 9 (user request: "plan the M4 data collection + write the Perplexity prompt + create the related files") | Three files exist under `docs/research/`; Perplexity prompt is single-paste-friendly; registry skeleton has at least 5 pre-filled rows (FactCheck.lk + 4 subreddits) |

## Session 16 — Admin-manageable survey limits

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-134 | **`survey_limits` DB singleton + service.** New Alembic migration `202605170001_survey_limits.py` (`down_revision = "202605160001"`) creates the `survey_limits` table with seed row `(id=1, sme_limit=10, annotator_limit=0, admin_limit=0)`. New ORM model `SurveyLimits(Base, TimestampMixin)` in `app/models/survey_limits.py`. New Pydantic schemas `SurveyLimitsOut` (from_attributes, includes `updated_at`) and `SurveyLimitsPatchIn` (`Field(ge=0, le=10_000)`) in `app/schemas/survey_limits.py`. New service `app/services/survey_limits_service.py` with `get_limits(db)` (get-or-create singleton row) and `update_limits(db, *, sme_limit, annotator_limit, admin_limit, actor)`. | 🟢 | session 16 (user: move limits from env-vars to DB so admins can configure them live) | `alembic upgrade head` creates the table with the seed row; `get_limits` returns the row; `update_limits` persists new values; `alembic downgrade -1` → `upgrade head` round-trips |
| F-135 | **Admin survey-limits API + wiring.** New router `app/api/v1/admin_survey_limits.py` (behind `require_admin`) at `/api/v1/admin/survey-limits`: `GET ""` → `SurveyLimitsOut`; `PATCH ""` → `SurveyLimitsOut`. Mounted in `app/api/v1/router.py`. `app/api/v1/survey_sessions.py` updated: removed `get_settings` dependency, added `survey_limits_service.get_limits(db)` call in `start_session` and `my_history`; replaced `HTTPException(detail=dict)` with `raise ForbiddenError(...)` so the frontend API client receives `{"code":"forbidden","message":"..."}` at the top level (not nested under `"detail"`). `app/settings.py`: removed `SURVEY_LIMIT_SME`, `SURVEY_LIMIT_ANNOTATOR`, `SURVEY_LIMIT_ADMIN` fields. | 🟢 | session 16 | `GET /api/v1/admin/survey-limits` as admin → 200 with current limits; `PATCH` with `{"sme_limit": 5, "annotator_limit": 0, "admin_limit": 0}` → 200, `updated_by == admin.email`; as non-admin → 403; survey start for an SME at their limit → 403 `code: "forbidden"`; the error message is readable in the frontend toast |
| F-136 | **Admin settings frontend page.** New `"use client"` page at `frontend/app/(admin)/admin/settings/page.tsx`. Fetches token via `useEffect → fetch("/api/auth/token")`; uses `useQuery` with `queryKey: ["survey-limits", token]`; `useEffect` syncs the query result into a local `form` state. Three `<Input type="number" min={0}>` fields in a `sm:grid-cols-3` grid via a `LimitField` sub-component (label + "0 = unlimited" muted help text). `useMutation` with `onMutate` → `toast.loading`, `onSuccess` → `toast.dismiss + toast.success`, `onError` → `toast.dismiss + toast.error`. Shows `updated_by` + `updated_at` as metadata below the form. Typed API client `frontend/lib/api/admin-survey-limits.ts` (`AdminSurveyLimitsApi.{get,update}`, `SurveyLimitsOut`, `SurveyLimitsPatch`). | 🟢 | session 16 | Admin navigates to `/admin/settings` → form shows current limits; editing a number + Save → success toast + metadata shows the admin's email + timestamp; `0` in a field saves as unlimited |
| F-137 | **Sidebar nav + i18n for admin settings.** `frontend/components/layout/sidebar.tsx`: `Settings` icon imported from `lucide-react` (alphabetical order); `{ href: "/admin/settings", label: "nav.adminSettings", icon: Settings }` added to `ADMIN_DATA_ITEMS`. i18n: `"adminSettings": "Settings"` added to the `nav` section; top-level `adminSettings` object (`title`, `subtitle`, `surveyLimits.{heading,description,sme,annotator,admin,save,saved,updatedBy}`) added to `en.json`, `si.json`, and `ta.json` (English values used as placeholders for si/ta). | 🟢 | session 16 | Admin sidebar shows "Settings" entry below Activity Log; clicking it navigates to `/admin/settings`; i18n keys exist in all three locales at parity |

## Session 17 — Hotfix: survey_limits ProgrammingError resilience

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-138 | **`get_limits` catches `ProgrammingError` + rolls back session.** When `alembic upgrade head` hasn't been run, `db.get(SurveyLimits, 1)` raises SQLAlchemy `ProgrammingError` (`asyncpg.exceptions.UndefinedTableError`). PostgreSQL then aborts the transaction, so all subsequent DB calls in the same request fail with "current transaction is aborted" — causing every `POST /survey-sessions/start` to 500, including for admins and annotators who have unlimited access. Fix: `survey_limits_service.get_limits(db)` now wraps the `db.get` call in `try/except ProgrammingError`, calls `await db.rollback()` to restore the session, and returns a transient `SurveyLimits(sme_limit=10, annotator_limit=0, admin_limit=0)` with `_EPOCH` timestamps — no DB writes on this path. | 🟢 | session 17 (user reported 500 on `/survey-sessions/start` even for admin/annotator on a pre-migration DB) | Without running the migration: admin can start a survey (annotator_limit=0=unlimited); SME can start a survey (sme_limit=10, default). With the migration run: the DB row is used. `await db.rollback()` must be called before returning, otherwise subsequent calls in the same request fail. |

## Session 19 — M1/M2/M3/M4 per-module feature documentation sweep

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-140 | **M2/M3/M4 feature detail documentation sweep (SETUP + research).** Updated `APP_GUIDE.md` (M0→M1, /risk is implemented, feature matrix split into /risk/qa/verify rows); `SETUP/06` (m3_field_mapping column, m2_knowledge_scores/m3_compliance_history/m3_behavioural_signals table subsections, M4 schema note); `SETUP/04` (m2.py, m3.py, admin_translations.py, dashboard.py routers; m1_regulation_service, m2_service, m2_scoring, m2_linkage_rules, m3_service services); `SETUP/05` (/risk as implemented, vulnerability-form.tsx, /qa+/verify as stubs); `SETUP/07` (M2/M3/dashboard/translations endpoint rows); `SETUP/10` (BUILD_08 + BUILD_09 table rows corrected; §5 M3 recommendation updated); `SETUP/11` (M4 row in module table, M4 note in §11); `SETUP/12` (M4 note in /surveys/module/[id], /qa+/verify stub section expanded); `SETUP/13` (M0→M1 fixes, per_module_m4 stub note, SurveyWizard module-m0→m1); `research/13` (M4 implementation status callout); `research/15` (not yet built callout). | 🟢 | session 19 | No `(M0)` or `module-m0` or `/risk.*501` or `/risk.*coming soon` in any SETUP or APP_GUIDE file; M2/M3 table subsections present in SETUP/06; m2_scoring + m3_service in SETUP/04 |
| F-141 | **Tracker update — Session 19.** SESSIONS.md (this entry), FEATURES.md (F-140/F-141), CHANGES.md (session 19 entry), SETUP_COVERAGE.md (executive summary updated to 14 files through Session 19). | 🟢 | session 19 | All four tracker files contain Session-19 references |
| F-142 | **Domain-based documentation restructuring (Session 20).** Moved 66 docs from flat SETUP/BUILD_PLAN/research tracks into 5 domain folders (backend/frontend/infra/ml/shared) matching the codebase structure. Added 6 new README.md index files. Fixed 65+ broken cross-links. Initialized docs/ as a standalone git repo. | 🟢 | session 20 | `Test-Path docs/SETUP` → False; `Test-Path docs/backend/README.md` → True; zero broken .md links; `git -C docs log --oneline` shows initial commit |

## Session 21 — M1 doc cleanup (section-header conformance + folder-path consistency sweep)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-143 | **M1 doc cleanup: section-header conformance + folder-path consistency sweep (Session 21).** Two maintenance passes on `enigmatrix-docs/m1/` aligning older docs to the canonical layout in `13_M1_Folder_Structure_and_Implementation_Flow.md`: (1) renamed `## How to start (if you're writing a new doc)` → `## How to start building` in `15_M1_6_Docs_Folder_Guide.md` so all six 15_M1_N guides match the locked 6-section skeleton; (2) 22 path replacements across 8 m1 docs covering three patterns — missing `backend/` prefix, missing `m1/` subdir under `backend/app/tasks/`, obsolete module names (`pipeline/*.py` → `ml/m1/extraction/*.py`; `app/ml/*` → `ml/m1/model/*`; `app/services/gazette_extractor.py` → `ml/m1/extraction/text_extractors.py`). Recon agent flagged 15; verification grep flushed 7 more spillover (README block + `15_M1_2_Backend_Folder_Guide.md`). Zero code drift. | 🟢 | session 21 | (1) `grep -c '^## How to start building' enigmatrix-docs/m1/15_M1_[1-6]_*.md` reports 1 per file; (2) 11-regex stale-path grep across 60 m1 docs (excluding doc 13) returns zero matches; (3) `git diff --stat frontend/ backend/ ml/ scripts/ scraper/` empty |

## Session 18 — Documentation sync

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-139 | **Documentation sync across all affected MD files.** Updated `docs/research/13_Unified_Survey_Architecture.md` (§2 mode strings, §6.1 actual schema, §6.2/6.3 module numbers, §9 actual session API, §10 survey limits, §11 module number convention); updated `docs/SETUP/13_Unified_Survey_Configuration.md` (§2 session loop, new §§11-14); added `docs/SETUP/11_Survey_System.md` §11 Phase 3 section. Tracker files updated: CHANGES.md (sessions 16–18 entries), FEATURES.md (this block), SESSIONS.md (sessions 16–18), RESEARCH_BUILD_TRACKER.md (last-updated date), BUILD_PLAN_COVERAGE.md (admin console section), SETUP_COVERAGE.md (exec summary + 11/13 entries). | 🟢 | session 18 (user: "update all the changes in the tracker folder files … as with the relevant information and also upgrade all the documentation files") | All affected MD files reflect the current codebase state; no incorrect mode strings, wrong schema columns, or stale endpoint paths remain in the updated docs |

---

## Session 22 — Backend seed refactor: demo dashboards populated on `make seed`

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-144 | **Backend seed refactor — demo dashboards populated on `make seed`.** Two new seed scripts close the empty-dashboard gap on a fresh DB. `seed_lookups.py` (new) splits the 9 `regulatory_domains` + 12 `sectors` seeding into a standalone idempotent script (`pg_insert(...).on_conflict_do_nothing()`), run *before* `seed_regulations` so the FK targets always exist up-front (previously they lived inside `seed_m23_questions.main()` which ran later). `seed_demo_responses.py` (new) creates 6 demo SMEs across sectors (retail, manufacturing, it, tourism, construction, food_beverage; `demo.<sector>@enigmatrix.lk` / `demo12345678`), each with full `SMEProfile`. Walks each through M1 awareness (12 q) + M2 knowledge (44 q) + M3 vulnerability (one `M3ComplianceHistory` + one `M3BehaviouralSignals` snapshot). M2 answers are scored via `m2_scoring.score_answer` and aggregated via `m2_service.recompute_knowledge_score`. Per-SME `skill ∈ [0.35, 0.85]` + `compliance_profile ∈ {strong, average, weak}` produce a 34–86 % M2 distribution with correlated compliance flags; deterministic across runs via `random.Random(hash(email))`. Idempotent: `_has_responses` short-circuits any SME with existing `survey_responses` rows. `seed_dev.py` modified to call both in the right order. | 🟢 | session 22 (admin dashboards rendered empty after fresh `make seed`) | `make seed` on a fresh DB → 336 / 6 / 6 / 6 / 8 across `survey_responses` / `m2_knowledge_scores` / `m3_compliance_history` / `m3_behavioural_signals` / `sme_profiles`; second `make seed` is a no-op; `/admin/m2/scores`, `/admin/m3/risk-signals`, `/admin/surveys/awareness/responses` render demo data |

## Session 23 — M1 Phase 2 Step 2a: Scrapy gazette spider MVP

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-145 | **M1 Phase 2 Step 2a — Scrapy gazette spider MVP (Session 23).** First concrete code-shipping step from [16_M1_Development_Roadmap.md](../m1/16_M1_Development_Roadmap.md). Scrapy project skeleton at `enigmatrix-backend/scraper/` (scrapy.cfg + __init__ + items + settings + spiders/ + pipelines) downloads gazette PDFs from `documents.gov.lk`'s extraordinary-gazette listing and writes one `m1_regulations` row per gazette with `status='ingested'`. Two-stage pipeline: `PDFDownloadPipeline` (writes to `STORAGE_LOCAL_PATH/m1/raw/<slug>.pdf` + SHA-256, idempotent skip if file exists) → `M1RegulationsInsertPipeline` (INSERT row, raises `DropItem` on UNIQUE conflict). Migration `202605220001` adds `status` VARCHAR(20) NOT NULL DEFAULT 'ingested' (CHECK over 7 enum values), `raw_pdf_path` VARCHAR(500), `gazette_number` VARCHAR(50) UNIQUE. ORM model `regulation.py` gains matching `Mapped[]` columns. **Celery dispatch deferred to Step 2b** — pipeline logs `TODO: enqueue extract_gazette(<reg_id>)`. Dep `scrapy>=2.11,<3` added; **user runs `uv sync` before testing**. 4 integration tests (sync parse test + 3 async pipeline tests via testcontainer Postgres) avoid `CrawlerProcess` (Twisted-reactor singleton); real-network smoke check documented in SESSIONS Session 23. Build root = `enigmatrix-backend/scraper/` (filesystem truth); doc 13's monorepo-root canon stays a future-state target. | 🟢 | session 23 | (1) `cd enigmatrix-backend && uv sync && uv run alembic upgrade head && uv run pytest app/tests/integration/test_gazette_spider.py` passes all 4 tests; (2) manual smoke `uv run scrapy crawl gazette_spider` against `documents.gov.lk/view/egz/egz_2026.html` produces ≥ 1 PDF in `storage/m1/raw/` + ≥ 1 new `m1_regulations` row with `status='ingested'`; (3) `git diff --stat enigmatrix-frontend/ enigmatrix-ml/ enigmatrix-infrastructure/ enigmatrix-docs/m1/` empty (no drift) |

## Session 56 — M1 raw PDF bulk extraction and classification (data-population workstream)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-199 | **M1 raw PDF bulk extraction + classification — 800 PDFs across 11 batches (Session 56).** Cowork-mode data-population workstream operating directly on the raw Sri Lankan extraordinary gazette PDF corpus at `C:\sme\03-Data-Sources\m1\raw\pdf\`. **Not** part of the production Celery/FastAPI extraction stack (BUILD_07) — this is a one-shot seed-data exercise using `pdfplumber` (+ `pdftotext` fallback for files exceeding the bash 45s timeout) and a regex/heuristic statute classifier (`outputs/build_csv.py`). **Phase A (batches 5–8, 203 PDFs):** continued the 203-PDF backlog from the prior Cowork session, closing with batch 8 (53 PDFs spanning pages 7–127, incl. `2479_36.pdf` at 127 pages and `2482_06.pdf` at 65 pages). Wrote `m1_regulations_next50_batch{5..8}.csv` + `m1_extraction_tracker_v11_addendum.csv` (203 rows) to `C:\sme\03-Data-Sources\m1\raw\csv\`. **Phase B (batches 9–11, 145 PDFs):** user dropped 145 fresh PDFs into the raw folder bringing the on-disk total from 655 → 800; Cowork detected them autonomously by diffing `os.listdir(pdf_dir)` against `v10 ∪ v11_addendum`, sorted smallest-page-count-first, and ran three more batches without re-prompting (per `"i dont give the input yu sutomatically makethm 50 each"`). Batch 9 (50 PDFs, 1–4 pages, mixed domains); batch 10 (50 PDFs, 4–5 pages, gazette-2483 customs day — 72% Customs Ordinance Ch 235 + unscoped); batch 11 (45 PDFs, 5–30 pages, Title Registration + Customs heavy). Wrote `m1_regulations_next50_batch{9..11}.csv` + `m1_extraction_tracker_v12_addendum.csv` (145 rows). **Classifier extensions made permanent in `outputs/build_csv.py`:** `PREFIX_FALLBACK` extended for gazette prefixes 2483–2486; new blocks for Pradeshiya Sabha Act No. 15 of 1987 (English + broken-Sinhala + Tamil + doubled-Tamil), Sri Lanka Electricity (Amendment) Act No. 36 of 2024 (separate from existing 2009 Act block), Sri Lanka Export Development Act No. 40 of 1979 (Section 14 tariff orders), Companies Act No. 07 of 2007 (Section 130A–I + 527 regulations); broadened Armed Services Long Service Medal pattern to accept newline-between-Service-and-Medal + typo `Sevices` + singular `Service`; added doubled-Tamil variants (`காாணிி எடுத்தற்`) to Land Acquisition Act Ch 460. **Deliverable shape note:** the CSVs landed in the user's `csv\` folder are ASCII-compacted (multilingual `title_si`/`title_ta`/`summary_si`/`summary_ta` blanked, `raw_text` set to `[see raw_pdf_path]`) to fit the file-tool single-call write budget; full multilingual + raw_text CSVs remain in the Cowork outputs dir at `/sessions/.../mnt/outputs/m1_regulations_next50_batchN.csv`. **Full 800-PDF coverage now:** `m1_regulations_v6.csv` (261 from earlier visual-read phase) + batches 1–4 (191) + tracker carry-overs covering batches 5–11 (348) = 800. Tracker logical-total = `v10.csv` (452) + `v11_addendum.csv` (203) + `v12_addendum.csv` (145) = 800. | 🟢 | session 56 (user: "now i have 800 pdf and also alraedt extracted 655 pdf … as per the m1 extration tacker make htem pahesese by extrat them the pending pdf s like the prevous 50 by 50 pdf … i dont give the input yu sutomatically makethm 50 each") | (1) Every PDF in `C:\sme\03-Data-Sources\m1\raw\pdf\` appears in some tracker file (v10 + v11_addendum + v12_addendum, total 800 rows); (2) `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch{5..11}.csv` exist with the 39-column `m1_regulations` schema; (3) no operator re-prompting between batches; (4) classifier extensions persisted in `outputs/build_csv.py` so future PDFs benefit; (5) all 4 tracker files reference Session 56 / F-199. |

## Session 51 — Completeness verify endpoint hardening (timeout, CORS, WS URL, state reset)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-178 | **Completeness verify endpoint hardening (Session 51).** Eight bugs fixed after the F-177 feature shipped: (1) `/api/v1` prefix missing in `lib/api/m1-completeness.ts` (3 URLs) → 404; (2) `httpx.ReadTimeout` on slow upstream → `httpx.Timeout(connect=10/read=120/write=10/pool=10)` + `_HTTP_RETRY_ATTEMPTS=2` retry-on-Timeout loop; (3) 500 without CORS headers (Starlette `ServerErrorMiddleware` sits above CORS) → catch-all `except Exception` in `_run_verify` raising `HTTPException(500, detail=f"Verification failed: {type(exc).__name__}: {first_line[:240]}")` plus `httpx.TimeoutException → 504` and `httpx.HTTPError → 502`; (4) WS dialled `ws://window.location.host` (Next dev port) → `NEXT_PUBLIC_API_BASE_URL` + `.replace(/^http/i,"ws")`; (5) WS reconnect storm → backend sends `{type:"done", terminal:true}` then `ws.close(code=1000)`; client respects either signal; (6) `AttributeError: 'URL' object has no attribute 'human_repr'` (httpx<0.27) → `resp.url.human_repr()` → `str(resp.url)`; (7) uvicorn dead between bugs → venv-activation recommended; (8) panel stale state on `taskId` switch → `useEffect([taskId])` resets `result`/`refetchResult`/`autoRan`. Belt-and-braces approach on WS termination + python-atomic-write protocol throughout. | 🟢 | session 51 (user-reported issues during integration of F-177) | (1) Verify on slow upstream completes within ~120s + retry, returns either result or clean 504 with CORS headers; (2) WebSocket connects once on terminal task, closes cleanly with code 1000, client stops trying; (3) switching historical runs shows fresh panel state immediately; (4) unexpected exceptions surface as `Verification failed: <ExceptionClass>: <msg>` in panel; (5) all 4 tracker files reference Session 51 / F-178. |

## Session 50 — Completeness check + re-fetch + spider EN→SI→TA fallback

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-177 | **Completeness check + re-fetch + spider EN→SI→TA fallback (Session 50).** User manually compared website list vs DB across two months — found 12% miss rate Feb 2026 (25 of 207) and 40% miss rate Jan 2026 (103 of 260, including 10-day Jan 19–28 blackout). Separately the spider silently dropped Sinhala/Tamil-only gazettes. Planning questions confirmed: fallback EN→SI→TA (no schema change), manual + auto verify, table with per-row + bulk re-fetch, "unparsed" pills. New backend service `app/services/m1_completeness_check.py` (265 lines, httpx + lxml listing diff). New endpoints module `app/api/v1/m1_completeness.py` (382 lines, three routes: `POST /verify/{task_id}`, `POST /verify`, `POST /refetch-missing`; Pydantic schemas `MissingItemOut`/`VerifyResultOut`/`VerifyByScopeIn`/`RefetchMissingIn`/`RefetchOneOut`/`RefetchMissingOut`; refetch mirrors `M1RegulationsInsertPipeline._insert_rows` then enqueues `extract_gazette.delay(...)`). Registered under `prefix="/admin/m1/extraction"`. Frontend `lib/api/m1-completeness.ts` typed client + `components/m1-extraction/missing-gazettes-panel.tsx` (372 lines, auto-trigger on terminal + manual button + table with `unparsed` pills + per-row & bulk re-fetch + refetch summary). Wired into source extraction page above `<ExtractionSummaryCard>`. Spider `gazette_spider.py` rewritten with `_LANG_PRIORITY = {"en":0,"si":1,"ta":2}` + `_language_for(url)` helper + `best_by_number` buffer-then-pick → one yield per gazette. | 🟢 | session 50 (user: "find them and fix them … check them and for the … chanegs also upgrade them") | (1) Click "Verify completeness" on a healthy upstream returns missing items within ~5s; (2) per-row "Re-fetch" inserts new `m1_regulations` + `m1_gazette_items` rows, enqueues `extract_gazette`, returns one of `queued`/`duplicate`/`missing_download_url`/`error`; (3) bulk "Re-fetch all" equivalent for all items with `download_url`; (4) spider ingests Sinhala-only / Tamil-only gazettes that were previously dropped, English-priority preserved when all three exist; (5) Session-23 integration test still passes; (6) all 4 tracker files reference Session 50 / F-177. |

## Session 49 — Live count polling + SpiderResultCard polish

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-175 | **Live count polling for `summaryForResume` (Session 49).** Page-level `summaryForResume` react-query had `staleTime: 8_000` but no `refetchInterval` — once spider's Celery task hit SUCCESS counts froze at "100 extracted · 0 preprocessed" even while extract_gazette + preprocess_gazette were still processing 100+ PDFs (manual reload was needed). Added `refetchInterval` callback that polls every 5s while any row is in flight; stops automatically when `ingested === 0 && extracted === 0 && preprocessed + extraction_failed >= in_scope`. Polling is opt-in per query so other panels stay unaffected. Page edit via python-atomic-write per Session-48 protocol. | 🟢 | session 49 (user: "when extraction started its crawling and extracting but in the frontend its not updated in real time when after reload its showing updated") | (1) After Celery SUCCESS, counts continue to update every 5s without manual reload; (2) polling stops once `ingested + extracted == 0` AND `preprocessed + failed >= in_scope`; (3) `PipelineRunStatusCard` phase derivation now reflects accurate counts live. |
| F-176 | **`SpiderResultCard` polished display (Session 49).** Replaced raw `<pre>{JSON.stringify(status.data.result, null, 2)}</pre>` block on source extraction page with new `components/m1-extraction/spider-result-card.tsx` (196 lines): status pill (Success/Failed driven by `status === "ok" && returncode === "0"`), 2×2 labelled grid (Source mono chip / Spider mono / Scope parsed `YYYY-MM-DD..YYYY-MM-DD` → `Date.toLocaleDateString` per locale / Return code), "Other fields" fallthrough for forward-compatibility with future backend keys. Works for every source (EGZ/GZ/BILL/ACT) because the route is shared. | 🟢 | session 49 (user: "in beautiful display the {…} in all the sources of extraction") | (1) Spider result rendered as polished card on all four source pages; (2) success → green pill + green border-left; non-zero returncode → red Failed pill + red border-left; (3) parsed scope reads "1 Feb 2026 → 28 Feb 2026" via locale; (4) unknown future fields appear under "Other fields" mono list. |

## Session 48 — Vault recovery from Write-tool truncation + backend IndentationError + NUL bytes

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-178 | **Vault recovery from Write-tool truncation + extract_gazette IndentationError + 755 NUL bytes (Session 48 incident).** `npm run build` reported walls of TS17008 / TS1005 / TS1010 / TS1002 errors. Hex dumps revealed mid-content truncation across 13 frontend files (`providers.tsx` 16 lines mid-comment; `next.config.mjs` 15 lines ending `swcMinify: t`; dashboard 359 lines mid-string; login 192 lines mid-className; sidebar 899 of 960; etc). Write/Edit tools had returned "file created successfully" on every truncating call. Backend `extract_gazette.py` had 755 trailing NUL bytes + IndentationError at line 212 (misplaced `_emit` block at wrong indent). Recovery: (1) emitted PowerShell `git restore` command for 13 frontend files; (2) removed misplaced `_emit` block at lines 200-211 in `extract_gazette.py`; stripped 755 NUL bytes via `data.rstrip(b'\x00').rstrip()` (15193 bytes, ends `    return body\n`); confirmed `ast.parse` clean; (3) swept sibling backend/ML files — all 0 nulls; (4) adopted python-atomic-write protocol for files >~25KB with verify-after-write (`wc -l` + `tail -3` + null-byte check + `ast.parse` for Python). Pattern of truncation correlates strongly with long writes containing em-dashes / special quotes / emoji. Separately user hit `ModuleNotFoundError: fastapi` running uvicorn from `/usr/lib/python3.14` (system Python) — pointed at `source /mnt/c/Reasearch/xyz/.venv/bin/activate && uvicorn app.main:app --reload`. | 🟢 | session 48 (npm build failure cascade + user-reported "ModuleNotFoundError") | (1) All 13 frontend files restored via `git restore`; `git status` clean for those paths; (2) `python3 -c "import ast; ast.parse(open('extract_gazette.py').read())"` exits 0; (3) sibling backend/ML files report 0 NUL bytes; (4) python-atomic-write protocol adopted for the remainder of the session; (5) Session-48 entry in SETUP_COVERAGE.md documents the Edit-tool truncation policy. |

## Session 47 — Frontend perf overhaul (dashboard streaming + login handoff + bundle/query tuning + recharts code-split)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-174 | **Frontend perf overhaul (Session 47).** User reported >10s login → dashboard delay. Three compounding causes: (1) login handler 4 sequential calls (`AuthApi.login()` → `/api/auth/establish` → `/api/auth/me` → `router.replace()` → `router.refresh()`); (2) dashboard server component awaited `Promise.all` of 5 backend fetches before returning any HTML; (3) react-query `refetchOnWindowFocus/Reconnect/Mount: true` caused redundant fetches per navigation. Rewrote dashboard with Suspense streaming (synchronous shell + 3 async subsections each in `<Suspense>`). `/api/auth/establish` now base64url-decodes JWT payload + returns role inline; login page drops `/me` and `router.refresh()`. `next.config.mjs` adds `productionBrowserSourceMaps:false / swcMinify:true / compress:true / output:"standalone" / poweredByHeader:false` + expanded `optimizePackageImports` (lucide-react/date-fns/recharts/framer-motion/6 @radix-ui/*) + AVIF/WebP images config with `documents.gov.lk` + `enigmatrix-backend.vercel.app` `remotePatterns`. QueryClient defaults: `staleTime: 60_000 / gcTime: 10*60_000 / refetchOn*: false / retry:1 / retryDelay:1_000`; mutations `retry:0`. Recharts components in `m1/pipeline/page.tsx` wrapped in `next/dynamic({ssr:false, loading: () => <Skeleton/>})`. Build collision `import dynamic from "next/dynamic"` vs `export const dynamic = "force-dynamic"` → renamed to `nextDynamic`. | 🟢 | session 47 (user: "when logged in getting more than 10sec to goes to the dashboard") | (1) Login → dashboard shell paint < 1s; (2) login network sequence reduced from 4 calls to 2 (no `/me`, no `refresh`); (3) back-navigation pops instantly from react-query cache; (4) m1 pipeline page first interactive paint faster (recharts deferred); (5) all 4 tracker files reference Session 47 / F-174. |

## Session 46 — Extraction running UX upgrade (PipelineRunStatusCard + WS sub-step scaffold + ML progress + backend Celery emits)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-173 | **Extraction running UX upgrade (Session 46).** User reported page said "Completed SUCCESS" while 2 rows still at `ingested` and 154 not yet preprocessed. Planning questions confirmed all three detail levels (per-step + per-PDF + sub-step), WebSocket transport, end-to-end completion, work across frontend + backend + ML. New frontend `components/m1-extraction/pipeline-run-status-card.tsx` (481 lines, exports `derivePipelinePhase(celeryStatus, counts)` returning one of 7 phases; three-phase tracker; live sub-step pill). New frontend `lib/hooks/useExtractionLiveFeed.ts` (141 lines, WebSocket client with exponential backoff 1s → 30s). Wired both into source extraction page replacing single-line Celery banner. New ML `enigmatrix-ml/m1/extraction/progress.py` (114 lines, canonical `ExtractionPhase` literal: `classifying | extracting_text | ocr | language_detection | wijesekara | preprocessing | indexing`; `PHASE_LABELS` dict; `emit(cb, ...)` helper). Re-exported from ML `__init__.py`: `PHASE_LABELS`, `ExtractionPhase`, `ProgressCallback`, `SubStepEvent`, `emit_progress`. New backend `app/services/m1_extraction_live_feed.py` (84 lines, sync `redis-py` publisher reusing `CELERY_BROKER_URL`). Modified `app/tasks/m1/extract_gazette.py` — `_emit(task_self, ...)` helper calls `self.update_state(state="PROGRESS", meta=payload)` AND `publish_substep(...)`; plumbed `task_self` through `_extract_gazette_async` → `_extract_gazette_body`; hooked at three phase boundaries (classify, extract/OCR, indexing). New backend `app/api/v1/m1_extraction_ws.py` (180 lines, FastAPI `@router.websocket("/extraction/{task_id}")` with token query-param auth via `decode_token(token, expected_kind="access")`; subscribes to source-scoped Redis pub/sub channel; three concurrent loops: pubsub pump / 25s heartbeat / 2s Celery terminal watcher). Registered WS router in `app/api/v1/router.py`. Source-scoped pub/sub channel is a pragmatic scaffold; per-task_id routing remains a follow-up. | 🟢 | session 46 (user: "show the extraction running … its steps … what are these executing of in each step … upgrade as some question for the planning and write the code") | (1) Page says "Pipeline complete" only when every in-scope row reaches `preprocessed`; (2) "Extracting PDFs · 154/156" shown while extract_gazette processing; (3) WS connects (when wired) and surfaces "Running OCR (Tesseract) — 2486/29" as a pill; (4) page works fully without WS (graceful degradation); (5) all 4 tracker files reference Session 46 / F-173. |

## Session 45 — M1 pipeline page responsiveness v2 (funnel overflow + sticky sidebar + collapsed footer)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-172 | **M1 pipeline page responsiveness v2 (Session 45).** Four screenshot-reported issues on `/admin/m1/pipeline`: (1) funnel chart bars overflowing card by 100× — `widthPct = (value / counts[TIERS[0].key]) * 100` divided by *ingested*; with `ingested=1, extracted=101, preprocessed=102` math produced 10,100% widths; (2) chunky native scrollbar in sidebar — earlier `* {scrollbar:none}` scoped to `html,body` only exposed sidebar's `overflow-y-auto`; (3) sidebar scrolled with page — earlier `overflow-x-hidden` on admin layout established a containing block disabling `position:sticky` on descendants; (4) collapsed sidebar footer cramped. Funnel uses `maxAcrossTiers = Math.max(1, ...TIERS.map(t => counts[t.key] ?? 0))` denominator + clamps `widthPct` to `[0,100]` + adds `overflow-hidden` + `bg-muted/30` on track parent. Sidebar gets `scrollbar-hide` on inner `<nav>` + `mx-1.5` collapsed margin + `p-1.5` collapsed bottom padding; `UserRow` rewritten — collapsed = vertical stack with Tooltip-wrapped avatar/logout, expanded = horizontal hover row, avatar gets `ring-1 ring-border/60`. Admin layout switches `overflow-x-hidden` → `overflow-x-clip` on both flex container + main column. | 🟢 | session 45 (user-shared screenshots) | (1) Funnel bars stay inside card regardless of count distribution; (2) sidebar sits still as page scrolls; (3) no native scrollbar visible in sidebar; (4) collapsed avatar + logout stacked vertically with hover tooltips; (5) `npm run build` clean. |

## Session 44 — Theme polish (view-transition animation smoothness + disable system tracking)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-171 | **Theme polish (Session 44).** Two unrelated theme issues. (1) View-transition reveal felt twitchy — `startViewTransition` clip-path used `ease-in-out` over 420 ms; universal `* { transition: ... }` rule competed underneath. (2) App auto-changed theme when OS flipped colour scheme. `theme-toggle.tsx` easing → `cubic-bezier(0.22, 1, 0.36, 1)` (easeOutQuint), duration 420ms → 600ms; subtle scale (1.015→1) + opacity (0.92→1) on new layer; scale (1→0.985) + opacity (1→0.6) on old layer; flips `theme-transitioning` class on `<html>`; Sun/Moon icon transitions synced to 500 ms. `globals.css` adds GPU promotion (`will-change`, `backface-visibility`, `transform-origin`) on `::view-transition-old/new(root)` + `html.theme-transitioning *` rule suppressing universal transitions during the view-transition window. `providers.tsx` `ThemeProvider`: `defaultTheme="system"` → `"light"`, `enableSystem={false}`, `storageKey="enigmatrix-theme"`. | 🟢 | session 44 (user: "fix this issues — its automatically changing theme switching … and theme animation smoother") | (1) Toggle reveal animates smoothly ~600ms, no flicker; (2) OS auto-flips colour scheme → app theme unchanged; (3) preference persists in localStorage under `enigmatrix-theme`; (4) `setTheme` only invoked from `theme-toggle.tsx` (verified via grep). |

## Session 43 — Spider runtime fix: close spider when scope exhausted

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-170 | **Spider runtime fix — close spider when scope exhausted (Session 43).** User reported the Gazette Extraction trigger task stayed in `STARTED` for 10+ minutes after all 16 in-scope regulations finished preprocessing (Run summary pill: `1m 21s` of actual work). **Root cause:** the Celery `run_gazette_spider` task waits for the scrapy subprocess to exit. Scrapy's Twisted reactor only shuts down when its queue is empty AND in-flight requests are retired. For a narrow scope (e.g. Jan 4–5 2026) on `egz_2026.html` (500+ PDF anchors), scrapy keeps walking the rest of the page after the last in-scope item, then waits for the reactor to drain under `DOWNLOAD_DELAY=2` + `AUTOTHROTTLE` — adding minutes of dead-time tail. **Fix (two layers):** (a) **Early-exit** — documents.gov.lk lists gazettes in descending date order; once `parse()` sees a row whose date is BEFORE `date_from`, no more in-scope items can appear. Spider now calls `self.crawler.engine.close_spider(self, "scope_exhausted")` and returns. (b) **Safety net** — `CLOSESPIDER_TIMEOUT_NO_ITEM=60` in `custom_settings` exits if 60s pass without a new item. (c) **Test coverage** — new `test_spider_closes_when_past_date_from` builds a 4-row descending-date fixture (May 15, Feb 10, Jan 5, Jan 3) with `date_from=2024-01-04, date_to=2024-01-05`; asserts only Jan 5 yields + `crawler.engine.close_spider` is called once with reason `"scope_exhausted"`. Uses `MagicMock` for `spider.crawler` (existing pattern, no reactor boot). Trims a Jan 4–5 crawl from ~10 min to ~1–2 min. Beat-scheduled invocation (no args) preserved: `date_from is None` → early-exit condition never met → behaviour unchanged. | 🟢 | session 43 (user: "already the regulation extracted but the task is still as running showing — why?") | (1) `cd enigmatrix-backend && uv run pytest app/tests/unit/test_gazette_scraper_task.py app/tests/integration/test_gazette_spider.py app/tests/integration/test_celery_extract_gazette.py app/tests/integration/test_celery_preprocess_gazette.py -q` → 22 passed (was 21; +1 new for F-170); (2) restart Celery worker (`Ctrl+C` → `uv run celery -A app.celery_config worker -l info --concurrency=2`); (3) re-trigger Jan 4 → Jan 5 2026 → worker log emits `closing spider (scope_exhausted): hit gazette … with date=… earlier than date_from=2024-01-04` shortly after the last in-scope item + `run_gazette_spider: scrapy completed cleanly` within ~1–2 min total (was: 10+ min); (4) FE status panel transitions to SUCCESS promptly after the last preprocessed row. |

## Session 42 — Date-range picker for Gazette Extraction

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-169 | **Date-range picker for Gazette Extraction (FE + BE, Session 42).** Replaces F-161/F-164's year + month-range Comboboxes with a beautiful calendar-based date-range picker so admins can scope a crawl to specific days (e.g. "Jan 15, 2026 → Feb 28, 2026"). **Frontend new components (3):** `components/ui/popover.tsx` (shadcn wrapper over `@radix-ui/react-popover`); `components/ui/calendar.tsx` (themed wrapper over `react-day-picker`'s `DayPicker` with primary-tinted range visualisation); `components/m1-extraction/date-range-picker.tsx` (button-triggered Popover with a 2-month side-by-side `<Calendar mode="range">` + 7 quick-pick chips: Last 7 days, Last 30 days, This year, Q1, Q2, Q3, Q4 + Clear/Apply footer). New deps: `react-day-picker@^8.10.1` + `@radix-ui/react-popover@^1.1.2`. v8 pin because v9 requires React 19 (project is on React 18). **Frontend rewires (5):** extraction page form swaps 3 Comboboxes for 1 DateRangePicker; new `rangeValid()` validator enforces same-year + `[2010, today]`; trigger.mutate sends `{ date_from, date_to }` ISO strings; status card "Scope:" label formats as "Jan 15 – Feb 28, 2026" via `date-fns/format`; API client + history validator + RecentTriggersBar pill labels all switch to the new shape. **Backend (4 + 2 tests):** `GazetteExtractionTriggerIn` Pydantic schema gains a `model_validator` enforcing same-year + within bounds + `date_from ≤ date_to` (HTTP 400 on violation). `/trigger` / `/progress` / `/summary` endpoints all take `date_from` + `date_to` instead of `year` + `month_start` + `month_end`. `run_gazette_spider(date_from_iso, date_to_iso)` Celery task signature + new `_parse_iso_date()` + rewritten `_validate_scope()` raise `ValueError` on bad input. Spider's `__init__` accepts `date_from` + `date_to`; derives year from `date_from` to build the canonical listing URL; `_in_scope()` does lexicographic ISO-string compare (simpler + equivalent to date compare). Service's `_extraction_scope_filter()` switched from `func.extract(year/month, ...)` to `M1Regulation.gazette_published_date.between(date_from, date_to)` — index-friendly perf win. 2 test files updated/added: 4 unit tests (forwards date strings, rejects inverted/cross-year/pre-2010), 3 integration tests (start_url from year, date-range drops out-of-scope, day-level boundary inclusion). 21 passed total (was 19). Breaking API shape change is admin-only with a single consumer (this same lap's FE); old-shape localStorage history silently dropped by validator filter. | 🟢 | session 42 (user: "even with the month and date as the day also be selected with the range … beautiful create the relevant component … advance and best user experience") | (1) `pnpm install` picks up new deps cleanly; (2) `npx tsc --noEmit` clean for all new + modified frontend files (only pre-existing `trace-timeline.tsx::sky` unrelated error remains); (3) `cd enigmatrix-backend && uv run pytest app/tests/unit/test_gazette_scraper_task.py app/tests/integration/test_gazette_spider.py app/tests/integration/test_celery_extract_gazette.py app/tests/integration/test_celery_preprocess_gazette.py -q` → 21 passed; (4) date-range picker UX: default range = current year Jan 1 → Dec 31; clicking trigger opens 2-month calendar with `mode="range"`; quick chips populate the range; Apply commits + Clear resets; cross-year selection shows destructive-toned validation hint; disables submit; (5) `curl -X POST /trigger -d '{"date_from":"2024-12-15","date_to":"2025-01-15"}'` → 400 "same calendar year"; `-d '{"date_from":"1999-01-01","date_to":"1999-12-31"}'` → 400 "year must be 2010 or later"; (6) manual smoke: pick 2026-01-15 → 2026-02-28 → Start extraction → POST 202 with task_id → status panel cycles PENDING → STARTED → SUCCESS → `/admin/m1/pipeline/recent` shows new rows in that window; (7) `localStorage.getItem('m1.gazette.recent_triggers')` returns valid JSON with the new shape. |

## Session 41 — Pipeline-flow cumulative-count display + Aiven cloud-Postgres pool sizing

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-167 | **Pipeline-flow tiles surface a cumulative "reached" count (Session 41).** User-reported confusion: after F-161's spider crawl preprocessed 448 rows cleanly, the `/admin/m1/pipeline` overview tiles showed "2a Scrapy: 0 rows", "2b Celery extract: 0 rows", "2c PDF classifier: 0 rows", "2d Language detection: 0 rows" — only 2e + 2f showed 448. Not a bug: each tile's `liveCount` shows rows **currently at** that status, and once a row completes preprocessing it sits at `'preprocessed'` permanently. Fix is FE-only: surface a **cumulative "reached" count** as the tile's headline number, derived from the existing `status_counts` response. New `reachedFromStatuses: PipelineStatusKey[]` field on each `PipelineStep` (in `lib/m1-pipeline/steps.ts`) lists the statuses that count as "this step has been reached or passed" — 2a entry covers all 8 statuses; 2b/c/d (extracted) covers extracted + preprocessed + classified + summarized + alerted + archived; 2e/f (preprocessed) covers preprocessed + classified + summarized + alerted + archived. `<PipelineFlowDiagram>` reduces `step.reachedFromStatuses` over `counts` to get `reachedCount` per tile and passes it as a new prop. `<StepTile>` swaps the bottom-row layout: reached count is the headline big-number; "N at <counterStatus>" is the muted secondary line. Tiles now read e.g. "2a Scrapy: **452** reached · 0 at ingested" — answering "did scrapy run?" at a glance. `extraction_failed` deliberately excluded from 2b/c/d's reached bucket (the failed-chip in the legend shows it separately). No backend or schema change. | 🟢 | session 41 (user: "even no extraction happened the relevant scrapy happened then only the relevant preprocessing… find them as what are the issues and fix them") | (1) Visit `/admin/m1/pipeline` after a successful extraction → 6 tiles each show a "Reached" headline number that equals the sum of rows at that step or beyond; (2) secondary muted line reads "N at <step.counterStatus>"; (3) the "currently at status" numbers + legend chips still match the existing global counts; (4) `npx tsc --noEmit` clean for the 3 modified files (only pre-existing `trace-timeline.tsx::sky` unrelated error remains); (5) on a fresh empty DB, all tiles read "0 reached · 0 at <status>". |
| F-168 | **Aiven cloud-Postgres pool sizing fix (Session 41).** `asyncpg.exceptions.TimeoutError` was firing on `/api/v1/admin/m1/extraction/summary` after 60-s asyncpg `connect()` timeouts. **Critical context the user supplied mid-session:** `DATABASE_URL` actually points at Aiven cloud Postgres (`*.aivencloud.com:16188/defaultdb?sslmode=require`), NOT the local docker container. Session 40's `max_connections=200` docker flag is therefore moot for this user — their app talks to Aiven, where entry-tier plans cap total connections at ~20-25 globally. With Celery's 8 prefork workers × Session 40's `pool_size=2, max_overflow=3` = 40 conn slots from Celery + uvicorn × 5 = 45 — comfortably over Aiven's hard cap. Queued asyncpg `connect()` calls then timed out at the 60-s default. **Fix:** drop `app/db/session.py` further to `pool_size=1, max_overflow=2, pool_timeout=10, pool_recycle=300` (3 max per worker process; fail-fast at 10 s). Document the recommended Celery startup flag: `uv run celery -A app.celery_config worker -l info --concurrency=2`. Math: 2 workers × 3 + uvicorn × 3 = 9 conns peak — fits 20-conn Aiven budget with 11-conn headroom. If the user moves to local docker Postgres only, they can raise back to `(pool_size=2, max_overflow=3)` per the inline comment in session.py. Runtime doc gains a ⚠️ "Are you on Aiven cloud Postgres? (read first)" callout at the top + a new troubleshooting row for the TimeoutError symptom. Also documented that `DB_SSL=false` is fine even with the URL's `?sslmode=require` — SQLAlchemy's asyncpg dialect translates the URL param into the asyncpg `ssl=` arg automatically, so SSL is still negotiated; the `DB_SSL` env flag is a redundant legacy. | 🟢 | session 41 | (1) After restarting Celery worker (with `--concurrency=2`) and uvicorn: `python -c "from app.db.session import engine; print(engine.pool.size(), engine.pool._max_overflow, engine.pool._timeout)"` → `1 2 10`; (2) trigger an extraction → `/summary` endpoint returns 200 within ~1 s (was: 500 after 60-s timeout); (3) worker logs show no `TimeoutError` / `TooManyConnectionsError`; (4) `cd enigmatrix-backend && uv run pytest app/tests -q` → 19 passed (no regressions; testcontainer Postgres uses its own pool, change is invisible to tests). |

## Session 40 — M1 Pipeline sub-nav fix + Postgres connection-limit hotfix

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-165 | **M1 Pipeline in-page sub-nav: Gazette Extraction entry (Session 40).** The sticky left rail under `/admin/m1/pipeline/*` (driven by `app/(admin)/admin/m1/pipeline/layout.tsx::NAV`) was missing the F-161 Gazette Extraction entry; only the main app sidebar got it in Session 38. Added a 5th `NAV` item with `DownloadCloud` icon pointing at `/admin/m1/pipeline/extraction`. Sub-nav now reads: Overview · Recent runs · Trace · Steps · Gazette Extraction. | 🟢 | session 40 | (1) Visit any page under `/admin/m1/pipeline/*` (e.g. `/admin/m1/pipeline/recent`) → sticky left rail shows 5 items including "Gazette Extraction"; (2) clicking the new entry navigates to `/admin/m1/pipeline/extraction`; (3) `npx tsc --noEmit` clean for the modified file (only pre-existing `trace-timeline.tsx::sky` unrelated error remains). |
| F-166 | **Postgres connection-limit hotfix for the extract→preprocess fan-out (Session 40).** `extract_gazette` Celery tasks were crashing with `asyncpg.exceptions.TooManyConnectionsError: sorry, too many clients already` (and the related `remaining connection slots are reserved for roles with the SUPERUSER attribute`) once the spider's `M1RegulationsInsertPipeline` dispatched `.delay()` for many rows in quick succession. **Root cause:** `app/db/session.py` had `pool_size=10, max_overflow=20` → 30 connections per worker process; with Celery's 8-worker prefork pool, theoretical burst max = 240 connections, well above Postgres's default `max_connections=100` (minus 3 superuser-reserved = 97 usable). The per-task `engine.dispose()` in `finally` only releases AFTER the task body finishes — during a simultaneous burst, all 8 worker pools may expand at once. **Fix:** (a) `app/db/session.py` pool dropped to `pool_size=2, max_overflow=3, pool_recycle=300` (5 conns/worker × 8 workers = 40 + uvicorn ≤ 5 = 45 ≪ 200); (b) `docker-compose.dev.yml` postgres service gains `command: ["postgres", "-c", "max_connections=200"]` for headroom. Task code itself unchanged — the `finally`-block dispose pattern was already correct. Documented in `phase2_admin_gazette_extraction.md` §7 troubleshooting with the post-pull `docker compose up -d --force-recreate postgres` step. | 🟢 | session 40 | (1) After `docker compose -f docker-compose.dev.yml up -d --force-recreate postgres`: `docker exec enigmatrix-postgres psql -U enigmatrix -d enigmatrix -c "SHOW max_connections;"` returns `200`; (2) restart Celery worker + uvicorn → trigger a fresh extraction (e.g. 2026 / Jan / Feb) → worker logs show NO `TooManyConnectionsError` even when 8 ForkPoolWorkers process tasks in parallel; (3) `cd enigmatrix-backend && uv run pytest app/tests -q` → 19 passed (no regressions; testcontainer Postgres uses its own pool, change is invisible to tests); (4) `python -c "from app.db.session import engine; print(engine.pool.size(), engine.pool._max_overflow)"` → `2 3`. |

## Session 39 — Token-key sweep + Gazette Extraction UX enrichments

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-163 | **Token-key sweep across 7 M1 admin pages (Session 39).** Latent `d.accessToken` → `j?.access` drift fixed. The `/api/auth/token` route returns `{ access }` but six M1 portal pages + the admin settings page were reading `d.accessToken`, leaving `token === null` and silently keeping their `useQuery` calls in skeleton state (queries gated by `enabled: !!token` never fire). Surfaced when the user couldn't see Session 38's successfully-extracted rows on `/admin/m1/pipeline/recent`. Patched all 7 pages to the same proven extraction-page pattern: `(r.ok ? r.json() : null) → j?.access ?? null` with a `catch(() => setToken(null))`. Pages: `/admin/m1/pipeline`, `/admin/m1/pipeline/recent`, `/admin/m1/pipeline/steps`, `/admin/m1/pipeline/steps/[stepId]`, `/admin/m1/pipeline/trace`, `/admin/m1/pipeline/trace/[regulationId]`, `/admin/settings`. Uniform 3-line block per file. | 🟢 | session 39 | (1) `grep "d\.accessToken" c:\Reasearch\xyz\enigmatrix-frontend\app` returns 0 matches; (2) sign in as admin → `/admin/m1/pipeline/recent` renders the last 50 runs (was previously a skeleton); (3) same for `/admin/m1/pipeline`, `/steps`, `/trace`, `/trace/<known-uuid>`, `/admin/settings`; (4) `npx tsc --noEmit` clean for all 7 modified files. |
| F-164 | **Gazette Extraction page UX enrichments — trigger history + summary card + per-row chips (Session 39).** Three layered improvements onto the Session 38 admin page at `/admin/m1/pipeline/extraction`. **Trigger history:** new `lib/m1-extraction/trigger-history.ts` (`loadHistory`/`pushHistory`/`clearHistory`, MAX_HISTORY=5) — last 5 triggers stored in `localStorage` under `m1.gazette.recent_triggers`. New `<RecentTriggersBar>` renders horizontal pill list (e.g. "2026 / Jan–Feb · 2h ago") above the form; clicking a pill replays that scope's progress panel. Auto-hydrates on mount + auto-restores the most-recent trigger as the active task (so a page reload mid-crawl or post-completion doesn't lose the progress panel). SSR-safe via `typeof window` guards. **Summary card:** new backend endpoint `GET /api/v1/admin/m1/extraction/summary` (admin-gated, same scope params as `/progress`) returns scope-aggregate stats — in_scope count + per-status (5 buckets) + per-extraction-method counts + total raw_chars + total cleaned_chars + total penalties + total sub_documents + first_created_at + last_updated_at. Service function uses 4 SQL queries; new private `_extraction_scope_filter()` helper now shared with the `/progress` endpoint to prevent drift. New `<ExtractionSummaryCard>` renders a 6-stat grid (in_scope / raw chars / cleaned chars / penalties / sub-docs / # methods) + status pill row + PDF-type chips ("Text PDF · 12, Scanned (OCR) · 3"). Polls every 10 s while task non-terminal; visibility-gated + terminal early-stop. **Richer per-row cards:** `regulation-progress-card.tsx` now shows a PDF-type StatusBadge derived from `extraction_method` (pymupdf="Text PDF" info / pdfplumber="Hybrid PDF" warning / tesseract="Scanned · OCR" warning) + an amendment_type badge ("Amendment" / "Repeal" / "New Act") in the collapsed view; `gazette_published_date` now renders prominently with a 📅 prefix. No schema changes — all fields already in the progress-endpoint response. | 🟢 | session 39 (user-requested: "even when extraction completes successfully and I navigate to see its related pipelines, it's not showing the recent data… include complete logs / per-PDF information / better UX") | (1) `cd enigmatrix-backend && uv run python -c 'from app.api.v1 import m1_gazette_extraction; print([r.path for r in m1_gazette_extraction.router.routes])'` shows `/summary` registered; (2) `cd enigmatrix-backend && uv run pytest app/tests -q` → 19 passed (M1 backend suite still green, no regressions); (3) `cd enigmatrix-frontend && npx tsc --noEmit` clean for all new + edited files (only pre-existing `trace-timeline.tsx::sky` error remains, unrelated); (4) manual smoke: trigger an extraction → reload the page → trigger restored from history pill bar → summary card shows live totals + PDF-type chips; per-row cards show PDF-type + amendment_type badges in the collapsed view; click a row → 3-step timeline + stats + PDF download still work (F-161 behaviours preserved); (5) `localStorage.getItem('m1.gazette.recent_triggers')` returns valid JSON with last 5 triggers (newest first). |

## Session 38 — Scoped gazette extraction + Celery integration-test loop fix

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-161 | **Year + month-range scoped gazette extraction trigger (Session 38).** Admin-only portal at `/admin/m1/pipeline/extraction` (sidebar entry under M1 Pipeline). Lets an admin pick year (2010 → current) + month range (autocomplete `Combobox` dropdowns) and kicks off a scoped Scrapy crawl over `documents.gov.lk/view/egz/egz_<year>.html`. **Backend (3 new + 4 modified):** `app/api/v1/m1_gazette_extraction.py` (NEW) — 2 routes under `Depends(require_admin)`: `POST /trigger` (Pydantic-validated `year>=2010`, `month_start∈[1,12]`, `month_end∈[1,12]`; runs additional `_validate_scope` for `month_start ≤ month_end` + year-not-in-future → 400 on bad input; calls `run_gazette_spider.delay(year, month_start, month_end)` and returns `GazetteExtractionTriggerOut(task_id, year, month_start, month_end, queued_at)`) and `GET /status/{task_id}` (wraps `AsyncResult(task_id, app=celery_app)` — populates `result` dict on SUCCESS, `traceback` on FAILURE; uses the Celery Redis result-backend already configured at `app/celery_config.py:22`). `app/schemas/m1_pipeline.py` gains `GazetteExtractionTriggerIn/Out` + `GazetteExtractionStatusOut(status: Literal["PENDING","STARTED","SUCCESS","FAILURE","RETRY","REVOKED"], result, traceback)`. `app/tasks/m1/gazette_scraper.py::run_gazette_spider(year, month_start, month_end)` signature extended; new `_validate_scope()` raises `ValueError` (defence-in-depth — same validator the API uses also fires for direct Python REPL invocations). Subprocess `["scrapy", "crawl", "gazette_spider"]` gains `-a year=<int> -a month_start=<int> -a month_end=<int>` when scope set. Return dict appends `scope` field for log/test introspection. `scraper/spiders/gazette_spider.py::__init__` extended with 3 optional kwargs (Scrapy passes `-a` flags as strings — coerced to `int` inside the spider); when `year` given without `start_url`, builds the canonical year-listing URL automatically; new `_in_scope(gazette_date)` filter inside `parse()` drops out-of-month rows BEFORE `yield`, so PDFs outside scope are never downloaded. `app/api/v1/router.py` registers the new router at `prefix="/admin/m1/extraction"`, tag `"admin-m1-extraction"`. **Frontend (2 new + 4 modified):** `app/(admin)/admin/m1/pipeline/extraction/page.tsx` (NEW) — `"use client"`. Three `Combobox` inputs (year + from-month + to-month) with typeahead-filter via the existing `components/ui/combobox.tsx`; client-side `monthStart ≤ monthEnd` validation surfaces a destructive-toned hint. `useMutation` POSTs to `/trigger` → stores task_id. `useQuery` polls `/status/{task_id}` every 5 s, gated by `usePageVisible()` (existing hook from `components/m1-pipeline/live-polling-indicator.tsx`) and early-stops on terminal states (`SUCCESS`/`FAILURE`/`REVOKED`). Status card border tone shifts per state (grey PENDING / amber STARTED with spinner / green SUCCESS / red FAILURE). On SUCCESS, shows a "View recent runs →" Link to `/admin/m1/pipeline/recent` (rows progress through `ingested → extracted → preprocessed` automatically — F-160 portal handles post-trigger observability). `lib/api/m1-gazette-extraction.ts` (NEW) — typed `M1GazetteExtractionApi.{trigger, getStatus}` wrappers mirroring the Pydantic shapes. `components/layout/sidebar.tsx` adds `DownloadCloud` icon import + new entry to `ADMIN_M1_PIPELINE_ITEMS`. `lib/i18n/messages/{en,si,ta}.json` add `nav.adminM1Extraction = "Gazette Extraction"` (English-only labels — internal admin tool, mirrors the F-160 decision). **Tests (5 new across 2 files):** `app/tests/unit/test_gazette_scraper_task.py` — forwards `year`/`month_start`/`month_end` as `-a` flags + rejects `month_start > month_end` + rejects pre-2010 year. `app/tests/integration/test_gazette_spider.py` — `_in_scope` filter drops out-of-month rows + `year=X` no `start_url` builds the canonical URL. **Token-key bug fix:** the new page's `fetch("/api/auth/token")` reads `j?.access` (not `d.accessToken` — the M1 portal pages' copy-paste pattern reads a key that doesn't exist in the route response, silently keeping `token === null` and disabling buttons / leaving queries in skeleton). | 🟢 | session 38 (user: "Year + month-range gazette extraction with autocomplete dropdowns + 'what should be running' runtime doc") | (1) `cd enigmatrix-backend && uv run pytest app/tests/unit/test_gazette_scraper_task.py app/tests/integration/test_gazette_spider.py -v` → 11 passed (5 task + 6 spider, including the 5 new); (2) `cd enigmatrix-frontend && npx tsc --noEmit` clean for `app/(admin)/admin/m1/pipeline/extraction/page.tsx` + `lib/api/m1-gazette-extraction.ts` (only the pre-existing `trace-timeline.tsx` `sky` color-key error remains, unrelated); (3) manual smoke with all 5 services running (Postgres + Redis + Celery worker + uvicorn + pnpm dev): admin signs in → sidebar shows "Gazette Extraction" under M1 Pipeline → picks 2024 / March / May → "Start extraction" button enables (token loaded) → POST returns 202 + task_id → status panel cycles PENDING → STARTED → SUCCESS over ~30s → "View recent runs →" link visible → `/admin/m1/pipeline/recent` shows new rows progressing through `ingested → extracted → preprocessed`; (4) `curl -X POST /api/v1/admin/m1/extraction/trigger -d '{"year": 1999, "month_start": 1, "month_end": 12}'` → 400 (year out of range); same with `month_start=8, month_end=3` → 400 ("month_start must be <= month_end"); (5) all 4 tracker files (SESSIONS, CHANGES, FEATURES + finding-detail) reference Session 38 / F-161; (6) new local-dev doc `phase2_admin_gazette_extraction.md` covers the 5-service runtime + 7-hop end-to-end flow + SQL verify + 5-row troubleshooting |
| F-162 | **Celery eager-mode + pytest-asyncio session-loop fix for integration tests (Session 38).** Long-latent bug — `asyncio.run()` inside the `extract_gazette` / `preprocess_gazette` Celery tasks was crashing under `pytest.mark.asyncio` with `RuntimeError: asyncio.run() cannot be called from a running event loop` because `pyproject.toml` configures `asyncio_default_test_loop_scope = "session"` (a single loop runs for the whole test session — when the test calls `task.delay(...).get()` synchronously, Celery eager-mode runs the task body inline in the test's loop, which already has `asyncio.run()` from the outer test). 8 integration tests across `test_celery_{extract,preprocess}_gazette.py` were silently broken. **Fix (test-side only — production task code unchanged because it's correct for a real worker):** new `_run_eager_task(task, *args, timeout)` helper inside each test file: (a) `await session_mod.engine.dispose()` first — clears any pooled asyncpg connections bound to the test's session loop, so the task in its own thread won't inherit a foreign-loop connection and crash with `Future attached to a different loop`; (b) `await asyncio.to_thread(lambda: task.delay(*args).get(timeout=timeout))` — pushes the synchronous eager invocation onto a worker thread that has no running loop, mirroring a real Celery worker. Inside the task, `asyncio.run()` then creates a fresh loop, mints fresh asyncpg connections on it, does work, disposes the engine in its own `finally` (existing production behaviour, no changes there), and returns. After the task returns, the test's loop continues — its next `async with patched_session() as session` rebuilds the pool on the test loop. Additionally: `patched_session` (preprocess test only) gained per-test `TRUNCATE TABLE m1_regulation_penalties, m1_sub_documents, m1_regulations RESTART IDENTITY CASCADE` for data isolation against the session-scoped testcontainer Postgres (previously: test 1 inserted gazette `2369/14` from the VAT-amendment fixture, tests 4-5 used the same fixture → `ix_m1_regulations_gazette_number` UNIQUE collision when the pipeline tried to set the same gazette number on a different row). | 🟢 | session 38 | (1) `uv run pytest app/tests/integration/test_celery_extract_gazette.py app/tests/integration/test_celery_preprocess_gazette.py -v` → 8 passed (was: 8 errored at fixture setup / failed mid-test with `asyncio.run() cannot be called from a running event loop` then `Future attached to a different loop` then UNIQUE constraint violation as each issue was peeled back); (2) helper reused by F-161's new spider/task tests transitively (any future Celery integration test in this codebase should use the same pattern); (3) production task code (`app/tasks/m1/{extract,preprocess}_gazette.py`) deliberately unchanged — `asyncio.run()` is correct for a real Celery worker (no running loop). |

## Session 37 — M1 Phase 2 verification + live pipeline observability portal

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-160 | **M1 Pipeline observability portal (Session 37).** Admin-only live showcase at `/admin/m1/pipeline/*` that visualizes the Phase 2 ingest+extraction pipeline (Scrapy spider → Celery extract → ml-package extraction → preprocessing → DB persistence). 3 new backend endpoints (`/overview`, `/recent`, `/trace/{id}`) under `require_admin` driven by a new `m1_pipeline_service` with Postgres aggregations + Celery introspection (`celery_app.control.inspect()` wrapped in 2 s asyncio.wait_for so a slow broker can't stall the overview). 14 React components + 5 routes + Recharts visualizations (AreaChart throughput, PieChart status distribution, hand-rolled CSS funnel) + 6-stage flow diagram with live counters + per-step detail pages with code refs/tests/spec links + per-regulation trace with timeline + side-by-side content diff + penalties + sub-documents grid. TanStack Query polls every 5 s, auto-pauses on tab hide via Page Visibility API. Static metadata (`lib/m1-pipeline/steps.ts`) carries each step's title/feature/session/inputs/outputs/DoD/code refs/spec docs sourced from BUILD_07 roadmap + Session findings. Also confirmed by file-level audit that all Phase 2 code (Steps 2a-2f + Session 34 cleanup) is correctly shipped — no defects, no NotImplementedError/TODO/FIXME in critical paths. | 🟢 | session 37 (user-requested verification + live demo portal: "live demo or real time show cases as according to plan those relavejt ui hiws can veimpimnente") | (1) `cd enigmatrix-backend && uv run alembic upgrade head` succeeds; `curl -H "Authorization: Bearer <admin-jwt>" http://localhost:8000/api/v1/admin/m1/pipeline/overview` returns 200 with valid JSON shape; same for `/recent` and `/trace/<known-uuid>`; (2) `cd enigmatrix-frontend && pnpm install && pnpm dev` → log in as admin → `/admin/m1/pipeline` renders the flow diagram with 6 step tiles + status counts + throughput chart + status donut + funnel + recent runs + error log + Celery health; LivePollingIndicator updates every 5 s; (3) click any step tile → `/admin/m1/pipeline/steps/[stepId]` renders the StepDetailCard with all 4 columns populated; (4) click any row in recent runs → `/admin/m1/pipeline/trace/[id]` shows the 6-event timeline + 4 tabs (Content/Penalties/Sub-documents/Raw row) all populated for a fully-preprocessed regulation; (5) switch tabs in browser → LivePollingIndicator shows "Paused" + polling stops; (6) log in as non-admin SME → `/admin/m1/pipeline` redirects via parent `(admin)/layout.tsx::requireRole("admin")`; (7) stop Redis + Celery worker → overview still renders but `<CeleryHealthCard>` shows "broker unreachable" while everything else degrades gracefully; (8) on a fresh DB with zero rows → flow diagram shows 0 counts + tables show empty states + no crashes; (9) all 4 tracker files reference Session 37 / F-160 |

## Session 36 — Research-log portal (admin-only /admin/research-log/* live vault-backed routes)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-159 | **Research-log portal (Session 36).** Admin-only Next.js portal at `/admin/research-log/*` that parses six Obsidian-vault surfaces (SESSIONS.md, CHANGES.md, FEATURES.md, per-module findings folders, RESEARCH_BUILD_TRACKER.md, RESEARCH_IDEAS.md) on every render and presents each with a domain-specific advanced UI (timeline + collapsible session cards, filterable+paginated changes table, subsection-tabbed kanban with stacked-bar donuts, module-tinted finding card grid + detail, per-module build-progress dashboard, RQ cards + section accordions). Live filesystem reads with 30 s mtime-keyed LRU + `dynamic = 'force-dynamic'` keep data fresh as the vault changes on disk. Unified F-### cross-reference index makes every feature id clickable everywhere → cross-ref detail route showing every session/change/finding that mentions it. Six homegrown regex parsers + a closed-allowlist filesystem reader (`..`-rejection, symlink rejection) keep the implementation zero-new-dep and trust-boundary-isolated from the existing `enigmatrix-docs/` reader. Gated by `NEXT_PUBLIC_VAULT_ENABLED=true` + `VAULT_PATH=c:\sme` env vars; in production both stay unset so the portal 404s + sidebar entry disappears. | 🟢 | session 36 | (1) `cd enigmatrix-frontend && pnpm dev` with `NEXT_PUBLIC_VAULT_ENABLED=true` + `VAULT_PATH=c:\sme` set in `.env.local`; visit `/admin/research-log` as an admin user → landing renders with 6 metric tiles + activity heatmap + 5 latest session cards; (2) navigate each surface and confirm SESSIONS timeline / CHANGES filterable table / FEATURES kanban (subsection tabs) / FINDINGS module-grouped grid / BUILD-TRACKER per-module progress / IDEAS RQ cards all render; (3) click any F-### chip anywhere → arrive at `/admin/research-log/features/F-###` with 3-column cross-ref view populated; (4) edit `c:\sme\08-Findings-Log\SESSIONS.md` to add a new dummy session, click the `<RefreshButton>` → new entry appears within one render cycle + the "Live · Ns ago" pill updates; (5) login as SME (non-admin) → `/admin/research-log` redirects via parent `requireRole` guard; (6) unset `NEXT_PUBLIC_VAULT_ENABLED` and rebuild → route returns 404 + sidebar entry disappears; (7) all 32 new TS files type-check via `pnpm typecheck`; (8) request `/admin/research-log/findings/m1/..%2F..%2F..%2Fetc%2Fpasswd` → 404 (reader allowlist rejects); (9) vault triplet (SESSIONS / CHANGES / FEATURES) shows Session 36 / F-159 at top |

## Session 35 — Local-dev handbook + per-area + per-phase setup docs in the vault

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-158 | **Local-dev documentation lap (Session 35).** Comprehensive local-dev guide in the Obsidian vault covering one-time prerequisites + day-to-day workflow + how to run every M1 Phase 2 milestone on Windows + WSL2 + PowerShell + Docker Desktop. **12 new markdown files** + 2 new folders. **Top-level handbook** (`04-Technology-Stack/00_LOCAL_DEV_HANDBOOK.md`) with tech-stack overview, prerequisites, repo clone (WSL home + Windows symlink), day-zero bring-up order, day-N workflow, standing cadence. **3 per-area platform docs** for backend WSL, frontend PowerShell, ml WSL (new `ml/SETUP/` folder counterpart to existing `backend/SETUP/`/`frontend/SETUP/`). **1 phase index + 7 per-phase how-to-run docs** at `02-Research-Modules/1 Module-1-Awareness-Gap/local-dev/`, one per Phase 2 milestone (Step 2a → 2f → Session 34 cleanup), each embedding prerequisites + run-suite + manual smoke + SQL/CLI verify + troubleshooting + after-verifying graphify-update commands per the standing cadence. | 🟢 | session 35 (user explicitly asked: "setup command for my local that where i use the wsl for the windows... use the power shell for the frontend... each phases of the ml related works that when run them in the local... how to work and check the changes after each step") | (1) `Get-ChildItem c:\sme\04-Technology-Stack -Filter "00_LOCAL_DEV_*" -Recurse` returns 4 files (handbook at root + backend WSL + frontend PowerShell + ml WSL); (2) `Get-ChildItem "c:\sme\02-Research-Modules\1 Module-1-Awareness-Gap\local-dev" -Filter "*.md"` returns 8 files (index + 7 phase docs); (3) `Get-ChildItem c:\sme\04-Technology-Stack\ml\SETUP` returns ≥ 1 file (new folder exists); (4) each phase doc cross-links to its `planned-for-devlopment/N_setup.md` retrospective spec; (5) handbook documents the WSL filesystem perf trap + recommends `~/repos/xyz` clone + `mklink /D C:\Reasearch\xyz <wsl-path>` symlink for graphify; (6) all 12 docs use absolute paths consistently and use markdown link syntax for cross-refs; (7) vault triplet (this entry + SESSIONS Session 35 + CHANGES F-158 row) all reference F-158; (8) no M1 finding entry created — doc-only lap per standing cadence; (9) walkthrough sanity check (fresh-WSL-distro volunteer reaches localhost:3000 in < 60 min) deferred to next time a fresh machine is stood up |

## Session 34 — M1 Phase 2 cleanup (segmenter + penalty enum + is_admin_set + m1_sub_documents)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-157 | **M1 Phase 2 cleanup (Session 34).** Closes all 5 deferred carry-forwards from Session 33: doc 02 §5 mermaid gains Stage B+ Preprocessing sub-node; `m1.extraction.segmenter` promoted to standalone module (`NOTICE_BOUNDARY_RE` + `detect_sections` + new `detect_sections_with_labels`); `m1_regulation_penalties.penalty_type` enum widened 3 → 7 values per doc spec; `is_admin_set BOOLEAN` flag added so admin-curated rows survive `preprocess_gazette_task` re-extractions; new `m1_sub_documents` junction with ORM + schema + persistence wiring + 6-value `section_type` CHECK. `03_M1_2_Gazette_Segmentation.md` flipped ⚠️ Partial → ✅ Shipped. Two migrations (`202605250001` enum + admin_set, `202605260001` sub_documents). `SectionInfo` dataclass lives in `m1.extraction.types` (circular-import fix); preprocessing.types re-exports for backward compat. **127/8 ml tests pass.** | 🟢 | session 34 | (1) `cd enigmatrix-ml && PYTHONPATH=$PWD pytest tests/m1 -v` → 127 passed, 8 skipped (14 new in test_segmenter.py + Step 2c/2d/2e regressions green); (2) `cd enigmatrix-backend && uv sync && uv run alembic upgrade head` succeeds; round-trip `uv run alembic downgrade -2 && uv run alembic upgrade head` clean (enum-collapse rescue path exercised on downgrade); (3) `uv run pytest app/tests/integration/test_celery_preprocess_gazette.py -v` → 6 tests pass (4 existing + 2 new for is_admin_set preservation + sub-docs persistence); (4) `python -c "from m1.preprocessing import preprocess_gazette; r = preprocess_gazette('PART I\\nbody\\nSchedule 1\\nbody'); print(len(r.sections), r.sections[0].section_type if r.sections else 'none')"` returns ≥ 2 sections including a `part`-typed entry; (5) all 17 new/modified Python files pass `python -m py_compile`; (6) all 4 tracker files reference Session 34 / F-157; (7) `grep -c "⚠️ Partial" 03_M1_2_Gazette_Segmentation.md` returns 0 on both vault + code |

## Session 33 — M1 Phase 2 doc catch-up (plan docs + spec status flips + roadmap + doc 02)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-156 | **M1 Phase 2 doc-only catch-up (Session 33).** Closes every documentation gap from Sessions 23/25/26/28/30/31/32. **6 new plan docs** in `enigmatrix-docs/m1/planned-for-devlopment/` for Steps 2d/2e/2f (3 trios of `N.md` + `N_setup.md`, each matching the Session-24 format), plus 3 setup-doc mirrors at vault `planned-for-development/`. **8 spec doc status markers flipped** from `🔲 Deferred (BUILD_07 — ...)` → `✅ Shipped Session XX / F-XXX (...)` (or `⚠️ Partial` for `03_M1_2_Gazette_Segmentation.md`), 16 file edits across vault + code. **`16_M1_Development_Roadmap.md`** gains a new Step 2f section + updated Phase 2 DoD line (`status='extracted'` → `status='preprocessed'`) + "Phase 2 complete as of 2026-05-17" marker; "Where M1 stands today" table row for Ingest pipeline flipped to ✅. **`02_M1_Data_Requirements.md`** §2.1 adds `'preprocessed'` to the status enum + new `cleaned_text` / `amendment_type` column rows; §2.8 (`m1_regulation_penalties`) restructured into Full-vision-spec + Shipped-subset format documenting the migration's actual DDL alongside the canonical aspiration. | 🟢 | session 33 | (1) `Get-ChildItem c:\Reasearch\xyz\enigmatrix-docs\m1\planned-for-devlopment\ -Name` returns 13 files (was 7 + 6 new); (2) Vault `planned-for-development\` returns 7 files (was 4 + 3 new); (3) `grep -l "🔲 Deferred (BUILD_07 — `ml/m1/"` on the 8 target spec doc pairs returns no matches (was 16); (4) `grep -c "Step 2f"` on `16_M1_Development_Roadmap.md` returns ≥ 1; (5) `grep -c "preprocessed"` on `02_M1_Data_Requirements.md` returns ≥ 1; (6) `grep -c "m1_regulation_penalties"` on doc 02 returns ≥ 1; (7) Vault triplet shows Session 33 / F-156 at top of SESSIONS / CHANGES / FEATURES; (8) `git push origin main` on `enigmatrix-docs` succeeds; `git push origin initiate` on `xyz` succeeds; (9) No code changes — purely documentation; the F-156 work doesn't require backend or ml test runs |

## Session 32 — M1 Phase 2 Step 2f (wire preprocessing into Celery pipeline + DB persistence)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-155 | **M1 Phase 2 Step 2f — Celery wiring + DB persistence for the Step-2e preprocessing chain (Session 32).** Closes the gap from F-154 (which shipped the ml-package code but persisted nothing). New `preprocess_gazette_task` chained automatically after `extract_gazette` flips `status='extracted' → 'preprocessed'` (new value extending the canonical doc 02 §2.1 7-state enum). Alembic migration `202605240001` adds `m1_regulations.cleaned_text TEXT` + `m1_regulations.amendment_type VARCHAR(20)` (CHECK over `('amendment','repeal','new_act')`), creates `m1_regulation_penalties` junction table for the multi-penalty list with DELETE-then-INSERT idempotency, and extends the status CHECK constraint. New `M1RegulationPenalty` ORM + `M1RegulationPenaltyOut` Pydantic schema (read-only). Task lazy-imports `from m1.preprocessing import preprocess_gazette` so the backend boots without the ml workspace member; admin-curated `gazette_number` / `effective_date` / `penalty_range_lkr` / `principal_act_amended` are preserved (pipeline only fills NULLs); `cleaned_text` + `amendment_type` always overwritten. extract_gazette enqueues the new task via the Session-26 lazy `.delay()` pattern — keeps existing tests passing. 4 integration tests (DoD round-trip on the spec's VAT-amendment worked example, skip-test, admin-authority, idempotency). Closes Phase 2 overall DoD ("all metadata fields populated"). | 🟢 | session 32 | (1) `cd enigmatrix-backend && uv sync && uv run alembic upgrade head` succeeds; downgrade -1 → upgrade head round-trip clean; (2) `uv run pytest app/tests/integration/test_celery_preprocess_gazette.py -v` → 4 tests pass (needs Docker testcontainer Postgres); (3) `uv run pytest app/tests/integration/test_celery_extract_gazette.py -v` → still passes (chaining didn't break Session-26 tests); (4) ml-package regression `cd ../enigmatrix-ml && PYTHONPATH=$PWD pytest tests/m1 -v` → 117 passed / 8 skipped (Step 2c/2d/2e regressions green); (5) manual smoke: seed an `ingested` row + raw_pdf_path → `extract_gazette.delay(reg_id)` from Python REPL → observe SQL shows row flips `ingested → extracted → preprocessed`, `cleaned_text` non-empty, 4 DoD metadata fields populated, ≥ 1 `m1_regulation_penalties` row; (6) all 4 tracker files reference Session 32 / F-155; (7) Doc 02 §2.1 enum amendment is a parallel doc-only follow-up (divergence logged in finding entry) |

## Session 31 — M1 Phase 2 Step 2e (preprocessing chain: cleaning + metadata + chunking)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-154 | **M1 Phase 2 Step 2e — Preprocessing chain at `enigmatrix-ml/m1/preprocessing/` (Session 31).** Executed [04_M1_Preprocessing_Pipeline.md] + 3 companions (04_M1_1 noise removal, 04_M1_2 metadata patterns, 04_M1_3 chunking strategy). New package with 5 modules: `cleaning.py` (8-step `NOISE_PIPELINE` per [04_M1_1] §2; two pipelines — `clean_gazette_text` keeps signature for thesis citations, `clean_for_classification` strips it for the classifier), `metadata_extractor.py` (4 anchored regex patterns + multi-penalty `finditer` + alternative merger → `penalty_type='both'` when fine + imprisonment within 30 chars connected by "or"/"either" + sanity-bounded effective_date + amendment_type discriminator), `chunking.py` (`NOTICE_BOUNDARY_RE` + `detect_sections` + `chunk_hybrid` section-aware sliding window with `MAX_LEN=512`/`STRIDE=64` + micro-section merger + padding-bias trailing chunk dropper; XLM-R tokenizer lazy-loaded via `@lru_cache` and auto-downloaded from HF Hub on first call ~1.1 GB), `types.py` (`Penalty`/`Chunk`/`PreprocessedGazette` dataclasses), `__init__.py` (orchestrator `preprocess_gazette(raw_text, *, regulation_id, published_date) -> PreprocessedGazette` glueing clean → language detect → metadata-on-raw-text → chunk on cleaned → assemble). Step 2d's `language_detection` module reused wholesale (no duplication). **pyproject.toml** gains `transformers>=4.40,<5` + `dateparser>=1.2,<2`. **DoD met:** cleaned text + 4 metadata fields + chunks ready for Stage D, all populated on the spec's VAT-amendment worked example. **Deferred:** `m1_regulation_penalties` junction table + `amendment_type` column migration → Step 2f (Celery wiring); DoD corpora (50-doc cleaning, 100-doc metadata gold, 100-doc Wijesekara) follow the fixture-gated pattern from Step 2c/2d. | 🟢 | session 31 | (1) `cd enigmatrix-ml && PYTHONPATH=$PWD M1_LID_MODEL_PATH=.../lid.176.bin python -m pytest tests/m1/preprocessing tests/m1/extraction -v` → 117 passed, 8 skipped (DoD harnesses + Tesseract-binary + tokenizer gates); (2) `python -c "from m1.preprocessing import preprocess_gazette; from datetime import date; r = preprocess_gazette('GAZETTE EXTRAORDINARY No. 2369/14 - FRIDAY, DECEMBER 22, 2023\\n\\nThe Value Added Tax (Amendment) Act, No. 8 of 2024, amends the Value Added Tax Act, No. 14 of 2002. The Act comes into operation on 1 January 2024.', published_date=date(2023,12,22)); print(r.gazette_number, r.effective_date, r.principal_act_amended, r.amendment_type)"` → `2369/14 2024-01-01 Value Added Tax Act, No. 14 of 2002 amendment`; (3) `python -c "from m1.preprocessing import clean_gazette_text; t = clean_gazette_text('regulat-\\nion shall apply'); assert t == 'regulation shall apply'"` succeeds (dehyphenation); (4) `python -m m1.preprocessing` import smoke clean; (5) all 4 tracker files reference Session 31 / F-154; (6) Step 2c/2d regression suite still green (41/41 pass) |

## Session 30 — M1 Phase 2 Step 2d (language detection + Wijesekara conversion + per-page OCR fallback)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-153 | **M1 Phase 2 Step 2d — language detection + Wijesekara conversion + per-page OCR fallback (Session 30).** Executed the explicit deferral from F-149 (Session 28). Ships three pieces together. (1) **fastText `lid.176.bin` document detection** at `enigmatrix-ml/m1/extraction/language_detection.py` — `detect_document_language(text, *, min_confidence=0.70, window_chars=500) -> LanguageDetection` (primary ∈ {en, si, ta, mixed}, top-3 confidence, env-tunable, lazy model load). Includes the Unicode-range per-line router (`is_sinhala_char` / `is_tamil_char` / `is_latin_char` / `line_language` / `route_lines_by_language` / `primary_language_by_line_count`) per doc-04 §3.2. lid.176.bin (125.2 MB) downloaded to `enigmatrix-ml/storage/models/m1/baseline/lid.176.bin` via new idempotent `scripts/download_lid_model.py`. (2) **Wijesekara → Unicode conversion** at `m1/extraction/wijesekara.py` + 87-entry `wijesekara_map.yaml`. `is_wijesekara_encoded(text)` applies the 0.40 indicator-char ratio heuristic (spec §3, min 50 ASCII-alpha chars). `convert_wijesekara(text)` greedy longest-match (4→3→2→1 chars; unmapped passes through). Map cached via `@lru_cache`. `ocr.wijesekara_to_unicode` stub replaced by thin delegate — backward-compatible. (3) **Per-page OCR fallback in `extract_with_chain`** — new signature `(pdf_path, *, enable_ocr_fallback=True, ocr_dpi=300, ocr_timeout=60)`. Low-yield PyMuPDF pages → rasterise just that page → fastText pick `--lang` → Tesseract → Wijesekara if detected → splice back as `PageResult(method='tesseract')`. Document-level method becomes `'hybrid'` when any page was OCR'd. Opt-out via `enable_ocr_fallback=False`. pyproject.toml gains `fasttext-wheel>=0.9.2,<1`, `numpy<2.0` (fasttext-wheel/numpy-2 incompat workaround), `PyYAML>=6,<7`. | 🟢 | session 30 | (1) `cd enigmatrix-ml && PYTHONPATH=$PWD pytest tests/m1/extraction -v` → 41 passed, 5 skipped (DoD harnesses + Tesseract-binary gates); (2) `python scripts/download_lid_model.py` is idempotent — re-run skips when file present with size ≥ 100MB; (3) `python -c "from m1.extraction import detect_document_language; print(detect_document_language('Hello world this is English').primary)"` returns `'en'`; (4) `python -c "from m1.extraction import convert_wijesekara; print(convert_wijesekara('w'))"` returns `'අ'` (Sinhala A); (5) `python -m m1.extraction.language_detection --measure-accuracy <fixture.tsv>` and `python -m m1.extraction.wijesekara --measure-accuracy <pairs.tsv>` exit 0 when accuracy ≥ 0.95 (DoDs gated on hand-labelled corpora — apparatus ships, data follows) |

## Session 29 — Frontend lap (auth split-panel + M1 portal data consolidation + survey config unified+multi-sector)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-150 | **Auth pages split-panel UI (Session 29).** New `enigmatrix-frontend/components/ui/travel-connect-signin.tsx` ports the travel-connect component: DotMap canvas (Sri Lankan world-map dots + animated route lines, theme-aware via `getComputedStyle(document.documentElement).getPropertyValue("--primary"/"--foreground")`) left + framer-motion fade-in form right. Exports `DotMap` (reused on both auth pages), `AuthSignInCard` (demo), default `Index`. `app/(auth)/layout.tsx` stripped to `min-h-screen` pass-through. `app/(auth)/login/page.tsx` rewritten as a split-panel `motion.div` preserving all auth logic (`useForm(loginSchema)`, `AuthApi.login()`, `/api/auth/establish`, role-based redirect, `useTranslations()`); `isPasswordVisible` state drives Eye/EyeOff; inputs `h-8 bg-background/80 focus-visible:ring-primary/50`. `app/(auth)/register/page.tsx` matches with a scrollable right panel for the 9+ fields and `SelectTrigger h-8`. Theme-token migration from initial hardcoded `#090b13`/`#13151f`/`#2a2d3a` → `auth-card`/`auth-visual-panel`/`brand-gradient`/`auth-gradient-text`/`bg-background/80`/`text-foreground` so light theme works. | 🟢 | session 29 | (1) `/login` + `/register` render the dark split card on `md+` and the right-panel form full-width on mobile; (2) Eye/EyeOff toggle switches the password input between `type=password` and `type=text`; (3) theme toggle still flips background/foreground without breaking the auth card; (4) login with valid creds redirects admin → `/admin/regulations`, SME → `/dashboard`; (5) register creates user + profile + auto-logs in to `/dashboard` |
| F-151 | **M1 docs portal data consolidation (Session 29).** Two parallel M1 portal data sources collapsed into one. Appended 11 portal-derived constants to `enigmatrix-frontend/lib/m1-docs.ts` under a clearly delimited banner: `M1_META`, `M1_PIPELINE_STAGES` (7 stages A–G), `M1_RESEARCH_QUESTIONS` (4 RQs), `M1_TECHNOLOGY_CHOICES` (8), `M1_DB_ENTITIES` (11), `M1_DIFFUSION_TIMELINE` (T0–T9), `M1_RESEARCH_FINDINGS` (F1–F6), `M1_ARCHITECTURE_LAYERS` (6), `M1_HAPPY_PATH` (9 rows), `M1_KEY_STATS` (6), `M1_INTER_MODULE_CONNECTIONS` (M1→M2/M3/M4). Dropped `M1_SECTIONS` (replaced by `M1_NON_TRACKING_SECTIONS` already in use). Updated 4 import sites: `components/docs/m1/{m1-pipeline,m1-timeline,m1-stats-counter}.tsx`, `app/(app)/docs/m1/page.tsx`. **Deleted** `enigmatrix-frontend/lib/m1-docs-data.ts`. Decision: portal constants stay typed in same file (not auto-derived) because they're cross-document syntheses, not per-section `metrics`/`tables`. | 🟢 | session 29 | (1) `grep -r "m1-docs-data" enigmatrix-frontend` returns no matches; (2) `/docs/m1` renders identically — pipeline rail, T0–T9 timeline, stats counter, RQ cards, findings cards, architecture/happy-path/inter-module/tech-choices tables, DB entities grid all present; (3) all 4 import sites pull from `@/lib/m1-docs`; (4) `lib/m1-docs-data.ts` no longer exists on disk; (5) TypeScript `tsc --noEmit` passes |
| F-152 | **Admin survey config: unified module option + multi-sector checkboxes — frontend only (Session 29).** `enigmatrix-frontend/components/forms/survey-config-form.tsx`: `MODULE_OPTIONS` grows from 4 to 5 entries — added `{ value: "unified", label: "Unified — All Modules" }`. zod schema becomes `module_number: z.union([z.literal("unified"), z.literal("1"), z.literal("2"), z.literal("3"), z.literal("4")])` with submit-time `"unified" → null` map. Single `sector_code` Combobox replaced by `sector_codes: z.array(z.string())` rendered as 12 multi-select checkboxes from `lib/constants/sectors.ts`; empty array means "all sectors universally" (hint string added). **Backend deferred** — until `survey_sessions.sector_code` column, `admin_surveys.module_number` nullable + `sector_codes` array, schema + service threading + new migration land, posting from the admin form will 422 on `sector_codes`. | 🟡 | session 29 | (1) Admin survey-create form renders the 5 module options including "Unified"; (2) sector checkboxes bind to `sector_codes: string[]`; (3) empty array submits validate-pass; (4) form submit POST currently 422s on backend until the matching schema lands (expected — tracked 🟡, NOT 🟢) |

## Session 28 — M1 Phase 2 Step 2c — canonical `ml/m1/extraction/` chain

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-149 | **M1 Phase 2 Step 2c — canonical extraction chain at `enigmatrix-ml/m1/extraction/` (Session 28).** Executed [3.md](../m1/planned-for-devlopment/3.md). Moved the Session-26 runtime MVP to its canonical home (per doc 13 + [15_M1_1_ML_Folder_Guide.md](../m1/15_M1_1_ML_Folder_Guide.md)); wired via uv workspace; added research-grade rigour (threshold-calibration harness, CER calculator, Wijesekara stub, full Tesseract 5.3.x flag set). New root `pyproject.toml` declares the workspace; `enigmatrix-ml/pyproject.toml` makes it a real hatchling package. **5 new canonical modules** (`types.py`, `pdf_classifier.py`, `text_extractors.py`, `ocr.py`, `__init__.py`). `classify_pdf` now returns a `Literal["text_pdf","hybrid","scanned"]`; calibration via `python -m m1.extraction.pdf_classifier --calibrate <dir>`. **Backend `app.extraction` reduced to a re-export adapter** (-90 LOC); `extract_gazette.py` + one unit test updated for the new signature. **3 new ml test files** (12 passed, 2 intentional skips). **Backend regression** 6/6 pass through the adapter. **Audit fixture dir** shipped empty with README. Deferred to Step 2d: Wijesekara table, `language_detection.py`, per-page OCR fallback, 50-doc + gold corpus datasets. | 🟢 | session 28 | (1) `uv sync` at repo root resolves both workspace members; (2) `cd enigmatrix-ml && uv run pytest tests/m1/extraction -v` → 12 passed, 2 skipped; (3) `cd enigmatrix-backend && uv run pytest app/tests/unit/test_{pdf_classifier,text_extractors,gazette_scraper_task}.py -v` → 6 passed (proves the adapter is byte-stable); (4) `uv run python -c "from app.extraction import classify_pdf; from m1.extraction import classify_pdf as direct; assert classify_pdf is direct"` succeeds; (5) `git diff --stat enigmatrix-frontend/ enigmatrix-infrastructure/` empty (zero non-target drift); (6) all 4 tracker files reference Session 28 / F-149 |

## Session 26 — M1 Phase 2 Step 2b — Celery + Stage-B PDF extraction

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-148 | **M1 Phase 2 Step 2b — Celery + Stage-B PDF extraction (Session 26).** Executed the approved [2.md](../m1/planned-for-devlopment/2.md) plan. **5 new deps** (`celery[redis]`, `PyMuPDF`, `pdfplumber`, `pytesseract`, `pdf2image`); **env vars** `CELERY_BROKER_URL` + `M1_PDF_TEXT_THRESHOLD=200` + `M1_PDF_SCANNED_THRESHOLD=30` (Pydantic-backed). **Alembic `202605230001`** (down-rev `202605220001`) adds `raw_text TEXT`, `extraction_method VARCHAR(20)` (CHECK enum), `extracted_at TIMESTAMPTZ` to `m1_regulations`; ORM model updated. **New `app/extraction/` module** — `pdf_classifier.py` (samples first 3 pages → route by mean chars/page) + `text_extractors.py` (three stage extractors, page-separated by `\f`). **Celery infra** — `app/celery_config.py` (Redis broker + result backend, `task_acks_late=True`, Beat `0 */6 * * *` for `run_gazette_spider`), `app/tasks/__init__.py`, `app/tasks/m1/__init__.py`. **Two tasks** — `gazette_scraper.run_gazette_spider` (subprocess `scrapy crawl`) + `extract_gazette.extract_gazette(reg_id)` (classify → extract → write columns → flip status `ingested → extracted`; failure → `extraction_failed` + autoretry max 3). **Spider pipeline wired** — replaces Session-23 TODO with `extract_gazette.delay(...)` (lazy import + try/except so spider still works without a live broker). **4 tests** — `test_pdf_classifier`, `test_text_extractors`, `test_gazette_scraper_task`, `test_celery_extract_gazette` (eager Celery; seeds an `ingested` row + asserts flip to `extracted`). Deferred to Step 2c: language detection + Wijesekara → Unicode. | 🟢 | session 26 | (1) `cd enigmatrix-backend && uv sync && uv run alembic upgrade head && uv run pytest app/tests/unit/test_pdf_classifier.py app/tests/unit/test_text_extractors.py app/tests/unit/test_gazette_scraper_task.py -v` passes; (2) with Redis + Docker running, `uv run pytest app/tests/integration/test_celery_extract_gazette.py -v` asserts row flips `ingested → extracted`; (3) manual smoke check — start worker (`uv run celery -A app.celery_config worker -l info`) and Beat (`uv run celery -A app.celery_config beat -l info`); trigger `run_gazette_spider.delay()` from Python REPL; observe gazettes get scraped and rows reach `extracted` with non-NULL `raw_text`; (4) all 4 tracker files reference Session 26 / F-148; (5) `git diff --stat enigmatrix-frontend/ enigmatrix-ml/ enigmatrix-infrastructure/` empty (zero non-backend drift) |

## Session 25 — Step 2a parse-bug fix + Step 2b planning docs

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-147 | **Step 2a parse-bug fix + Step 2b planning docs (Session 25).** Two deliverables in one session. **(1) Spider parse bug fix** in `enigmatrix-backend/scraper/spiders/gazette_spider.py`: xpath `../*//text()` (only walked the anchor's parent `<td>` subtree, missing sibling cells in the same `<tr>`) → `ancestor::tr//text()` with non-table fallback. Reported by user: Session-23's parse test FAILED with "expected 1 item, got 0"; the real-network `scrapy crawl gazette_spider` also scraped 0 items. Root cause + fix documented inline with a ~10-line comment block. **(2) Step 2b plan documents** added to `enigmatrix-docs/m1/planned-for-devlopment/`. `2.md` covers the full Celery wiring + Stage-B extraction plan: Celery infra (Redis broker, `celery_config.py`, `tasks/__init__.py`, `tasks/m1/__init__.py`), 2 Celery tasks (`gazette_scraper` + `extract_gazette` with PyMuPDF/pdfplumber/Tesseract chain), Alembic migration `202605230001` adding `raw_text`/`extraction_method`/`extracted_at`, 5 new deps, 4 new tests, spider pipeline updated to enqueue `extract_gazette.delay(...)` replacing the TODO log. `2_setup.md` is the paired forward-looking setup + verification guide (prerequisites, 3-terminal startup, automated verification, manual smoke check, rollback, 7-row troubleshooting table). Step 2b's actual execution stays as a separate turn per the Session-24 procedural rule. | 🟢 | session 25 | (1) `cd enigmatrix-backend && uv run pytest -k test_spider_parse_yields_gazette_item -v` reports 1 passed (no Docker required); (2) `enigmatrix-docs/m1/planned-for-devlopment/` contains all 4 paired files (`1.md`, `1_setup.md`, `2.md`, `2_setup.md`); (3) all 4 tracker files reference Session 25 / F-147; (4) `git diff --stat enigmatrix-backend/` shows only `scraper/spiders/gazette_spider.py` changed (1 file in the backend); zero changes in `enigmatrix-frontend/`, `enigmatrix-ml/`, `enigmatrix-infrastructure/` |

## Session 24 — `planned-for-devlopment/` folder convention + Step 2a setup doc + Vercel URLs

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-146 | **`planned-for-devlopment/` folder convention + Step 2a setup doc + Vercel URLs noted (Session 24).** Doc-only follow-up to Session 23. Established a new procedural rule: every plan I produce from this turn onward also lands in `enigmatrix-docs/m1/planned-for-devlopment/` (folder name kept verbatim with the user's typo) as a paired `<N>.md` + `<N>_setup.md`. Added the paired `1_setup.md` for Session 23's Scrapy gazette spider — 10-section user-actionable guide covering deployment context (live URLs <https://enigmatrix-backend.vercel.app/> + <https://enigmatrix-frontend.vercel.app/>), prerequisites, local setup (`uv sync` + `.env` + `alembic upgrade head`), automated verification (`uv run pytest`), manual smoke check against real `documents.gov.lk`, rollback, and what's deferred to Step 2b. Notes that the Vercel-deployed FastAPI app does NOT run the Scrapy spider — Vercel is serverless; the spider runs locally (or on Fly.io in production per doc 13). Next paired files will be `2.md` + `2_setup.md` when Step 2b's plan lands. | 🟢 | session 24 | (1) `enigmatrix-docs/m1/planned-for-devlopment/1_setup.md` exists; (2) all 4 tracker files reference Session 24 / F-146; (3) `git diff --stat enigmatrix-backend/ enigmatrix-frontend/ enigmatrix-ml/ enigmatrix-infrastructure/` empty (zero code drift); (4) both Vercel URLs appear in `1_setup.md` deployment-context section |

---

## Session 53 — M1 pipeline admin UX audit

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-184 | **M1 pipeline admin UX/UI audit — 14-finding report (Session 53).** Hands-on exploratory audit of all six `/admin/m1/pipeline` admin areas (Overview, Trace, Recent Runs, Raw Extraction, Gazette Table, Step Detail pages 2a–2f) via Claude-in-Chrome + `javascript_tool` + `read_network_requests`, logged in as `admin@enigmatrix.lk`. Identified 14 UX/UI findings (1 Critical, 4 High, 6 Medium, 3 Low) and 7 positive observations. Generated Word document `M1_Pipeline_UX_Audit.docx` (44 KB) using `python-docx` (npm docx package blocked by registry 403). Output saved to `C:\Users\Administrator\Documents\Claude\Projects\Understanding Information Barriers to Regulatory Compliance Among Sri Lankan SMEs\M1_Pipeline_UX_Audit.docx`. Report includes: cover, executive summary with severity table, 14-row summary table, per-finding detail blocks (problem + recommendation + impact), What Works Well section, sprint-ordered action plan, test methodology appendix. See [plan](plans/2026-05-22_Plan%20M1%20pipeline%20admin%20UX%20audit%20—%2014%20findings%20report.md). | 🟢 | session 53 | `M1_Pipeline_UX_Audit.docx` exists in workspace folder; contains 14 finding blocks, severity summary table, and prioritised action plan. |

---

## Session 52 — AnimatedList + FuzzyText UI component suite + theme toggle refactor

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-179 | **AnimatedList component suite (Session 52).** Created `components/ui/animated-list.tsx` (exports `AnimatedItem` — scroll-triggered `motion.div` with `useInView({ amount: 0.15, once: false })`; entrance: `scale 0.95→1`, `opacity 0→1`, `y 8→0`, 220ms easeOut; `AnimatedListScrollable` — scrollable container with gradient overlays; default `AnimatedList` — string-array list with keyboard nav) and `components/ui/animated-list.css` (theme-aware via HSL CSS vars: `--background`, `--card`, `--border`, `--muted`; scrollbar styled with `--muted-foreground`; `.sticky-thead` helper). Uses `framer-motion` v11 `useInView`. | 🟢 | session 52 (user: "Integrate the <AnimatedList /> component from React Bits … upgrade them in the whole code base") | `components/ui/animated-list.tsx` + `animated-list.css` exist; `AnimatedItem` wrapping any `ReactNode` triggers scale+fade+y entrance on viewport entry. |
| F-180 | **AnimatedItem applied to 6 list/card pages + sticky table headers across 13 table files (Session 52).** Applied `AnimatedItem` wrappers with staggered `delay` to: steps page (StepTile grid), sources page (SourceCard grid), research-log page (6 MetricTile + SessionCard list), docs page (docs cards), surveys page (module cards + recent sessions), dashboard page (regulation cards). Changed all 13 table wrappers from `overflow-hidden` to `className="relative overflow-auto rounded-lg border bg-background max-h-[520px]"` and added `<TableHeader className="sticky top-0 z-10 bg-background">`. Tables: surveys/history, activity-log, m3/risk-signals, m2/scores, m2/questions, m1/pdf-records, questions-client, surveys-client, surveys/awareness/responses, regulations-client, users-client, survey/questions, docs/m1. | 🟢 | session 52 | Items on all 6 list pages animate in on scroll entry; table headers stick at top while bodies scroll; max height 520px on all 13 tables. |
| F-181 | **FuzzyText component (Session 52).** Created `components/ui/fuzzy-text.tsx` — full TypeScript port of React Bits FuzzyText canvas component; per-row pixel displacement with `requestAnimationFrame` loop. Key adaptation: `color` defaults to `"currentColor"`, resolved at runtime via `window.getComputedStyle(canvas).color` so the canvas automatically tracks the CSS `color` property from the parent element — correct in both light and dark mode and per-section colour classes. Full prop surface: `fontSize`, `fontWeight`, `fontFamily`, `color`, `enableHover`, `baseIntensity`, `hoverIntensity`, `fuzzRange`, `fps`, `direction` (horizontal/vertical/both), `transitionDuration`, `clickEffect`, `glitchMode`, `glitchInterval`, `glitchDuration`, `gradient`, `letterSpacing`, `className`. Also created `components/ui/fuzzy-not-found-hero.tsx` — thin `"use client"` island wrapping FuzzyText at display scale for use by the server `not-found.tsx` page. | 🟢 | session 52 (user: "Integrate the <FuzzyText /> component from React Bits … correctly and beautifully") | Canvas renders at correct size; `getComputedStyle` returns the inherited colour hex in both themes; glitch mode fires at configured interval; `clickEffect` bursts on click; `transitionDuration` smooths intensity ramp. |
| F-182 | **FuzzyText applied to 404, ComingSoon, Platform Map, Research Log (Session 52).** (1) `app/not-found.tsx` — imports `FuzzyNotFoundHero`; "404" rendered at `fontSize="clamp(5rem,20vw,14rem)"`, `glitchMode`, `glitchInterval=3500`, `clickEffect`. (2) `components/coming-soon.tsx` — added `"use client"`; title rendered with `fontSize="1.125rem"`, `baseIntensity=0.06`, `hoverIntensity=0.32`, wrapped in `aria-label + role="heading" aria-level={2}` div. (3) `app/(app)/docs/platform-map/page.tsx` — stat hero numbers use `fontSize="1.375rem"`, `glitchMode`, `glitchInterval=3600`; section headers use `fontSize="1rem"`, `enableHover`; both inherit section accent colour via CSS cascade. (4) `app/(admin)/admin/research-log/page.tsx` — MetricTile metric numbers use `fontSize="1.25rem"`, `glitchMode`, `glitchInterval=4800`; inherits tone class colour. (5) `components/layout/page-header.tsx` — FuzzyText was applied to `h1` then **reverted** after user feedback: page header plain `h1` style is intentionally unchanged. | 🟢 | session 52 | 404 "404" canvas renders with glitch+click effects; ComingSoon title fuzzes on hover; platform-map stat numbers fuzz+glitch in section accent colour; research-log metrics fuzz+glitch in tone colour; `page-header.tsx` bit-for-bit identical to pre-session state. |
| F-183 | **Theme toggle: dropdown → single-click toggle button + View Transitions API circular reveal (Session 52).** Rewrote `components/layout/theme-toggle.tsx`: removed shadcn dropdown menu (Light/Dark/System options); replaced with a single `<Button variant="ghost" size="icon">` whose `onClick` computes `(x,y)` from `MouseEvent.clientX/Y` and `endRadius = Math.hypot(max(x,W-x), max(y,H-y))`; calls `document.startViewTransition` (feature-detected, graceful fallback); new layer gets `clip-path circle(0→endRadius)` + `scale(1.015→1)` + `opacity(0.92→1)` over 600ms `cubic-bezier(0.22,1,0.36,1)` (easeOutQuint); old layer gets `opacity(1→0.6)` + `scale(1→0.985)`; `theme-transitioning` class added/removed on `<html>` for the transition's lifetime. Sun/Moon icon spans swap with `rotate+scale+opacity` over 500ms easeOutQuint. `components/providers.tsx`: removed `disableTransitionOnChange`. `app/globals.css`: `::view-transition-old/new(root)` rules (no default animation, z-index layering); `html.theme-transitioning * { transition:none!important }` suppresses competing universal colour transitions; GPU promotion (`will-change`, `backface-visibility`, `transform-origin`) on pseudo-elements; universal colour-token transition block (200ms on `*`, 150ms on interactive elements); `prefers-reduced-motion: reduce` safety. | 🟢 | session 52 (user: "make the theme switching button as not the drop down just toggle or on click its change the theme and when them changing its should be beautifully changes") | Circular reveal expands from click point ~600ms; old snapshot fades+scales under; icons swap with easeOutQuint; `theme-transitioning` absent outside 600ms window; reduced-motion → instant swap; no `disableTransitionOnChange` in `providers.tsx`. |

## Session 55 — Railway production deployment + Stages 1-4 (cancel/rollback, PDF metadata, PDF Records page, audit)

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-193 | **Railway production deployment of `enigmatrix-backend` (Session 55).** First production push of the FastAPI + Celery worker + Beat backend to Railway service `satisfied-prosperity` (production env) at `https://enigmatrix-backend-production.up.railway.app`. Topology: Vercel frontend ↔ Railway single-service container running uvicorn + Celery worker (`--concurrency=2`) + Beat sharing a 10 GB `/data/storage` volume ↔ Railway Redis plugin (CELERY_BROKER_URL = `${{Redis.REDIS_URL}}` variable reference) ↔ external Aiven managed Postgres. Five iterative build failures fixed (workspace=true in pyproject; push-without-stage; Railway cached image; uv doesn't expand env vars in URLs; final fix via git `insteadOf` URL-rewrite). Final architecture uses `ARG GITHUB_TOKEN` at build time + matching env-var injection at runtime startup (`start_railway.sh`) so both `uv sync --frozen --no-dev` and lazy `uv run` codepaths authenticate cleanly. **Known security risk (pending #12):** PAT leaked in build log via Docker's `RUN` expansion + baked into image `/root/.gitconfig`; operator deferred rotation + BuildKit-secrets refactor. See [plan](plans/2026-05-22_Plan%20Railway%20production%20deployment%20—%20backend%20deploy%20chain%20and%20PAT%20leak.md). | 🟢 | Session 55 (Railway-Deployment-Plan.md execution) | `https://enigmatrix-backend-production.up.railway.app/health` returns 200; admin can trigger a Bills extraction and worker logs show `extract_gazette: ... succeeded` lines; CORS preflight from Vercel returns 200. |
| F-194 | **Cancel + rollback admin endpoint — `POST /api/v1/admin/m1/extraction/cancel/{task_id}` (Session 55).** Destructive admin endpoint that revokes a running Celery task (`celery_app.control.revoke(task_id, terminate=True, signal="SIGTERM")`) AND deletes every `m1_regulations` row + on-disk PDF the run produced. Scope filters mirror the original `/trigger` payload (date_from, date_to, source_id, queued_at). `force=true` default wipes rows in every pipeline stage including `preprocessed`; `force=false` preserves rows at status='preprocessed'. PDFs deleted before DB rows so a mid-rollback crash leaves orphan files (recoverable via existing `/reconcile`) rather than orphan DB rows. Path-traversal guard reuses `/raw-pdf` endpoint's `relative_to(storage_root)` check. SQLAlchemy `cascade="all, delete-orphan"` on `M1Regulation.penalties` + `M1Regulation.sub_documents` handles child rows automatically. Broker-down recovery: if Redis is unreachable, `revoked=false` returned but DB rollback still runs so state matches operator intent. New file `app/services/m1_extraction_cancel.py` (~150 lines). Schemas `CancelExtractionIn` + `CancelExtractionOut` added to `app/schemas/m1_pipeline.py`. See [plan](plans/2026-05-22_Plan%20M1%20cancel%20+%20rollback%20endpoint%20and%20frontend%20button.md). | 🟢 | Session 55 (operator request: "able to revoke ... rollback to initial state where delete all the extracted and download ... PDF in the storage") | `POST /cancel/{task_id}` with valid scope returns `CancelExtractionOut` with `revoked=true` + `deleted_rows`/`deleted_pdfs` counts; rows + PDFs in scope are gone post-call; cascade-delete handles penalties + sub_documents. |
| F-195 | **Cancel button + ConfirmDialog + inline result panel on extraction status card (Session 55).** Destructive "Cancel & roll back" button added to the running-task status card on `app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx`. Visible only while `isRunning` (task status NOT in TERMINAL_STATUSES), destructive variant + XCircle icon. ConfirmDialog (reuses `components/ui/confirm-dialog.tsx`, danger variant) warns with the task_id prefix + scope + source + "This cannot be undone." `useMutation` posts the scope from `task` with `force: true`; on success invalidates `["m1-source", sourceId]`, `["m1-extraction-progress", task?.task_id]`, `["m1-extraction-summary", task?.task_id]` so the UI updates immediately without waiting for the 5s poll. Inline result panel shows deleted_rows + deleted_pdfs + skipped_pdfs + errors (first 5 + "…and N more" overflow). Added `CancelExtractionIn` / `CancelExtractionOut` interfaces + `cancel()` method to `lib/api/m1-gazette-extraction.ts`. See [plan](plans/2026-05-22_Plan%20M1%20cancel%20+%20rollback%20endpoint%20and%20frontend%20button.md). | 🟢 | Session 55 | Button only appears during running task; click opens dialog with full destructive copy; confirm calls the endpoint; on success the page reflects the rollback within ~1s; on error the dialog stays open with the error message inline. |
| F-196 | **Per-PDF metadata columns + extract-time population (migration `202605280001` + `pdf_metadata.py` helper) (Session 55).** Adds 4 columns to `m1_regulations` via migration `alembic/versions/202605280001_m1_pdf_metadata.py` (NEW): `file_size_bytes` BigInteger nullable, `sha256` String(64) nullable, `pdf_pages` SmallInteger nullable, `language` String(10) nullable; plus indexes on `sha256` (dedup checks) and `language` (UI filter). New helper `app/extraction/pdf_metadata.py` (NEW): `compute_file_size`, `compute_sha256` (streams 1 MiB chunks), `compute_pdf_pages` (via PyMuPDF), `detect_language` (Unicode-codepoint ratio across Sinhala/Tamil/Latin blocks), `compute_pdf_metadata` (resilient aggregator). Helper subsequently revised (by user/linter) to accept `bytes | Path` input via `PdfSource` union and language codes aligned to ISO 639-1 two-letter (`'si'/'ta'/'en'/'unknown'`) — intentionally diverges from ml's heavier fasttext implementation to avoid 125 MB `lid.176.bin` model in the Railway image. Extract task wiring in `app/tasks/m1/extract_gazette.py` calls `compute_pdf_metadata` after successful extraction and writes the 4 fields onto `row`. Plumbed through `ExtractionProgressRow` schema + service row builder + unknown-list row builder + frontend TS interface. Legacy rows stay NULL until `/re-extract` reruns them — no backfill script. See [plan](plans/2026-05-22_Plan%20Per-PDF%20metadata%20schema%20and%20population.md). | 🟢 | Session 55 (operator request: "upgrade the schema where I can be able to save the relevant PDF metadata") | `alembic upgrade head` applies migration `202605280001`; newly-extracted rows populate all 4 metadata fields; `/progress` + `/unknown` responses include the new fields; frontend types match backend schema. |
| F-197 | **PDF Records browse-all admin page — `/admin/m1/pdf-records` + `GET /pdf-records` endpoint (Session 55).** New backend endpoint `GET /api/v1/admin/m1/extraction/pdf-records` returns every gazette PDF the pipeline has ever ingested, with filters (`source_id`, `status`, `language`, `date_from`, `date_to`, `search` ILIKE on gazette_number/regulation_short_code) + pagination (`page` default 1, `page_size` default 50, max 200). Sort: `gazette_published_date DESC NULLS LAST, created_at DESC`. Lean `PdfRecordOut` shape drops `raw_text`/`cleaned_text` payloads. New frontend page `app/(admin)/admin/m1/pdf-records/page.tsx` — filter bar with 6 controls + paginated table + per-row icon actions (open source URL, download PDF via `/raw-pdf` endpoint, view pipeline trace). Subsequently refactored (by user/linter) to use `AdminPageLayout` sticky-sidebar + `Combobox` + project `Pagination`/`Table` + new `useAuthToken()` hook + shared `pipelineStatusTone` util; filter UX flipped to immediate-apply for dropdowns/dates + Enter-to-apply for search. Sidebar nav entry added under M1 Pipeline group (FileText icon); i18n key `nav.adminM1PdfRecords` added to en/si/ta JSON files (Sinhala/Tamil keep English placeholder for now). UX choices distilled from invoked `design:ux-copy` / `design:user-research` / `design:design-system` skills: concrete next-step empty-state copy, pagination over infinite-scroll, lean row shape. See [plan](plans/2026-05-22_Plan%20PDF%20Records%20browse-all%20admin%20page.md). | 🟢 | Session 55 (operator request: "separate view or UI that can view the relevant PDF download or extracted record of it") | `/admin/m1/pdf-records` renders with filter bar + paginated table; per-row Download opens PDF via `/raw-pdf`; per-row Open opens `source_url` in new tab; per-row View goes to `/pipeline/trace/<id>`; sidebar shows "PDF Records" under M1 Pipeline group. |
| F-198 | **Cross-repo Stage 4 code-quality audit + three HIGH inline fixes (Session 55).** Six parallel Explore agents audited backend code, ml code, frontend code, root + deploy configs, security posture, and cross-stack API contract drift — producing 60+ findings. Verified the highest-stakes claims by reading source: caught **one false positive** (path traversal in `/categorize` was a non-issue) and confirmed **one real latent bug** (`app/deps.py:44` references `ForbiddenError` without importing it; masked in production because all real admin users have `role == "admin"`). Three HIGH-severity inline fixes applied: **(H2)** added `ForbiddenError` to imports in `app/deps.py`; **(H3)** added `.gitignore` Cowork-artifacts block (`.claude/`, `.cowork/`, `outputs/`, `local-agent-mode-sessions/`, `MEMORY.md`) to all 4 `.gitignore` locations (backend, frontend, ml, xyz root); **(H4)** trimmed `ExtractionRowStatus` to backend's real 4 values — subsequently **reverted by user** to keep forward-compat with planned BUILD_07 Phase 3+ stages. Five MEDIUM follow-ups deferred as discrete tasks #18–22 (reconcile detect_language duplication; status enum + magic-constants refactor; i18n parity; useAuthToken + statusTone shared utils; rate-limit + CORS + Celery hardening). PAT rotation (#12) cross-referenced and remains operator-deferred. See [plan](plans/2026-05-22_Plan%20Cross-repo%20code%20quality%20audit%20—%20Stage%204.md). | 🟢 | Session 55 | Six audit angles run by parallel subagents; high-stakes claims verified by source reads; three HIGH inline fixes landed; five MEDIUM follow-ups captured as discrete tasks; PAT-rotation risk explicitly tracked. |

## Session 54 — M1 extraction UX improvements + run history persistence + pool fix

| id | Feature | Status | Source | Acceptance |
|----|---------|--------|--------|------------|
| F-185 | **Sticky M1 extraction inner sidebar (Session 54).** Added `lg:items-start` to the grid container in `app/(admin)/admin/m1/pipeline/layout.tsx` so the `<aside>` no longer stretches to full content height (which was blocking `position:sticky`). Added `max-h-[calc(100vh-2rem)] overflow-y-auto` to the nav card for independent scrolling when the nav list is taller than the viewport. | 🟢 | Session 54 (task #16) | Left nav stays fixed at top of viewport while content scrolls; nav card itself scrolls independently if nav list overflows. |
| F-186 | **Resume/restart extraction for partial-state runs (Session 54).** Created `components/m1-extraction/resume-extraction-card.tsx` (NEW). Rendered when a run is terminal and `extractedCount > 0 || ingestedCount > 0` (rows stuck mid-pipeline). Two actions: (1) **Resume preprocessing** — fetches all extracted rows via `getProgress`, calls `rePreprocess` on each serially; (2) **Restart from scratch** — re-triggers the same date range via the trigger mutation, calls `onRestart` callback with the new trigger so the history table and progress panel update. | 🟢 | Session 54 (task #17) | Card appears on terminal runs with extracted/ingested rows; "Resume preprocessing" submits `rePreprocess` for each extracted row; "Restart from scratch" fires a new trigger and updates the active trigger context. |
| F-187 | **Full extraction history table — replaces 5-item pill strip (Session 54).** Raised `MAX_HISTORY` 5 → 20 in `lib/m1-extraction/trigger-history.ts`. Created `components/m1-extraction/extraction-history-table.tsx` (NEW) replacing `RecentTriggersBar`. Table columns: task_id (truncated mono), date range, queued (relative time), live Celery status badge, source_id. Status badge polling: `useQueries` with 10-second `refetchInterval`; interval returns `false` once a terminal status (SUCCESS, FAILURE, REVOKED) is observed. Active row highlighted with blue dot + "viewing" pill. | 🟢 | Session 54 (task #18) | All history rows visible (up to page_size, default 20); status badges update live; polling stops for terminal-status rows; active row visually distinct. |
| F-188 | **Auto-scroll to progress panel on history row select (Session 54).** Added `progressRef` (React `useRef`) to the extraction page and `scrollIntoView({ behavior: 'smooth' })` call inside `selectHistoricalTrigger`. Clicking any row in the extraction history table automatically scrolls the viewport to the progress panel below, eliminating the need to manually scroll on long pages. | 🟢 | Session 54 (task #19) | Clicking a history table row triggers smooth scroll to the progress/trace panel. |
| F-189 | **`m1_extraction_runs` DB model + Alembic migration (Session 54).** Created `app/models/m1_extraction_run.py` (NEW): `M1ExtractionRun` SQLAlchemy 2.0 model with columns `run_id` (UUID PK), `task_id` (TEXT UNIQUE), `source_id`, `date_from/to`, `queued_at`, `queued_by_id` (FK → `users.id` ON DELETE SET NULL), `queued_by_email` (denormalised snapshot), `celery_status` (default PENDING), `result` (JSONB), `traceback`, `completed_at`, `rows_ingested/extracted/preprocessed/failed` (nullable INT), `created_at/updated_at` (TimestampMixin). Composite index `(source_id, queued_at)`. Updated `app/models/__init__.py`. Created Alembic migration `202605210002_m1_extraction_runs.py` (`revision='202605210002'`, `down_revision='202605280001'`; FK `users.id`; 4 indexes). | 🟢 | Session 54 (task #20) | `alembic upgrade head` succeeds; `m1_extraction_runs` table exists with all columns + indexes. |
| F-190 | **Backend: persist run on trigger + status sync on poll/cancel + `GET /runs` endpoint (Session 54).** (1) `trigger_extraction`: added `db: AsyncSession = Depends(get_db)`; INSERTs `M1ExtractionRun` after `run_scraper.delay()` (non-fatal — DB error swallowed, task still proceeds). (2) `extraction_status`: added `db` dep; side-effect UPDATEs `celery_status` + `completed_at` on first terminal transition only. (3) `cancel_extraction`: UPDATEs `celery_status='REVOKED'` after rollback. (4) New `GET /api/v1/admin/m1/extraction/runs` endpoint: paginated (max 100), filterable by `source_id`, ordered `queued_at DESC`. Added `ExtractionRunOut` + `ExtractionRunsListOut` to `app/schemas/m1_pipeline.py`. | 🟢 | Session 54 (task #21) | POST /trigger inserts run row; GET /status updates celery_status on terminal; POST /cancel sets REVOKED; GET /runs returns paginated list. |
| F-191 | **Frontend: run history from `GET /runs` + localStorage write-through cache (Session 54).** Added `ExtractionRunOut`, `ExtractionRunsListOut` interfaces and `listRuns(token, {sourceId, page, pageSize})` to `lib/api/m1-gazette-extraction.ts`. Extraction page: `runsQuery` (`queryKey: ["m1-extraction-runs", sourceId]`) fetches from `GET /runs`; `toTriggerOut()` normalizer converts `ExtractionRunOut → GazetteExtractionTriggerOut`; `history` useMemo returns API data when available, falls back to `localHistory` (localStorage); trigger mutation invalidates `["m1-extraction-runs", sourceId]` on success. | 🟢 | Session 54 (task #22) | History table shows API data (server-side, unlimited, shared across sessions); localStorage used as fast-paint fallback only. |
| F-192 | **SQLAlchemy QueuePool fix — `pool_size=3`, `max_overflow=5` (Session 54).** Increased `pool_size: 1 → 3` and `max_overflow: 2 → 5` in `app/db/session.py`. Root cause: adding `db: AsyncSession` deps to `trigger_extraction` and `extraction_status` (previously stateless) raised per-request connection demand; `AuditMiddleware._write_passive_log` already consumed one pool slot per request via detached `asyncio.create_task(SessionLocal())`. Old cap (pool_size=1, max_overflow=2 = 3 total) exhausted on two overlapping admin requests. New budget: 8 uvicorn + ~2 Celery = ~10 total, within Aiven's ~20–25 connection limit. | 🟢 | Session 54 (QueuePool hotfix) | `GET /api/v1/users/me` no longer returns HTTP 500 under normal admin concurrent usage; `QueuePool limit` error does not appear in uvicorn logs. |
| F-200 | **Slice 3 — backend-local XLSX parser wrapper + ML parser handle close (Session 57).** Created `enigmatrix-backend/app/services/m1_xlsx_parser.py`, re-exporting `read_canonical_excel`, `CANONICAL_FIELDS`, `HEADER_ALIASES` (as `CANONICAL_FIELD_ALIASES`) from the sibling `enigmatrix-ml` package via the existing `sys.path` shim, plus a new `parse_xlsx_to_canonical_rows(payload_bytes) -> (rows, warnings_by_key)` bytes adapter that wraps a temp-file round-trip. Refactored `app/services/m1_dataset_upload.py` to import from the wrapper instead of the inline `_resolve_xlsx_reader()` helper (deleted). Added `wb.close()` to `enigmatrix-ml/m1/evaluation/xlsx_reader.read_canonical_excel` so Windows callers do not leak file handles past the function return (openpyxl's `read_only=True` mode otherwise pins the file). | 🟢 | Slice 3 plan §B1 | `from app.services.m1_xlsx_parser import parse_xlsx_to_canonical_rows` succeeds; upload pipeline path unchanged; ML parser closes handles. |
| F-201 | **Slice 3 — reproducible 5-row M1 master xlsx test fixture (Session 57).** Wrote `enigmatrix-backend/app/tests/fixtures/_build_m1_master_sample.py` (~60 lines) generating `m1_master_sample.xlsx` (5411 bytes) with 9 header columns — 8 canonical or aliased + 1 unmapped `Notes` column — and 5 data rows exercising: a fully-populated baseline, a Sinhala-only title row (round-trips as native `str`), an out-of-range `severity_level=9` (warning path), an unparseable `gazette_published_date='not a date'` (warning path), and a clean closing row. Generated once via `python app/tests/fixtures/_build_m1_master_sample.py`; recipe committed alongside so the binary fixture is reproducible. Same file copied to `enigmatrix-frontend/tests/e2e/fixtures/m1_master_sample.xlsx` for the Playwright spec. | 🟢 | Slice 3 plan §B2 | Round-trip via `parse_xlsx_to_canonical_rows` yields 5 rows; Sinhala title is a `str` with at least one Sinhala codepoint; warnings dict has keys `2261/17` and `2261/18`; every row's `_extras` carries the `Notes` column. |
| F-202 | **Slice 3 — parser unit test suite (Session 57).** Wrote `enigmatrix-backend/app/tests/unit/test_m1_xlsx_parser.py` (9 tests, all passing locally): asserts `CANONICAL_FIELDS` has 21 entries; spot-checks `CANONICAL_FIELD_ALIASES` for the master-Excel headers (`gazette no.`, `published date`, `severity`, `is sme relevant`); verifies the 5-row roundtrip; checks the Sinhala title is `str` and contains at least one Sinhala codepoint (U+0D80–U+0DFF); confirms empty optional cells coerce to `None` without emitting warnings; confirms severity-out-of-range and unparseable-date both produce per-row warnings; confirms unmapped `Notes` column survives in each row's `_extras`; confirms `read_canonical_excel(Path)` (the upload service's path-based call site) still produces a dict keyed by regulation_key. | 🟢 | Slice 3 plan §B3 | `python -m pytest -q app/tests/unit/test_m1_xlsx_parser.py` → 9 passed (verified locally with `--confcutdir=app/tests/unit` to bypass the testcontainers conftest). |
| F-203 | **Slice 3 — upload integration test suite (Session 57).** Wrote `enigmatrix-backend/app/tests/integration/test_m1_dataset_upload.py` (5 tests) reusing the existing `client` + `db_session` fixtures from `app/tests/conftest.py`. Tests cover the slice-3 §Gate guarantees: (1) `test_upload_creates_sealed_version_and_rows` — POST to `/api/v1/m1/datasets`, then upload, then assert 5 rows persisted with regulation_key set `{2261/15..19}`; (2) `test_upload_same_sha_returns_409` — `version_already_exists` on bit-identical re-upload; (3) `test_pending_unsealed_upload_returns_423` — `upload_in_progress` even for a different-SHA upload while a previous unsealed version is pending; (4) `test_upload_writes_audit_row` — `audit_log` row with `event_type='m1.dataset.version.upload'` references the new `version_id`; (5) `test_promote_to_ground_truth_demotes_previous` — chained promote(A) → promote(B) leaves exactly one `is_ground_truth=true` row (partial-unique-index guarantee). Includes an autouse `_isolate_storage` fixture that redirects `STORAGE_LOCAL_PATH` into the per-test `tmp_path` and clears the settings cache. | 🟡 | Slice 3 plan §B4 | Tests syntactically validated; full execution requires testcontainers + Docker (not available in this Windows-native env). Run via `make test` or `cd enigmatrix-backend && uv run pytest app/tests/integration/test_m1_dataset_upload.py` once the proper Linux/WSL venv is available. |
| F-204 | **Slice 3 — `/admin/m1/datasets/new` create page + form component (Session 57).** Closed the broken-link bug on the list view: clicking "Create dataset" at `/admin/m1/datasets` now lands on a working route instead of a 404. Created `enigmatrix-frontend/app/(admin)/admin/m1/datasets/new/page.tsx` as a server component using `getTranslations` + `getAccessToken`, with `PageHeader` + breadcrumb (Dashboard → M1 Datasets → Create dataset). Created `enigmatrix-frontend/components/forms/m1-dataset-create-form.tsx` as a `"use client"` form using `react-hook-form` + `zodResolver(datasetCreateSchema)` with `Input`/`Select`/`Textarea`/`Label` shadcn primitives; fields: name (autoFocus, required, 1–200), kind (enum select defaulting to `manual_excel`, labels pulled from `m1Datasets.list.filters.*` to reuse the existing strings), description (optional textarea), tags (comma-separated parsed into `string[]`). On submit calls `datasetApi.create(token, payload)` and routes to `/admin/m1/datasets/${created.dataset_id}`; on error surfaces the message in an `Alert`. i18n: reuses the pre-existing `m1Datasets.create.*` namespace (no new keys needed; SI/TA carry `[TODO]` placeholders per the established repo pattern). | 🟢 | Slice 3 plan §F1 | `tsc --noEmit` produces zero errors attributable to either new file; visiting `/admin/m1/datasets/new` renders the form; submitting creates a dataset and redirects to its detail page. |
| F-205 | **Slice 3 — Playwright E2E spec for create/upload/promote/idempotency (Session 57).** Wrote `enigmatrix-frontend/tests/e2e/admin_m1_datasets.spec.ts` modelled on the existing `admin_regulations.spec.ts`. Uses `test.describe.serial` with four scenarios: (1) admin logs in, navigates to `/admin/m1/datasets`, clicks "Create dataset", lands on `/new`, submits a uniquely-named dataset, lands on detail; (2) admin uploads the canonical 5-row fixture (`tests/e2e/fixtures/m1_master_sample.xlsx`) via `setInputFiles` on `input[type="file"]`, asserts "upload complete" + "sealed" appear; (3) admin promotes the dataset to ground truth from the list view, asserts the green "ground truth" badge surfaces in the same table row (auto-accepts the `window.confirm` prompt); (4) admin re-uploads the same bytes and the UI surfaces a `version_already_exists` / `upload_failed_409` error string. Fixture file copied from the backend fixture into `tests/e2e/fixtures/`. | 🟡 | Slice 3 plan §F2 | Spec compiles under `tsc --noEmit` with zero spec-specific errors; full execution requires `make up && make dev-backend && make dev-frontend` + admin seed before `pnpm exec playwright test tests/e2e/admin_m1_datasets.spec.ts`. |
| F-206 | **Datasets restructure — i18n migration m1Datasets.* → datasets.m1.* + new datasets.hub.* namespace (Session 58).** In all three of `lib/i18n/messages/{en,si,ta}.json`: dropped the top-level `m1Datasets` namespace; added a new top-level `datasets` namespace with two sub-namespaces — `datasets.hub.{title, subtitle, comingSoonBadge, modules.{m1,m2,m3}.{title,description,cta}}` (new hub-only strings; EN gets real copy, SI/TA carry `[TODO si]`/`[TODO ta]` prefixes per established repo pattern) and `datasets.m1.*` (verbatim relocation of every existing `m1Datasets.*` key — list/create/upload/detail/version/promote sub-namespaces — no content change, just nested under a different parent). Replaced flat `nav.adminDatasets` key with three new keys: `nav.adminDatasetsGroup` (collapsible parent label "Datasets"), `nav.adminDatasetsHub` ("Hub"), `nav.adminDatasetsM1` ("M1"). All three files validated via `json.load` and same top-level key count (19 each); spot-checked `datasets.m1.list.columns.name`, `datasets.hub.modules.m1.title`, and absence of `m1Datasets` / `nav.adminDatasets`. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | 🟢 | Session 58 plan §4 | All three i18n files validated; `tsc --noEmit` plus exhaustive grep show no stale `m1Datasets` / `nav.adminDatasets` references anywhere in the frontend. |
| F-207 | **Datasets restructure — moved M1 pages under /admin/datasets/m1/* + new hub at /admin/datasets (Session 58).** PowerShell `Move-Item` of five page files: `app/(admin)/admin/m1/datasets/{page,new/page,[datasetId]/page,[datasetId]/upload/page,[datasetId]/versions/[versionId]/page}.tsx` → `app/(admin)/admin/datasets/m1/...` (same shape). After move, edited each file in place: every `useTranslations("m1Datasets")` (4 sites across pages + 2 in `components/forms/m1-dataset-create-form.tsx`) became `useTranslations("datasets.m1")`; every hardcoded `/admin/m1/datasets` link (~20 sites total across all five pages + the form's `router.push` + `Cancel` link + comment headers) became `/admin/datasets/m1`. Hub page (`app/(admin)/admin/datasets/page.tsx`) overwrites the prior 4-line `ComingSoon` placeholder with a real server component: `getTranslations()` + `PageHeader` (breadcrumb `Dashboard → Datasets`) + a `grid grid-cols-1 md:grid-cols-3 gap-4` of three module cards. M1 card (`Database` icon, full opacity, wrapped in `<Link href="/admin/datasets/m1">`, default-variant CTA button). M2 (`Brain` icon) and M3 (`GaugeCircle` icon) cards rendered with `opacity-60 pointer-events-none`, disabled outline-variant CTA, and a `Badge variant="secondary"` reading `datasets.hub.comingSoonBadge`. Old `app/(admin)/admin/m1/datasets/` subtree removed via PowerShell `Remove-Item -Recurse`; `m1/pdf-records/` and `m1/pipeline/` siblings intact. `tsc --noEmit` produces zero NEW errors attributable to any file authored or edited (hub, form, sidebar, spec); the 4 pre-existing `PageHeader children` drift errors travel with the moved files (already flagged as a Session-57 follow-up). Backend API path (`/api/v1/m1/datasets`) and `lib/api/m1-datasets.ts` client unchanged — frontend-only restructure. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | 🟢 | Session 58 plan §§1–3 | Visiting `/admin/datasets` renders the hub with 3 cards (M1 active linking to `/admin/datasets/m1`); all five M1 pages function at their new URLs; old `/admin/m1/datasets/*` URLs 404 cleanly. |
| F-220 | **Phase 3b — `research/data/labeling/batch_01.csv` (demo) + Makefile `labeling-batch` targets (Session 61).** Demo batch written by `scripts/sample_for_labeling.py --demo`: 200 rows (150 stratified + 40 k-means + 10 handpick); EN=103, SI=50, TA=47; years 2022–2025. `batch_01_provenance.json` sidecar with full sampling metadata. Makefile: `labeling-batch` (live DB, `BATCH ?= 1`) and `labeling-batch-demo` targets. **To produce the real batch:** `make labeling-batch` against production DB (requires `DATABASE_URL` env var). | 🟢 | Phase 3b roadmap §Step 3b | `research/data/labeling/batch_01.csv` written with 200 rows spanning 3 languages; provenance JSON present. `make labeling-batch-demo` passes without error. |
| F-219 | **Phase 3b — `scripts/sample_for_labeling.py` sampling CLI (Session 61).** CLI for the M1 annotation sampling pipeline. Loads from DB (async SQLAlchemy + `asyncio.run`) or `--demo` (synthetic 300-doc corpus). Heuristic pre-annotation (`predicted_category`) via keyword signals across all 12 categories. Calls `enigmatrix-ml/m1/data/samplers.sample_for_labeling()` with 150+40+10 split. Writes `research/data/labeling/batch_<N>.csv` + JSON provenance sidecar. Overwrite guard. `--dry-run` for count preview. `--batch N`, `--status`, `--seed`, `--k` flags. | 🟢 | Phase 3b roadmap §Step 3b | `python scripts/sample_for_labeling.py --demo --dry-run` → 200 rows summary; `--demo` → writes `batch_01.csv` (85 KB, 200 rows). |
| F-218 | **Phase 3b — `enigmatrix-ml/m1/data/samplers.py` sampling library (Session 61).** Full stratified + k-means + AL library. `stratified_sample()`: year × language cells, small-cell pass-through (< 5 → take all), `min(len, 20)` otherwise. `kmeans_diversity_sample()`: TF-IDF + `MiniBatchKMeans` (k=20 from silhouette pilot max 0.24), proportional cluster allocation, correct rounding. `find_minority_candidates()`: under-represented category detection by label count. `select_uncertainty_batch()`: margin-based AL (`1 - max_prob`). `sample_for_labeling()`: three-step entry point, optional `ALBaseline` for step 2. `ALBaseline(version)` / `ProductionBaseline()`: same TF-IDF+LR architecture, separate artefact paths. `compute_silhouette_curve()`: re-run quarterly as corpus grows. | 🟢 | Phase 3b roadmap §Step 3b / 05_M1_1_Sampling_Strategy.md | `python -c "from m1.data.samplers import sample_for_labeling"` imports without error; demo end-to-end run produces 200 rows. |
| F-217 | **Phase 3a — `research/data/calibration_set_v1.csv` annotator calibration set (Session 61).** 20 reference docs spanning all 12 regulatory categories, EN/SI/TA, and 4 edge-case types. Edge cases: `multi_penalty_type` (cal_014 EPF non-remittance penalty — must choose PENALTY_ENFORCEMENT not EPF_ETF_CHANGE; cal_017 Companies Act annual return late fee — PENALTY_ENFORCEMENT not BUSINESS_REGISTRATION), `no_sme_impact` (cal_016 State land vesting — no SME compliance), `gazette_with_tables` (cal_018 WHT table — annotators must parse tabular data), `repeal_gazette` (cal_019 ETF scheme repeal — transition not new obligation). Expert labels locked in `expert_change_category` / `expert_affected_sectors`. `annotator_notes` explains each decision. Stored at `research/data/calibration_set_v1.csv`. | 🟢 | Phase 3a roadmap §Step 3a / 09_M1_2_Annotation_Workflow_IAA_Protocol.md §Step 1 | File exists with 20 rows; all 12 `expert_change_category` values present; 3 languages represented; 4 edge-case flags set. |
| F-216 | **Phase 3a — `research/data/label_studio_config.xml` Label Studio project config (Session 61).** Full Label Studio annotation interface XML. Interface elements: metadata header strip (`gazette_number`, `regulation_key`, language, year, sample_type); `<Text>` display of `$classification_chunk`; single-label `<Choices name="change_category">` (12 values with `hint=` decision criteria matching 09_M1_Annotation_Guidelines §2); multi-label `<Choices name="affected_sectors">` (10 sectors, `showInline`); `<Choices name="is_sme_relevant">` binary; `<Rating name="confidence" maxRating=3>`; `<TextArea name="annotator_notes">`. XML comments include Docker setup, project creation, import/export workflow, and calibration-test separation instructions. Column→variable mapping table. | 🟢 | Phase 3a roadmap §Step 3a / 09_M1_Annotation_Guidelines.md §1.2 | Paste into Label Studio Settings → Labeling Interface → Code tab; import `batch_01.csv`; annotation UI renders correctly with all 12 categories and hints. |
| F-209 | **Slice 8 — `backfill_legacy_baseline.py` idempotent backfill script (Session 60).** Created `enigmatrix-backend/scripts/backfill_legacy_baseline.py`. Async script materialises all `m1_regulations` rows at `status IN ('preprocessed','extracted')` and `archived_at IS NULL` into a new `M1Dataset` (name="Legacy baseline (snapshot of m1_regulations 2026-05-23)", kind=`extraction_run`) + `M1DatasetVersion` (source=`backfill`, `frozen_at=NOW()`) + batched `M1DatasetRow` inserts (chunk=200). Computes content SHA-256, sets it on the version row, writes `m1.dataset.version.backfill` audit row via `audit_service.record()`. Idempotency guard: aborts with log message if a dataset with the same name already exists and `archived_at IS NULL`. `--dry-run` flag emits a row count but makes no DB changes. Admin email required arg (populates `changed_by` on the audit row). | 🟢 | Slice 8 plan §Task 8.1 | Script runs without error on a fresh DB. `--dry-run` prints row count. Re-running after first successful execution exits early with "already exists" message; no duplicate rows in `m1_datasets`. |
| F-210 | **Slice 8 — GE-style data quality suites + `validate_dataset_version` Celery task (Session 60).** Four JSON expectation suites under `enigmatrix-backend/data_quality/expectations/`: `m1_dataset_rows.json` (5: regulation_key_format `^\d+/\d+$`, fields_non_empty, raw_text_null_or_non_empty, raw_text_present_when_method_set conditional, gazette_date_parseable), `m1_extraction_profiles.json` (3: profile_name_unique, entrypoint_non_empty, exactly_one_active_legacy), `m1_measurement_scores.json` (4: status_in_enum, score_range 0.0–1.0, metric_version_present, regulation_key_format), `m1_regulations.json` (4: status_in_enum, raw_text_non_empty_when_extracted, gazette_number_format, severity_level_range). Checkpoint `data_quality/checkpoints/post_extraction_check.yaml` (mode=report, fail_on_error=false). New Celery task `app/tasks/m1/validate_dataset_version.py` — fire-and-forget on sealed version; runs `_run_dataset_rows_suite()`, persists violations into `m1_dataset_versions.validation_warnings`; `max_retries=2, ignore_result=True`. Wired into `app/tasks/m1/run_extraction.py` post-seal via `try/except` best-effort dispatch. `"app.tasks.m1.validate_dataset_version"` added to `celery_config.py` include list. | 🟢 | Slice 8 plan §Task 8.2 | Suites import and parse without error. `validate_dataset_version.delay(version_id)` dispatches without exception (queue worker must be running for actual execution). `post_extraction_check.yaml` references both expectation suite paths correctly. |
| F-211 | **Slice 8 — `regenerate_thesis_tables.py` + `make thesis-artifacts` (Session 60).** Script at `scripts/regenerate_thesis_tables.py` reads latest complete `M1MeasurementRun` rows from DB (`state='complete'`, ordered by `started_at DESC`). Emits 6 artefacts to `data/thesis/`: `table_4_1_per_field_accuracy.csv` (profiles × fields, primary-metric mean), `table_4_2_per_stratum_cer.csv` (placeholder pending slice-2 gold set), `table_4_3_profile_comparison.csv` (pairwise delta + Wilcoxon p placeholder), `figure_4_1_calibration.svg` (overall-score bar chart SVG), `figure_4_2_status_distribution.svg` (stacked bar of completeness statuses SVG), `RUN_PROVENANCE.md` (run IDs, git SHA, version SHAs). Supports `--demo` (synthetic data, no DB) and `--runs <uuid1>,<uuid2>` (restrict to specific runs). Uses `asyncio.run()` with `SessionLocal`. Makefile: added `thesis-artifacts` to `.PHONY` and target `cd enigmatrix-backend && uv run python ../scripts/regenerate_thesis_tables.py`. | 🟢 | Slice 8 plan §Task 8.3 | `python scripts/regenerate_thesis_tables.py --demo` writes all 6 files to `data/thesis/` without error. `make thesis-artifacts` invokes the script correctly. |
| F-212 | **Slice 8 — `retire_old_versions.py` nightly Celery Beat task (Session 60).** Created `enigmatrix-backend/app/tasks/m1/retire_old_versions.py`. Celery task `retire_old_dataset_versions()` runs nightly at 20:30 UTC (02:00 Sri Lanka LKT = UTC+5:30). `RETENTION_DAYS = 30`. Protection logic: always keeps `current_version_id` of every dataset, always keeps the second-most-recent sealed version per dataset (for rollback), always keeps any version with `keep: true` in `notes` JSONB. Sets `retired_at = NOW()` on qualifying versions; returns summary dict with retired count. `ignore_result=False, max_retries=2, default_retry_delay=120`. Added to `celery_config.py`: `"app.tasks.m1.retire_old_versions"` in `include` list; `"retire-old-dataset-versions"` in `beat_schedule` at `crontab(hour=20, minute=30)`. | 🟢 | Slice 8 plan §Task 8.4 | Task imports without error. Beat schedule entry is present in `celery_config.py`. Task fires on `crontab(hour=20, minute=30)` schedule (verify in staging next morning). |
| F-213 | **Slice 8 — `phase3_dataset_card.md` Phase-3 handoff document (Session 60).** Created `enigmatrix-docs/phase3_dataset_card.md`. Sections: (1) Training dataset — SQL for selecting `is_ground_truth=TRUE` version + `current_version_id` + version SHA pinning pattern; (2) Label schema — 12 change categories, 10 sector codes, SME relevance binary; (3) Temporal 70/15/15 split — by `gazette_published_date` year (not random), with SQL recipe; (4) Augmentation policy — back-translation ≤5×, synonym substitution ≤2×, noise injection ≤1× (training-set only); (5) `model_registry.json` schema — version, git_sha, dataset_version_id, trained_at, metrics; (6) Phase-3 acceptance criteria — macro-F1 ≥ 0.92, EN ≥ 0.93, SI ≥ 0.88, TA ≥ 0.86, slice cliff < 8 pp, INT8 within 1.5 pp, p95 latency ≤ 2 s; (7) Cross-references to all spec docs. | 🟢 | Slice 8 plan §Task 8.5 | File exists and is internally consistent. All cross-referenced spec files exist. |
| F-214 | **Slice 8 — Measurements page UX polish + recent-runs chevron fix (Session 60).** (a) `enigmatrix-frontend/app/(admin)/admin/datasets/m1/measurements/page.tsx`: added sort bar with `SortButton` (by date/score, asc/desc); keyboard shortcuts (`n` → navigate to `/run`, `?` → open help modal) via `useKeyboardShortcuts` hook; `Sparkline` SVG mini bar chart showing last 8 completed run scores; `ShortcutHelpModal` with `<kbd>` key display; `<kbd>n</kbd>` hint on the Run New button; `sortedData` memo replaces plain `data.map`; new sub-components: `Sparkline`, `SortButton`, `ShortcutHelpModal`. (b) `enigmatrix-frontend/components/m1-pipeline/recent-runs-table.tsx`: Session-53 audit bug closed — `extraction_failed` rows now always render the drill-down `ChevronRight` in `text-destructive` (was `opacity-0 group-hover:opacity-100` for all statuses). New conditional: `r.status === "extraction_failed" ? "block text-destructive" : "block text-primary opacity-0 group-hover:opacity-100 ..."`. | 🟢 | Slice 8 plan §Task 8.6 | Measurements page renders sort bar above filter strip. Keyboard shortcut `n` navigates to new-run page. `?` opens `ShortcutHelpModal`. Sparkline appears when ≥ 2 complete runs exist. Failed rows show chevron without hover. |
| F-215 | **Slice 8 — CI workflow + Docker image pins (Session 60).** Created `.github/workflows/ci-m1-phase2.yml` with three jobs: `backend` (Ubuntu, postgres:16 + redis:7-alpine services, `uv sync --frozen` → `alembic upgrade head` → linearity check counting `alembic heads` → `pytest -q -m "not slow"`), `ml` (Ubuntu, `uv sync --frozen` → `pytest -q -m "not slow"`), `frontend` (Ubuntu, `pnpm install --frozen-lockfile` → `pnpm lint` → `pnpm typecheck` → Playwright `--grep "@phase2"`). Triggers: push to main/initiate/`feature/**`/`slice/**`, PR to main/initiate. Alembic linearity check counts `alembic heads` output lines; fails CI if count ≠ 1. Created `infra/docker-image-pin.txt` with placeholder digest entries for enigmatrix-backend (`python:3.12-slim`), enigmatrix-ml (`python:3.12-slim`), postgres:16, redis:7-alpine, and instructions for filling via `docker inspect`. | 🟢 | Slice 8 plan §Task 8.7 | CI file parses as valid YAML. `infra/docker-image-pin.txt` exists with placeholder entries and fill instructions. CI runs on push to main. |
| F-208 | **Datasets restructure — sidebar collapsible group + Playwright spec URL refresh (Session 58).** Sidebar (`components/layout/sidebar.tsx`): dropped the flat `{ href: "/admin/datasets", label: "nav.adminDatasets", icon: Database }` entry from `ADMIN_ML_ITEMS`; added `ADMIN_DATASETS_ITEMS: NavItem[]` ( `{href:"/admin/datasets", label:"nav.adminDatasetsHub", icon: BookOpen}` + `{href:"/admin/datasets/m1", label:"nav.adminDatasetsM1", icon: Layers}` ); added `adminDatasetsActive = pathname?.startsWith("/admin/datasets")` + `useState(adminDatasetsActive)` for `adminDatasetsOpen`; rendered a new collapsible block mirroring the existing `adminM1PipelineGroup` pattern (Database icon, `nav.adminDatasetsGroup` parent label, chevron toggle, sub-items rendered with `compact`) placed right after `ADMIN_ML_ITEMS` and before the M1 Pipeline collapsible. Playwright spec (`tests/e2e/admin_m1_datasets.spec.ts`): 5 plain URL strings + 2 URL-shape regexes (`\/admin\/m1\/datasets\/new$` + `\/admin\/m1\/datasets\/[0-9a-f-]{36}$`) rewritten to `/admin/datasets/m1/...` equivalents; test description and `test.describe.serial` name kept unchanged. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | 🟢/🟡 | Session 58 plan §§5–6 | Sidebar renders Datasets as a collapsible group with Hub + M1 sub-items; active-state highlights for any `/admin/datasets/*` URL. Playwright spec compiles; full run still gated on dev-server + seeded admin. |

## Open questions (carry forward)

- **OQ12 — partially resolved (Session 11).** `/admin/questions` is live; the hard `/admin/m2/questions` → `/admin/questions?module=2` redirect is the only piece left.
- **OQ30 (new — Session 11)** — should `next_question_rules.goto_question_code` be enforced as a valid graph at write time, or kept soft-warn? Provisional: soft-warn — matches the engine's graceful linear fallback.
- **OQ31 (new — Session 11)** — should the authoring wizard auto-wire M1.next_question_rules → first M2 yes/no questions on save, or leave it explicit? Provisional: explicit for now; auto-wire revisitable once the wizard sees usage.
- **OQ32 (new — Session 11)** — the M2 partial-credit rubric (`first_and_last_only` etc.) is exposed only via the raw `scoring_rubric_json` field; no UI yet. Provisional: defer until BUILD_13 polish. *(Distinct from the Session-12 "M3 partition mapping is hardcoded" follow-up — that one is **RESOLVED** by F-132's `survey_questions.m3_field_mapping` column + admin card.)*
- **OQ33 (new — Session 15)** — the standalone `/surveys/{awareness,knowledge,vulnerability}` pages + the "By module" hub tab are kept (demoted: `/surveys` → "By regulation" is now the obvious SME entry). Remove them once usage data shows the per-regulation flow is the only path SMEs use? Provisional: keep — they're cheap (DB-driven) and useful for admin/testing.
- **OQ34 (new — Session 15)** — rule authoring is unconstrained: an admin can wire a rule from a question linked to regulation A to a question linked to regulation B (the engine handles it — out-of-scope `goto_question_code` → linear fallback — but a reg-scoped flow dead-ends if a chain's mid-link is out of scope). User declined a soft-warn/hard-block guardrail for now; the mitigation is the SETUP/11 §10.9 "junction-link *all* questions in a chain" recipe. Revisit if it bites.

- **OQ1–OQ4** from [`BUILD_00`](../shared/BUILD_PLAN/BUILD_00_INDEX.md) — pinned versions, locked hyperparameters, ethics approval, NEDA partnership.
- **OQ5 — RESOLVED.** Append-only `survey_responses`, `question_id` versioned as `<instrument>.<version>.<qcode>`. New denormalisation columns let history stay queryable after th