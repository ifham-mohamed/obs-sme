# Changes Log

Append-only. One row per logical change. Most recent at the top.

| Date | Features | Change | Files |
|------|----------|--------|-------|
| 2026-05-23 | F-208 | **Session 58 — Sidebar collapsible Datasets group + Playwright spec URL refresh.** Sidebar (`enigmatrix-frontend/components/layout/sidebar.tsx`): dropped the flat `{ href: "/admin/datasets", label: "nav.adminDatasets", icon: Database }` entry from `ADMIN_ML_ITEMS`; added `ADMIN_DATASETS_ITEMS: NavItem[]` with Hub + M1 entries; added `adminDatasetsActive = pathname?.startsWith("/admin/datasets")` + `useState(adminDatasetsActive)` driving `adminDatasetsOpen`; new collapsible block mirrors the existing `adminM1PipelineGroup` shape (Database icon, `nav.adminDatasetsGroup` parent label, chevron toggle, sub-items rendered with the `compact` NavLink variant) placed right after `ADMIN_ML_ITEMS` and before the M1 Pipeline collapsible. In `collapsed` mode the group renders its sub-items flat (matching the existing convention for the M1 pipeline group). Playwright spec (`tests/e2e/admin_m1_datasets.spec.ts`): 5 plain URL strings + 2 URL-shape regexes rewritten to the new path; `test.describe.serial` test-name string kept unchanged. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-frontend/components/layout/sidebar.tsx` · `enigmatrix-frontend/tests/e2e/admin_m1_datasets.spec.ts` |
| 2026-05-23 | F-207 | **Session 58 — Moved 5 M1 dataset pages under /admin/datasets/m1/* + replaced the /admin/datasets ComingSoon placeholder with a real hub.** PowerShell `Move-Item` of all five page files under `app/(admin)/admin/m1/datasets/` → `app/(admin)/admin/datasets/m1/` (same internal tree shape preserved: `page.tsx`, `new/page.tsx`, `[datasetId]/page.tsx`, `[datasetId]/upload/page.tsx`, `[datasetId]/versions/[versionId]/page.tsx`). After move, every `useTranslations("m1Datasets")` call site (4 across pages + 2 in `components/forms/m1-dataset-create-form.tsx`) was rewritten to `useTranslations("datasets.m1")`; every hardcoded `/admin/m1/datasets` literal (~20 sites incl. the form's `router.push` on submit and `Cancel` link, plus comment headers) became `/admin/datasets/m1`. Hub page (`app/(admin)/admin/datasets/page.tsx`) overwrote the 4-line `ComingSoon` placeholder with a server component using `getTranslations()` + `PageHeader` (breadcrumb `Dashboard → Datasets`) + a `grid grid-cols-1 md:grid-cols-3 gap-4` of three module cards. M1 card uses the `Database` icon, full opacity, wraps the card in `<Link href="/admin/datasets/m1">`, default-variant CTA button. M2 (`Brain` icon) and M3 (`GaugeCircle` icon) cards are rendered with `opacity-60 pointer-events-none`, disabled outline-variant CTA, and a `Badge variant="secondary"` reading `datasets.hub.comingSoonBadge`. Old `app/(admin)/admin/m1/datasets/` subtree removed via `Remove-Item -Recurse`; `m1/pdf-records/` and `m1/pipeline/` siblings intact. `tsc --noEmit` produces zero NEW errors attributable to any file authored or edited (hub, form, sidebar, spec); the 4 pre-existing `PageHeader children` drift errors travel with the moved files (already flagged in Session 57 follow-ups). Backend API at `/api/v1/m1/datasets` and `lib/api/m1-datasets.ts` client unchanged — frontend-only restructure. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-frontend/app/(admin)/admin/datasets/page.tsx` (REWRITE) · `enigmatrix-frontend/app/(admin)/admin/datasets/m1/page.tsx` (MOVED+edited) · `enigmatrix-frontend/app/(admin)/admin/datasets/m1/new/page.tsx` (MOVED+edited) · `enigmatrix-frontend/app/(admin)/admin/datasets/m1/[datasetId]/page.tsx` (MOVED+edited) · `enigmatrix-frontend/app/(admin)/admin/datasets/m1/[datasetId]/upload/page.tsx` (MOVED+edited) · `enigmatrix-frontend/app/(admin)/admin/datasets/m1/[datasetId]/versions/[versionId]/page.tsx` (MOVED+edited) · `enigmatrix-frontend/components/forms/m1-dataset-create-form.tsx` · `enigmatrix-frontend/app/(admin)/admin/m1/datasets/` (DELETED subtree) |
| 2026-05-23 | F-206 | **Session 58 — i18n namespace migration m1Datasets.* → datasets.m1.* + new datasets.hub.* namespace + nav.adminDatasets{Group,Hub,M1}.** In all three `lib/i18n/messages/{en,si,ta}.json` files: dropped the top-level `m1Datasets` namespace; added a new top-level `datasets` namespace with two sub-namespaces. `datasets.hub.{title, subtitle, comingSoonBadge, modules.{m1,m2,m3}.{title,description,cta}}` carries new hub-only strings — EN gets real copy; SI/TA carry `[TODO si]`/`[TODO ta]` prefixes per the established repo pattern. `datasets.m1.*` is the verbatim relocation of every existing `m1Datasets.*` key (list/create/upload/detail/version/promote sub-namespaces) — no content change, just nested one level deeper. Replaced the flat `nav.adminDatasets` key with three new keys: `nav.adminDatasetsGroup` (collapsible parent label "Datasets"), `nav.adminDatasetsHub` ("Hub"), `nav.adminDatasetsM1` ("M1"). Validated each file via `json.load` and asserted matching 19 top-level keys, presence of `datasets.{hub,m1}`, presence of all three new nav keys, and absence of the dropped `m1Datasets` / `nav.adminDatasets` keys. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-frontend/lib/i18n/messages/en.json` · `enigmatrix-frontend/lib/i18n/messages/si.json` · `enigmatrix-frontend/lib/i18n/messages/ta.json` |
| 2026-05-23 | F-205 | **Session 57 — Slice 3 Playwright E2E spec.** Wrote `enigmatrix-frontend/tests/e2e/admin_m1_datasets.spec.ts` (~80 lines, 4 serial tests) modelled on the existing `admin_regulations.spec.ts`. Scenarios: (1) admin login → `/admin/m1/datasets/new` → submit form → land on detail; (2) upload the 5-row fixture via `setInputFiles` on `input[type="file"]` → assert "upload complete" + "sealed" surface; (3) promote dataset to ground truth from the list view (auto-accepts `window.confirm`) → assert green ground-truth badge in the row; (4) re-upload same bytes → assert `version_already_exists`/`upload_failed_409` text appears (the upload page rethrows the API client's 409 message as-is). Fixture lives at `tests/e2e/fixtures/m1_master_sample.xlsx`, copied from the backend fixture. Full execution requires both dev servers + a seeded admin (`make up && make seed && make dev-backend && make dev-frontend`) before `pnpm exec playwright test tests/e2e/admin_m1_datasets.spec.ts`. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-frontend/tests/e2e/admin_m1_datasets.spec.ts` (NEW) · `enigmatrix-frontend/tests/e2e/fixtures/m1_master_sample.xlsx` (NEW, copy of backend fixture) |
| 2026-05-23 | F-204 | **Session 57 — Slice 3 create-dataset page + form component.** Closed the broken-link bug on `/admin/m1/datasets`: the list view's "Create dataset" button linked to `/admin/m1/datasets/new` which previously did not exist (404). Added `enigmatrix-frontend/app/(admin)/admin/m1/datasets/new/page.tsx` — server component using `getTranslations` + `getAccessToken`, renders `PageHeader` with breadcrumb (Dashboard → M1 Datasets → Create dataset) + `M1DatasetCreateForm`. Added `enigmatrix-frontend/components/forms/m1-dataset-create-form.tsx` — `"use client"` form using `react-hook-form` + `zodResolver(datasetCreateSchema)` with shadcn `Input`/`Select`/`Textarea`/`Label` primitives. Fields: name (autoFocus, 1–200), kind (enum select defaulting to `manual_excel`; labels resolved via the existing `m1Datasets.list.filters.{manualExcel,extractionRun,expertReview}` keys to avoid duplicating strings), description (optional), tags (comma-separated input parsed to `string[]`). On submit calls `datasetApi.create(token, payload)` → `router.push(/admin/m1/datasets/${created.dataset_id})`; on error surfaces in an `Alert`. i18n: reuses pre-existing `m1Datasets.create.*` namespace (already populated in en.json; si.json/ta.json carry `[TODO]` placeholders per repo convention). `tsc --noEmit` produces zero errors attributable to the new files. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-frontend/app/(admin)/admin/m1/datasets/new/page.tsx` (NEW) · `enigmatrix-frontend/components/forms/m1-dataset-create-form.tsx` (NEW) |
| 2026-05-23 | F-203 | **Session 57 — Slice 3 upload integration test suite.** Wrote `enigmatrix-backend/app/tests/integration/test_m1_dataset_upload.py` (5 tests) reusing the existing `client` + `db_session` fixtures from `app/tests/conftest.py`. Tests cover the slice-3 §Gate guarantees: (1) `test_upload_creates_sealed_version_and_rows` — POST `/api/v1/m1/datasets` to create a `manual_excel` dataset, upload the 5-row fixture, assert 5 rows persisted with regulation_keys `{2261/15..19}`; (2) `test_upload_same_sha_returns_409` — second upload of bit-identical bytes returns 409 with `version_already_exists`; (3) `test_pending_unsealed_upload_returns_423` — initial upload with `seal=false` leaves a pending version; second upload (even of different-SHA bytes — mutated last byte) returns 423 `upload_in_progress`; (4) `test_upload_writes_audit_row` — `audit_log` row with `event_type='m1.dataset.version.upload'` references the new `version_id`; (5) `test_promote_to_ground_truth_demotes_previous` — chained promote(A) → promote(B) leaves exactly one `is_ground_truth=true` row (partial-unique-index guarantee). Includes an autouse `_isolate_storage` fixture that redirects `STORAGE_LOCAL_PATH` into per-test `tmp_path` + `get_settings.cache_clear()`. Tests are AST-valid; full execution deferred until testcontainers + Docker available (Windows-native env in this session lacks both, and the backend `.venv` had broken zero-byte python symlinks from cross-OS sync). See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-backend/app/tests/integration/test_m1_dataset_upload.py` (NEW) |
| 2026-05-23 | F-202 | **Session 57 — Slice 3 parser unit test suite (9 tests, all passing).** Wrote `enigmatrix-backend/app/tests/unit/test_m1_xlsx_parser.py`: asserts `CANONICAL_FIELDS` has 21 entries with expected keys present; spot-checks `CANONICAL_FIELD_ALIASES` for master-Excel headers (`gazette no.`, `published date`, `severity`, `is sme relevant`); verifies the 5-row round-trip via `parse_xlsx_to_canonical_rows`; checks the Sinhala title in row `2261/16` is a `str` containing ≥1 Sinhala codepoint (U+0D80–U+0DFF); confirms the empty English title coerces to `None` without warning; confirms severity-out-of-range (row `2261/17`) and unparseable-date (row `2261/18`) produce per-row warnings; confirms unmapped `Notes` column lands in every row's `_extras` blob; confirms `read_canonical_excel(Path)` (the upload service's path-based call site) still produces a dict keyed by regulation_key. Verified locally: `python -m pytest -q --confcutdir=app/tests/unit app/tests/unit/test_m1_xlsx_parser.py` → `9 passed in 0.58s` (`--confcutdir` bypasses the parent `conftest.py` that imports `testcontainers` + `pytest_asyncio`, which aren't available on this Windows-native env). See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-backend/app/tests/unit/test_m1_xlsx_parser.py` (NEW) |
| 2026-05-23 | F-201 | **Session 57 — Slice 3 reproducible 5-row M1 master xlsx fixture.** Wrote `enigmatrix-backend/app/tests/fixtures/_build_m1_master_sample.py` (~60 lines) generating `m1_master_sample.xlsx` (5411 bytes). Schema: 9 header columns — 8 mappable via the existing `HEADER_ALIASES` map (`Gazette No.` → regulation_key, `title_en`/`title_si` direct, `Published Date` → gazette_published_date, `Effective Date` → effective_date, `Severity` → severity_level, `Is SME Relevant` → is_sme_relevant, `Category` → change_category) + 1 unmapped `Notes` column (lands in `_extras` per row). 5 data rows exercising every interesting code path: (a) `2261/15` fully populated baseline; (b) `2261/16` Sinhala-only title with empty English title (None coercion, no warning — per current parser semantics); (c) `2261/17` `severity_level=9` (out-of-range warning); (d) `2261/18` `gazette_published_date='not a date'` (unparseable-date warning); (e) `2261/19` clean closing row. Generated once via `python app/tests/fixtures/_build_m1_master_sample.py`; recipe committed alongside so the binary fixture is reproducible. Same file copied to `enigmatrix-frontend/tests/e2e/fixtures/m1_master_sample.xlsx` (5411 bytes) for the Playwright spec. Required `pip install --user openpyxl` against `C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe` because the backend `.venv` had zero-byte Python symlinks (cross-OS sync from Linux container). See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-backend/app/tests/fixtures/_build_m1_master_sample.py` (NEW) · `enigmatrix-backend/app/tests/fixtures/m1_master_sample.xlsx` (NEW, 5411 bytes) · `enigmatrix-frontend/tests/e2e/fixtures/m1_master_sample.xlsx` (NEW, copy) |
| 2026-05-23 | F-200 | **Session 57 — Slice 3 backend-local XLSX parser wrapper + ML parser file-handle close.** Created `enigmatrix-backend/app/services/m1_xlsx_parser.py` as the single backend import point for the canonical Excel parser. Public surface: `read_canonical_excel` (re-export), `CANONICAL_FIELDS` (re-export, 21-element frozenset), `CANONICAL_FIELD_ALIASES` (re-export of `HEADER_ALIASES`), `parse_xlsx_to_canonical_rows(payload_bytes) -> (list[dict], dict[str, list[str]])` (new bytes adapter that round-trips through a temp `.xlsx` file, since the underlying openpyxl call wants a path; tempfile is cleaned up in a `finally`). Sys.path shim for the sibling `enigmatrix-ml` package moved into the wrapper's `_resolve_ml_parser()` private function — identical behaviour, single source of truth. Refactored `enigmatrix-backend/app/services/m1_dataset_upload.py` to drop the inline `_resolve_xlsx_reader()` helper and `import sys` + import `read_canonical_excel`, `CANONICAL_FIELDS` from the wrapper instead. **Behaviour-neutral one-line fix in `enigmatrix-ml/m1/evaluation/xlsx_reader.read_canonical_excel`:** wrapped the body in `try: ... finally: wb.close()` because openpyxl's `read_only=True` mode otherwise keeps the file handle open past function return, which on Windows blocks `Path.unlink()` of the temp xlsx the wrapper writes to (manifested as `PermissionError: [WinError 32] The process cannot access the file because it is being used by another process`). Linux callers (the upload pipeline's path-based call site) are unaffected. See [plan](plans/2026-05-23_as-frm-the-idea-glowing-fog.md). | `enigmatrix-backend/app/services/m1_xlsx_parser.py` (NEW) · `enigmatrix-backend/app/services/m1_dataset_upload.py` · `enigmatrix-ml/m1/evaluation/xlsx_reader.py` |
| 2026-05-22 | F-199 | **Session 56 — Tracker v12 addendum + full 800-PDF coverage marker.** Wrote `C:\sme\03-Data-Sources\m1\raw\csv\m1_extraction_tracker_v12_addendum.csv` (145 rows) marking every Phase B PDF (batches 9–11) as `status='extracted'` with `extraction_method='pdfplumber+claude_classify'`, `extracted_at='2026-05-18T16:15:00Z'`, and per-row `notes` of the form `Classified in m1_regulations_next50_batchN.csv`. The full 800-PDF tracker is now the concatenation of `m1_extraction_tracker_v10.csv` (452 rows) + `m1_extraction_tracker_v11_addendum.csv` (203 rows from batches 5–8 of the prior session) + `m1_extraction_tracker_v12_addendum.csv` (145 rows from batches 9–11 of this session) = 800. Tracker schema unchanged from v6: `regulation_short_code, gazette_number, pdf_filename, page_count, size_kb, status, extraction_method, extracted_at, notes`. See [plan](plans/2026-05-22_Plan%20M1%20raw%20PDF%20bulk%20extraction%20and%20classification%20—%20800%20PDFs%20across%2011%20batches.md). | `C:\sme\03-Data-Sources\m1\raw\csv\m1_extraction_tracker_v12_addendum.csv` (NEW) |
| 2026-05-22 | F-199 | **Session 56 — Batch 11 CSV (final 45 PDFs of Phase B).** Wrote `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch11.csv` (45 rows) covering the long-tail of new PDFs (5–30 pages). Acts breakdown: Customs Ordinance (Chapter 235) = 20, Title Registration Act = 18, Land Acquisition Act (Chapter 460) = 1, Industrial Disputes Act (Chapter 131) = 1, Sri Lanka Armed Services Long Service Medal = 1, generic = 3. Largest in batch: `2484_07.pdf` (30 pages). Classifier-driven extractions on doubled-Tamil `காாணிி எடுத்தற்` Land Acquisition pattern and Pradeshiya Sabha typo variants both fired here. Schema unchanged from prior batches: full 39-column `m1_regulations` shape with ASCII compaction (multilingual columns blanked, `raw_text='[see raw_pdf_path]'`). Full multilingual+raw_text version retained in Cowork outputs at `/sessions/.../mnt/outputs/m1_regulations_next50_batch11.csv`. See [plan](plans/2026-05-22_Plan%20M1%20raw%20PDF%20bulk%20extraction%20and%20classification%20—%20800%20PDFs%20across%2011%20batches.md). | `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch11.csv` (NEW) |
| 2026-05-22 | F-199 | **Session 56 — Batch 10 CSV (gazette-2483 customs day, 50 PDFs).** Wrote `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch10.csv` (50 rows, 4–5 pages each). This batch was 72% **Customs Ordinance** (Chapter 235 = 26 rows, unscoped = 10 rows) because gazette 2483 published 80+ Customs Ordinance notifications on a single day (2026-04-10) — likely batched tariff/import-duty changes following a budget/policy event. Other acts: Title Registration = 3, Land Acquisition = 1, generic = 10. Schema unchanged from prior batches. See [plan](plans/2026-05-22_Plan%20M1%20raw%20PDF%20bulk%20extraction%20and%20classification%20—%20800%20PDFs%20across%2011%20batches.md). | `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch10.csv` (NEW) |
| 2026-05-22 | F-199 | **Session 56 — Batch 9 CSV (first 50 newly-detected PDFs).** Wrote `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch9.csv` (50 rows, 1–4 pages each). Domain breakdown: general 27, lands 9, elections 6, customs 5, finance 1, local_government 1, labour 1. Generic-rate of 54% on first classifier pass was caused by missing `PREFIX_FALLBACK` entries for gazette prefixes 2483–2486 AND missing classifier blocks for several recurring statutes — all fixed in subsequent classifier extensions (see F-199 classifier-extensions entry below). Schema unchanged from prior batches: full 39-column `m1_regulations` shape, ASCII compaction. See [plan](plans/2026-05-22_Plan%20M1%20raw%20PDF%20bulk%20extraction%20and%20classification%20—%20800%20PDFs%20across%2011%20batches.md). | `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch9.csv` (NEW) |
| 2026-05-22 | F-199 | **Session 56 — Phase B classifier extensions: `PREFIX_FALLBACK` 2483–2486 + Pradeshiya Sabha + Electricity 2024 + Export Development + Companies Act + doubled-Tamil Land Acquisition + Armed Services typo.** Edits to `outputs/build_csv.py` (the regex/heuristic statute classifier — runs in Cowork bash, not the production extraction pipeline): **(1)** `PREFIX_FALLBACK` dict extended with `'2483': '2026-04-06', '2484': '2026-04-13', '2485': '2026-04-20', '2486': '2026-04-27'` (previous map ended at `'2482': '2026-03-30'`). Without this, all new-PDF rows defaulted to `2026-01-01`. **(2)** New classifier block for **Pradeshiya Sabha Act, No. 15 of 1987** — matches English `Pradeshiya Sabha Act`, Sinhala `1987 අංක 15 දරන ප්‍රාදේශීය සභා`, broken-Sinhala `1987 අංක 15 දරන [^\n]{0,30}සභා` (after CID-stripping), Sinhala-fragment `ාෙය සභා පනෙ` (typical post-CID survival), Tamil `பிரதேச சபை`, doubled-Tamil `பிிரதேேச சபை`. Triggered by `2482_07.pdf` (Kegalle Pradeshiya Sabha roads). **(3)** New block for **Sri Lanka Electricity (Amendment) Act, No. 36 of 2024** — separate from existing 2009 Act block; matches `2024 අංක 36 දරන ශ්‍රී ලංකා විදුලිබල (සංශෝධන)`, English `Sri Lanka Electricity (Amendment) Act, No. 36 of 2024`, and Section 18(2)(a) "Transmission Plan" phrase `පැවරුම් සැලැස්ම`. Triggered by `2479_36.pdf` (127-page transmission/restructuring plan). **(4)** New block for **Sri Lanka Export Development Act, No. 40 of 1979** — Section 14 tariff/import-duty orders by Minister of Industry & Entrepreneurship Development; matches `Sri Lanka Export Development Act`, `1979 අංක 40 දරන ශ්‍රී ලංකා අපනයන සංවර්ධන පනත`, Tamil `1979 ஆம் ஆண்டின் 40 ஆம் இலக்க இலங்கை ஏற்றுமதி அபிவிருத்தி`. Triggered by `2478_04.pdf`. **(5)** New block for **Companies Act, No. 07 of 2007** — matches `Companies Act,?\s*No\.\s*0?7 of 2007`, doubled-Tamil `2007 ஆம் ஆண்டின் 07 ஆம் இலக்க, ?கம்பெபினிிகள் சட்டம்`, Sinhala `2007 අංක 07 දරන සමාගම් පනත`. Triggered by `2480_48.pdf` (Companies (Consumer Protection) Regulations by Minister of Trade). **(6)** Broadened **Armed Services Long Service Medal** pattern to match newline between `Service\n` and `Medal`, accept typo `Sevices`, accept singular `Service` (gazette `2478_25.pdf` literal source has both `THE SRI LANKA ARMED SERVICE LONG SERVICE MEDAL AND CLASP` and `Sri Lanka Armed Sevices Long Service\nMedal`). **(7)** Added doubled-Tamil variants `காாணிி எடுத்தற்`, `காாணிிப்பகுதிி`, `காாணிி எத`, `காாணிி எடுத்தற் சட்டம்` to **Land Acquisition Act (Chapter 460)** pattern list — previously missed because of doubled-character Tamil from PDF font issues. Triggered by `2481_05.pdf` (Section 5 declaration in Tamil). All extensions land in the Cowork outputs `outputs/build_csv.py` source — not in `enigmatrix-backend` or `enigmatrix-ml`. See [plan](plans/2026-05-22_Plan%20M1%20raw%20PDF%20bulk%20extraction%20and%20classification%20—%20800%20PDFs%20across%2011%20batches.md). | `outputs/build_csv.py` (Cowork sandbox; not in any git repo) |
| 2026-05-22 | F-199 | **Session 56 — Phase A batch 8 CSV (53 PDFs, completes the 203-PDF backlog).** Wrote `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch8.csv` (53 rows) closing out the Phase A backlog from the prior Cowork session. This batch spanned pages 7–127 — long-tail incl. `2479_36.pdf` at 127 pages (Sri Lanka Electricity 2024 Transmission Plan), `2482_06.pdf` at 65 pages (CID-corrupted Part-I-General), `2479_13.pdf` at 46 pages, `2478_39.pdf` at 46 pages. Acts breakdown: Title Registration Act = 40, Provincial Councils Act = 2, Pradeshiya Sabha Act = 1, Sri Lanka Export Development Act = 1, Industrial Disputes Act = 1, Companies Act = 1, Land Acquisition Act = 1, Veterinary Surgeons Act = 1, Inland Revenue Act = 1, Sri Lanka Electricity (Amendment) Act = 1, Sri Lanka Armed Services Long Service Medal = 1, generic-lands = 1 (2479/39), generic = 1 (2482/06 — only cover page extracted). Used `pdftotext` (poppler) instead of `pdfplumber` for the four PDFs that exceeded the bash 45 s timeout (`2481_04.pdf`, `2481_15.pdf` at 3.5 MB; `2482_06.pdf` at 65 pages; `2479_36.pdf` at 127 pages). See [plan](plans/2026-05-22_Plan%20M1%20raw%20PDF%20bulk%20extraction%20and%20classification%20—%20800%20PDFs%20across%2011%20batches.md). | `C:\sme\03-Data-Sources\m1\raw\csv\m1_regulations_next50_batch8.csv` (NEW) |
| 2026-05-22 | F-199 | **Session 56 — Tracker v11 addendum (203 rows for Phase A batches 5–8).** Wrote `C:\sme\03-Data-Sources\m1\raw\csv\m1_extraction_tracker_v11_addendum.csv` (203 rows) marking every PDF processed in batches 5–8 of Phase A as `status='extracted'` with `extraction_method='pdfplumber+claude_classify'`, `extracted_at='2026-05-18T13:30:00Z'`, and per-row `notes` referencing the batch CSV. The 800-PDF tracker is now `m1_extraction_tracker_v10.csv` (452 rows) + this addendum (203) = 655 — covering everything that existed on disk at the start of this session. Tracker schema same as v6: `regulation_short_code, gazette_number, pdf_filename, page_count, size_kb, status, extraction_method, extracted_at, notes`. See [plan](plans/2026-05-22_Plan%20M1%20raw%20PDF%20bulk%20extraction%20and%20classification%20—%20800%20PDFs%20across%2011%20batches.md). | `C:\sme\03-Data-Sources\m1\raw\csv\m1_extraction_tracker_v11_addendum.csv` (NEW) |
| 2026-05-22 | F-198 | **Session 55 — Cross-repo Stage 4 audit + three HIGH inline fixes (`.gitignore` Cowork block, `ForbiddenError` import, `ExtractionRowStatus` trim).** Six parallel Explore agents audited backend code, ml code, frontend code, root + deploy configs, security posture, and cross-stack API contract drift — producing 60+ findings. Verified the most surprising claims by reading source directly: caught **one false positive** (path traversal in `/categorize` was a non-issue because `current_abs.name` is just the basename and cannot escape `new_dir`) and confirmed **one real latent bug** (`app/deps.py:44` references `ForbiddenError` but only `UnauthorizedError` is imported; currently masked because every production user with an admin token has `role == "admin"` so the raise branch never fires — a non-admin token hitting any admin endpoint would crash with `NameError` instead of returning a clean 403). Three HIGH-severity inline fixes applied: **(a)** `enigmatrix-backend/app/deps.py` — changed `from app.exceptions import UnauthorizedError` to `from app.exceptions import ForbiddenError, UnauthorizedError`; **(b)** `.gitignore` additions in all four locations (backend, frontend, ml, xyz root) — added a "Claude Cowork / agent-mode artifacts — never commit these" block listing `.claude/`, `.cowork/`, `outputs/`, `local-agent-mode-sessions/`, `MEMORY.md`; **(c)** `enigmatrix-frontend/lib/api/m1-gazette-extraction.ts` — trimmed `ExtractionRowStatus` from 8 values to the 4 the backend actually writes today, **subsequently reverted by user** to restore forward-compat with planned BUILD_07 Phase 3+ stages (classified/summarized/alerted/archived). Five MEDIUM follow-ups deferred as discrete tasks: #18 reconcile detect_language duplication backend vs ml, #19 status enum + magic-constants refactor, #20 i18n parity for admin nav, #21 extract useAuthToken hook + statusTone util (partially landed during PDF Records page rework), #22 rate-limit + CORS + Celery hardening. Pending task #12 (URGENT PAT rotation + Dockerfile buildx secrets) cross-referenced — operator continues to defer. See [plan](plans/2026-05-22_Plan%20Cross-repo%20code%20quality%20audit%20—%20Stage%204.md). | `enigmatrix-backend/app/deps.py` · `enigmatrix-backend/.gitignore` · `enigmatrix-frontend/.gitignore` · `enigmatrix-ml/.gitignore` · `xyz/.gitignore` · `enigmatrix-frontend/lib/api/m1-gazette-extraction.ts` (revert net-effect for status enum) |
| 2026-05-22 | F-197 | **Session 55 — PDF Records browse-all admin page (backend `GET /pdf-records` endpoint + new `/admin/m1/pdf-records` page + sidebar nav + i18n).** Backend: added `PdfRecordOut` (lean per-row shape — drops raw_text/cleaned_text in favour of `*_length` counts) and `PdfRecordsListOut` (items+total+page+page_size+server_time) schemas; added `GET /api/v1/admin/m1/extraction/pdf-records` to `app/api/v1/m1_gazette_extraction.py`. Query params: `source_id`, `status` (Query-aliased — collides with FastAPI's `status` module import), `language`, `date_from`, `date_to`, `search` (ILIKE substring on `gazette_number` OR `regulation_short_code`, `min_length=1, max_length=100`), `page` (default 1), `page_size` (default 50, max 200). Sort: `gazette_published_date DESC NULLS LAST, created_at DESC`. Behind `require_admin`. Frontend: added `PdfRecord`/`PdfRecordsListOut`/`PdfRecordsFilters` interfaces + `listPdfRecords(token, filters)` method to `lib/api/m1-gazette-extraction.ts`. Created `app/(admin)/admin/m1/pdf-records/page.tsx` (NEW, ~350 lines initial draft) — filter bar with 6 controls + draft-vs-applied state (search applies on Enter only) + responsive table + per-row icon actions (ExternalLink to source_url, Download to existing `/raw-pdf` endpoint, Eye to existing `/admin/m1/pipeline/trace/<id>`) + pagination (Previous/Next + page-of-total) + `export const dynamic = "force-dynamic"`. **Subsequent rework (by user/linter):** page refactored to use `AdminPageLayout` sticky-sidebar pattern + `Combobox` + project `Pagination`/`Table` + new `useAuthToken()` hook + shared `pipelineStatusTone` util; filter UX flipped to immediate-apply for dropdowns/dates + Enter-to-apply for search. Visible columns and endpoint contract unchanged. Sidebar nav: added fifth `NavItem` to `ADMIN_M1_PIPELINE_ITEMS` in `components/layout/sidebar.tsx` (FileText icon, href `/admin/m1/pdf-records`, label key `nav.adminM1PdfRecords`). i18n: added `"adminM1PdfRecords": "PDF Records"` to `nav` block in `lib/i18n/messages/en.json`, `si.json`, `ta.json` (Sinhala/Tamil keep English string for now — translation tracked as follow-up #20). UX choices distilled from `design:ux-copy`/`design:user-research`/`design:design-system` skills: concrete next-step empty-state copy, pagination over infinite-scroll for stable shareable links, lean row shape. See [plan](plans/2026-05-22_Plan%20PDF%20Records%20browse-all%20admin%20page.md). | `enigmatrix-backend/app/schemas/m1_pipeline.py` · `enigmatrix-backend/app/api/v1/m1_gazette_extraction.py` · `enigmatrix-frontend/lib/api/m1-gazette-extraction.ts` · `enigmatrix-frontend/app/(admin)/admin/m1/pdf-records/page.tsx` (NEW) · `enigmatrix-frontend/components/layout/sidebar.tsx` · `enigmatrix-frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-22 | F-196 | **Session 55 — Per-PDF metadata columns + extract-time population (migration `202605280001` + `pdf_metadata.py` helper).** Adds 4 columns to `m1_regulations` via migration `alembic/versions/202605280001_m1_pdf_metadata.py` (NEW; `revision="202605280001"`, `down_revision="202605270001"`): `file_size_bytes` BigInteger nullable, `sha256` String(64) nullable, `pdf_pages` SmallInteger nullable, `language` String(10) nullable; plus indexes `ix_m1_regulations_sha256` (dedup checks) and `ix_m1_regulations_language` (UI filter). Clean `downgrade()`. Model: `app/models/regulation.py` — added `BigInteger` to the SQLAlchemy import line + 4 `Mapped[...]` declarations on `M1Regulation` with commentary block. New helper `app/extraction/pdf_metadata.py` (NEW): `compute_file_size`, `compute_sha256` (streams 1 MiB chunks to avoid loading 50 MB PDFs into memory), `compute_pdf_pages` (via PyMuPDF `fitz.open(...).page_count`), `detect_language` (Unicode-codepoint ratio across Sinhala U+0D80–0DFF / Tamil U+0B80–0BFF / Latin), `compute_pdf_metadata` (resilient aggregator — any single field that fails logs + skips, never breaks the extraction). Returns `TypedDict[PdfMetadata]`. **Subsequent revision (by user/linter):** helper updated to accept either `bytes` or `Path` input via `PdfSource = Union[bytes, Path]` (new in-memory flow where Celery fetches PDF via httpx); language codes aligned to ISO 639-1 two-letter (`'si'/'ta'/'en'/'unknown'`) matching ml repo's `m1/extraction/language_detection.py`. Documents intentional divergence from ml's fastText-based detector (image-size constraints — `lid.176.bin` is 125 MB). Extract task wiring: `app/tasks/m1/extract_gazette.py` imports `compute_pdf_metadata` and calls it after successful extraction, writing 4 fields onto `row` via `.get()`-style semantics. Schema response: `ExtractionProgressRow` gains 5 new nullable fields (4 metadata + `source_url`). Row builders in `app/services/m1_pipeline_service.py` (`get_extraction_progress`) and `app/api/v1/m1_gazette_extraction.py` (`list_unknown_regulations`) populate the new fields. Frontend types in `lib/api/m1-gazette-extraction.ts` `ExtractionProgressRow` mirror the additions. Legacy rows stay NULL until `/re-extract` reruns them — no backfill script in this PR. See [plan](plans/2026-05-22_Plan%20Per-PDF%20metadata%20schema%20and%20population.md). | `enigmatrix-backend/alembic/versions/202605280001_m1_pdf_metadata.py` (NEW) · `enigmatrix-backend/app/models/regulation.py` · `enigmatrix-backend/app/extraction/pdf_metadata.py` (NEW) · `enigmatrix-backend/app/tasks/m1/extract_gazette.py` · `enigmatrix-backend/app/schemas/m1_pipeline.py` · `enigmatrix-backend/app/services/m1_pipeline_service.py` · `enigmatrix-backend/app/api/v1/m1_gazette_extraction.py` · `enigmatrix-frontend/lib/api/m1-gazette-extraction.ts` |
| 2026-05-22 | F-195 | **Session 55 — Cancel button + ConfirmDialog + result panel on extraction status card (frontend Stage 1).** Adds destructive "Cancel & roll back" button to the running-task status card on `app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx`. **API client:** Added `CancelExtractionIn` (date_from, date_to, source_id?, queued_at, force=true) and `CancelExtractionOut` (task_id, revoked, rolled_back_at, deleted_rows, deleted_pdfs, skipped_pdfs, errors) TypeScript interfaces + `cancel(token, taskId, payload)` method on `M1GazetteExtractionApi` in `lib/api/m1-gazette-extraction.ts`. **Page wiring:** added XCircle icon import + `ConfirmDialog` import + `CancelExtractionOut` type import. New state: `cancelOpen` (boolean), `cancelResult` (CancelExtractionOut | null). New `cancel = useMutation<CancelExtractionOut, ApiError, void>(...)` mutation posting the scope from `task` (date_from, date_to, source_id falling back to current sourceId, queued_at) with `force: true`. On success, `queryClient.invalidateQueries` for `["m1-source", sourceId]`, `["m1-extraction-progress", task?.task_id]`, `["m1-extraction-summary", task?.task_id]` — UI updates immediately without waiting for the 5s poll. Cancel button visible only when `isRunning` (task status NOT in `TERMINAL_STATUSES`), destructive variant + XCircle icon + "Cancel & roll back" label. Inline result panel renders deleted_rows + deleted_pdfs + skipped_pdfs + errors (first 5 + "…and N more" overflow). ConfirmDialog (reusing `components/ui/confirm-dialog.tsx`): title "Cancel extraction and roll back?", description includes task_id prefix + scope + source + "This cannot be undone.", `confirmLabel="Cancel and delete"`, `cancelLabel="Keep running"`, danger variant. **Errors fixed during impl:** initial Edit appended stray `}` after `/>` of `<ConfirmDialog />` — caught by re-reading file post-edit; fixed with second Edit. See [plan](plans/2026-05-22_Plan%20M1%20cancel%20+%20rollback%20endpoint%20and%20frontend%20button.md). | `enigmatrix-frontend/lib/api/m1-gazette-extraction.ts` · `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx` |
| 2026-05-22 | F-194 | **Session 55 — Cancel + rollback admin endpoint (backend Stage 1; `POST /api/v1/admin/m1/extraction/cancel/{task_id}`).** Adds destructive admin endpoint that revokes a running Celery extraction task AND deletes every `m1_regulations` row + on-disk PDF the run produced. **Service:** Created `app/services/m1_extraction_cancel.py` (NEW, ~150 lines). `cancel_and_rollback(db, *, task_id, date_from, date_to, source_id, queued_at, force=True)` does four things in order: **(1)** `celery_app.control.revoke(task_id, terminate=True, signal="SIGTERM")` — broker-down recovery: catches the exception, sets `revoked=False`, and still runs the DB rollback so state stays consistent; **(2)** selects rows in scope where `created_at >= queued_at` AND matching source/date filters (date filter includes rows with NULL `gazette_published_date` so very-fresh inserts aren't missed); **(3)** when `force=False`, excludes rows already at `status='preprocessed'`; **(4)** deletes on-disk PDFs **before** DB rows (so a mid-rollback crash leaves orphan files — recoverable via existing `/reconcile` — rather than orphan DB rows pointing at deleted files). Path-traversal guard uses the same `relative_to(storage_root)` check as the existing `/raw-pdf` streaming endpoint. SQLAlchemy ORM `cascade="all, delete-orphan"` on `M1Regulation.penalties` + `M1Regulation.sub_documents` relationships handles child rows automatically. **Schemas:** Added `CancelExtractionIn` (date_from, date_to, source_id? = SourceIdLiteral|None, queued_at, force=True) and `CancelExtractionOut` (task_id, revoked, rolled_back_at, deleted_rows, deleted_pdfs, skipped_pdfs, errors) to `app/schemas/m1_pipeline.py`. **Endpoint:** `POST /api/v1/admin/m1/extraction/cancel/{task_id}` in `app/api/v1/m1_gazette_extraction.py` behind `require_admin`. Validates scope via existing `_validate_scope()` + `doctype_for_source()`. Lazy-imports the service module so the hot `/trigger` + `/status` cold-start path isn't affected. **Note:** pre-dates the `m1_extraction_runs` table (F-189 from Session 54); uses scope+queued_at params instead of `run_id` lookup — could be migrated in a future pass. No rate limit yet (tracked as follow-up #22). See [plan](plans/2026-05-22_Plan%20M1%20cancel%20+%20rollback%20endpoint%20and%20frontend%20button.md). | `enigmatrix-backend/app/services/m1_extraction_cancel.py` (NEW) · `enigmatrix-backend/app/schemas/m1_pipeline.py` · `enigmatrix-backend/app/api/v1/m1_gazette_extraction.py` |
| 2026-05-22 | F-193 | **Session 55 — Railway production deployment of `enigmatrix-backend` (5 build failures, final fix via git `insteadOf` URL-rewrite; PAT leaked in build log).** First production push of the FastAPI + Celery worker + Beat backend to Railway service `satisfied-prosperity` (production env). Companion topology: Vercel hosts the frontend; Railway runs FastAPI + Celery worker (`--concurrency=2`) + Beat in a single container sharing a 10 GB `/data/storage` volume; Redis plugin in the same Railway project; managed Postgres external (Aiven). Service URL: `https://enigmatrix-backend-production.up.railway.app`. Operator side-effects: created Railway project, added Redis plugin, added 10 GB Volume mounted `/data/storage` on backend service, set 10 service env vars (`APP_ENV=production`, `APP_SECRET_KEY=<generated>`, `JWT_SECRET=<generated>`, `DATABASE_URL=<managed Postgres>`, `DB_SSL=true`, `STORAGE_LOCAL_PATH=/data/storage`, `CORS_ORIGINS=["https://enigmatrix-frontend.vercel.app"]`, `CORS_ORIGIN_REGEX=^https://enigmatrix-frontend(-[a-z0-9-]+)?\.vercel\.app$`, `CELERY_BROKER_URL=${{Redis.REDIS_URL}}` (Railway variable reference, NOT the literal Redis URL), `GITHUB_TOKEN=<fine-grained PAT>`). GitHub PAT issued under `g.hub.fri@gmail.com` for owner `ghubfri-bot` (NOT `Enigmatrixx` as the Railway plan said), repo `enigmatrix-ml`, Contents: Read-only. **Five build failures iteratively diagnosed and fixed:** (1) uv sync errored `enigmatrix-ml references a workspace ... but is not a workspace member` because `[tool.uv.sources]` had `workspace = true` (only works from xyz monorepo root) — fix: switch to a Git source. (2) Push didn't land on `main` (ran `git push` before `git add`, push was empty commit) — fix: explicit `git add pyproject.toml && uv lock && git add uv.lock && git commit && git push`. (3) Railway used cached snapshot image — every COPY/RUN layer `cached 0ms`, image showed `python:3.11-slim` while Dockerfile said `3.12-slim` — fix: force Deploy from latest commit. (4) uv runtime error `fatal: could not read Password for 'https://$%7BGITHUB_TOKEN%7D@github.com'` — **uv does NOT expand env vars inside `tool.uv.sources` URLs**; the literal `${GITHUB_TOKEN}` got URL-encoded and passed as the git username. Fix: switch pyproject URL to plain HTTPS and inject auth via git `insteadOf` URL-rewrite. (5) Final fix — three file edits: (a) `pyproject.toml` line 65 URL changed to plain `https://github.com/ghubfri-bot/enigmatrix-ml.git`; (b) `Dockerfile` added `ARG GITHUB_TOKEN` + `RUN if [ -n "$GITHUB_TOKEN" ]; then git config --global url."https://x-access-token:${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"; fi` before `uv sync`; (c) `scripts/start_railway.sh` added matching git config injection at runtime startup (after `set -e`) because `uv run alembic upgrade head` triggers lazy `uv sync` that also needs git auth; (d) regenerated `uv.lock`. **Deploy success log:** `Updated https://github.com/ghubfri-bot/enigmatrix-ml.git (cfb51ba...)` → `Built enigmatrix-ml @ git+https://...` → `Installed 25 packages in 2.24s` → `alembic.runtime.migration` → `Application startup complete` on port 8080 → `celery@d041fdf5158f ready` connected to `redis://default:**@redis.railway.internal:6379//` → `celery beat v5.6.3 (recovery) is starting` → `[1/1] Healthcheck succeeded!` → `GET /health HTTP/1.1" 200 OK`. **Verified end-to-end:** operator triggered a Bills extraction (task `a33612c1-54a4-4698-9536-797ab1016be0`); worker logs show `extract_gazette: regulation <uuid> extracted via pymupdf (22082 chars)` succeeding in ~5s/row; 10 PDFs in scope, 6 preprocessed, 4 extracted, 58 sub-documents detected; CORS-preflighted `OPTIONS` from Vercel returned 200. **🚨 CRITICAL SECURITY ISSUE (still pending as task #12):** Docker's `RUN if [ -n "$GITHUB_TOKEN" ]; then ...` command expanded the env var **into the executed RUN command string**, which Railway logged verbatim — the full PAT is now visible in plaintext in the build log, and was baked into the container image's `/root/.gitconfig`. Docker itself emitted `SecretsUsedInArgOrEnv: Do not use ARG or ENV instructions for sensitive data (ARG "GITHUB_TOKEN")` warning in the same build. Operator explicitly chose to defer rotation rather than block on the BuildKit-secrets refactor. Remediation sequence (when ready): revoke PAT → audit Docker Hub/Railway registry for visible layers → rebuild with `RUN --mount=type=secret,id=github_token` BuildKit secrets → generate fresh PAT → rotate Railway env var → add pre-commit hook to catch `GITHUB_TOKEN` patterns. See [plan](plans/2026-05-22_Plan%20Railway%20production%20deployment%20—%20backend%20deploy%20chain%20and%20PAT%20leak.md). | `enigmatrix-backend/pyproject.toml` · `enigmatrix-backend/uv.lock` · `enigmatrix-backend/Dockerfile` · `enigmatrix-backend/scripts/start_railway.sh` · Railway dashboard: project `satisfied-prosperity` / Volume `/data/storage` 10 GB / 10 service env vars |
| 2026-05-22 | F-192 | **Session 54 — SQLAlchemy QueuePool exhaustion fix.** Increased `pool_size: 1 → 3` and `max_overflow: 2 → 5` in `app/db/session.py`. Root cause: `AuditMiddleware._write_passive_log` opens a fresh `SessionLocal()` on every API request via a detached `asyncio.create_task`, and the extraction `trigger` + `status` endpoints now both hold `db: AsyncSession` dependencies. With the old cap of 3 connections (pool_size=1 + max_overflow=2), two overlapping admin requests generated ≥4 concurrent connection attempts, triggering `QueuePool limit of size 1 overflow 2 reached, connection timed out, timeout 10.00`. Error first surfaced on `GET /api/v1/users/me` (auth dep) returning HTTP 500. New budget: 8 uvicorn conns + ~2 Celery workers = ~10 total, well within Aiven's ~20–25 connection limit. Updated code comment in `session.py` to document the calculation. See [plan](plans/2026-05-22_Plan%20M1%20extraction%20run%20history%20—%20server-side%20persistence%20and%20pool%20fix.md). | `enigmatrix-backend/app/db/session.py` |
| 2026-05-22 | F-189, F-190, F-191 | **Session 54 — M1 extraction run history: server-side persistence (model + migration + endpoints + frontend).** Three-layer change. **(1) Backend model + migration:** Created `app/models/m1_extraction_run.py` (NEW — `M1ExtractionRun` SQLAlchemy 2.0 model; columns: `run_id` UUID PK, `task_id` TEXT UNIQUE, `source_id`, `date_from/to`, `queued_at`, `queued_by_id` FK→`users.id` ON DELETE SET NULL, `queued_by_email` denormalised snapshot, `celery_status` default PENDING, `result` JSONB, `traceback`, `completed_at`, `rows_ingested/extracted/preprocessed/failed` nullable INT; composite index `(source_id, queued_at)`). Updated `app/models/__init__.py` (import + `__all__`). Created `alembic/versions/202605210002_m1_extraction_runs.py` (NEW; `revision='202605210002'`, `down_revision='202605280001'`; FK `users.id`; 4 indexes). **(2) Backend endpoints:** Added `db: AsyncSession = Depends(get_db)` to `trigger_extraction` — INSERTs `M1ExtractionRun` row after `run_scraper.delay()` (non-fatal on error); `queued_by_id=admin.id`. Added `db` dep to `extraction_status` — side-effect UPDATEs `celery_status` + `completed_at` on first terminal transition. Updated `cancel_extraction` — UPDATEs `celery_status='REVOKED'` after rollback. Added `GET /api/v1/admin/m1/extraction/runs` endpoint (paginated max 100, filterable by `source_id`, ordered `queued_at DESC`). Added `ExtractionRunOut` + `ExtractionRunsListOut` schemas to `app/schemas/m1_pipeline.py`. **(3) Frontend:** Added `ExtractionRunOut`, `ExtractionRunsListOut` interfaces and `listRuns(token, {sourceId, page, pageSize})` to `lib/api/m1-gazette-extraction.ts`. In extraction page: `runsQuery` TanStack Query to `GET /runs`; `toTriggerOut()` normalizer (`ExtractionRunOut → GazetteExtractionTriggerOut`); `history` useMemo (API primary, localStorage fallback); trigger mutation invalidates `["m1-extraction-runs", sourceId]`. **Errors fixed during impl:** (a) Alembic multiple-heads — `down_revision` corrected from `"202605210001"` to `"202605280001"` (actual chain tip); (b) FK column mismatch — `users.user_id` → `users.id` in both migration + model + trigger endpoint; (c) PowerShell line-continuation — `\` → backtick for multi-line git commands. See [plan](plans/2026-05-22_Plan%20M1%20extraction%20run%20history%20—%20server-side%20persistence%20and%20pool%20fix.md). | `enigmatrix-backend/app/models/m1_extraction_run.py` (NEW) · `enigmatrix-backend/app/models/__init__.py` · `enigmatrix-backend/alembic/versions/202605210002_m1_extraction_runs.py` (NEW) · `enigmatrix-backend/app/api/v1/m1_gazette_extraction.py` · `enigmatrix-backend/app/schemas/m1_pipeline.py` · `enigmatrix-frontend/lib/api/m1-gazette-extraction.ts` · `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx` |
| 2026-05-22 | F-185, F-186, F-187, F-188 | **Session 54 — M1 extraction page UX improvements: sticky sidebar, resume/restart, history table, auto-scroll.** Four improvements to the extraction admin page. **(F-185) Sticky inner sidebar:** Added `lg:items-start` to grid container in `app/(admin)/admin/m1/pipeline/layout.tsx` (prevents CSS grid `align-items:stretch` from making `position:sticky` a no-op); added `max-h-[calc(100vh-2rem)] overflow-y-auto` to the nav card for independent nav scrolling. **(F-186) Resume/restart card:** Created `components/m1-extraction/resume-extraction-card.tsx` (NEW). Shown when `isTerminal && (extractedCount > 0 || ingestedCount > 0)`. Two actions: "Resume preprocessing" (fetches all extracted rows via `getProgress`, calls `rePreprocess` serially) and "Restart from scratch" (re-triggers same date range, calls `onRestart` callback). **(F-187) Full history table:** Raised `MAX_HISTORY` 5 → 20 in `lib/m1-extraction/trigger-history.ts`. Created `components/m1-extraction/extraction-history-table.tsx` (NEW) replacing `RecentTriggersBar` pill strip; columns: task_id (truncated mono), date range, queued (relative), live Celery status badge (via `useQueries` polling, 10s interval, stops on terminal), source_id; active row: blue dot + "viewing" pill. **(F-188) Auto-scroll:** Added `progressRef` + `scrollIntoView` on `selectHistoricalTrigger` in the extraction page — clicking a history row scrolls viewport to the progress panel. Also: hoisted `summaryForResume` query to page level; `runsQuery` (`["m1-extraction-runs", sourceId]`) as API-backed history primary source. See [plan](plans/2026-05-22_Plan%20M1%20extraction%20page%20UX%20improvements%20—%20sticky%20sidebar%20resume%20restart%20history%20auto-scroll.md). | `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/layout.tsx` · `enigmatrix-frontend/components/m1-extraction/resume-extraction-card.tsx` (NEW) · `enigmatrix-frontend/lib/m1-extraction/trigger-history.ts` · `enigmatrix-frontend/components/m1-extraction/extraction-history-table.tsx` (NEW) · `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx` |
| 2026-05-22 | F-184 | **Session 53 — M1 pipeline UX audit: Word document deliverable.** Generated `M1_Pipeline_UX_Audit.docx` (44 KB) using `python-docx` v1.2.0 (npm `docx` package blocked by npmjs.org 403 in Cowork sandbox — registry access denied). Script `ux_report.py` written with full report structure: cover page, executive summary + severity-count table, 14-row findings summary table, per-finding detail blocks (problem + recommendation + impact), What Works Well (7 positives), sprint-ordered prioritised action plan (14 rows), test methodology appendix. Colour-coded severity cells (Critical red #FEE2E2, High orange #FFEDD5, Medium amber #FEF9C3, Low green #DCFCE7); teal header branding (#007A87). Fix applied: Python source byte-level Unicode replacement for curly quotes / em-dash / arrows causing `SyntaxError: invalid character`. Output path: `C:\Users\Administrator\Documents\Claude\Projects\Understanding Information Barriers to Regulatory Compliance Among Sri Lankan SMEs\M1_Pipeline_UX_Audit.docx`. | `outputs/ux_report.py` (NEW — Cowork outputs dir) · `M1_Pipeline_UX_Audit.docx` (NEW — workspace folder) |
| 2026-05-22 | F-184 | **Session 53 — M1 pipeline admin UX audit: 14 findings documented.** Hands-on exploratory test of all six `/admin/m1/pipeline` admin areas at localhost:3000, logged in as `admin@enigmatrix.lk`. Tools: Claude-in-Chrome `javascript_tool` (DOM enumeration; `find`/`read_page` blocked for localhost by extension `host_permissions`) + `read_network_requests` (HTTP 503 diagnosis on Recent Runs RSC fetch) + computer screenshots. Route discovery: direct URL `/admin/m1/pipeline` silently redirects to `/admin/regulations` — sub-menu only visible after clicking Survey Management in sidebar. Confirmed 14 UX/UI findings: F-01 (Medium) misleading paused badge; F-02 (Medium) funnel rates 7,700%/1,283%; F-03 (Low) QUEUED count clipped; F-04 (Low) Details link no affordance; F-05 (Medium) no step prev/next nav; F-06 (Critical) Recent Runs always 503; F-07 (High) post-failure steps show "pending" + duplicate "Step 2b"; F-08 (Medium) trace table lazy load; F-09 (High) Reconcile no confirmation; F-10 (High) Start Extraction no feedback; F-11 (Medium) stale badge same amber for daily+weekly; F-12 (High) server path in error including typo "/mnt/c/Reasearch/"; F-13 (Low) row icons no tooltips; F-14 (Low) date format US not LK. Additional observation: failed rows in Overview table have no ">" drill-down chevron (only `preprocessed` rows do). 7 positive findings also documented. | No codebase files changed — audit + documentation only |
| 2026-05-22 | F-183 | **Session 52 — Theme toggle: dropdown → single-click toggle button + View Transitions API circular reveal.** Rewrote `components/layout/theme-toggle.tsx`: removed shadcn dropdown (Light/Dark/System); replaced with `<Button variant="ghost" size="icon">` whose `onClick` fires `document.startViewTransition` (feature-detected; fallback: instant `setTheme`). Click position `(x, y)` captured from `MouseEvent`; `endRadius = Math.hypot(max(x, W-x), max(y, H-y))`. New layer (`::view-transition-new(root)`): `clip-path circle(0→endRadius)` + `scale(1.015→1)` + `opacity(0.92→1)` over 600ms `cubic-bezier(0.22,1,0.36,1)` (easeOutQuint), `fill:"both"`. Old layer: `opacity(1→0.6)` + `scale(1→0.985)` same timing. `theme-transitioning` class added to `<html>` before `startViewTransition`, removed in `transition.finished.finally(...)`. Sun/Moon icon spans: `position:absolute`, swap via `rotate+scale+opacity`, 500ms easeOutQuint. `components/providers.tsx`: removed `disableTransitionOnChange`. `app/globals.css`: `::view-transition-old/new(root)` rules stripping browser default cross-fade; z-index 9999 (new) / 1 (old); `html.theme-transitioning * { transition:none!important }` suppresses competing colour transitions during window; GPU promotion (`will-change:clip-path,transform,opacity`, `backface-visibility:hidden`, `transform-origin:center`) on pseudo-elements; universal colour-token transition (`background-color, border-color, color, fill, stroke, box-shadow`) 200ms on `*`/150ms on interactive elements; `@media (prefers-reduced-motion:reduce)` → `animation:none` safety. | `enigmatrix-frontend/components/layout/theme-toggle.tsx` · `enigmatrix-frontend/components/providers.tsx` · `enigmatrix-frontend/app/globals.css` |
| 2026-05-22 | F-179, F-180 | **Session 52 — AnimatedList component suite + applied to 6 list/card pages + sticky table headers on 13 table files.** Created `components/ui/animated-list.tsx` (three exports: `AnimatedItem` — `motion.div` + `useInView({ amount:0.15, once:false })`, entrance `scale 0.95→1 / opacity 0→1 / y 8→0` 220ms easeOut, `delay/index/className/onMouseEnter/onClick` props; `AnimatedListScrollable` — scrollable container with `.al-top-gradient` + `.al-bottom-gradient` overlays; default `AnimatedList` — string-array with keyboard arrow nav). Created `components/ui/animated-list.css` (theme-aware via `hsl(var(--background/card/border/muted))`; `.sticky-thead` helper; scrollbar via `--border` + `--muted-foreground/0.5`). Applied `AnimatedItem` to 6 pages: steps (`delay={index*0.05}`), sources, research-log (MetricTile grid + SessionCard list, `delay={index*0.06}`), docs (`delay={index*0.06}`), surveys (module cards + recent sessions), dashboard (regulation cards, `delay={index*0.05}`). Made 13 table wrappers `overflow-auto rounded-lg border bg-background max-h-[520px]` + `<TableHeader className="sticky top-0 z-10 bg-background">` on: surveys/history, activity-log, m3/risk-signals, m2/scores, m2/questions, m1/pdf-records, questions-client, surveys-client, surveys/awareness/responses, regulations-client, users-client, survey/questions, docs/m1. | `enigmatrix-frontend/components/ui/animated-list.tsx` (NEW) · `enigmatrix-frontend/components/ui/animated-list.css` (NEW) · 6 list pages (AnimatedItem wrapping) · 13 table files (sticky header + overflow-auto wrapper) |
| 2026-05-22 | F-181, F-182 | **Session 52 — FuzzyText component + applied to 404, ComingSoon, platform-map, research-log.** Created `components/ui/fuzzy-text.tsx` — TypeScript port of React Bits FuzzyText; canvas per-row pixel displacement with RAF loop; `color="currentColor"` resolved at runtime via `window.getComputedStyle(canvas).color` (theme-aware, inherits CSS colour from parent). Full prop surface: `fontSize/fontWeight/fontFamily/color/enableHover/baseIntensity/hoverIntensity/fuzzRange/fps/direction/transitionDuration/clickEffect/glitchMode/glitchInterval/glitchDuration/gradient/letterSpacing/className`. Created `components/ui/fuzzy-not-found-hero.tsx` — `"use client"` island for server `not-found.tsx`. Modified `app/not-found.tsx` — `FuzzyNotFoundHero` for "404" (`glitchMode/clickEffect/fontSize clamp(5rem,20vw,14rem)`). Modified `components/coming-soon.tsx` — added `"use client"`, title uses FuzzyText (`baseIntensity=0.06/hoverIntensity=0.32/fuzzRange=16/fps=45`, `aria-label+role="heading" aria-level={2}`). Modified `app/(app)/docs/platform-map/page.tsx` — stat hero numbers (`fontSize=1.375rem/glitchMode/glitchInterval=3600`) + section headers (`fontSize=1rem/enableHover`) both via `currentColor`→section accent inheritance. Modified `app/(admin)/admin/research-log/page.tsx` — MetricTile numbers (`fontSize=1.25rem/glitchMode/glitchInterval=4800`) inherit tone class colour. `components/layout/page-header.tsx` changed then **reverted** (user rejected FuzzyText on h1). | `enigmatrix-frontend/components/ui/fuzzy-text.tsx` (NEW) · `enigmatrix-frontend/components/ui/fuzzy-not-found-hero.tsx` (NEW) · `enigmatrix-frontend/app/not-found.tsx` · `enigmatrix-frontend/components/coming-soon.tsx` · `enigmatrix-frontend/app/(app)/docs/platform-map/page.tsx` · `enigmatrix-frontend/app/(admin)/admin/research-log/page.tsx` · `enigmatrix-frontend/components/layout/page-header.tsx` (net: no change — reverted) |
| 2026-05-21 | F-178 | **Session 51 — Completeness verify endpoint hardening (timeout, CORS, WS URL, state reset).** Eight bugs surfaced + fixed: (1) `/api/v1` prefix missing in `lib/api/m1-completeness.ts` (3 URLs) → 404; (2) `httpx.ReadTimeout` on documents.gov.lk → replaced bare `_HTTP_TIMEOUT_S=30.0` with `httpx.Timeout(connect=10/read=120/write=10/pool=10)` + `_HTTP_RETRY_ATTEMPTS=2` retry-on-Timeout loop; (3) 500 without CORS headers (Starlette `ServerErrorMiddleware` sits above CORS) → added catch-all `except Exception` in `_run_verify` raising `HTTPException(500, detail=f"Verification failed: {type(exc).__name__}: {first_line[:240]}")` + `logger.exception(...)` plus explicit `httpx.TimeoutException → 504` and `httpx.HTTPError → 502`; (4) WS dialled `ws://window.location.host` (Next dev port) → `getWebSocketUrl` now uses `NEXT_PUBLIC_API_BASE_URL` + `.replace(/^http/i, "ws")`; (5) WS reconnect storm on terminal task → server sends `{type:"done", celery_status, terminal:true}` then `ws.close(code=1000, reason="task terminal")`; client `onmessage` flips `cancelledRef.current=true` on `frame.terminal`, `onclose` short-circuits on `event.code===1000`; (6) `AttributeError: 'URL' object has no attribute 'human_repr'` (httpx<0.27) → `resp.url.human_repr()` → `str(resp.url)` two sites; (7) `uvicorn --reload` silently died → recommended `source .venv/bin/activate && uvicorn app.main:app --reload`; (8) panel stale state on `taskId` switch → `useEffect(() => { setResult(null); setRefetchResult(null); setAutoRan(false); }, [taskId])` placed before auto-trigger effect. | `enigmatrix-backend/app/{services/m1_completeness_check.py, api/v1/m1_completeness.py, api/v1/m1_extraction_ws.py}` · `enigmatrix-frontend/{lib/api/m1-completeness.ts, lib/hooks/useExtractionLiveFeed.ts, components/m1-extraction/missing-gazettes-panel.tsx}` |
| 2026-05-21 | F-177 | **Session 50 — Completeness check + re-fetch + spider EN→SI→TA fallback.** User had manually compared the source website's published gazette list against the DB across two months — found 12% miss rate Feb 2026 (25 of 207) and 40% miss rate Jan 2026 (103 of 260, including 10-day Jan 19-28 blackout). Separately the existing `gazette_spider.py` silently dropped Sinhala-only / Tamil-only gazettes. Planning questions confirmed: language fallback (no schema change), both manual + auto verify, table with per-row + bulk re-fetch, "unparsed" pills for missing fields. New backend service `app/services/m1_completeness_check.py` (265 lines) — httpx + lxml listing fetch with EN-priority dedup, per-source URL templates + regex maps, `find_missing(*, source_id, date_from, date_to, session)` diffs against `m1_gazette_items`. New backend endpoints module `app/api/v1/m1_completeness.py` (382 lines) — three routes `POST /verify/{task_id}` + `POST /verify` + `POST /refetch-missing` with Pydantic schemas (`MissingItemOut`, `VerifyResultOut`, `VerifyByScopeIn`, `RefetchMissingIn`, `RefetchOneOut`, `RefetchMissingOut`); `refetch_missing` mirrors `M1RegulationsInsertPipeline._insert_rows` then enqueues `extract_gazette.delay(...)`. Registered under `prefix="/admin/m1/extraction"` in `router.py`. Frontend `lib/api/m1-completeness.ts` typed client + `components/m1-extraction/missing-gazettes-panel.tsx` (372 lines, auto-trigger on Celery terminal + manual button + missing-items table + per-row & bulk re-fetch + refetch summary). Wired into source extraction page above `<ExtractionSummaryCard>`. Spider rewritten: `_LANG_PRIORITY = {"en":0,"si":1,"ta":2}`, `_language_for(url)` helper, `parse()` buffers into `best_by_number[document_number] = (priority, dict(item))`, one yield per gazette. | `enigmatrix-backend/app/services/m1_completeness_check.py` (NEW) · `enigmatrix-backend/app/api/v1/m1_completeness.py` (NEW) · `enigmatrix-backend/app/api/v1/router.py` · `enigmatrix-backend/scraper/spiders/gazette_spider.py` · `enigmatrix-frontend/lib/api/m1-completeness.ts` (NEW) · `enigmatrix-frontend/components/m1-extraction/missing-gazettes-panel.tsx` (NEW) · `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx` |
| 2026-05-21 | F-175, F-176 | **Session 49 — Live count polling for `summaryForResume` + `SpiderResultCard` polish.** (1) Page-level `summaryForResume` react-query had `staleTime: 8_000` but no `refetchInterval` — once spider's Celery task hit SUCCESS counts froze at "100 extracted · 0 preprocessed" even while extract_gazette + preprocess_gazette were still processing 100+ PDFs. Added `refetchInterval` callback that polls every 5 s while any row is in flight; stops automatically when `ingested === 0 && extracted === 0 && preprocessed + extraction_failed >= in_scope`. (2) Raw `<pre>{JSON.stringify(status.data.result, null, 2)}</pre>` block replaced with new `components/m1-extraction/spider-result-card.tsx` (196 lines) — status pill (Success/Failed driven by `status === "ok" && returncode === "0"`), 2×2 grid (Source mono chip / Spider mono / Scope parsed `YYYY-MM-DD..YYYY-MM-DD` → `Date.toLocaleDateString` / Return code), "Other fields" fallthrough for forward-compat. Works for every source (EGZ/GZ/BILL/ACT). Page edits via python-atomic-write per Session-48 protocol. | `enigmatrix-frontend/components/m1-extraction/spider-result-card.tsx` (NEW) · `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx` |
| 2026-05-21 | F-178 | **Session 48 — Vault recovery from Write-tool truncation + backend IndentationError + 755 NUL bytes + uvicorn venv guidance.** `npm run build` reported walls of TS17008 / TS1005 / TS1010 / TS1002 errors. Hex dumps (`xxd` on trailing bytes) revealed mid-content truncation across 13 frontend files: `providers.tsx` 16 lines (mid-comment), `next.config.mjs` 15 lines ending `swcMinify: t`, dashboard page 359 lines mid-string-literal, login page 192 lines mid-className, sidebar 899 of expected 960, etc. Write/Edit tools had returned "file created successfully" on every truncating call. Backend `extract_gazette.py` additionally had 755 trailing NUL bytes (Python `ast.parse` rejects null bytes outright). Previous WebSocket-emit Edit had placed `_emit(...)` block at 4-space indent outside the `async with`, leaving the next `if not download_url` block at 8-space dangling → `IndentationError: unexpected indent` at line 212. Recovery: (1) emitted exact PowerShell `git restore` command for 13 frontend files; (2) for `extract_gazette.py` — removed misplaced `_emit` block at lines 200-211, stripped 755 trailing NUL bytes via `data.rstrip(b'\x00').rstrip()` (file ends `    return body\n`, 15193 bytes), `ast.parse` confirmed clean; (3) swept sibling backend/ML files (`m1_extraction_live_feed.py`, `m1_extraction_ws.py`, `router.py`, ML `progress.py`, ML `__init__.py`) for NUL bytes — all 0; (4) adopted python-atomic-write protocol (`tmp = path + ".tmp"; open(tmp, "w", encoding="utf-8", newline="\n").write(out); os.replace(tmp, path)`) for any file >~25KB with verify-after-write (`wc -l`, `tail -3`, null-byte check, `ast.parse` for Python). Pattern correlates with long writes containing em-dashes / special quotes / emoji. Separately user hit `ModuleNotFoundError: No module named 'fastapi'` running uvicorn from `/usr/lib/python3.14` — pointed at `source /mnt/c/Reasearch/xyz/.venv/bin/activate && uvicorn app.main:app --reload`. | `enigmatrix-backend/app/tasks/m1/extract_gazette.py` · 13 frontend files reverted via `git restore` (`components/{providers,layout/sidebar,layout/theme-toggle}.tsx`, `components/m1-pipeline/{funnel-chart,pipeline-flow-diagram}.tsx`, `app/(admin)/layout.tsx`, `app/(app)/dashboard/page.tsx`, `app/(auth)/login/page.tsx`, `app/api/auth/establish/route.ts`, `app/(admin)/admin/m1/pipeline/page.tsx`, `app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx`, `app/globals.css`, `next.config.mjs`) |
| 2026-05-21 | F-174 | **Session 47 — Frontend perf overhaul (dashboard streaming, login handoff, bundle/query tuning, recharts code-split).** User reported >10 s login → dashboard delay. Three compounding causes: (1) login handler did 4 sequential calls (`AuthApi.login()` → `/api/auth/establish` → `/api/auth/me` → `router.replace()` triggers SSR → `router.refresh()` throws away tree); (2) dashboard server component awaited `Promise.all` of 5 backend fetches before returning HTML; (3) react-query defaults `refetchOnWindowFocus/Reconnect/Mount: true` caused redundant fetches per navigation. Rewrote `app/(app)/dashboard/page.tsx` with Suspense streaming — synchronous shell + welcome banner, three async server-component subsections (`StatCardsSection`, `PendingRegulationsSection`, `LowerSection`) each wrapped in `<Suspense fallback={<Skeleton/>}>`. `/api/auth/establish` now base64url-decodes JWT payload server-side and returns `{ ok: true, role }` inline. Login page reads `role` from establish response, drops `/me` fetch AND `router.refresh()`. `next.config.mjs` adds `productionBrowserSourceMaps:false / swcMinify:true / compress:true / output:"standalone" / poweredByHeader:false` + `optimizePackageImports: [lucide-react, date-fns, recharts, framer-motion, 6 @radix-ui/* packages]` + `images: { formats: ["image/avif", "image/webp"], minimumCacheTTL: 60, remotePatterns: [documents.gov.lk, enigmatrix-backend.vercel.app] }`. `components/providers.tsx` QueryClient defaults: `staleTime: 60_000 / gcTime: 10*60_000 / refetchOn*: false / retry: 1 / retryDelay: 1_000`, mutations `retry: 0`. `app/(admin)/admin/m1/pipeline/page.tsx` wraps `ThroughputChart / StatusDistribution / FunnelChart` in `next/dynamic({ssr: false, loading: () => <Skeleton/>})`. Build error: `import dynamic from "next/dynamic"` collided with `export const dynamic = "force-dynamic"` route-segment config → renamed to `import nextDynamic from "next/dynamic"`. | `enigmatrix-frontend/app/(app)/dashboard/page.tsx` · `enigmatrix-frontend/app/api/auth/establish/route.ts` · `enigmatrix-frontend/app/(auth)/login/page.tsx` · `enigmatrix-frontend/next.config.mjs` · `enigmatrix-frontend/components/providers.tsx` · `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/page.tsx` |
| 2026-05-21 | F-173 | **Session 46 — Extraction running UX upgrade (PipelineRunStatusCard + WS sub-step scaffold + ML progress module + backend Celery emits).** User reported page said "Completed SUCCESS" while 2 rows still at `ingested` and 154 not yet preprocessed. Planning questions confirmed all three progress detail levels (per-step + per-PDF + sub-step), WebSocket transport, end-to-end completion semantics, work across frontend + backend + ML. New frontend `components/m1-extraction/pipeline-run-status-card.tsx` (481 lines) — exports `derivePipelinePhase(celeryStatus, counts) → "scraping" | "extracting" | "preprocessing" | "completed" | "partial" | "failed" | "cancelled"`; three-phase tracker; live sub-step pill. New frontend `lib/hooks/useExtractionLiveFeed.ts` (141 lines) — WebSocket client with exponential backoff (1s → 30s). Wired both into source extraction page replacing the single-line Celery banner. New ML `enigmatrix-ml/m1/extraction/progress.py` (114 lines) — canonical `ExtractionPhase` literal (`classifying | extracting_text | ocr | language_detection | wijesekara | preprocessing | indexing`), `PHASE_LABELS` dict, `emit(cb, ...)` helper. Re-exported from ML package `__init__.py`: `PHASE_LABELS`, `ExtractionPhase`, `ProgressCallback`, `SubStepEvent`, `emit_progress`. New backend `app/services/m1_extraction_live_feed.py` (84 lines) — sync `redis-py` publisher reusing `CELERY_BROKER_URL`. Modified `app/tasks/m1/extract_gazette.py` — added `_emit(task_self, *, regulation_id, display, phase, source_id, fraction=None)` helper that calls `self.update_state(state="PROGRESS", meta=payload)` AND `publish_substep(...)`; plumbed `task_self` through `_extract_gazette_async` → `_extract_gazette_body`; hooked at three phase boundaries (classify, extract/OCR, indexing). New backend `app/api/v1/m1_extraction_ws.py` (180 lines) — FastAPI `@router.websocket("/extraction/{task_id}")` with token query-param auth via `decode_token(token, expected_kind="access")`; resolves source via `m1_extraction_runs` row; subscribes to source-scoped Redis pub/sub channel; three concurrent loops (pubsub pump / 25s heartbeat / 2s Celery terminal watcher). Registered WS router in `app/api/v1/router.py`. | `enigmatrix-frontend/components/m1-extraction/pipeline-run-status-card.tsx` (NEW) · `enigmatrix-frontend/lib/hooks/useExtractionLiveFeed.ts` (NEW) · `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/sources/[sourceId]/extraction/page.tsx` · `enigmatrix-ml/m1/extraction/progress.py` (NEW) · `enigmatrix-ml/m1/extraction/__init__.py` · `enigmatrix-backend/app/services/m1_extraction_live_feed.py` (NEW) · `enigmatrix-backend/app/api/v1/m1_extraction_ws.py` (NEW) · `enigmatrix-backend/app/api/v1/router.py` · `enigmatrix-backend/app/tasks/m1/extract_gazette.py` |
| 2026-05-21 | F-172 | **Session 45 — M1 pipeline page responsiveness v2 (funnel overflow, sticky sidebar, collapsed footer).** Four UI issues on `/admin/m1/pipeline`: (1) funnel chart bars overflowed card by 100× — `widthPct = (value / counts[TIERS[0].key]) * 100` divides by *ingested*; once rows drained downstream (`ingested=1, extracted=101, preprocessed=102`) math produced 10,100% widths; (2) chunky native scrollbar visible in sidebar — earlier fix scoped `* {scrollbar:none}` to `html,body` only, exposing sidebar's `overflow-y-auto`; (3) sidebar scrolled with page instead of pinning — previous fix used `overflow-x-hidden` on admin layout flex containers; `overflow:hidden` on any axis establishes a containing block that disables `position:sticky` on descendants; (4) collapsed sidebar's user/locale footer cramped. Fixes: funnel uses `maxAcrossTiers = Math.max(1, ...TIERS.map(t => counts[t.key] ?? 0))` denominator + clamps `widthPct` to `[0,100]` + adds `overflow-hidden` + `bg-muted/30` on track parent; sidebar gets `scrollbar-hide` class on inner `<nav>` + tighter `mx-1.5` collapsed card margin + `p-1.5` collapsed bottom-section padding; `UserRow` rewritten to branch on `collapsed` — stack avatar above logout pill with `<Tooltip>`-wrapped triggers when collapsed, horizontal row with hover bg when expanded, avatar gets `ring-1 ring-border/60`; admin layout switches `overflow-x-hidden` → `overflow-x-clip` on both flex container + main column. | `enigmatrix-frontend/components/m1-pipeline/funnel-chart.tsx` · `enigmatrix-frontend/components/layout/sidebar.tsx` · `enigmatrix-frontend/app/(admin)/layout.tsx` |
| 2026-05-21 | F-171 | **Session 44 — Theme polish (view-transition animation smoothness + disable system tracking).** Two unrelated theme issues. (1) View-transition reveal felt twitchy — `startViewTransition` clip-path animation used `ease-in-out` over 420 ms, and the universal `* { transition: ... }` rule in `globals.css` was running underneath the view-transition snapshot producing two parallel animations. (2) App auto-changed theme when OS flipped colour scheme. Fixes: `theme-toggle.tsx` easing → `cubic-bezier(0.22, 1, 0.36, 1)` (easeOutQuint), duration 420 ms → 600 ms, subtle scale (1.015→1) + opacity (0.92→1) on new layer, scale (1→0.985) + opacity (1→0.6) on old layer; flip `theme-transitioning` class on `<html>` for transition's lifetime; sync Sun/Moon icon transitions to 500 ms with same bezier. `globals.css` adds GPU promotion (`will-change: clip-path, transform, opacity`, `backface-visibility: hidden`, `transform-origin: center center`) on `::view-transition-old/new(root)` + `html.theme-transitioning *` rule suppressing universal colour transitions during the view-transition window. `providers.tsx` `ThemeProvider` props: `defaultTheme="system"` → `defaultTheme="light"`, added `enableSystem={false}`, added `storageKey="enigmatrix-theme"`. Confirmed via grep that `setTheme` is only invoked from `theme-toggle.tsx`. | `enigmatrix-frontend/components/layout/theme-toggle.tsx` · `enigmatrix-frontend/app/globals.css` · `enigmatrix-frontend/components/providers.tsx` |
| 2026-05-18 | F-170 | **Session 43 — Spider runtime fix: close spider when scope exhausted.** User reported the Gazette Extraction trigger task stayed in `STARTED` for 10+ minutes after all 16 in-scope regulations finished preprocessing (Run summary: `1m 21s`). **Root cause:** the Celery `run_gazette_spider` task waits for the scrapy subprocess to exit. Scrapy's Twisted reactor only shuts down when its queue is empty AND in-flight requests are retired. For a narrow scope (e.g. Jan 4–5 2026) on `egz_2026.html` (500+ PDF anchors), scrapy keeps walking the rest of the page after the last in-scope item, then waits for the reactor to drain under `DOWNLOAD_DELAY=2` + `AUTOTHROTTLE` — adding minutes of dead-time tail. **Fix (two layers):** (a) **Early-exit in `parse()`** — documents.gov.lk lists gazettes in descending date order; once the spider walks past a row whose `gazette_date < self.date_from`, no more in-scope items can possibly appear. Spider now logs `closing spider (scope_exhausted): hit gazette N with date=D, earlier than date_from=DF`, calls `self.crawler.engine.close_spider(self, "scope_exhausted")`, then returns. (b) **Safety net** — `CLOSESPIDER_TIMEOUT_NO_ITEM=60` added to `custom_settings`; exits if 60 s pass without a new item. (c) **Test coverage** — new `test_spider_closes_when_past_date_from` builds a 4-row descending-date fixture (May 15, Feb 10, Jan 5, Jan 3) with `date_from=2024-01-04, date_to=2024-01-05`; asserts only Jan 5 yields + `crawler.engine.close_spider` called once with reason `"scope_exhausted"`. Uses `MagicMock` for `spider.crawler` (existing pattern; no Twisted reactor boot). Trims a Jan 4–5 crawl from ~10 min to ~1–2 min. Beat-scheduled invocation (no args) preserved: `date_from is None` → early-exit never fires → behaviour unchanged. 22 passed (was 21; +1 new). **Decisions:** two-layer fix (early-exit for optimal performance + 60 s safety net for robustness); `<` not `<=` for the comparison (equality is in scope, handled by existing `_in_scope`); explicit log line names the trigger gazette + dates for diagnosability. **Risk noted:** early-exit relies on descending date order — if documents.gov.lk ever changes the sort, in-scope items could be missed; the 60-s safety net still applies. | `enigmatrix-backend/{scraper/spiders/gazette_spider.py, app/tests/integration/test_gazette_spider.py}`, `c:\sme\{08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md, 02-Research-Modules/1 Module-1-Awareness-Gap/local-dev/phase2_admin_gazette_extraction.md}` |
| 2026-05-18 | F-169 | **Session 42 — Date-range picker for Gazette Extraction (FE + BE).** Replaces F-161/F-164's year + month-range Comboboxes with a beautiful calendar-based date-range picker so admins can scope a crawl to specific days (e.g. "Jan 15, 2026 → Feb 28, 2026"). **Frontend new components (3):** `components/ui/popover.tsx` (shadcn wrapper over `@radix-ui/react-popover`); `components/ui/calendar.tsx` (themed `DayPicker` with primary-tinted range visualisation); `components/m1-extraction/date-range-picker.tsx` (Popover-triggered 2-month side-by-side `<Calendar mode="range">` + 7 quick-pick chips: Last 7 days / Last 30 days / This year / Q1-Q4 + Clear/Apply footer). New deps: `react-day-picker@^8.10.1` + `@radix-ui/react-popover@^1.1.2`. Pinned react-day-picker to v8 because v9 requires React 19 (project is on React 18.3). **Frontend rewires (5):** extraction page swaps 3 Comboboxes for 1 DateRangePicker; new `rangeValid()` validator enforces same-year + `[2010, today]`; trigger.mutate sends `{ date_from, date_to }` ISO strings; status card "Scope:" label formats as "Jan 15 – Feb 28, 2026" via `date-fns/format`; lib/api/m1-gazette-extraction.ts types updated; lib/m1-extraction/trigger-history.ts validator switches to new shape (old localStorage entries silently dropped); RecentTriggersBar pill labels render via `parseISO + format`. **Backend (5 modified):** `GazetteExtractionTriggerIn` Pydantic schema gains a `model_validator` enforcing same-year + within bounds + `date_from ≤ date_to` (HTTP 400 on violation). `/trigger` / `/progress` / `/summary` endpoints all take `date_from` + `date_to` (ISO YYYY-MM-DD) instead of `year` + `month_start` + `month_end`. `run_gazette_spider(date_from_iso, date_to_iso)` task signature + new `_parse_iso_date()` + rewritten `_validate_scope()`. Spider's `__init__` derives `year` from `date_from.split("-")[0]` to build the canonical listing URL; `_in_scope()` does lexicographic ISO-string compare. Service's `_extraction_scope_filter()` switched from `func.extract(year/month, ...)` to `M1Regulation.gazette_published_date.between(date_from, date_to)` — index-friendly perf win. **Tests (2 files):** 4 unit tests (date strings as `-a` flags, inverted/cross-year/pre-2010 rejection) + 3 integration tests (start_url from year, date range drops out-of-scope, day-level boundary inclusion). 21 passed total (was 19). **Decisions:** picker over native `<input type="date">` because the user explicitly asked for "beautiful... advanced and best user experience"; broke the API shape (admin-only, single consumer, accepted); lexicographic ISO compare in `_in_scope` (simpler + equivalent to date compare for ISO YYYY-MM-DD); `BETWEEN date_from AND date_to` in service (Postgres can use the date column's btree). **Deferred:** cross-year ranges (would need N spider chains); backend integration test for `/progress` + `/summary` with new shape (pure aggregations, smoke-testable via curl). | `enigmatrix-frontend/{package.json, components/ui/{popover.tsx (NEW), calendar.tsx (NEW)}, components/m1-extraction/{date-range-picker.tsx (NEW), recent-triggers-bar.tsx}, app/(admin)/admin/m1/pipeline/extraction/page.tsx, lib/api/m1-gazette-extraction.ts, lib/m1-extraction/trigger-history.ts}`, `enigmatrix-backend/{app/schemas/m1_pipeline.py, app/api/v1/m1_gazette_extraction.py, app/tasks/m1/gazette_scraper.py, scraper/spiders/gazette_spider.py, app/services/m1_pipeline_service.py, app/tests/unit/test_gazette_scraper_task.py, app/tests/integration/test_gazette_spider.py}`, `c:\sme\{08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md, 02-Research-Modules/1 Module-1-Awareness-Gap/local-dev/phase2_admin_gazette_extraction.md}` |
| 2026-05-18 | F-167 | **Session 41 — Pipeline-flow tiles now surface a cumulative "reached" count.** User-reported confusion after F-161's spider crawl preprocessed 448 rows cleanly: the `/admin/m1/pipeline` overview tiles showed "2a Scrapy: 0 rows", "2b Celery extract: 0 rows", "2c PDF classifier: 0 rows", "2d Language detection: 0 rows" — only 2e + 2f showed 448. Not a bug: each tile's `liveCount` shows rows currently at that status, and once a row completes preprocessing it sits at `'preprocessed'` permanently (leaves 0 at every earlier status). **Fix (FE-only):** surface a cumulative "reached" count as the tile's headline number, derived purely from the existing `status_counts` response. Added `reachedFromStatuses: PipelineStatusKey[]` to each `PIPELINE_STEPS` entry (2a → all 8 statuses for entry; 2b/c/d → extracted + preprocessed + classified + summarized + alerted + archived; 2e/f → preprocessed + classified + summarized + alerted + archived). `<PipelineFlowDiagram>` computes `step.reachedFromStatuses.reduce((sum, s) => sum + (counts[s] ?? 0), 0)` per tile and passes it as a new `reachedCount` prop. `<StepTile>` bottom-row layout swapped: reached count is the headline big-number; "N at <counterStatus>" becomes the muted secondary line. Tiles now read e.g. "2a Scrapy: **452** reached · 0 at ingested" — answers the "did scrapy run?" question instantly. `extraction_failed` deliberately excluded from 2b/c/d's reached bucket (legend chip shows it separately). No backend / schema change. `npx tsc --noEmit` clean for the 3 modified files. | `enigmatrix-frontend/{lib/m1-pipeline/steps.ts, components/m1-pipeline/{pipeline-flow-diagram, step-tile}.tsx}`, `c:\sme\08-Findings-Log\{SESSIONS,CHANGES,FEATURES}.md`, `c:\sme\02-Research-Modules\1 Module-1-Awareness-Gap\local-dev\phase2_admin_gazette_extraction.md` |
| 2026-05-18 | F-168 | **Session 41 — Aiven cloud-Postgres pool sizing fix for the `asyncpg TimeoutError` on `/summary`.** `/api/v1/admin/m1/extraction/summary` was timing out after 60s with `asyncpg.exceptions.TimeoutError` at the connection-establishment step. **Critical context the user surfaced mid-session:** `DATABASE_URL` points at Aiven cloud Postgres (`*.aivencloud.com:16188/defaultdb?sslmode=require`), NOT the local docker container — so Session 40's `command: ["postgres", "-c", "max_connections=200"]` flag was completely moot for this user's setup. Aiven entry-tier plans cap total connections at ~20-25 globally. Session 40's `pool_size=2, max_overflow=3` × 8 Celery workers + uvicorn = 45 connection slots wanted, ≫ Aiven's hard cap. Queued asyncpg `connect()` calls timed out at the default 60-s budget. **Fix:** drop `app/db/session.py` further to `pool_size=1, max_overflow=2, pool_timeout=10, pool_recycle=300` (3 max per worker process; fail-fast at 10s instead of default 30s). Add a Celery `--concurrency=2` recommendation in the runtime doc (math: 2 workers × 3 + uvicorn × 3 = 9 conns peak — fits 20-conn Aiven budget with 11-conn headroom). For users on local docker Postgres only, an inline comment in session.py says to raise back to `(2, 3)` (local container has `max_connections=200`). Runtime doc gains a ⚠️ "Are you on Aiven cloud Postgres?" callout at the top + new troubleshooting row for the TimeoutError symptom. Also clarified `DB_SSL=false` is fine alongside the URL's `?sslmode=require` — SQLAlchemy's asyncpg dialect translates the URL param into the asyncpg `ssl=` connection arg automatically; the legacy `DB_SSL` env flag is redundant. **To apply after pull:** restart Celery worker with `--concurrency=2` + restart uvicorn. Verify: `python -c "from app.db.session import engine; print(engine.pool.size(), engine.pool._max_overflow)"` → `1 2`. Backend regression: 19 passed (testcontainer Postgres uses its own pool, change is invisible to tests). | `enigmatrix-backend/app/db/session.py`, `c:\sme\02-Research-Modules\1 Module-1-Awareness-Gap\local-dev\phase2_admin_gazette_extraction.md`, `c:\sme\08-Findings-Log\{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-18 | F-165 | **Session 40 — M1 Pipeline in-page sub-nav gains Gazette Extraction entry.** The sticky left rail at `app/(admin)/admin/m1/pipeline/layout.tsx::NAV` was missing the F-161 Gazette Extraction entry; only the main app sidebar got it in Session 38. Added a 5th `NAV` item with `DownloadCloud` icon → `/admin/m1/pipeline/extraction`. Sub-nav order now: Overview · Recent runs · Trace · Steps · Gazette Extraction. One-line addition + one import. `npx tsc --noEmit` clean for the modified file. | `enigmatrix-frontend/app/(admin)/admin/m1/pipeline/layout.tsx`, `c:\sme\08-Findings-Log\{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-18 | F-166 | **Session 40 — Postgres connection-limit hotfix for extract→preprocess fan-out.** Workers were crashing during the F-161 fan-out with `asyncpg.exceptions.TooManyConnectionsError: sorry, too many clients already` (and the related `remaining connection slots are reserved for roles with the SUPERUSER attribute`). **Root cause:** `app/db/session.py` had `pool_size=10, max_overflow=20` (30 conns per worker process); Celery's 8-worker prefork topology → theoretical burst max 240 connections, well above Postgres's default `max_connections=100` (minus 3 superuser-reserved = 97 usable). Per-task `engine.dispose()` in `finally` only releases AFTER the task body; during a simultaneous burst, all 8 worker pools may expand at once before any disposes. **Fix:** (a) `app/db/session.py` pool dropped to `pool_size=2, max_overflow=3, pool_recycle=300` (5 conns/worker × 8 = 40 + uvicorn ≤ 5 = 45 ≪ 200); (b) `docker-compose.dev.yml` postgres service gains `command: ["postgres", "-c", "max_connections=200"]` for headroom. Task code unchanged — the `finally`-block dispose pattern was already correct; the bug was purely a sizing mistake. **To apply after pull:** `docker compose -f docker-compose.dev.yml up -d --force-recreate postgres` (re-creates the container with the new flag; volume + data preserved), then restart Celery worker + uvicorn to pick up the new SQLAlchemy pool config. Verify: `docker exec enigmatrix-postgres psql -U enigmatrix -d enigmatrix -c "SHOW max_connections;"` → `200`. Backend regression: 19 passed (testcontainer Postgres uses its own pool — change is invisible to tests). Documented in `phase2_admin_gazette_extraction.md` §7 troubleshooting. **Decisions:** lower the pool globally (vs branching `NullPool` for Celery) — simpler + sufficient because each task uses 1 conn at a time anyway; use `command:` override (vs mounting a `postgresql.conf`) — idiomatic dev-compose pattern. | `enigmatrix-backend/app/db/session.py`, `docker-compose.dev.yml`, `c:\sme\02-Research-Modules\1 Module-1-Awareness-Gap\local-dev\phase2_admin_gazette_extraction.md`, `c:\sme\08-Findings-Log\{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-18 | F-163 | **Session 39 — Token-key sweep across 7 M1 admin pages.** Fixed the latent `d.accessToken` → `j?.access` drift identified during Session 38: `/api/auth/token` returns `{ access }` but the M1 portal + admin settings pages were reading `d.accessToken`, leaving `token === null` and silently keeping their `useQuery` calls in skeleton state (since they're gated by `enabled: !!token`). Surfaced when the user couldn't see Session 38's successfully-extracted rows on `/admin/m1/pipeline/recent`. Uniform 3-line patch to all 7 pages, matching the proven extraction-page pattern: `(r.ok ? r.json() : null) → j?.access ?? null` with `catch(() => setToken(null))`. `grep "d\.accessToken" c:\Reasearch\xyz\enigmatrix-frontend\app` returns 0 matches afterward. | `enigmatrix-frontend/app/(admin)/admin/{m1/pipeline/{page, recent/page, steps/page, steps/[stepId]/page, trace/page, trace/[regulationId]/page}.tsx, settings/page.tsx}`, `c:\sme\08-Findings-Log\{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-18 | F-164 | **Session 39 — Gazette Extraction page UX enrichments (trigger history + summary card + per-row chips).** Three layered improvements onto the Session 38 admin page at `/admin/m1/pipeline/extraction`. **Trigger history:** new `lib/m1-extraction/trigger-history.ts` module (`loadHistory`/`pushHistory`/`clearHistory`, `MAX_HISTORY=5`) persists last 5 triggers in `localStorage` under `m1.gazette.recent_triggers`. New `<RecentTriggersBar>` renders horizontal pill list above the form (e.g. "2026 / Jan–Feb · 2h ago"); clicking a pill replays that scope's progress panel + summary card. Page auto-hydrates history on mount + auto-restores the most-recent trigger as active task — fixes the "lose progress panel on page reload during a multi-hour crawl" UX gap. SSR-safe (every `localStorage` access guards `typeof window`). Dedupes by `task_id` so replaying a past trigger doesn't double-stack the list. **Summary card:** new backend endpoint `GET /api/v1/admin/m1/extraction/summary` (admin-gated, same scope params as `/progress`) returns scope-aggregated stats — in_scope count + per-status (5 buckets) + per-extraction-method counts + total `raw_text` length + total `cleaned_text` length + total penalties (via JOIN on `m1_regulation_penalties`) + total sub_documents (via JOIN on `m1_sub_documents`) + first_created_at + last_updated_at (drives a duration display). Service function `m1_pipeline_service.get_extraction_summary()` uses 4 SQL aggregate queries; new private `_extraction_scope_filter()` helper now shared between `/progress` + `/summary` to prevent scope-predicate drift. New `<ExtractionSummaryCard>` component renders a 6-stat grid (in_scope / raw chars / cleaned chars / penalties / sub-docs / method count) + a horizontal status pill row + PDF-type chips ("Text PDF · 12, Scanned (OCR) · 3, Detecting · 1"). Polls every 10 s while task non-terminal; visibility-gated + terminal early-stop (matches the `<ExtractionProgressPanel>` polling pattern). **Richer per-row cards:** `regulation-progress-card.tsx` now shows a PDF-type StatusBadge derived from `extraction_method` (pymupdf="Text PDF" info / pdfplumber="Hybrid PDF" warning / tesseract="Scanned · OCR" warning / null=omitted) + an amendment_type badge (amendment=info / repeal=warning / new_act=success) in the collapsed view; `gazette_published_date` now renders prominently next to the gazette_number with a 📅 prefix (was previously omitted from collapsed view). No schema changes — all fields already exist in the `/progress` response. `cd enigmatrix-backend && uv run pytest app/tests -q` → 19 passed (no regressions); `cd enigmatrix-frontend && npx tsc --noEmit` clean (only pre-existing `trace-timeline.tsx::sky` color-key error remains, unrelated). **Decisions:** filter at API + service layer (defence-in-depth); reuse `_extraction_scope_filter()` for DRY; auto-restore latest trigger on page-load (UX); PDF-type chips on collapsed view for fast at-a-glance scanning. **Deferred:** `primary_language` column migration (column doesn't exist on `m1_regulations` today; lives only in `preprocess_gazette_task` return dict); backend integration test for `/summary` (pure aggregations against covered tables — smoke-testable via curl); PDF page count (needs migration + extraction-task update); spider-side real-time "page X of Y" granularity. | `enigmatrix-backend/{app/services/m1_pipeline_service.py, app/schemas/m1_pipeline.py, app/api/v1/m1_gazette_extraction.py}`, `enigmatrix-frontend/{lib/m1-extraction/trigger-history.ts (NEW), components/m1-extraction/{recent-triggers-bar.tsx (NEW), extraction-summary-card.tsx (NEW), regulation-progress-card.tsx}, lib/api/m1-gazette-extraction.ts, app/(admin)/admin/m1/pipeline/extraction/page.tsx}`, `c:\sme\{08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md, 02-Research-Modules/1 Module-1-Awareness-Gap/findings/2026-05-17-m1-extraction-ux-enrichments.md (NEW)}` |
| 2026-05-17 | F-161 | **Session 38 (doc maintenance) — Troubleshooting expansion for the admin gazette extraction runtime doc.** User hit four real-world gotchas on first triggering an extraction from the new admin portal; logged each into the `phase2_admin_gazette_extraction.md` §7 troubleshooting table. (1) **Stale Celery worker after task-signature edit** — `TypeError: run_gazette_spider() takes 1 positional argument but 4 were given` fires when the worker was started before the new 4-arg signature was deployed; Python caches imports at worker boot and the worker keeps the pre-edit version until restart. Source-of-truth `app/tasks/m1/gazette_scraper.py:67-72` was already correct on disk — pure runtime issue. New §2.3.1 callout under "Celery worker (terminal A)" explicitly calls out: restart the worker after every edit under `app/tasks/**` or the spider, with an optional `watchmedo auto-restart` recipe (needs `watchdog`) for dev ergonomics. (2) **Spider-runs-but-zero-rows** — user picked year=2026/Jan-Jan but `documents.gov.lk/view/egz/egz_2026.html` only lists May 2026 entries today, so the `_in_scope` filter correctly drops every row and the spider exits with zero PDFs downloaded; not a bug, just confusing because the worker logs "scrapy completed cleanly". Fix: pick a non-restrictive range or visit the year-page in a browser first to see what's listed. (3) **`extract_gazette: regulation <uuid> not found` WARNING** — orphan chained task from an earlier trigger / test where the row was deleted or never committed but the `.delay(reg_id)` was already in Redis; the task correctly returns `{"status": "missing"}` and the worker moves on, no defect. To clear the queue noise: `docker exec enigmatrix-redis redis-cli FLUSHDB`. (4) **3 downloads + 2 DropItem warnings per gazette** — pre-existing behaviour predating F-161; each row on documents.gov.lk has 3 PDF anchors (English/Sinhala/Tamil), spider yields 1 item per anchor, `ix_m1_regulations_gazette_number` UNIQUE constraint dedupes after the first insert. Two redundant downloads per gazette, no data corruption. Logged as a known limitation; future enhancement would prefer the English anchor or store all 3 with a `language` discriminator. **Doc-only — no source-code changes; the on-disk task signature was already correct.** | `c:\sme\02-Research-Modules\1 Module-1-Awareness-Gap\local-dev\phase2_admin_gazette_extraction.md`, `c:\sme\08-Findings-Log\CHANGES.md` |
| 2026-05-17 | F-161 | **Session 38 — Year + month-range scoped gazette extraction trigger (FE + BE).** New admin route `/admin/m1/pipeline/extraction` (sidebar entry under M1 Pipeline, icon `DownloadCloud`) lets an admin pick year (2010 → today) + month range via three autocomplete `Combobox` dropdowns and trigger a scoped Scrapy crawl over `documents.gov.lk/view/egz/egz_<year>.html`. **Backend:** new router `app/api/v1/m1_gazette_extraction.py` with 2 endpoints under `Depends(require_admin)`: `POST /trigger` (Pydantic-validated payload + `_validate_scope` check for `month_start ≤ month_end` and year-not-in-future → HTTP 400 on bad input; calls `run_gazette_spider.delay(year, month_start, month_end)`; returns `GazetteExtractionTriggerOut(task_id, year, month_start, month_end, queued_at)`) and `GET /status/{task_id}` (wraps Celery `AsyncResult` via the already-configured Redis result-backend — populates `result` dict on SUCCESS, `traceback` on FAILURE). Schemas added to `app/schemas/m1_pipeline.py` (`GazetteExtractionTriggerIn/Out` + `GazetteExtractionStatusOut(status: Literal["PENDING","STARTED","SUCCESS","FAILURE","RETRY","REVOKED"])`). `app/tasks/m1/gazette_scraper.py::run_gazette_spider(year, month_start, month_end)` signature extended; new `_validate_scope()` raises `ValueError` (defence-in-depth — also fires for direct Python REPL invocations); subprocess `["scrapy", "crawl", "gazette_spider"]` gains `-a year=… -a month_start=… -a month_end=…` when set. `scraper/spiders/gazette_spider.py::__init__` extended with 3 optional kwargs (Scrapy passes `-a` flags as strings — coerced to `int` inside the spider); when `year` is given without `start_url`, builds the canonical URL automatically; new `_in_scope(gazette_date)` filter inside `parse()` drops out-of-month rows BEFORE `yield`, so PDFs outside scope are never downloaded. Router registered in `app/api/v1/router.py` at `prefix="/admin/m1/extraction"`, tag `"admin-m1-extraction"`. **Frontend:** new page at `app/(admin)/admin/m1/pipeline/extraction/page.tsx` (`"use client"`) — three `Combobox` inputs (`components/ui/combobox.tsx`, single-select searchable) with `monthStart ≤ monthEnd` client-side validation, `useMutation` POST → stores task_id, `useQuery` polling `/status/{task_id}` every 5 s (visibility-gated via existing `usePageVisible()` hook + early-stop on terminal states); status card border tone shifts per state (grey PENDING / amber STARTED with spinner / green SUCCESS / red FAILURE); on SUCCESS shows "View recent runs →" Link to `/admin/m1/pipeline/recent`. New typed client `lib/api/m1-gazette-extraction.ts` with `M1GazetteExtractionApi.{trigger, getStatus}`. Sidebar (`components/layout/sidebar.tsx`) adds `DownloadCloud` icon import + 1 entry to `ADMIN_M1_PIPELINE_ITEMS`. i18n: `nav.adminM1Extraction = "Gazette Extraction"` added to all three `lib/i18n/messages/{en,si,ta}.json`. **Bug fix bundled in:** the new page reads `j?.access` from `/api/auth/token` (not `d.accessToken` — the M1 portal pages' copy-paste pattern reads a key that doesn't exist in the route response, silently keeping `token === null` and disabling buttons / leaving queries in skeleton). **Tests:** 5 new across 2 files. Unit: forwards scope as `-a` flags + rejects `month_start > month_end` + rejects pre-2010 year. Integration: spider `_in_scope` filter drops out-of-month rows (inline HTML fixture with March/May/August) + `year=X` no `start_url` builds canonical URL. `uv run pytest app/tests/unit/test_gazette_scraper_task.py app/tests/integration/test_gazette_spider.py -v` → 11 passed. **Decisions:** filter at `parse()` not at the pipeline (saves bandwidth); validation in both layers (API → HTTP 400; task → ValueError); no `extraction_jobs` DB table — F-160 portal covers post-trigger observability via `/admin/m1/pipeline/recent`; autocomplete `Combobox` over plain `Select` (typeahead "Aug" beats scrolling); F-162 split from F-161 to keep DX fix discoverable. **Deferred:** sweep `accessToken → access` across 6 other M1 pipeline pages (latent, currently degrades to skeleton); backend integration test for the new endpoints (Celery task covered, FastAPI surface only smoke-verified); drop Beat-scheduled invocation's hard-coded `egz_2026.html` default once admins can trigger on-demand. | `enigmatrix-backend/{app/api/v1/m1_gazette_extraction.py (NEW), app/api/v1/router.py, app/schemas/m1_pipeline.py, app/tasks/m1/gazette_scraper.py, scraper/spiders/gazette_spider.py, app/tests/unit/test_gazette_scraper_task.py, app/tests/integration/test_gazette_spider.py}`, `enigmatrix-frontend/{app/(admin)/admin/m1/pipeline/extraction/page.tsx (NEW), lib/api/m1-gazette-extraction.ts (NEW), components/layout/sidebar.tsx, lib/i18n/messages/{en,si,ta}.json}`, `c:\sme\{08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md, 02-Research-Modules/1 Module-1-Awareness-Gap/findings/2026-05-17-m1-admin-gazette-extraction.md (NEW), 02-Research-Modules/1 Module-1-Awareness-Gap/local-dev/phase2_admin_gazette_extraction.md (NEW)}` |
| 2026-05-17 | F-162 | **Session 38 — Celery eager-mode + pytest-asyncio session-loop fix for integration tests.** Test-side only — production task code unchanged because `asyncio.run()` is correct for a real Celery worker (no running loop). Long-latent bug: `pyproject.toml` configures `asyncio_default_test_loop_scope = "session"`, so when an `@pytest.mark.asyncio` test calls `task.delay(...).get()` synchronously and Celery eager-mode runs the task body inline in the test's loop, the task's own `asyncio.run()` crashes with `RuntimeError: asyncio.run() cannot be called from a running event loop`. 8 integration tests across `test_celery_extract_gazette.py` + `test_celery_preprocess_gazette.py` were silently broken. **Fix:** new `_run_eager_task(task, *args, timeout)` helper inside each test file: (a) `await session_mod.engine.dispose()` first — clears pooled asyncpg connections bound to the test's session loop, otherwise the task in its own thread inherits a foreign-loop connection and crashes with `Future attached to a different loop`; (b) `await asyncio.to_thread(lambda: task.delay(*args).get(timeout=timeout))` — pushes the synchronous eager invocation onto a worker thread that has no running loop, mirroring a real worker. Inside the task, `asyncio.run()` then creates a fresh loop, mints fresh asyncpg connections on it, does work, disposes the engine in its own `finally` (existing production behaviour unchanged), returns. After the task returns, the test's next `async with patched_session()` rebuilds the pool on the test loop. **Additional fix in `patched_session` (preprocess test only):** per-test `TRUNCATE TABLE m1_regulation_penalties, m1_sub_documents, m1_regulations RESTART IDENTITY CASCADE` for data isolation against the session-scoped testcontainer Postgres (previously: test 1 inserted gazette `2369/14` from the VAT-amendment fixture, tests 4-5 reused the same fixture → `ix_m1_regulations_gazette_number` UNIQUE collision when the pipeline tried to set the same gazette number on a different row). Replaced 9 `.delay().get()` callsites (7 in preprocess, 2 in extract) with the new helper. `uv run pytest app/tests/integration/test_celery_extract_gazette.py app/tests/integration/test_celery_preprocess_gazette.py -v` → 8 passed (was: 8 erroring / failing). | `enigmatrix-backend/app/tests/integration/{test_celery_extract_gazette.py, test_celery_preprocess_gazette.py}` |
| 2026-05-17 | F-160 | **Session 37 — M1 Phase 2 verification + live pipeline observability portal at `/admin/m1/pipeline/*`.** Two halves: (1) file-level audit confirms all 6 Phase 2 steps (2a-2f) + Session 34 cleanup are ✅ shipped — every named file exists, 5 Alembic migrations chain cleanly, Celery tasks registered + auto-chained, ORM relationships present, zero NotImplementedError/TODO/FIXME in critical paths; (2) new admin observability portal. **Backend (3 endpoints + service + schemas):** `app/services/m1_pipeline_service.py` with `get_status_counts` (GROUP BY status, zeros filled), `get_throughput(since)` (extracted_at + status='preprocessed' AND updated_at filters), `get_recent_runs` (selectinload eager-loads of penalties + sub_documents), `get_regulation_trace` (synthesizes 6-event pipeline timeline from row state — no `m1_regulation_events` table yet), `get_celery_health` (wraps `celery_app.control.inspect()` in 2 s asyncio.wait_for timeout with graceful "broker unreachable" fallback), `build_overview()` composer. Test stats hard-coded as `ML_TEST_STATS={passed:127, skipped:8}` + `BACKEND_TEST_STATS={passed:6, skipped:0}`. `app/schemas/m1_pipeline.py` with `PipelineOverviewOut`, `RegulationRunOut`, `RegulationTraceOut`, `RegulationRowOut`, `PenaltyOut`, `SubDocumentOut`, `PipelineTimelineEvent`, `StatusCounts`, `ThroughputBucket`, `CeleryHealthOut`. `app/api/v1/admin_m1_pipeline.py` with 3 `Depends(require_admin)` endpoints — `GET /api/v1/admin/m1/pipeline/{overview, recent?limit=, trace/{regulation_id}}` (404 on missing). Wired into `api/v1/router.py` under `/admin/m1/pipeline`. **Frontend (recharts ^2.13.0 added; 21 new files + 5 modified):** static metadata `lib/m1-pipeline/steps.ts` (typed `PipelineStep` interface + 6-entry array with id/title/feature/session/icon/color/statusBefore/statusAfter/counterStatus/inputs/outputs/doD/codeRefs/specDocs/testStats). Typed API client `lib/api/m1-pipeline.ts`. **14 components in `components/m1-pipeline/`:** `live-polling-indicator` (pill + `usePageVisible()` hook), `step-tile`, `recent-runs-table` (filter + status select + relative timestamps), `error-log-panel`, `celery-health-card`, `throughput-chart` (Recharts AreaChart, gradient fills, 7-point synthesized series), `status-distribution` (Recharts donut PieChart + legend), `funnel-chart` (hand-rolled CSS funnel with conversion %), `pipeline-flow-diagram` (centerpiece: 6 StepTiles connected by SVG gradient arrows, horizontal on xl/vertical on small), `step-detail-card` (hero + 4-col grid Inputs/Outputs/Tests/Status + 2-col code-refs/spec-docs), `trace-timeline` (vertical event rail with reached/pending/failed states), `trace-content-diff` (side-by-side raw vs cleaned with delta % + expand/collapse), `trace-penalties-table` (formatted LKR ranges, imprisonment, admin-vs-pipeline source), `trace-sub-documents-grid` (module-tinted cards per section_type). **5 routes under `app/(admin)/admin/m1/pipeline/`:** layout (sticky left-rail nav + force-dynamic), `page.tsx` overview (Phase 2 status banner + 4 metric tiles + flow diagram + 3 charts + recent runs + error log + Celery health, polls every 5 s pausing on tab hide), `recent/page.tsx` (full filterable table polling 50 rows), `steps/page.tsx` (index of all 6 step tiles), `steps/[stepId]/page.tsx` (per-step detail + filtered recent runs), `trace/page.tsx` (UUID search + recent table), `trace/[regulationId]/page.tsx` (header metadata + timeline + 4 tabs: Content / Penalties / Sub-documents / Raw row JSON). **Sidebar nav:** new `ADMIN_M1_PIPELINE_ITEMS` const (4 entries) rendered under a new "M1 Pipeline" uppercase header, always visible to admins; `GitMerge` icon added to import. **i18n:** 4 keys `nav.adminM1{Overview,Steps,Recent,Trace}` added to en/si/ta (English-only labels for now). **Architecture decisions:** TanStack Query `refetchInterval: 5000` + visibility-pause; closed allowlist of statuses; `updated_at` proxy for missing `preprocessed_at`; Celery degrades to "broker unreachable" via 2 s timeout; Recharts (~40 KB gzip, admin-only) over hand-rolled SVG; test counts as hard-coded constants. **Deferred:** dedicated `preprocessed_at` migration; retry-state surface from Celery autoretry; CI-emitted `test_stats.json`; backend integration tests for the 3 new endpoints. Manual smoke pending — user runs `pnpm install` + `pnpm dev` and visits `/admin/m1/pipeline` as admin. | `enigmatrix-backend/{app/schemas/m1_pipeline.py (NEW), app/services/m1_pipeline_service.py (NEW), app/api/v1/admin_m1_pipeline.py (NEW), app/api/v1/router.py}`, `enigmatrix-frontend/{lib/m1-pipeline/steps.ts (NEW), lib/api/m1-pipeline.ts (NEW), app/(admin)/admin/m1/pipeline/{layout, page, recent/page, steps/page, steps/[stepId]/page, trace/page, trace/[regulationId]/page}.tsx (7 NEW), components/m1-pipeline/{live-polling-indicator, step-tile, recent-runs-table, error-log-panel, celery-health-card, throughput-chart, status-distribution, funnel-chart, pipeline-flow-diagram, step-detail-card, trace-timeline, trace-content-diff, trace-penalties-table, trace-sub-documents-grid}.tsx (14 NEW), package.json (recharts), components/layout/sidebar.tsx, lib/i18n/messages/{en,si,ta}.json}`, `c:\sme\08-Findings-Log\{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-17 | F-159 | **Session 36 — Research-log portal: admin-only `/admin/research-log/*` Next.js routes surfacing the vault tracker triplet + per-module findings live.** Six surfaces wired as a new admin portal: SESSIONS.md (timeline + per-session detail with Done/Decisions/Risks/Files breakouts) + CHANGES.md (filterable data table with date/F-ID/full-text filters + per-row expand) + FEATURES.md (subsection-tabbed 5-column kanban by status + donut bar) + per-module findings folders (M1 today, M2/M3/M4 stub-ready, with module-tinted card grid + per-finding detail) + RESEARCH_BUILD_TRACKER.md (per-module stacked-bar progress + subgroup tables) + RESEARCH_IDEAS.md (RQ cards + remaining sections). **Live-read architecture** — every Server Component is `dynamic = 'force-dynamic'`; `lib/vault/reader.ts` enforces a closed `VAULT_FILES` allowlist + 30 s mtime-keyed LRU + `..`/absolute-path/symlink rejection; root is `process.env.VAULT_PATH ?? c:\sme`. **Six homegrown regex parsers** at `lib/vault/parsers/{sessions,changes,features,findings,build-tracker,ideas}.ts` mirror the existing `scripts/extract-m1-docs.mjs` convention (zero new npm deps; one shared `splitMarkdownRow()` handling code-span pipes + escaped `\|`). **Unified F-### cross-reference index** at `lib/vault/cross-ref.ts` built per request from sessions + changes + features + findings — `Map<FeatureId, { feature, sessions[], changes[], findings[] }>` powers the universal `<FeatureLink>` component (tooltip with live status + nav to `/features/[featureId]` detail with 3-column cross-ref view). **F-### chips show current status** (looked up from FEATURES.md every render), not the inline 🟢 at write-time — eliminates stale-emoji drift across SESSIONS entries. **11 React components** at `components/research-log/`: `feature-id-link.tsx` (with `inlineFeatureIds()` helper that auto-linkifies any text fragment), `session-card.tsx` + `session-timeline.tsx` (sticky-month grouping, dotted-rail dots, collapsible section breakouts), `changes-table.tsx` (full-text + F-ID + date filter, paginated 25/50/100/200), `feature-matrix.tsx` (kanban by status, group tabs split topic vs session and order session-newest-first), `finding-card.tsx` + `finding-detail.tsx` (module-tinted left border M1 blue / M2 green / M3 amber / M4 magenta), `cross-ref-panel.tsx`, `activity-heatmap.tsx` (GitHub-style 26-week × 7-day grid keyed off session dates; intensity = sum of done/decisions/risks bullets), `module-progress.tsx`, `refresh-button.tsx` (client `router.refresh()` with spinning icon + `useTransition`), `vault-status-pill.tsx` ("Live · 7s ago" pulse-dot pill with per-file mtime tooltip). **11 routes** under `app/(admin)/admin/research-log/`: landing (6 metric tiles + blocked-features banner + activity heatmap + latest 5 sessions), `sessions/page.tsx` + `[sessionId]`, `changes/page.tsx`, `features/page.tsx` + `[featureId]`, `findings/page.tsx` + `[module]/[slug]`, `build-tracker/page.tsx`, `ideas/page.tsx`. The `(admin)` parent layout already enforces `requireRole('admin')`; the research-log layout adds a `notFound()` gate when `NEXT_PUBLIC_VAULT_ENABLED !== 'true'` (default off — the portal is invisible in prod) plus a sticky-left rail nav between surfaces. **Sidebar nav** (`components/layout/sidebar.tsx`) gains a new "Research" section + single "Research log" entry under "ML Pipeline", visible only when `NEXT_PUBLIC_VAULT_ENABLED === 'true'` (Next.js dead-code-eliminates the branch in prod because `NEXT_PUBLIC_*` inlines at build). **i18n** — `nav.adminResearchLog` added to `lib/i18n/messages/{en,si,ta}.json` (English-only label for now; vault is internal-dev). **`.env.example` updated** with `NEXT_PUBLIC_VAULT_ENABLED=false` (opt-in) + `VAULT_PATH=c:\sme`. **Architecture decisions:** (a) live reads not build-time generation — vault edits hourly; (b) homegrown regex over `remark`/`unified` — zero new deps + matches `extract-m1-docs.mjs`; (c) closed allowlist reader not extending `readDocFile()` — different trust boundary; (d) dev-only flag — vault is local working set, prod = 404; (e) per-request cross-ref index — no persistence overhead; (f) F-### chips show live status not write-time emoji. **Deferred:** vault file watcher / SSE push (manual refresh button only); graph view for cross-refs (3-column textual panels today); prod snapshot mode; FEATURES.md heuristics tuning if subsection misclassification surfaces. **Manual smoke pending** — user runs `pnpm dev` with `NEXT_PUBLIC_VAULT_ENABLED=true` + `VAULT_PATH=c:\sme` in `.env.local` and visits `/admin/research-log` as admin. | `enigmatrix-frontend/lib/vault/{types,reader,status-map,cross-ref,index}.ts (5 NEW)`, `enigmatrix-frontend/lib/vault/parsers/{sessions,changes,features,findings,build-tracker,ideas}.ts (6 NEW)`, `enigmatrix-frontend/components/research-log/{feature-id-link,vault-status-pill,refresh-button,session-card,session-timeline,changes-table,feature-matrix,finding-card,finding-detail,cross-ref-panel,activity-heatmap,module-progress}.tsx (12 NEW)`, `enigmatrix-frontend/app/(admin)/admin/research-log/{layout,page,sessions/page,sessions/[sessionId]/page,changes/page,features/page,features/[featureId]/page,findings/page,findings/[module]/[slug]/page,build-tracker/page,ideas/page}.tsx (11 NEW)`, `enigmatrix-frontend/{components/layout/sidebar.tsx, lib/i18n/messages/{en,si,ta}.json, .env.example}`, `c:\sme\08-Findings-Log\{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-17 | F-158 | **Session 35 — Local-dev handbook + per-area + per-phase setup docs in the vault.** Doc-only lap creating a comprehensive local-dev guide for the user's Windows + WSL + PowerShell + Docker Desktop setup. **12 new markdown files** + 2 new folders (`04-Technology-Stack/ml/SETUP/` and `02-Research-Modules/1 Module-1-Awareness-Gap/local-dev/`). **Top-level handbook** (`04-Technology-Stack/00_LOCAL_DEV_HANDBOOK.md`, 10 sections) — tech-stack overview, one-time prerequisites (WSL2+Ubuntu 24.04, Docker Desktop, PowerShell 7.4+, SSH symlink, VS Code+WSL, apt deps, uv, Node 20 via volta), repo clone (Option A: WSL home `~/repos/xyz` + Windows symlink `mklink /D C:\Reasearch\xyz <wsl-path>` for graphify), day-zero bring-up (Docker → WSL `docker compose up -d postgres redis` → backend `uv run alembic upgrade head && uv run uvicorn app.main:app --reload` → PowerShell `pnpm dev` → verify localhost:3000), day-N workflow, per-area + per-phase index, standing cadence, filesystem map, troubleshooting, cross-references. **3 per-area platform docs**: `backend/SETUP/00_LOCAL_DEV_WSL.md` (13 sections: pre-flight + 8 env vars + Postgres+Redis bring-up + uv sync + alembic + seed_dev + uvicorn 0.0.0.0 + unit/integration tests + 3-terminal Celery + edit-verify loop + psql cheatsheet + troubleshooting); `frontend/SETUP/00_LOCAL_DEV_POWERSHELL.md` (10 sections: pre-flight + 3 env vars + `pnpm install` NOT `npm install` + `pnpm dev` + typecheck/lint/build + project layout + frontend↔backend wiring + verify-changes loop + troubleshooting port-3000/exec-policy); `ml/SETUP/00_LOCAL_DEV_WSL.md` (12 sections: uv workspace member of root pyproject + apt deps + uv sync + download_lid_model.py + xlm-roberta-base pre-warm + 127/8 pytest + per-module CLI smokes for pdf_classifier/ocr/language_detection/wijesekara/preprocess_gazette/segmenter + edit-verify loop + adding new module + HF cache mgmt + storage layout + troubleshooting fasttext-NumPy-2 pin). **1 phase index + 7 per-phase how-to-run docs** in `02-Research-Modules/1 Module-1-Awareness-Gap/local-dev/`: `00_INDEX.md` (7-row table linking Phase 2 steps → local-dev doc → feature ID → terminal type; recommended walkthrough; per-step quick-launch); `phase2_step2a_scrapy_spider.md` (F-145 — 4 integration tests + real-network smoke `CLOSESPIDER_PAGECOUNT=2` + psql verify + 5-row troubleshooting); `phase2_step2b_celery_extract.md` (F-148 — 3-terminal Redis+worker+REPL flow + asyncio.run snippet for first_ingested() + expected log lines + auto-chain to Step 2f); `phase2_step2c_extraction_chain.md` (F-149 — 12/2 ml tests + 6/6 backend regression through re-export adapter + threshold-calibration CLI + CER CLI + classify_pdf smoke + public surface check); `phase2_step2d_language_wijesekara.md` (F-153 — pre-stage lid.176.bin + 41/5 tests + lang detect CLI (en + si examples) + Wijesekara `"w"→අ` `"wd!"→ඈ` greedy-match + heuristic 50-char min + per-line router REPL + end-to-end per-page OCR fallback smoke (text-PDF all pages pymupdf, scanned-PDF flips to hybrid)); `phase2_step2e_preprocessing.md` (F-154 — XLM-R pre-warm + 72/2 tests + end-to-end orchestrator on VAT-amendment example → 2369/14 + 2024-01-01 + 'Value Added Tax Act, No. 14 of 2002' + amendment + full ml regression 127/8 + per-area edit-verify); `phase2_step2f_celery_wiring.md` (F-155 — alembic migration + round-trip + 4 integration tests + manual smoke `ingested → extracted → preprocessed` + SQL verify for cleaned_text + amendment_type + 4 admin-curated fields + ≥ 1 m1_regulation_penalties row + 7-row troubleshooting); `phase2_session34_cleanup.md` (F-157 — both Session 34 migrations + round-trip exercises enum-collapse rescue + 127/8 ml regression + 6 integration tests + segmenter CLI smoke for all 6 section types + admin-set preservation manual smoke + sequence_idx renumber verification + sub_documents persistence check). All docs use absolute paths consistently (`c:\Reasearch\xyz\enigmatrix-*` for code, `c:\sme\...` for vault, `~/repos/xyz/...` for WSL counterparts). Each phase doc ends with "After verifying" graphify-update cadence per `feedback_vault_sync_cadence.md`. **Decisions:** separate file per phase (user-confirmed) vs consolidated; `ml/SETUP/` created as new folder counterpart to existing `backend/SETUP/`/`frontend/SETUP/`/`infra/SETUP/`; recommended repo clone is WSL home + Windows symlink (10× faster Python I/O than `/mnt/c/`); cross-spelling preserved (`planned-for-development/` vault correct + `planned-for-devlopment/` code typo per Session 24 / F-146). No M1 finding entry per standing cadence (doc-only lap). **Deferred:** fresh-WSL-distro walkthrough exercise; markdown-link-checker run over the 12 new docs; `make local-dev-verify-phase-2` Makefile automation. | `c:\sme\04-Technology-Stack\{00_LOCAL_DEV_HANDBOOK.md (NEW), backend/SETUP/00_LOCAL_DEV_WSL.md (NEW), frontend/SETUP/00_LOCAL_DEV_POWERSHELL.md (NEW), ml/SETUP/00_LOCAL_DEV_WSL.md (NEW; ml/SETUP/ folder created)}`, `c:\sme\02-Research-Modules\1 Module-1-Awareness-Gap\local-dev\{00_INDEX, phase2_step2a_scrapy_spider, phase2_step2b_celery_extract, phase2_step2c_extraction_chain, phase2_step2d_language_wijesekara, phase2_step2e_preprocessing, phase2_step2f_celery_wiring, phase2_session34_cleanup}.md (8 NEW; local-dev/ folder created)`, `c:\sme\08-Findings-Log\{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-17 | F-157 | **Session 34 — M1 Phase 2 cleanup: segmenter promotion + penalty enum widening + is_admin_set + m1_sub_documents.** Closes all 5 carry-forwards flagged at end of Session 33. **(B) Segmenter standalone module** — new `enigmatrix-ml/m1/extraction/segmenter.py` with `NOTICE_BOUNDARY_RE` (moved from chunking.py), `detect_sections(text)` (moved), and new `detect_sections_with_labels(text) -> list[SectionInfo]` that classifies sections into `part`/`schedule`/`section`/`notice`/`numbered_clause`/`preamble`. `chunking.py` imports + re-exports both for backward compat. **Circular-import fix:** `SectionInfo` lives in `m1.extraction.types`, not `m1.preprocessing.types` (preprocessing.types re-exports for backward compat). **(C+D) Penalty enum widening + is_admin_set** — migration `202605250001_m1_penalties_enum_widen_and_admin_set.py` (down-rev `202605240001`) widens `ck_m1_regulation_penalties_type` from 3 → 7 values per doc 02 §2.8 (adds `license_revocation`/`business_closure`/`public_naming`/`asset_seizure`) AND adds `is_admin_set BOOLEAN NOT NULL DEFAULT FALSE` column + partial index `WHERE is_admin_set=TRUE`. Careful downgrade collapses extended-enum rows to `'fine'` before reapplying the narrower CHECK. ORM + schemas + ml types Literal all updated to match the 7-value enum. **(E) `m1_sub_documents` junction** — migration `202605260001_m1_sub_documents.py` (down-rev `202605250001`) creates table per doc 02 §2.10: sub_id UUID PK, regulation_id FK CASCADE, sequence_idx SMALLINT, section_label VARCHAR(200), section_type VARCHAR(50) CHECK over 6 values, char_offset_start/end INT, text TEXT, timestamps; UNIQUE(regulation_id, sequence_idx); INDEX on regulation_id. New M1SubDocument ORM + M1SubDocumentOut Pydantic schema + sub_documents relationship on M1Regulation (cascade="all, delete-orphan"). **`preprocess_gazette_task` extended** — penalty DELETE-then-INSERT filtered by `is_admin_set=FALSE`, pipeline rows re-numbered from `max(admin_sequence_idx)+1` to avoid UNIQUE collision; new DELETE-then-INSERT block writes `m1_sub_documents` from `pp.sections`. **`PreprocessedGazette.sections: list[SectionInfo]`** field added; orchestrator `preprocess_gazette()` populates via `detect_sections_with_labels(cleaned)`. **(A) Doc 02 §5 mermaid** gains Stage B+ Preprocessing sub-node showing the new flow; Classification arrow re-routed from `B6 → C1` to `BPLUS3 → C1`. **Doc 02 §2.8** "⚠️ Partial" header replaced with "✅ Shipped" (Sessions 32 + 34); Shipped-subset DDL updated for 7-value enum + is_admin_set + idempotency semantics paragraph. **Doc 02 new §2.10** documents the `m1_sub_documents` schema; existing §2.10 Indexing Strategy renumbered to §2.11. **`03_M1_2_Gazette_Segmentation.md`** (vault + code) flipped from `⚠️ Partial` to `✅ Shipped Session 34 / F-157`. **Tests:** new `tests/m1/extraction/test_segmenter.py` (14 tests: 5 pattern + 3 detect_sections + 6 detect_sections_with_labels); `test_chunking.py` loses 5 moved tests (net +9 in ml suite). 2 new integration tests in `test_celery_preprocess_gazette.py` (admin-set preservation + sub-documents persistence). **127/8 ml tests pass** (10 new in test_segmenter + all 117 Step 2c/2d/2e regressions still green). Backend runtime tests not run in env (no uv, no Docker); all 17 backend + ml files pass `python -m py_compile`. **Deferred follow-ups:** admin UI for penalty curation (BUILD_13); Stage E summariser to consume `m1_sub_documents` (Phase 4); boundary-detection F1 benchmark on 50-doc corpus. | `enigmatrix-ml/{m1/extraction/{segmenter (NEW),types,__init__}.py, m1/preprocessing/{chunking,types,__init__}.py, tests/m1/extraction/test_segmenter (NEW).py, tests/m1/preprocessing/test_chunking.py}`, `enigmatrix-backend/{alembic/versions/{202605250001_m1_penalties_enum_widen_and_admin_set (NEW), 202605260001_m1_sub_documents (NEW)}.py, app/models/{__init__,regulation,m1_regulation_penalty,m1_sub_document (NEW)}.py, app/schemas/{m1_regulation_penalty,m1_sub_document (NEW)}.py, app/tasks/m1/preprocess_gazette.py, app/tests/integration/test_celery_preprocess_gazette.py}`, vault + code: `{02_M1_Data_Requirements,03_M1_2_Gazette_Segmentation}.md`, `c:\sme\{08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md, 02-Research-Modules/1 Module-1-Awareness-Gap/findings/2026-05-17-m1-phase2-cleanup-segmenter-penalty-subdocs.md (NEW)}` |
| 2026-05-17 | F-156 | **Session 33 — M1 Phase 2 doc catch-up (plan docs + spec status flips + roadmap + doc 02 schema).** Doc-only lap closing every documentation gap from Sessions 23/25/26/28/30/31/32. **6 new plan docs** at `enigmatrix-docs/m1/planned-for-devlopment/`: `4.md`+`4_setup.md` (Step 2d retrospective spec — language detection + Wijesekara + per-page OCR fallback), `5.md`+`5_setup.md` (Step 2e — preprocessing chain), `6.md`+`6_setup.md` (Step 2f — Celery wiring + DB persistence). Each pair follows the Session-24 format (Context/Decisions/Files/Execution-order for `N.md`; Prerequisites/Run-suite/Manual-smoke/Rollback for `N_setup.md`). **3 vault setup mirrors** at `c:\sme\02-Research-Modules\1 Module-1-Awareness-Gap\planned-for-development\` (note: vault uses `planned-for-development/` with 'e' while code uses `planned-for-devlopment/` typo per Session-24 / F-146). **8 spec doc status markers flipped** from `🔲 Deferred (BUILD_07 — ...)` to `✅ Shipped Session XX / F-XXX (...)` (vault + code = 16 file edits): `03_M1_1_PDF_Extraction_Chain.md` → Session 28 / F-149; `03_M1_2_Gazette_Segmentation.md` → ⚠️ Partial Session 31 / F-154 (only `NOTICE_BOUNDARY_RE` inlined in chunking.py); `04_M1_1_Gazette_Noise_Removal.md` → Session 31 / F-154; `04_M1_2_Metadata_Extraction_Patterns.md` → Session 31 / F-154 (junction persistence Session 32 / F-155); `04_M1_3_Text_Chunking_Strategy.md` → Session 31 / F-154; `04_M1_Preprocessing_Pipeline.md` → new status line added pointing at Steps 2e + 2f; `10_M1_1_Language_Detection_Routing.md` → Session 30 / F-153; `10_M1_2_OCR_Wijesekara_Conversion.md` → Tesseract Step 2c + Wijesekara Step 2d combined. Edits used Edit tool + Python in-place byte-replace for the 5 vault files where Edit's CRLF/Unicode byte-tracking glitched. **`16_M1_Development_Roadmap.md`** (vault + code): "Where M1 stands today" table row updated to `✅ Shipped — Sessions 23/25/26/28/30/31/32 (F-145 → F-155). Phase 2 complete`; new **Step 2f** section inserted after Step 2e; Phase 2 DoD line updated from `status='extracted'` to `status='preprocessed'`; **Phase 2 complete as of 2026-05-17** marker added. **`02_M1_Data_Requirements.md`** (vault + code): §2.1 status enum row gains `'preprocessed'`; new `cleaned_text TEXT` + `amendment_type VARCHAR(20)` rows inserted with `Stage B+ (preprocessed)` source-stage tag; `penalty_range_lkr` description notes the legacy single-string vs §2.8 junction. §2.8 (`m1_regulation_penalties`) restructured into "Full vision (spec)" + "Shipped subset (Session 32 / F-155)" with the actual migration's DDL (3-value enum + `BIGINT` + `sequence_idx` + `context` + timestamps); "Differences vs the spec" bullet list documents the divergence (`violation_type`/`additional_consequences`/`legal_basis_section` not yet extracted; penalty_type enum subset of 3). **Deferred:** doc 02 §A1 mermaid architecture diagram still doesn't show a `Stage B+` sub-node — future doc-only follow-up. **No M1 finding entry created** — doc-only laps don't warrant one per the standing cadence. | `enigmatrix-docs/m1/planned-for-devlopment/{4,4_setup,5,5_setup,6,6_setup}.md (6 NEW)`, `c:\sme\02-Research-Modules\1 Module-1-Awareness-Gap/planned-for-development/{4_setup,5_setup,6_setup}.md (3 NEW)`, vault + code: `{03_M1_1_PDF_Extraction_Chain, 03_M1_2_Gazette_Segmentation, 04_M1_1_Gazette_Noise_Removal, 04_M1_2_Metadata_Extraction_Patterns, 04_M1_3_Text_Chunking_Strategy, 04_M1_Preprocessing_Pipeline, 10_M1_1_Language_Detection_Routing, 10_M1_2_OCR_Wijesekara_Conversion, 16_M1_Development_Roadmap, 02_M1_Data_Requirements}.md (20 edits)`, `c:\sme\08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-17 | F-155 | **Session 32 — M1 Phase 2 Step 2f: wire preprocessing into Celery pipeline + DB persistence.** Persists the Step 2e ml-package output (Session 31 / F-154) into the database via a new `preprocess_gazette_task` Celery task chained automatically after `extract_gazette` (Session 26 / F-148). **Alembic migration `202605240001`** (down-rev `202605230001`) adds `m1_regulations.cleaned_text TEXT` + `m1_regulations.amendment_type VARCHAR(20)` (CHECK enum `('amendment','repeal','new_act')`), extends `ck_m1_regulations_status` to include a new `preprocessed` value inserted between `extracted` and `classified` (deviates from canonical doc 02 §2.1's 6+1 enum — divergence logged, doc-02 amendment is a parallel doc-only task), and creates `m1_regulation_penalties` junction table (`penalty_id UUID PK DEFAULT gen_random_uuid()`, `regulation_id UUID FK ON DELETE CASCADE`, `sequence_idx SMALLINT`, `penalty_type VARCHAR(20)` CHECK over `('fine','imprisonment','both')`, `min_lkr BIGINT`, `max_lkr BIGINT`, `imprisonment_months INT`, `context TEXT`, `created_at`/`updated_at TIMESTAMPTZ` DEFAULT now(); UNIQUE `(regulation_id, sequence_idx)` for DELETE-then-INSERT idempotency; INDEX on `regulation_id`). Careful downgrade: any `status='preprocessed'` rows collapse to `'extracted'` before the old CHECK is re-applied; junction dropped first. **New ORM `M1RegulationPenalty`** at `app/models/m1_regulation_penalty.py` with `back_populates="penalties"` relationship to `M1Regulation`; registered in `app/models/__init__.py` for Alembic autogenerate visibility. **`M1Regulation` extended** with `cleaned_text` + `amendment_type` columns and `penalties: Mapped[list[...]]` relationship (`cascade="all, delete-orphan"`, `order_by="M1RegulationPenalty.sequence_idx"`). **`M1RegulationPenaltyOut` Pydantic schema** at `app/schemas/m1_regulation_penalty.py` (`from_attributes=True`; not exposed in any router this slice but defined for future admin UI). **New task `app/tasks/m1/preprocess_gazette.py`** — `bind=True, autoretry_for=(Exception,), retry_backoff=True, max_retries=3, acks_late=True` (matches Session-26 extract_gazette policy). Loads row by id, requires `status='extracted'` + non-empty raw_text. **Lazy imports `from m1.preprocessing import preprocess_gazette`** so backend boots without the ml workspace member in test envs. **Always overwrites** `cleaned_text` + `amendment_type`. **Admin-curated fields preserved** — `gazette_number` / `effective_date` / `penalty_range_lkr` / `principal_act_amended` only filled when currently NULL (admin curation is authoritative). **Penalties rebuilt** via DELETE-then-INSERT (`sequence_idx` preserves `extract_all_penalties()` order; handles list-shrink/grow/reorder uniformly without ON CONFLICT branching). Flips `status='preprocessed'` on success; `extraction_failed` + raise on exception (Celery retry kicks in). **`extract_gazette.py` chains** the new task via the Session-26 lazy-import + try/except `.delay(...)` pattern — failure to enqueue logs a warning and leaves the row at `extracted` so a later worker run picks it up. `celery_config.py include=[...]` extended; `app/tasks/m1/__init__.py` re-exports `preprocess_gazette_task`. **4 integration tests** at `test_celery_preprocess_gazette.py` (eager Celery + `patched_session` monkeypatch matching Session-26 pattern): DoD round-trip on spec's VAT-amendment input → status flip + 4 DoD fields + amendment_type='amendment' + 1+ junction rows with penalty_type='both'/imprisonment_months=6/max_lkr=1_000_000; skip-test for non-extracted status; admin-curated values survive; idempotent penalties (same count after re-run). **Backend runtime tests NOT run** in this environment (no `uv` on PATH, no backend venv, no Docker for testcontainer Postgres) — all 10 new/modified files pass `python -m py_compile` syntax checks; user verifies end-to-end via `cd enigmatrix-backend && uv sync && uv run alembic upgrade head && uv run pytest app/tests/integration/test_celery_preprocess_gazette.py -v`. | `enigmatrix-backend/{alembic/versions/202605240001_m1_preprocessing_columns_and_penalties.py (NEW), app/models/m1_regulation_penalty.py (NEW), app/models/__init__.py, app/models/regulation.py, app/schemas/m1_regulation_penalty.py (NEW), app/tasks/m1/preprocess_gazette.py (NEW), app/tasks/m1/__init__.py, app/tasks/m1/extract_gazette.py, app/celery_config.py, app/tests/integration/test_celery_preprocess_gazette.py (NEW)}`, `c:\sme\{08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md, 02-Research-Modules/1 Module-1-Awareness-Gap/findings/2026-05-17-m1-step2f-preprocess-wiring.md (NEW)}` |
| 2026-05-17 | F-154 | **Session 31 — M1 Phase 2 Step 2e: preprocessing chain (cleaning + metadata + chunking).** Executed the user-specified Step 2e brief covering [04_M1_Preprocessing_Pipeline.md] + 3 companions (04_M1_1 noise, 04_M1_2 metadata, 04_M1_3 chunking). New package `enigmatrix-ml/m1/preprocessing/` with 5 modules. **`cleaning.py`** ships the 8-step `NOISE_PIPELINE` (unicode_normalize_nfkd → dehyphenate_line_breaks → strip_gazette_header → strip_page_numbers → strip_horizontal_rules → strip_signature_blocks → strip_repeated_blank_lines → collapse_inner_whitespace) plus two public entry points: `clean_gazette_text()` keeps signature (citation-faithful, for DB `raw_text`); `clean_for_classification()` strips it (for classifier input). **`metadata_extractor.py`** has 4 anchored regex patterns + `extract_all_penalties` using `finditer` to surface every penalty + alternative merger (fine + imprisonment within 30 chars connected by "or"/"either" → `penalty_type='both'`) + `classify_amendment_type` (`repeal` > `amendment` > `new_act`; verb regexes accept all forms: `repeal(s|ed|ing)`, `amend(s|ed|ing|ment)`) + `derive_penalty_range_lkr` for the legacy single-string column + sanity-bounded effective_date (-1y to +5y around published_date). **`chunking.py`** ships `NOTICE_BOUNDARY_RE` (inline; matches PART/SECTION/Schedule/Notice/numbered-clause boundaries — moves to shared module when 03_M1_2 segmentation ships), `detect_sections` returning `(start, end)` tuples, `chunk_hybrid` orchestrating section split → micro-section merger (< 100 tokens absorbed) → per-section sliding window (MAX_LEN=512, STRIDE=64) → trailing chunk dropper (< 50 tokens, padding-bias guard), `classification_input`/`summarise_input`. Tokenizer lazy-loaded via `@lru_cache` (auto-downloads xlm-roberta-base ~1.1 GB to HF cache); `is_tokenizer_cached()` lets tests gate cleanly. **Orchestrator `preprocess_gazette()` in `__init__.py`** glues: clean → detect_document_language → (mixed: route_lines_by_language to extract 'en' bucket) → extract_metadata on RAW text (so gazette header strip doesn't remove "No. 2486/22") → chunk_hybrid → assemble `PreprocessedGazette`. **Step 2d's `language_detection` module reused wholesale** — no duplication of `route_lines_by_language`/`line_language`/`detect_document_language`. **71 new tests + 41 Step-2c/2d regression = 117 passed / 8 skipped** (DoD-corpus and tokenizer gates). DoD round-trip on the spec's VAT-amendment input passes: cleaned_text + gazette_number "2369/14" + effective_date 2024-01-01 + penalty_range_lkr "LKR 1,000,000" + principal_act_amended "Value Added Tax Act, No. 14 of 2002" + amendment_type 'amendment'. **pyproject.toml** gains `transformers>=4.40,<5` + `dateparser>=1.2,<2` (numpy<2 pin from Step 2d still applies). **Decisions:** metadata on raw text (cleaning strips the gazette header which contains the number); signature stripping is two pipelines not a flag; NFKD codepoint-count invariant is `after >= before` not equality (Tamil U+0BCA decomposes to U+0BC6 + U+0BBE). **Deferred:** `m1_regulation_penalties` junction table + `amendment_type` column migration → Step 2f (Celery wiring). 50-doc cleaning corpus + 100-doc metadata gold + 100-doc per-field PR DoDs gated on hand-labelled corpora. | `enigmatrix-ml/{pyproject.toml, m1/preprocessing/{__init__,types,cleaning,metadata_extractor,chunking}.py (5 NEW), tests/m1/preprocessing/{__init__,test_cleaning,test_metadata_extractor,test_chunking,test_pipeline}.py (5 NEW)}`, `c:\sme\{08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md, 02-Research-Modules/1 Module-1-Awareness-Gap/findings/2026-05-17-m1-step2e-preprocessing-pipeline.md (NEW)}` |
| 2026-05-17 | F-153 | **Session 30 — M1 Phase 2 Step 2d: language detection + Wijesekara conversion + per-page OCR fallback.** Executed the explicit Session-28 deferral. **fastText document detection** — new `enigmatrix-ml/m1/extraction/language_detection.py` ships `detect_document_language(text, *, min_confidence=0.70, window_chars=500) -> LanguageDetection` (lazy-loaded fasttext-wheel + lid.176.bin, top-3 with confidence, primary ∈ {en, si, ta, mixed}). Env-tunable via `M1_LID_WINDOW_CHARS` / `M1_LID_MIN_CONFIDENCE` / `M1_LID_MODEL_PATH`. **lid.176.bin downloaded** (125.2 MB) to `enigmatrix-ml/storage/models/m1/baseline/lid.176.bin` via new idempotent `scripts/download_lid_model.py` (size-verified; skip if present). **Per-line Unicode-range router** in the same module (`is_sinhala_char` / `is_tamil_char` / `is_latin_char` / `line_language` / `route_lines_by_language` / `primary_language_by_line_count`) per doc-04 §3.2. **Wijesekara conversion** at `m1/extraction/wijesekara.py` + 87-entry YAML map (`wijesekara_map.yaml` — independent vowels, all 5 consonant series, vowel signs, special marks, high-frequency conjunct shorthands). Heuristic `is_wijesekara_encoded(text)` applies the 0.40 indicator-char ratio per spec §3 (min 50 ASCII-alpha chars; short texts return False). `convert_wijesekara(text)` greedy longest-match (4→3→2→1); unmapped chars fall through verbatim. Map cached via `@lru_cache`. **`ocr.wijesekara_to_unicode` stub replaced** by a thin delegate to `convert_wijesekara` — backward-compatible. **Per-page OCR fallback wired** in `text_extractors.extract_with_chain` — new signature `(pdf_path, *, enable_ocr_fallback=True, ocr_dpi=300, ocr_timeout=60)`: low-yield PyMuPDF pages (< 100 chars) get rasterised via `pdf2image.convert_from_path(first_page=N, last_page=N)`, language-routed via fastText (en→eng, si→eng+sin, ta→eng+tam, mixed→eng+sin+tam, fallback if model unavailable), Tesseract'd, Wijesekara-converted if heuristic detects, spliced back as `PageResult(method='tesseract')`. Document-level method becomes `'hybrid'` if any page was OCR'd. **`__init__.py`** re-exports the new public surface alongside Step 2c. **pyproject.toml** gains `fasttext-wheel>=0.9.2,<1`, `numpy<2.0` (fasttext-wheel 0.9.2 hits a numpy-2.x `np.array(probs, copy=False)` ValueError; pin until upstream catches up), `PyYAML>=6,<7`. **41/41 extraction tests pass** — 17 new in `test_language_detection.py`, 14 new in `test_wijesekara.py`, `test_ocr.py::test_wijesekara_stub_raises` replaced by `test_wijesekara_round_trip_through_re_export`, `test_text_extractors.py::test_per_page_hybrid_routing` split into `_no_fallback` + `_invokes_tesseract` (monkeypatches `pdf2image.convert_from_path` + `m1.extraction.ocr._ocr_one_page` to verify wiring without the Tesseract binary). 5 intentional skips (DoD harnesses gated on hand-labelled corpora + Tesseract-binary-gated smoke). **Deferred:** 100-doc hand-labelled LID set + 100 pre-2010 Wijesekara docs are research deliverables; apparatus ships, data follows. `storage/models/m1/baseline/lid.176.bin` must be `.gitignore`'d before any commit. | `enigmatrix-ml/{pyproject.toml, scripts/download_lid_model.py (NEW), m1/extraction/{__init__,ocr,text_extractors}.py, m1/extraction/{language_detection,wijesekara}.py (NEW), m1/extraction/wijesekara_map.yaml (NEW), tests/m1/extraction/{test_ocr,test_text_extractors}.py, tests/m1/extraction/{test_language_detection,test_wijesekara}.py (NEW)}`, `enigmatrix-ml/storage/models/m1/baseline/lid.176.bin (ARTIFACT, 125.2 MB)`, `c:\sme\{08-Findings-Log/{SESSIONS,CHANGES,FEATURES}.md, 02-Research-Modules/1 Module-1-Awareness-Gap/findings/2026-05-17-m1-step2d-lang-detect-wijesekara.md (NEW)}` |
| 2026-05-17 | F-150 | **Session 29 — Auth split-panel UI.** New `enigmatrix-frontend/components/ui/travel-connect-signin.tsx` ports the travel-connect component to Enigmatrix: DotMap canvas (Sri Lankan world-map dots + animated route lines, theme-aware via `getComputedStyle(document.documentElement).getPropertyValue("--primary"/"--foreground")`) on the left, framer-motion fade-in form on the right. `cn` → `@/lib/utils`; custom `Button`/`Input` → `@/components/ui/{button,input}`. Exports `DotMap` (reused on login + register), `AuthSignInCard` (demo), default `Index`. `app/(auth)/layout.tsx` stripped to `min-h-screen` pass-through (the old header is gone; branding lives on the card's left panel). `app/(auth)/login/page.tsx` rewritten as a split-panel `motion.div` preserving all logic (`useForm(loginSchema)`, `AuthApi.login()`, `/api/auth/establish`, role-based redirect, `useTranslations()`); `isPasswordVisible` state drives Eye/EyeOff; inputs `className="h-8 bg-background/80 focus-visible:ring-primary/50"`. `app/(auth)/register/page.tsx` matches with a scrollable right panel for the 9+ fields and `SelectTrigger` `h-8`. Theme-token migration (`auth-card`, `auth-visual-panel`, `brand-gradient`, `auth-gradient-text`, `bg-background/80`, `text-foreground`) so light theme still works. | `enigmatrix-frontend/{components/ui/travel-connect-signin.tsx (NEW), app/(auth)/{layout,login/page,register/page}.tsx}`, `c:\sme\08-Findings-Log\{SESSIONS,FEATURES,CHANGES}.md` |
| 2026-05-17 | F-151 | **Session 29 — M1 docs portal data consolidation.** Two parallel M1 portal data sources collapsed into one. Appended 11 portal-derived constants to `enigmatrix-frontend/lib/m1-docs.ts` under a clearly delimited banner: `M1_META`, `M1_PIPELINE_STAGES` (7 stages A–G), `M1_RESEARCH_QUESTIONS` (4 RQs), `M1_TECHNOLOGY_CHOICES` (8), `M1_DB_ENTITIES` (11), `M1_DIFFUSION_TIMELINE` (T0–T9), `M1_RESEARCH_FINDINGS` (F1–F6), `M1_ARCHITECTURE_LAYERS` (6), `M1_HAPPY_PATH` (9 rows), `M1_KEY_STATS` (6), `M1_INTER_MODULE_CONNECTIONS` (M1→M2/M3/M4). Dropped `M1_SECTIONS` (replaced by `M1_NON_TRACKING_SECTIONS` already in use by `m1-section-grid.tsx`). Updated 4 import sites: `components/docs/m1/{m1-pipeline,m1-timeline,m1-stats-counter}.tsx`, `app/(app)/docs/m1/page.tsx` (merged into the existing `from "@/lib/m1-docs"` import). **Deleted** `enigmatrix-frontend/lib/m1-docs-data.ts`. Verified via grep: no remaining `m1-docs-data` references. Decision: portal constants stay co-located as typed constants — they're cross-document syntheses (pipeline, T0–T9, F1–F6, RQs+methods, inter-module connections) that don't map 1:1 to any per-section `metrics`/`tables`, so auto-deriving from the generated JSON would require fragile markdown-table parsers. | `enigmatrix-frontend/{lib/m1-docs.ts, components/docs/m1/{m1-pipeline,m1-timeline,m1-stats-counter}.tsx, app/(app)/docs/m1/page.tsx, lib/m1-docs-data.ts (DELETED)}`, `c:\sme\08-Findings-Log\{SESSIONS,FEATURES,CHANGES}.md` |
| 2026-05-17 | F-152 | **Session 29 — Survey config: unified module + multi-sector (frontend only — 🟡).** `enigmatrix-frontend/components/forms/survey-config-form.tsx`: `MODULE_OPTIONS` grows from 4 to 5 entries — added `{ value: "unified", label: "Unified — All Modules" }`. zod schema becomes `module_number: z.union([z.literal("unified"), z.literal("1"), z.literal("2"), z.literal("3"), z.literal("4")])` with submit-time `"unified" → null` map. Single `sector_code` Combobox replaced by `sector_codes: z.array(z.string())` rendered as 12 multi-select checkboxes from `lib/constants/sectors.ts`; empty array means "all sectors universally" (hint string added). **Backend deferred** to a separate session — `app/models/survey_session.py` `sector_code` column, `admin_survey.py` nullable `module_number` + `sector_codes` JSONB/junction, `app/schemas/survey_session.py` + `app/services/survey_session_service.py` threading, new migration. Until the backend lands, posting from the admin form will 422 on `sector_codes` — tracked 🟡, NOT 🟢. | `enigmatrix-frontend/components/forms/survey-config-form.tsx`, `c:\sme\08-Findings-Log\{SESSIONS,FEATURES,CHANGES}.md` |
| 2026-05-15 | F-149 | **Session 28 — M1 Phase 2 Step 2c: canonical `ml/m1/extraction/` chain.** Executed [3.md](../m1/planned-for-devlopment/3.md). Moved the Session-26 PDF-extraction code to its canonical home at `enigmatrix-ml/m1/extraction/` (per doc 13 + [15_M1_1_ML_Folder_Guide.md](../m1/15_M1_1_ML_Folder_Guide.md)) and added the research-grade rigour Session 26 deferred. **uv workspace** — new root `pyproject.toml` declares `[tool.uv.workspace] members=["enigmatrix-backend","enigmatrix-ml"]`; backend gains `"enigmatrix-ml"` dep + `[tool.uv.sources] enigmatrix-ml = { workspace = true }`; `uv sync` at the root resolves both. **`enigmatrix-ml/` is now a real Python package** with its own `pyproject.toml` (hatchling, package `m1`, 4 PDF deps). **Canonical modules:** `m1/extraction/types.py` (`ExtractedText` + `PageResult` dataclasses); `m1/extraction/pdf_classifier.py` (returns `Literal["text_pdf","hybrid","scanned"]` instead of Session-26's dict; env-tunable thresholds via `os.environ`; `_threshold_calibration(audit_dir)` walks the 5 candidate pairs from §2 + `--calibrate <dir>` CLI emits confusion matrix + objective `min(text_pdf_recall, scanned_precision)`); `m1/extraction/text_extractors.py` (PyMuPDF with `TEXTFLAGS_TEXT` for Sinhala/Tamil ligature preservation; pdfplumber with `layout=True` + table extraction; `extract_pymupdf_per_page` + `extract_with_chain` returning per-page `PageResult[]`); `m1/extraction/ocr.py` (Tesseract per §5: `--oem 1 --psm 6 --lang eng+sin+tam`, `dpi=300`, per-page 60s timeout via `concurrent.futures.ThreadPoolExecutor`; `character_error_rate(pred, gold)` via Levenshtein; `wijesekara_to_unicode()` stub raising `NotImplementedError` → Step 2d; `--measure-cer` CLI). **Backend `app.extraction` is now a thin adapter** (-90 LOC) — `__init__.py` re-exports 11 names; `pdf_classifier.py` + `text_extractors.py` are 3-line re-export shims so Session-26 submodule imports still resolve. **API signature change:** `classify_pdf` returns the literal label instead of `{"type":..., "method":...}` — `extract_gazette.py` now maps PDF type → `(method, extractor)` via a new `_EXTRACTORS` dict; `test_pdf_classifier.py` updated. **Dockerfile** apt installs `tesseract-ocr tesseract-ocr-sin tesseract-ocr-tam poppler-utils`; **Makefile** gets `test-extraction` target; **requirements.txt** pins the 4 PDF deps. **3 new ml tests** (12 passed, 2 intentional skips for the 50-doc audit + OCR-gold corpus DoDs). **Backend regression suite green** (6/6 pass through the adapter). **Audit fixture directory** `enigmatrix-ml/tests/m1/fixtures/audit/` shipped with a README pointing at the populate procedure in `3_setup.md §6`. **Deferred to Step 2d:** Wijesekara conversion table, `language_detection.py` (fastText), per-page OCR fallback wiring, 50-doc + gold corpus datasets. | `pyproject.toml` (root, NEW), `enigmatrix-ml/{pyproject.toml, m1/__init__.py, m1/extraction/{__init__,types,pdf_classifier,text_extractors,ocr}.py, tests/{__init__,m1/__init__,m1/extraction/__init__}.py, tests/m1/extraction/{test_pdf_classifier,test_text_extractors,test_ocr}.py, tests/m1/fixtures/{sample_gazette_2486_22.pdf,audit/README.md}, Dockerfile, Makefile, requirements.txt}`, `enigmatrix-backend/{pyproject.toml, app/extraction/{__init__,pdf_classifier,text_extractors}.py, app/tasks/m1/extract_gazette.py, app/tests/unit/test_pdf_classifier.py}`, `AI_WORK_LOG.md`, `enigmatrix-docs/tracker/{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-15 | F-148 | **Session 26 — M1 Phase 2 Step 2b: Celery + Stage-B PDF extraction.** Executed the approved [2.md](../m1/planned-for-devlopment/2.md) plan from Session 25. **5 new deps** added to `enigmatrix-backend/pyproject.toml`: `celery[redis]>=5.3,<6`, `PyMuPDF>=1.24,<2`, `pdfplumber>=0.11,<1`, `pytesseract>=0.3.13,<1`, `pdf2image>=1.17,<2`. **Env vars** `CELERY_BROKER_URL=redis://localhost:6379/0` + `M1_PDF_TEXT_THRESHOLD=200` + `M1_PDF_SCANNED_THRESHOLD=30` added to `.env.example` and matching Pydantic fields on `app/settings.py`. **Alembic migration `202605230001_m1_regulations_extraction_columns.py`** (down-rev `202605220001`) adds `raw_text TEXT`, `extraction_method VARCHAR(20)` (CHECK enum `pymupdf`|`pdfplumber`|`tesseract`), `extracted_at TIMESTAMPTZ` to `m1_regulations`; ORM model gets matching `Mapped[]` columns. **New `app/extraction/` module** — `pdf_classifier.py` (samples first 3 pages with PyMuPDF, routes by mean chars/page against the two thresholds) + `text_extractors.py` (three stage extractors `extract_pymupdf`/`extract_pdfplumber`/`extract_tesseract`, page-separated by `\f`). **Celery infrastructure** — `app/celery_config.py` (Celery app `enigmatrix-m1`, Redis broker + result backend, JSON serialiser, `task_acks_late=True`, `worker_prefetch_multiplier=1`, Beat schedule `0 */6 * * *` for `run_gazette_spider`); `app/tasks/__init__.py` + `app/tasks/m1/__init__.py` re-exporting the two tasks. **Two tasks** — `app/tasks/m1/gazette_scraper.py` runs `scrapy crawl gazette_spider` as a subprocess from the project root (keeps Twisted reactor out of Celery's pool); `app/tasks/m1/extract_gazette.py` is the Stage-B core: loads the row, requires `status='ingested'` + `raw_pdf_path` set, classifies the PDF, runs the chosen extractor, writes `raw_text`/`extraction_method`/`extracted_at`, flips status to `extracted` (on any failure → `extraction_failed` + raise → autoretry max 3, exponential backoff). **Scraper pipeline wired** — `scraper/pipelines.py` replaces the Session-23 `TODO` log with `extract_gazette.delay(str(row.regulation_id))`; lazy import + try/except so the spider keeps working in dev without a live broker (row stays `ingested`; idempotent re-run picks it up later). **4 tests** — `test_pdf_classifier.py` (shape + determinism, skipped if fixture missing), `test_text_extractors.py` (PyMuPDF + pdfplumber return strings; Tesseract skipped), `test_gazette_scraper_task.py` (mocks `subprocess.run`, asserts task wiring + non-zero exit raises), `test_celery_extract_gazette.py` (eager Celery; seeds `ingested` row pointing at the fixture PDF copied to tmp `STORAGE_LOCAL_PATH`; asserts task returns `{"status":"extracted","method":...}` + DB row reflects it; plus a non-`ingested` skip test). **DoD met:** the row flips `ingested → extracted` with cleaned text written, per [16_M1_Development_Roadmap.md Phase 2 Step 2b](../m1/16_M1_Development_Roadmap.md). **Deferred to Step 2c:** language detection + per-line routing, Wijesekara → Unicode conversion, production Beat hosting on Fly.io. | `enigmatrix-backend/{pyproject.toml, .env.example, app/settings.py, app/models/regulation.py, app/celery_config.py, app/extraction/{__init__,pdf_classifier,text_extractors}.py, app/tasks/{__init__,m1/__init__,m1/gazette_scraper,m1/extract_gazette}.py, alembic/versions/202605230001_m1_regulations_extraction_columns.py, scraper/pipelines.py, app/tests/unit/{test_pdf_classifier,test_text_extractors,test_gazette_scraper_task}.py, app/tests/integration/test_celery_extract_gazette.py}`, `AI_WORK_LOG.md`, `enigmatrix-docs/tracker/{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-15 | F-147 | **Session 25 — Step 2a parse-bug fix + Step 2b planning docs.** User ran Session-23's deliverables and surfaced 2 issues: (a) `test_spider_parse_yields_gazette_item` FAILED (`AssertionError: expected 1 item, got 0`) and the real-network `scrapy crawl` against `documents.gov.lk/view/egz/egz_2026.html` also scraped 0 items; (b) 2 other tests errored on `docker.errors.DockerException` (environmental — Docker daemon not running). **Bug fix:** spider's xpath `../*//text()` only walked the anchor's parent `<td>` subtree, missing the sibling `<td>` cells in the same `<tr>` where the gazette number lives. Swapped to `ancestor::tr//text()` with a fallback `ancestor::*[self::li or self::p or self::div][1]//text()` for non-table listings; ~10-line edit to `enigmatrix-backend/scraper/spiders/gazette_spider.py` including the explanatory comment block. **Step 2b planning:** added paired `2.md` + `2_setup.md` to `enigmatrix-docs/m1/planned-for-devlopment/`. `2.md` is the full Step 2b plan — Celery infrastructure (Redis broker + `celery_config.py` + `tasks/__init__.py` + `tasks/m1/__init__.py`), 2 Celery tasks (`gazette_scraper` wrapping the spider + `extract_gazette` running PyMuPDF/pdfplumber/Tesseract chain), Alembic migration `202605230001` adding `raw_text`/`extraction_method`/`extracted_at` columns, 5 new deps (`celery[redis]`, `PyMuPDF`, `pdfplumber`, `pytesseract`, `pdf2image`), spider pipeline updated to enqueue `extract_gazette.delay(...)` replacing the TODO log, 4 new tests. `2_setup.md` is the forward-looking setup + verification guide — prerequisites (Redis + Tesseract sin+tam packs + poppler + Docker), 3-terminal worker/Beat startup, automated verification via `task_always_eager=True`, manual smoke check (trigger spider as Celery task; watch DB rows flip `ingested → extracted`), rollback, 7-row troubleshooting table, what's deferred to Step 2c. **Step 2b execution stays a separate turn** per the Session-24 procedural rule. Scrapy deprecation warnings (`process_item() requires a spider argument`) noted as a Step 2b cleanup item. | `enigmatrix-backend/scraper/spiders/gazette_spider.py`, `enigmatrix-docs/m1/planned-for-devlopment/{2,2_setup}.md`, `AI_WORK_LOG.md`, `enigmatrix-docs/tracker/{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-15 | F-146 | **Session 24 — `planned-for-devlopment/` folder convention + Step 2a setup doc + Vercel URLs noted.** Doc-only follow-up to Session 23. Two things landed: (1) live deployment URLs surfaced — backend at <https://enigmatrix-backend.vercel.app/> (health check `GET /health` → `{"status":"ok","service":"enigmatrix-api"}`) and frontend at <https://enigmatrix-frontend.vercel.app/>; (2) new procedural rule + folder. Between turns the user created `enigmatrix-docs/m1/planned-for-devlopment/` (deliberate spelling — kept verbatim) and placed `1.md` (the Session 23 Step-2a plan) into it. From this turn on, **every plan lands here as `<N>.md` + paired `<N>_setup.md`**. This session added the paired `1_setup.md` — a user-actionable setup + verification guide for Session 23's Scrapy gazette spider. 10 sections: Deployment context (the Vercel URLs + the "Vercel can't run Scrapy/Celery — workers run locally or on Fly.io" caveat) → Prerequisites → Local setup (`cd enigmatrix-backend && uv sync` → copy `.env.example` → `uv run alembic upgrade head`) → Automated verification (`uv run pytest app/tests/integration/test_gazette_spider.py -v` → 4 tests pass) → Manual smoke check (real network: `uv run scrapy crawl gazette_spider` against `documents.gov.lk/view/egz/egz_2026.html`; expected 1–5 new rows in `m1_regulations` with `status='ingested'` + matching PDFs in `storage/m1/raw/`) → Rollback (`uv run alembic downgrade -1` + `rm -rf storage/m1/raw/`) → What's deferred to Step 2b (Celery dispatch, PDF extraction, language detection, worker hosting) → Cross-references. Folder name `planned-for-devlopment/` kept verbatim with the user's typo — rename is a one-line `git mv` + sed pass when wanted. `1.md` not edited — treated as the user's append-only record of the Session 23 plan. No code drift: only docs touched. Next paired files will be `2.md` + `2_setup.md` when Step 2b's plan lands. | `enigmatrix-docs/m1/planned-for-devlopment/1_setup.md`, `AI_WORK_LOG.md`, `enigmatrix-docs/tracker/{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-15 | F-145 | **Session 23 — M1 Phase 2 Step 2a: Scrapy gazette spider MVP.** First concrete code-shipping step from [16_M1_Development_Roadmap.md](../m1/16_M1_Development_Roadmap.md). Built a Scrapy spider that downloads gazette PDFs from `documents.gov.lk`'s extraordinary-gazette listing and writes one `m1_regulations` row per gazette with `status='ingested'`. Celery dispatch of the downstream `extract_gazette` task is **deferred to Step 2b** — the pipeline logs a `TODO: enqueue extract_gazette(<reg_id>)` line where it will go. **Migration `202605220001`** adds three columns to `m1_regulations`: `status VARCHAR(20) NOT NULL DEFAULT 'ingested'` with CHECK constraint over the 7 pipeline-state enum values per [02_M1_Data_Requirements.md §2.1](../m1/02_M1_Data_Requirements.md); `raw_pdf_path VARCHAR(500)` nullable; `gazette_number VARCHAR(50)` UNIQUE nullable. Two indexes: UNIQUE on `gazette_number`; partial `ix_m1_regulations_status_active` excluding `archived`. ORM model `app/models/regulation.py` gains the matching `Mapped[]` columns. **Scrapy project skeleton** at `enigmatrix-backend/scraper/`: `scrapy.cfg`, `__init__.py`, `items.py` (`GazetteItem`), `settings.py` (DOWNLOAD_DELAY=2 + RETRY on 500/503/429 + autothrottle + User-Agent per [03_M1_Data_Collection.md §1.3](../m1/03_M1_Data_Collection.md)), `spiders/__init__.py`, `spiders/gazette_spider.py` (parses anchors with `.pdf` href + gazette-number regex `\d{4}/\d{1,3}` in surrounding text), `pipelines.py` (two pipelines: `PDFDownloadPipeline` writes to `STORAGE_LOCAL_PATH/m1/raw/<slug>.pdf` + SHA-256, idempotent skip if file exists; `M1RegulationsInsertPipeline` INSERTs the row with `status='ingested'`, raises `DropItem` on UNIQUE conflict so re-runs don't duplicate). Dependency `scrapy>=2.11,<3` added to `pyproject.toml`; **user runs `cd enigmatrix-backend && uv sync` before testing**. **4 integration tests** in `app/tests/integration/test_gazette_spider.py` covering parse output, PDF download (mocked `engine.download`), DB insert (real testcontainer Postgres via `initialised_engine`), duplicate idempotency. **Avoids `CrawlerProcess`** in tests (Twisted-reactor singleton; can't be re-launched in pytest) — instead composes spider's `parse()` output through the pipelines directly; same logical path; reproducible in CI. Real-network smoke check (real `scrapy crawl` against `documents.gov.lk`) documented in the Session 23 SESSIONS entry. **Build root = `enigmatrix-backend/scraper/`** (filesystem truth) rather than the doc-canonical root-level `scraper/` — reconciling the `enigmatrix-*` split with doc 13's `backend/` canon is a separate task. | `enigmatrix-backend/{scrapy.cfg, scraper/__init__.py, scraper/items.py, scraper/settings.py, scraper/pipelines.py, scraper/spiders/__init__.py, scraper/spiders/gazette_spider.py, alembic/versions/202605220001_m1_regulations_status_columns.py, app/models/regulation.py, app/tests/integration/test_gazette_spider.py, app/tests/fixtures/gazette_listing.html, app/tests/fixtures/sample_gazette_2486_22.pdf, pyproject.toml}`, `enigmatrix-docs/tracker/{SESSIONS,CHANGES,FEATURES}.md`, `AI_WORK_LOG.md` |
| 2026-05-15 | F-144 | **Session 22 — Backend seed refactor: demo dashboards populated on `make seed`.** Two new seed scripts close the gap where admin dashboards (`/admin/m2/scores`, `/admin/m3/risk-signals`, `/admin/surveys/awareness/responses`) rendered empty after a fresh seed. (1) `backend/app/scripts/seed_lookups.py` (new) — extracts `regulatory_domains` (9 rows) + `sectors` (12 rows) seeding into its own idempotent script (`on_conflict_do_nothing`); previously these lived inside `seed_m23_questions.main()` which ran *after* `seed_regulations`, so the FK targets weren't guaranteed to exist when consumers ran. (2) `backend/app/scripts/seed_demo_responses.py` (new) — creates 6 demo SMEs across sectors (`demo.{retail,manufacturing,it,tourism,construction,foodbev}@enigmatrix.lk` / `demo12345678`), walks each through M1 awareness (12 q) + M2 knowledge (44 q) + M3 vulnerability (one compliance + one behavioural snapshot). M2 answers scored via `m2_scoring.score_answer`; aggregate rolled up via `m2_service.recompute_knowledge_score`. Per-SME `skill ∈ [0.35, 0.85]` + `compliance_profile ∈ {strong, average, weak}` produce a plausible 34–86 % M2 spread with correlated compliance flags. Idempotent: `_has_responses` short-circuits any SME with existing `survey_responses` rows; deterministic via `random.Random(hash(email))`. (3) `seed_dev.py` modified to run `seed_lookups` before `seed_regulations` and `seed_demo_responses` last. After `make seed`: 336 `survey_responses`, 6 `m2_knowledge_scores`, 6 `m3_compliance_history`, 6 `m3_behavioural_signals`, 8 `sme_profiles`. | `backend/app/scripts/{seed_lookups,seed_demo_responses,seed_dev}.py`, `enigmatrix-docs/backend/SETUP/06_Database_and_Migrations.md`, `enigmatrix-docs/tracker/{SESSIONS,CHANGES,FEATURES}.md`, `AI_WORK_LOG.md` |
| 2026-05-15 | F-143 | **Session 21 — M1 doc cleanup: section-header conformance + folder-path consistency sweep.** Two maintenance passes on `enigmatrix-docs/m1/` closing loose ends after the (out-of-session) creation of the `14_M1_*` tracking workflows, `15_M1_*` folder guides, and `16_M1_Development_Roadmap.md`. (1) Section-header rename in `15_M1_6_Docs_Folder_Guide.md` (`## How to start (if you're writing a new doc)` → `## How to start building`) so all six `15_M1_N` guides match the locked 6-section skeleton; skeleton-conformance grep now `6 / 6` everywhere. (2) Folder-path consistency sweep — 22 path replacements across 8 files (60 m1 docs scanned). Three patterns: missing `backend/` prefix (e.g. `app/api/v1/m1_regulations.py` → `backend/app/api/v1/m1_regulations.py`); missing `m1/` subdir under `backend/app/tasks/` (e.g. `app/tasks/classify_gazette.py` → `backend/app/tasks/m1/classify_gazette.py`); obsolete module names (`pipeline/inspect.py` → `ml/m1/extraction/pdf_classifier.py`, `pipeline/segment.py` → `ml/m1/extraction/segmenter.py`, `app/services/gazette_extractor.py` → `ml/m1/extraction/text_extractors.py`, `app/ml/inference.py` → `ml/m1/model/inference.py`, `from app.ml.gazette_classifier` → `from ml.m1.model.architecture`). Recon agent flagged 15; verification grep flushed out 7 more in spillover files (README + `15_M1_2_Backend_Folder_Guide.md` were missed by the audit but caught by the post-edit sweep). After the pass, an 11-regex stale-pattern grep across all 60 m1 docs (excluding doc 13, the canonical) returns zero matches; `git diff --stat` for code dirs is empty (zero code drift). Doc 13 is now the canonical source; every other m1 doc agrees with it. Lesson recorded: always run a stale-pattern grep after a "find all X" agent pass — the audit was ~70% complete. | `enigmatrix-docs/m1/{02_M1_Data_Requirements,03_M1_Data_Collection,07_M1_Deployment_Integration,08_M1_Full_System_Architecture,11_M1_API_Reference,12_M1_Monitoring_Maintenance,15_M1_2_Backend_Folder_Guide,15_M1_6_Docs_Folder_Guide,README}.md`, `AI_WORK_LOG.md`, `enigmatrix-docs/tracker/{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-12 | F-142 | **Session 20 — Domain-based documentation restructuring.** Reorganized 66 markdown files from flat `SETUP/`, `BUILD_PLAN/`, `research/` tracks into 5 domain folders matching the codebase structure: `backend/` (16 files — FastAPI, PostgreSQL, scoring engine, M1–M4 BUILD specs + module research), `frontend/` (6 files — Next.js, UI screens, admin console + APP_GUIDE), `infra/` (8 files — Docker, deployment, CI/CD, observability), `ml/` (10 files — ML training pipeline BUILD spec + research methodology for all 4 modules), `shared/` (17 files — cross-cutting architecture, auth, testing, research proposal + claude-chats). `tracker/` (8 files) stays at docs/ root unchanged. Added 6 new `README.md` domain index files (root + 5 domains), each with domain overview, implementation status table, and per-file listing. Fixed 65+ broken relative links across all moved files (domain-specific fix tables applied to backend/SETUP, frontend/SETUP, infra/SETUP, ml/research, shared/research, shared/SETUP, and all tracker files). Verified zero broken .md links remain. Initialized `docs/` as a standalone git repository; committed all 73 files (65 .md + 2 PDFs + 6 new READMEs). | `docs/README.md`, `docs/{backend,frontend,infra,ml,shared}/README.md`, all moved files (cross-link fixes), `docs/tracker/{SESSIONS,CHANGES,FEATURES}.md` |
| 2026-05-12 | F-140–F-141 | **Session 19 — M1/M2/M3/M4 per-module feature documentation sweep.** Phase 3 of the ongoing docs accuracy sweep. Key corrections: (1) `APP_GUIDE.md` §3.13 — `/risk` page described as fully implemented (M2 domain score + M3 signals two-card layout) rather than "coming soon 501 stub"; feature matrix split into separate `/risk` (✅), `/qa` (coming soon BUILD_08), `/verify` (coming soon BUILD_10) rows. (2) `SETUP/06` — added `m3_field_mapping` JSONB column to survey_questions list; added dedicated table subsections for `m2_knowledge_scores` (cached M2 score aggregate, recomputed on every submit), `m3_compliance_history` (append-only snapshot, deadline/penalty/audit flags), `m3_behavioural_signals` (append-only snapshot, filing method / books / barriers). (3) `SETUP/04` — added `m2.py`, `m3.py`, `admin_translations.py`, `dashboard.py` to api/v1 directory; added `m1_regulation_service.py`, `m2_service.py`, `m2_scoring.py`, `m2_linkage_rules.py`, `m3_service.py` to services. (4) `SETUP/05` — fixed `/risk` from stub to implemented; added `vulnerability-form.tsx`. (5) `SETUP/07` — added 10 new endpoint rows (M2/M3/dashboard/translations). (6) `SETUP/10` — fixed BUILD_09 row (M3 tables + /risk are done; only ML model pending); fixed §5 M3 recommendation. (7) `SETUP/11` — added M4 row to module table and M4 status note to §11. (8) `SETUP/12` — M4 stub notes in /surveys/module/[id] and /qa+/verify sections. (9) `SETUP/13` — M0→M1 fixes throughout; per_module_m4 stub note in modes table; module-m0→m1 in SurveyWizard description. (10) `research/13` — M4 implementation status callout at top. (11) `research/15` — "not yet built" callout at top. | `docs/APP_GUIDE.md`, `docs/SETUP/{04,05,06,07,10,11,12,13}_*.md`, `docs/research/{13,15}_*.md`, `docs/tracker/{SESSIONS,FEATURES,CHANGES,SETUP_COVERAGE}.md` |
| 2026-05-12 | F-139 | **Session 18 — Documentation sync.** `docs/research/13_Unified_Survey_Architecture.md`: §2 survey modes table corrected (`module_1/2/3/4` → `per_module_m1/m2/m3/m4`; caps changed from 8–10 to exact 10/20); §6.1 `survey_sessions` schema updated to actual implemented columns (removed `question_cap`, `last_question_id`, `meta`; added `questions_shown`, `recruitment_channel`; corrected `CHECK` constraint values); §6.2/6.3 `module_number` check corrected from `(1,2,3,4)` to `(0,2,3,4)`; §9 Session-Based API Reference replaced with the six actual implemented endpoints (`/start`, `/next-question`, `/answer`, `/complete`, `/my-history`, `/{id}`); old §10 renamed §13; new §10 Survey Submission Limits (role caps, `survey_limits` singleton DDL, admin config, enforcement flow); new §11 Module Number Convention callout (0=awareness, 2=knowledge, 3=vulnerability, 4=misinformation — intentional gap at 1). `docs/SETUP/13_Unified_Survey_Configuration.md`: §2 rewritten from old `GET /survey-flow/start` + `POST /survey-flow/answer` loop to session-based loop with `SessionOut`/`FlowNextOut` shapes and `survey_mode` values table; new §11 Frontend Session Components (`SurveyLauncher` create/resume, `SurveyWizard` one-question renderer + accent + back-nav + progress); new §12 Survey Submission Limits (DB singleton, admin UI at `/admin/settings`, service resilience); new §13 SME Profile Auto-Creation (get-or-create in `deps.py`); old §11 Common Mistakes renumbered §14 + two new rows (survey start 500, limit error not displayed). `docs/SETUP/11_Survey_System.md`: new §11 Phase 3 — Session-Based Survey Architecture documenting the additive session layer, coexistence table, and key additions (survey_sessions, session API, survey_limits, SurveyLauncher/SurveyWizard, get-or-create, migration resilience). Tracker files: sessions 16–18 added to `SESSIONS.md`; F-134–F-139 added to `FEATURES.md`; `RESEARCH_BUILD_TRACKER.md` last-updated date + session log updated; `BUILD_PLAN_COVERAGE.md` admin-console section updated; `SETUP_COVERAGE.md` executive summary + per-doc entries updated. | `docs/research/13_Unified_Survey_Architecture.md`, `docs/SETUP/{11_Survey_System,13_Unified_Survey_Configuration}.md`, `docs/tracker/{CHANGES,FEATURES,SESSIONS,RESEARCH_BUILD_TRACKER,BUILD_PLAN_COVERAGE,SETUP_COVERAGE}.md` |
| 2026-05-11 | F-138 | **Session 17 — Hotfix: survey_limits ProgrammingError resilience.** `start_session` unconditionally calls `survey_limits_service.get_limits(db)` which queries `survey_limits`. When `alembic upgrade head` hasn't been run, PostgreSQL raises `UndefinedTableError` → SQLAlchemy `ProgrammingError`; the transaction is then aborted, causing all subsequent DB calls in the same request to fail with "current transaction is aborted" — meaning even admins and annotators (who have 0/unlimited limits) could not start a survey. Fix: wrap `db.get(SurveyLimits, 1)` in `try/except ProgrammingError`, call `await db.rollback()` to restore the session, and return a transient `SurveyLimits(sme_limit=10, annotator_limit=0, admin_limit=0)` object with `_EPOCH` timestamps. No DB writes on this path. | `backend/app/services/survey_limits_service.py` |
| 2026-05-11 | F-134–F-137 | **Session 16 — Admin-manageable survey limits.** Moved survey submission limits from environment variables (`SURVEY_LIMIT_SME/ANNOTATOR/ADMIN`) to a DB singleton table so admins can configure them live. **Backend:** new Alembic migration `202605170001_survey_limits.py` (`down_revision = "202605160001"`) creates `survey_limits` table with seed row `(id=1, sme_limit=10, annotator_limit=0, admin_limit=0)`; new ORM model `app/models/survey_limits.py` (`SurveyLimits(Base, TimestampMixin)`); new Pydantic schemas `SurveyLimitsOut` (includes `updated_at`) + `SurveyLimitsPatchIn` (`Field(ge=0, le=10_000)`) in `app/schemas/survey_limits.py`; new service `app/services/survey_limits_service.py` with `get_limits` (get-or-create singleton) and `update_limits`; new admin router `app/api/v1/admin_survey_limits.py` (`GET + PATCH /api/v1/admin/survey-limits`, admin-only); mounted in `app/api/v1/router.py`. `app/api/v1/survey_sessions.py`: removed `get_settings` import, replaced `ForbiddenError` detail with `raise ForbiddenError(f"Survey limit reached ({completed}/{limit})")` (fixing the `HTTPException(detail=dict)` format that the frontend API client could not parse — `ForbiddenError` produces `{"code":"forbidden","message":"..."}` at the top level via the exception handler), added `survey_limits_service.get_limits(db)` call in both `start_session` and `my_history`. `app/settings.py`: removed `SURVEY_LIMIT_SME`, `SURVEY_LIMIT_ANNOTATOR`, `SURVEY_LIMIT_ADMIN` fields. **Frontend:** new typed API client `frontend/lib/api/admin-survey-limits.ts` (`SurveyLimitsOut`, `SurveyLimitsPatch`, `AdminSurveyLimitsApi.{get,update}`); new admin settings page `frontend/app/(admin)/admin/settings/page.tsx` (client component; `useQuery` + `useEffect` sync; three `<Input type="number" min={0}>` in `sm:grid-cols-3`; `useMutation` with `onMutate/onSuccess/onError` toast pattern; `updated_by` + `updated_at` metadata; `LimitField` sub-component with "0 = unlimited" help text). Sidebar: `Settings` icon added to `ADMIN_DATA_ITEMS` (`/admin/settings`, `nav.adminSettings`). i18n: `nav.adminSettings` + top-level `adminSettings.*` keys (title, subtitle, `surveyLimits.{heading,description,sme,annotator,admin,save,saved,updatedBy}`) added to en/si/ta. | `backend/alembic/versions/202605170001_survey_limits.py`, `backend/app/models/survey_limits.py`, `backend/app/schemas/survey_limits.py`, `backend/app/services/survey_limits_service.py`, `backend/app/api/v1/{admin_survey_limits,survey_sessions,router}.py`, `backend/app/settings.py`, `frontend/lib/api/admin-survey-limits.ts`, `frontend/app/(admin)/admin/settings/page.tsx`, `frontend/components/layout/sidebar.tsx`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-15 | F-133 | **Docs reconciliation + sidebar `/surveys` consolidation.** `SETUP/11_Survey_System.md`: added the M0-vs-"M1 Regulations" naming callout, documented the M2 per-sector partition (§4), added the `VAT_SSCL_MERGE_2026` worked chain to §10.3 (alongside the existing q04 example) with the reachability/`start_flow`-fallback note, made §10.4 say M3 projection is driven by `m3_field_mapping` (canonical defaults), added the per-regulation-chain recipe + an M3-target-column extend bullet to §10.9, and a new §10.10 documenting `survey_responses.linked_regulation_id` (now always populated) + `meta.from_question_code`/`from_rule` breadcrumb; fixed the stale §1 table ("hardcoded in awareness.ts" → DB-driven). `SETUP/03_Architecture.md` §5 ERD redrawn: `survey_question_regulations` M:N junction box, `survey_questions` gains `is_baseline`/`m3_field_mapping`/`linked_regulation_id`-as-cached-primary, `survey_responses.linked_regulation_id` annotated "now always populated" + a `meta` line, `audit_log` columns spelled out, `created_by`/`updated_by` on `users`/`sme_profiles`/`survey_questions`/`m1_regulations`/junction, "DDL sources" line updated. `BUILD_07/08/09`: each gains a "Cross-module linkage" subsection cross-referencing SETUP/11 §3 (C1/C2/C3) and the seeded VAT/SSCL example; BUILD_09's "Cross-module wiring" callout extended for the `m3_field_mapping`-driven projection. `research/module_2_and_3_data_architecture.md` DOMAIN 1 + `module_1_and_4_data_architecture.md` §A2.3: VAT/SSCL-merge worked-example notes. Sidebar: the SME nav's first survey entry now points at `/surveys` (the unified two-tab hub) with `nav.surveys` instead of `/regulations` with the misleading `nav.regulations`="Surveys hub" (Session-13 follow-up); `ClipboardList` icon; the three standalone `/surveys/{instrument}` entries kept, demoted below it. `nav.surveys` already existed at en/si/ta parity. `pnpm typecheck` clean. | `docs/SETUP/{11_Survey_System,03_Architecture}.md`, `docs/BUILD_PLAN/BUILD_{07_Module1_Awareness,08_Module2_Knowledge,09_Module3_Risk}.md`, `docs/research/{module_2_and_3,module_1_and_4}_data_architecture.md`, `frontend/components/layout/sidebar.tsx` |
| 2026-05-15 | F-132 | **OQ32 resolved — data-driven M3 question→risk-column mapping.** New nullable `survey_questions.m3_field_mapping` JSONB column (migration `202605150001`, `down_revision = 202605140001`; no data backfill — NULL means "fall back to the built-in default for this code, if any"). Shape: `{"target": "compliance_history"|"behavioural_signals", "column": "<col>", "coerce": "yes_no"|"band"|"int"|"csv_list"}`. `survey_service._project_m3_snapshots` rewritten: batch-fetches the M3 questions in the submitted batch, resolves each row's mapping (custom `m3_field_mapping` → else `_M3_DEFAULT_MAPPINGS[code]` → else skip), coerces via `_COERCERS`, routes into `m3_compliance_history`/`m3_behavioural_signals` by `target` — byte-identical behaviour for the canonical `M3_HIST_*`/`M3_BEH_*`/`M3_STR_*` codes (their default mapping == the old hardcoded entry; the `M3_HIST_003` CSV-special-case is now just `coerce: "csv_list"`). The writable-column allow-list (`M3_MAPPING_COLUMNS`) is derived at import time from the two M3 models; `survey_service.validate_m3_mapping` rejects bad ones (used both by the projection — to skip bad data — and by `survey_question_service` create/update — to reject bad input). New `M3FieldMapping` schema on `SurveyQuestionCreateIn`/`UpdateIn`/`AdminOut`; `survey_question_service.create_question`/`update_question`/`duplicate_question` accept + validate + persist + copy it. Frontend: `QuestionForm` gains an "M3 risk-signal mapping" card (module-3 only) — three `<Select>`s (target / column / coerce, with a "(none)" option) assembled into the nested object on submit; new `M3FieldMapping` TS type + `m3_field_mapping` on `SurveyQuestionAdmin`/`SurveyQuestionCreatePayload`; new flat `m3_mapping_*` validator fields + `M3_MAPPING_{TARGETS,COERCERS,COLUMNS}` constants mirroring the backend; new `questions.section.m3Mapping` + `questions.m3Mapping.*` i18n at en/si/ta parity. The seeded `M3_VAT_SSCL_MERGE_*` follow-ups carry no mapping (don't project) — an admin can opt them in. Migration round-trips (`alembic downgrade -1` → `upgrade head`); `pnpm typecheck` clean. | `backend/alembic/versions/202605150001_survey_questions_m3_field_mapping.py`, `backend/app/models/survey_question.py`, `backend/app/schemas/survey_question.py`, `backend/app/services/{survey_service,survey_question_service}.py`, `frontend/components/forms/question-form.tsx`, `frontend/lib/types/index.ts`, `frontend/lib/api/admin-survey-questions.ts`, `frontend/lib/validators/survey-question.ts`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-15 | F-131 | **Worked end-to-end scenario `VAT_SSCL_MERGE_2026` seeded.** New M1 regulation (domain VAT, effective 2026-04-01, severity 5, real-world example + penalty range spelling out "SSCL abolished, folded into VAT at a single 20% rate — a restructuring of two line items, not a real 2-point increase"). New M0 awareness `awareness.v1.q13` (`is_branching_root=true`, `is_baseline=false` so the generic 12-question survey is unchanged, `linked_regulation_short="VAT_SSCL_MERGE_2026"`, 4-way options `yes_restructure`/`yes_increase`/`no`/`unsure` with `next_question_rules` routing the non-ideal three → `VAT_SSCL_MERGE_FACT_001`); a new `is_baseline: bool = True` kwarg was added to `seed_awareness_questions._q`. New M2 `VAT_SSCL_MERGE_FACT_001` ("which two charges does the 20% VAT replace?" → A: VAT 18% + SSCL ✓) + `VAT_SSCL_MERGE_APP_001` ("compute VAT on LKR 1M → one 20% line"), both added to `CROSS_MODULE_LINKAGE` (the factual routes wrong answers → `M3_VAT_SSCL_MERGE_PRACTICE`; the application is a linear sibling). New M3 `M3_VAT_SSCL_MERGE_PRACTICE` ("one 20% line, or still VAT+SSCL separately?" → wrong → `M3_VAT_SSCL_MERGE_PENALTY` "back-VAT + 25-50% penalty + re-issuing non-compliant invoices"); `seed_vulnerability_questions._q`/`main()` gained `linked_regulation_short` support (resolves short→id, sets `linked_regulation_id`, writes the `SurveyQuestionRegulation` primary junction row) so all four questions are junction-linked to `VAT_SSCL_MERGE_2026` and the regulation-scoped flow (`/surveys/regulation/<id>`) traverses the whole chain. Demo SMEs answer q13 + the M2 questions automatically (they're picked up by the existing `_walk_awareness`/`_walk_knowledge`). | `backend/app/scripts/{seed_regulations,seed_awareness_questions,seed_m23_questions,seed_vulnerability_questions}.py` |
| 2026-05-15 | F-130 | **`survey_responses.meta.from_question_code` / `meta.from_rule` flow breadcrumb + `start_flow` fallback.** `survey_flow.py::_flow_breadcrumb` (new): before persisting an answer, fetch the SME's prior `survey_responses` row, load its question, and if any of its `next_question_rules` resolves to the question being answered (via `_resolve_rule`/`_answer_matches` against the prior answer) record `{from_question_code: <parent>, from_rule: <rule>}` — passed to `survey_service.submit` as `flow_context` and written into `meta` by `_build_row`. Absent on the branching root, on linearly-reached rows, and on batch submits from the standalone instrument pages (a batch isn't a flow). The full M0→M2→M3 path is reconstructible by walking `from_question_code` backwards. Also: `survey_question_service.start_flow` now falls back to the first in-scope question by `(module_number, sort_order, question_code)` when a regulation-scoped flow has no in-scope `is_branching_root` (fixes the latent bug where e.g. `VAT_THRESHOLD_2026`-scoped flows could return `None`). `SurveyResponse.meta` model docstring updated. | `backend/app/api/v1/survey_flow.py`, `backend/app/services/{survey_service,survey_question_service}.py`, `backend/app/models/survey.py` |
| 2026-05-15 | F-129 | **`survey_responses.linked_regulation_id` populated end-to-end.** `survey_service.submit` / `_build_row` take an optional `regulation_id` (threaded from `POST /survey-flow/answer?regulation_id=…`) — when set it's authoritative; otherwise `_build_row` falls back to the answered question's cached `survey_questions.linked_regulation_id` (so standalone-page submits and un-scoped flow answers still link rows for single-regulation questions). New helpers `_question_code_candidates` / `_question_linked_regulation` resolve the question from the row's `question_id` (dotted-or-flat). Consequences (all previously broken): the `survey.submitted` audit event's `regulation_ids` is non-empty for regulation-scoped flows; `GET /api/v1/dashboard/pending-regulations` marks a regulation "answered" once any linked question is answered; the admin Activity Log keeps the regulation context. The Activity-Log `renderDetail` for `survey.submitted` now also shows "N regulation(s)" when present. `import app.api.v1.router` clean; migration round-trips. | `backend/app/services/survey_service.py`, `backend/app/api/v1/survey_flow.py`, `backend/app/models/survey.py`, `frontend/app/(admin)/admin/activity-log/page.tsx` |
| 2026-05-14 | F-128 | **Authorship footer on the admin edit pages.** New `frontend/components/admin/authorship-meta.tsx` — an async server component that renders a muted, dashed-border line: `Created by {created_by ?? "—"}{created_at && " · " + ...}` · `Last edited by {updated_by ?? "—"}{updated_at && " · " + ...}` + a "View this record's history" link with a `History` icon. Wired into `/admin/regulations/[id]/edit` (link → `/admin/activity-log?record_id=${id}`, placed just below the verified-meta card) and `/admin/questions/[code]/edit` (link → `/admin/activity-log?record_key=${code}`). Uses the new `activity.authorship.{createdBy,editedBy,viewHistory}` i18n keys (ICU `{name}` interpolation) at en/si/ta parity. Reads the `created_by`/`updated_by`/`created_at`/`updated_at` fields now present on `RegulationAdmin` / `SurveyQuestionAdmin`. `pnpm typecheck` + `pnpm build` clean. | `frontend/components/admin/authorship-meta.tsx`, `frontend/app/(admin)/admin/regulations/[id]/edit/page.tsx`, `frontend/app/(admin)/admin/questions/[code]/edit/page.tsx`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-14 | F-127 | **Admin Activity Log UI.** New server-rendered page `/admin/activity-log` (`dynamic = "force-dynamic"`) — fetches `AdminAuditApi.list(...)` + `AdminAuditApi.eventTypes(...)` in parallel (both `.catch` to empty), renders `<PageHeader>` + `<ActivityFilters>` (client form: event-type `<Select>`, table `<Select>`, actor `<Input>`, since/until date inputs, Apply/Clear → `router.push` with rebuilt querystring) + a `<Table>` (When / Event / Target / User / Details) + a link-mode `<Pagination>` that preserves the active filters. **Event** = colour-coded `<Badge>` via `eventBadgeVariant()` (created/linked/register → success, updated/primary-changed/translation/admin_update → accent, archived/unlinked/deactivate/delete/login.failure → destructive, verified/bulk_verified/restored/duplicated → secondary, login.success/refresh/activate/password_reset/survey.submitted → default, else outline) labelled via `eventLabel()`. **Target** = `table_name` + `record_key ?? record_id?.slice(0,8)`. **Details** = `renderDetail(entry)` — a type-aware switch: `survey.submitted` → "instrument · N answers · scored · risk snapshot", `*.updated`/`user.admin_update` → `fields_changed`/`changed` chips, `translation.completed` → bulk message or chips, `*.bulk_verified` → "N verified by X", `auth.login.failure` → email/reason, `*.duplicated` → new code, fallback → `<details>` collapsing the raw JSON. New `frontend/lib/audit-events.ts` (hardcoded `EVENT_TYPE_LABELS` map + `eventLabel()` + `eventBadgeVariant()` — kept out of i18n because next-intl treats `.` in keys as nesting, so dotted event-type strings can't be flat keys; admin-only chrome stays English, mirroring the rest of the admin surface). New `loading.tsx` (renders `<AnimatedLoadingSkeleton>`). Sidebar gains a `ScrollText` "Activity log" admin entry (`nav.adminActivityLog`). New `activity.*` i18n namespace (`listTitle`, `listSubtitle`, `empty`, `columns.{when,event,target,user,details}`, `filters.{event,table,actor,actorPlaceholder,since,until,allEventTypes,allTables,apply,clear}`, `authorship.*`) at en/si/ta parity. `pnpm typecheck` + `pnpm build` clean. | `frontend/app/(admin)/admin/activity-log/{page,loading}.tsx`, `frontend/components/admin/activity-filters.tsx`, `frontend/lib/audit-events.ts`, `frontend/components/layout/sidebar.tsx`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-14 | F-126 | **Admin Activity Log API + typed client.** New router `app/api/v1/admin_audit.py` (behind `require_admin`), mounted at `/api/v1/admin/activity-log`: `GET ""` — query params `event_type`, `table_name`, `actor` (case-insensitive substring of the acting user's email), `record_id` (UUID), `record_key` (str), `since`/`until` (datetime), `q` (free-text over `event_data_json`), `page` (≥1), `size` (1–100) → `AuditLogPage` ordered `occurred_at DESC, log_id DESC`; `GET "/event-types"` → `AuditEventTypesOut` (distinct `event_type`s + `table_name`s) for the filter dropdowns. New schemas `app/schemas/audit.py` (`AuditLogOut` `from_attributes`, `AuditLogPage`, `AuditEventTypesOut`). Frontend `lib/api/admin-audit.ts` typed client (`AdminAuditApi.list/eventTypes`, `AuditLogParams`); new TS types `AuditLogEntry` / `AuditLogPage` / `AuditEventTypes` in `lib/types/index.ts`. | `backend/app/api/v1/admin_audit.py`, `backend/app/schemas/audit.py`, `backend/app/api/v1/router.py`, `frontend/lib/api/admin-audit.ts`, `frontend/lib/types/index.ts` |
| 2026-05-14 | F-125 | **`AuthorshipMixin` — `created_by` / `updated_by` on the content + identity tables.** New `app/db/mixins.py::AuthorshipMixin` (`created_by`/`updated_by` — plain nullable `String`, **not** FKs: denormalised acting-user email, survives user deletion, matches the existing `user_name` / `*_verified_by` convention). Applied to `M1Regulation`, `SurveyQuestion`, `SurveyQuestionRegulation`, `User`, `SMEProfile`. **Not** applied to the static lookups (`RegulatoryDomain`, `Sector`), the append-only `AuditLog` (has `user_name`), or the append-only response/score/snapshot tables (already carry `sme_id`). The service layer stamps them on **every** create / update / verify / archive / restore / link / translation-patch — *including soft-deletes* (`is_active=false` is an update → `updated_by` is set): `m1_regulation_service` (`create`/`duplicate` set both = `actor_email`; `update`/`verify`/`archive`/`restore`/`bulk_verify` set `updated_by`), `survey_question_service` (same, plus `SurveyQuestionRegulation.created_by` on link-create / `updated_by` on unlink + primary-change — `_sync_primary_junction` gained a required `actor_email` param), `auth_service` (`register` → own email on `User` + the auto-created `SMEProfile`; `admin_create_user` → `actor.email` on both; `admin_update_user` with a `profile_touched` flag so the profile's `updated_by` only moves when a profile field actually changed; `set_active` activate/deactivate/delete + `reset_password` → `updated_by`), `admin_translations` patch handlers (`q.updated_by` / `reg.updated_by` when changed). Surfaced in `RegulationAdminOut`, `SurveyQuestionAdminOut`, `UserOut` (+ `updated_at` added to `UserOut`). Migration `202605140001` adds the 10 columns + `audit_log.record_key` + `ix_audit_log_record_key`; no data backfill (historical authorship genuinely unknown — existing rows stay `NULL`, new writes populate). | `backend/app/db/mixins.py`, `backend/app/models/{regulation,survey_question,user,sme_profile,audit_log}.py`, `backend/app/services/{m1_regulation_service,survey_question_service,auth_service}.py`, `backend/app/api/v1/admin_translations.py`, `backend/app/schemas/{regulation,survey_question,user}.py`, `backend/alembic/versions/202605140001_authorship_and_audit_record_key.py`, `frontend/lib/types/index.ts` |
| 2026-05-14 | F-124 | **Audit-write consolidation + coverage closure.** New `app/services/audit_service.py` — `record(db, *, event_type, actor, table_name=None, record_id=None, record_key=None, data=None)` is the single audit write path (`actor` is a `User` → its `.email`, or a bare email string; joins the caller's transaction, no separate commit), plus `list_events(...)` (conditional `.where`: `event_type ==`, `table_name ==`, `func.lower(user_name).contains(actor.lower())`, `record_id ==`, `record_key ==`, `occurred_at >= since` / `<= until`, `cast(event_data_json, String).ilike(f"%{q}%")`; ordered `occurred_at DESC, log_id DESC`; size clamped 1–100) and `distinct_event_types(...)`. Migrated every open-coded `insert(AuditLog).values(...)` onto `record(...)` — behaviour-preserving (no `event_type` string or `event_data_json` shape changed; only `record_key` *added*): `auth_service` (11 sites; `_audit` kept as a thin shim over `record`), `m1_regulation_service` (7 sites), `survey_question_service` (10 sites — all now carry `record_key=q.question_code`), `api/v1/admin_translations.py` (3 sites). New `audit_log.record_key` column (indexed) carries the natural key for string-keyed entities (`survey_questions.question_code`) so the Activity Log can filter without a UUID. Closed the one real coverage gap: `survey_service.submit` now writes a single `survey.submitted` row after the M2/M3 side-effects — `{ instrument, answered_count, regulation_ids: sorted({…linked_regulation_id}) \| null, m2_scored, m3_snapshots_projected }` (the score recompute + snapshot projection are deterministic side-effects of the same submit → flagged in the JSON, not separate rows). `flow.started_for_regulation` was never emitted in code (only a stale `11_Survey_System.md` line) — dropped, not added. Backend `import app.api.v1.router` clean (no circular import). | `backend/app/services/audit_service.py`, `backend/app/models/audit_log.py`, `backend/app/services/{auth_service,m1_regulation_service,survey_question_service,survey_service}.py`, `backend/app/api/v1/admin_translations.py`, `docs/SETUP/{07_Auth_and_Roles,11_Survey_System,03_Architecture}.md` |
| 2026-05-13 | F-123 | Bug-fix batch from QA: (1) **Missing i18n keys** — the Session-12 surveys-hub + dashboard-widget code referenced `surveys.hub.{tabRegulation,tabModule,regulationEmpty,regulationLead,moreRegulations,moduleLead,moduleAwarenessTitle,moduleAwarenessBody,moduleKnowledgeTitle,moduleKnowledgeBody,moduleVulnerabilityTitle,moduleVulnerabilityBody,pageTitle,pageSubtitle}`, `surveys.empty`, and `dashboard.{pendingRegulationsTitle,viewAllRegulations,morePendingRegulations}` — none existed in the message files, so the UI rendered the raw key strings. Added all of them to en/si/ta. The `/surveys` hub header now uses `surveys.hub.pageTitle/pageSubtitle` instead of the misleading `surveys.flowTitle` ("Regulatory check-in / One question at a time…") which belongs on the per-regulation flow, not the chooser. (2) **"By regulation" tab / dashboard widget always empty** — `GET /api/v1/dashboard/pending-regulations` treated the `"universal"` sentinel sector as a specific sector, so the five seeded regulations (all linked only to `"universal"`) never matched any SME. Fixed the query to match `sector_code IN (sme.sector, "universal")`. (3) **Question form: regulation-link combobox empty** — added a `regulationsLoaded` flag + an empty-state hint ("No regulations yet — create one first: New regulation") plus a normal helper hint when populated, so the admin always knows what to do. (4) **Auto `question_code`** — `<QuestionForm>` (create mode) and `<FlowQuestionDrawer>` now auto-suggest the code as `<instrument>.v<N>.qNN` from the selected module (0→awareness, 2→knowledge, 3→vulnerability) + version, with the NN auto-incremented past the highest existing code matching that prefix; the admin can still type a custom code (auto-tracking stops the moment they do). New helper `lib/surveys/question-code.ts`; the create-validator regex relaxed from `[A-Z0-9_.]+` to `[A-Za-z0-9_.]+` (the existing `awareness.v1.qNN` codes are lowercase); the drawer's code input no longer force-uppercases. New i18n keys `questions.fields.{questionCodeHint,linkedRegulationHint,noRegulationsHint}`. `pnpm typecheck` + `pnpm build` clean. | `frontend/lib/i18n/messages/{en,si,ta}.json`, `frontend/app/(app)/surveys/page.tsx`, `frontend/app/api/...` (n/a), `backend/app/api/v1/dashboard.py`, `frontend/components/forms/question-form.tsx`, `frontend/components/forms/flow-question-drawer.tsx`, `frontend/lib/surveys/question-code.ts`, `frontend/lib/validators/survey-question.ts` |
| 2026-05-13 | F-122 | App-themed `<Pagination>` (`components/ui/pagination.tsx`) — a dependency-free replacement for the Ant Design `<Pagination>` reference (rejected installing `antd`: a whole UI framework with its own CSS-in-JS + token system that can't read the project's HSL contract, so "use app colors" would be impossible). Built on the `<Button>`/`<Select>`/`<Input>` primitives + theme tokens: page numbers (with `…` collapses around the current page), prev/next, optional page-size changer, optional quick-jumper, "Total N items". Two modes — controlled (`onPageChange`/`onPageSizeChange`, for client components) and link (`buildHref`, renders `<Link>`s, for server components). Wired into all eight admin tables: `/admin/regulations` + `/admin/questions` (URL `?page=&size=`), `/admin/m2/questions` + `/admin/users` (client-side slice of the flat list), `/admin/translations` (`<TranslationsQueue>` — refactored its state: `pageData`/`pageNum`/`pageSize`, re-fetch on filter/page/size change), `/admin/surveys/awareness/responses` + `/admin/m2/scores` + `/admin/m3/risk-signals` (link mode via `?page=&size=`). New `pagination.*` i18n namespace (en/si/ta parity). Demo at `components/ui/pagination-demo.tsx`. The stale `regulations.pagination` / `questions.pagination` i18n keys are now unused (harmless; left in place). `pnpm typecheck` + `pnpm build` clean. | `frontend/components/ui/pagination.tsx`, `frontend/components/ui/pagination-demo.tsx`, `frontend/app/(admin)/admin/{regulations,questions,m2/questions,users,surveys/awareness/responses,m2/scores,m3/risk-signals}/page.tsx`, `frontend/components/admin/translations-queue.tsx`, `frontend/lib/i18n/messages/{en,si,ta}.json`, `docs/SETUP/12_UI_Screens_and_Loading.md` |
| 2026-05-13 | F-121 | New SETUP doc `12_UI_Screens_and_Loading.md` — screen-by-screen map of every SME + admin page (route · purpose · key components · why · what-you-see), the reusable-component catalog (24 `components/ui/` primitives + the `components/forms\|surveys\|admin\|layout` composites), the design-system recap (HSL tokens, dark-first, per-module accents, trilingual, radius), and the loading-state strategy (§6-§7). Linked from `00_INDEX.md`; `11_Survey_System.md` footer now points to `12`; `05_Frontend_Development.md` gains a `§10 Loading states` stub pointing at `12 §4/§6-§7`. | `docs/SETUP/12_UI_Screens_and_Loading.md`, `docs/SETUP/00_INDEX.md`, `docs/SETUP/05_Frontend_Development.md`, `docs/SETUP/11_Survey_System.md` |
| 2026-05-13 | F-120 | Streaming + skeleton wiring. New `loading.tsx` segments (each renders `<AnimatedLoadingSkeleton>` in the page's width container) for the server-rendered surfaces: `app/(app)/surveys/`, `app/(app)/surveys/regulation/[id]/`, `app/(app)/dashboard/`, `app/(admin)/admin/regulations/[id]/{flow,edit}/`, `app/(admin)/admin/translations/`. Replaced the plain `"Loading…"` text fallbacks in the client-rendered admin tables (`/admin/regulations`, `/admin/questions`, `/admin/m2/questions`, `/admin/users`) and in `<LinkedQuestionsPanel>` + `<TranslationsQueue>` with `<AnimatedLoadingSkeleton>` (chrome stripped via `className` so it sits inside table borders). Also fixed pre-existing errors that were blocking `pnpm typecheck` / `pnpm build`: `question-renderer.tsx` (`useWatch` `name` was `string[] \| undefined` → always pass the array; `undefined` would have watched the whole form), `survey-form.tsx` (`valuesToAnswers` filter predicate `a is SurveyAnswer` was wider than the input union → `a is NonNullable<typeof a>`), a missing `domain_code`/`change_category`/`severity_level` on the `RegulationPublic` TS type (backend `RegulationPublicOut` ships them), and `flow-question-to-ui.ts` (`const module = …` → `moduleNumber`; Next's `no-assign-module-variable` lint rule was failing the build). `pnpm typecheck` is clean and `pnpm build` succeeds. | `frontend/app/(app)/surveys/loading.tsx`, `frontend/app/(app)/surveys/regulation/[id]/loading.tsx`, `frontend/app/(app)/dashboard/loading.tsx`, `frontend/app/(admin)/admin/regulations/[id]/flow/loading.tsx`, `frontend/app/(admin)/admin/regulations/[id]/edit/loading.tsx`, `frontend/app/(admin)/admin/translations/loading.tsx`, `frontend/app/(admin)/admin/{regulations,questions,m2/questions,users}/page.tsx`, `frontend/components/forms/linked-questions-panel.tsx`, `frontend/components/admin/translations-queue.tsx`, `frontend/components/forms/question-renderer.tsx`, `frontend/components/forms/survey-form.tsx`, `frontend/lib/surveys/flow-question-to-ui.ts`, `frontend/lib/types/index.ts` |
| 2026-05-13 | F-119 | Integrated the supplied `animated-loading-skeleton` shadcn-pattern component (framer-motion search-icon sweep over a shuffling responsive card grid) into `components/ui/`, adapted to the project's dark-first theme: `bg-white` → `bg-card`, `from-gray-50/to-gray-100` → `from-muted/40 to-muted`, `bg-blue-500/20`/`text-blue-600` → `bg-primary/15`/`text-primary` (so it inherits `.module-m{1,2,3}` accents), `rgba(59,130,246,…)` glow keyframes → `hsl(var(--primary) / …)`, and the three inner placeholder `motion.div`s → the existing `<Skeleton>` primitive. `"use client"`; named `AnimatedLoadingSkeleton({ className? })` export + default alias; `Search` icon from `lucide-react`; `aria-busy`/`sr-only "Loading…"` for a11y. Added the supplied demo as `components/ui/animated-loading-skeleton-demo.tsx`. New dep: `framer-motion@^11` (installed 11.18.2 via pnpm — first non-`tailwindcss-animate` animation dep). | `frontend/package.json`, `frontend/pnpm-lock.yaml`, `frontend/components/ui/animated-loading-skeleton.tsx`, `frontend/components/ui/animated-loading-skeleton-demo.tsx` |
| 2026-05-12 | F-118 | Docs: Session 12 entry in `tracker/SESSIONS.md`; new BUILD_PLAN_COVERAGE row "Session 12 — Regulation-anchored authoring + DB-driven surveys"; flipped SETUP_COVERAGE verdict for `11_Survey_System.md` 📌 → ✅ once §10.7-10.8 land. F-IDs F-106 → F-117 documented inline in this CHANGES sheet. | `docs/tracker/{SESSIONS,BUILD_PLAN_COVERAGE,SETUP_COVERAGE,FEATURES}.md`, `docs/SETUP/11_Survey_System.md` |
| 2026-05-12 | F-117 | Per-regulation flow page `/surveys/regulation/[id]` calls `SurveyFlowApi.start({regulation_id})` and renders `<SurveyWizard regulationId>` (extended to thread the param into the answer-submit path). Resume + autosave behaviour preserved. | `frontend/app/(app)/surveys/regulation/[id]/page.tsx`, `frontend/components/forms/survey-wizard.tsx`, `frontend/lib/api/survey-flow.ts` |
| 2026-05-12 | F-116 | Surveys-hub redesigned to two tabs (`?view=regulation\|module`). "By regulation" cards (`<RegulationCard>`) per pending sector-relevant regulation route to `/surveys/regulation/[id]`; "By module" keeps the three abstract instrument cards (now DB-driven). New `<SurveysHubTabs>` + `<RegulationCard>` components. | `frontend/app/(app)/surveys/page.tsx`, `frontend/components/surveys/surveys-hub-tabs.tsx`, `frontend/components/surveys/regulation-card.tsx` |
| 2026-05-12 | F-115 | Dashboard widget "Regulations awaiting your assessment" (max 3 cards) reads `/api/v1/dashboard/pending-regulations`. Falls back gracefully when the SME has no pending regulations or no profile yet. | `frontend/app/(app)/dashboard/page.tsx`, `frontend/lib/api/dashboard.ts` |
| 2026-05-12 | F-114 | DB-driven SME pages: `surveys/{awareness,knowledge,vulnerability}` now fetch via `SurveysApi.questionsForInstrument` and convert via new `lib/surveys/db-question-adapter.ts`. Knowledge page builds the regulation-context map dynamically from each row's `linked_regulation` (no more hardcoded April 2026 VAT card). New client shell `<VulnerabilityForm>` owns the partition-on-submit logic. Hardcoded `lib/surveys/{awareness,m3-vulnerability}.ts` are now dead code (kept on disk pending cleanup PR). | `frontend/app/(app)/surveys/{awareness,knowledge,vulnerability}/page.tsx`, `frontend/components/forms/vulnerability-form.tsx`, `frontend/lib/surveys/db-question-adapter.ts`, `frontend/lib/api/surveys.ts` |
| 2026-05-12 | F-113 | Translations admin: new page at `/admin/translations` + `<TranslationsQueue>` component. Lists every `survey_question` with `needs_translation=true` + every `m1_regulation` missing any SI/TA field. Inline edit per row; bulk-mark-translated for questions. New `lib/api/admin-translations.ts` typed client; new `Languages` sidebar entry. | `frontend/app/(admin)/admin/translations/page.tsx`, `frontend/components/admin/translations-queue.tsx`, `frontend/lib/api/admin-translations.ts`, `frontend/components/layout/sidebar.tsx`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-12 | F-112 | Visual flow canvas: new admin page at `/admin/regulations/[id]/flow` with three-column M0 → M2 → M3 layout. `<FlowCanvas>` renders one node per question with answer-option chips; clicking a chip opens `<FlowQuestionDrawer>` to add a follow-up (slim `<QuestionForm>`-shaped form). Existing rules surface as "→ TARGET" badges next to chips. Built on CSS grid + existing UI primitives (no ReactFlow dep). `<FlowValidationBanner>` surfaces forward-ref / cycle / archived-target soft-warns from the new validate-flow endpoint. Edit page gains an "Open flow canvas" CTA. | `frontend/app/(admin)/admin/regulations/[id]/flow/page.tsx`, `frontend/app/(admin)/admin/regulations/[id]/edit/page.tsx`, `frontend/components/forms/{flow-canvas,flow-question-drawer,flow-validation-banner}.tsx` |
| 2026-05-12 | F-111 | Frontend types + API extensions: `SurveyQuestionAdmin` gains `is_baseline`, `linked_regulations`, `validation_warnings`. New types `LinkedRegulationRef`, `BranchingValidationWarning`, `SurveyQuestionPublic`, `SurveysInstrumentQuestions`, `TranslationKind`, `TranslationLocale`, `TranslationQueueItem`, `TranslationQueuePage`. `AdminSurveyQuestionsApi` gains `linkRegulation`, `unlinkRegulation`, `setPrimaryRegulation`, `validateFlow`. | `frontend/lib/types/index.ts`, `frontend/lib/api/admin-survey-questions.ts` |
| 2026-05-12 | F-110 | Dashboard pending-regulations endpoint: `GET /api/v1/dashboard/pending-regulations?limit=N` returns active regulations sector-relevant to the SME with no `survey_responses.linked_regulation_id` matches yet. Server-side join (no N+1). Backed by new `app/api/v1/dashboard.py` router. | `backend/app/api/v1/dashboard.py`, `backend/app/api/v1/router.py` |
| 2026-05-12 | F-109 | Translation queue admin endpoints: `GET /admin/translations` (UNION across questions+regulations needing SI/TA), `PATCH /admin/translations/question/{code}` + `/regulation/{id}` for inline locale edits, `POST /admin/translations/bulk-mark-translated` for batch `needs_translation=false`. New schemas in `app/schemas/translation.py`. All mutations emit `translation.completed` audit events. | `backend/app/api/v1/admin_translations.py`, `backend/app/schemas/translation.py`, `backend/app/api/v1/router.py` |
| 2026-05-12 | F-108 | Per-instrument DB-driven question fetch: `GET /api/v1/surveys/{instrument}/questions?sector?=&regulation_id?=&include_baseline=true` returns the merged baseline + regulation-linked rows for awareness/knowledge/vulnerability with i18n fallback applied. New SME-safe schemas `SurveyQuestionPublicOut` (strips correct_answer + ground_truth + scoring_rubric) and `SurveysInstrumentQuestionsOut`. New service function `questions_for_instrument` orders by `is_baseline DESC`, `effective_date ASC`, `sort_order ASC`. | `backend/app/api/v1/surveys.py`, `backend/app/services/survey_question_service.py`, `backend/app/schemas/survey_question.py` |
| 2026-05-12 | F-107 | Regulation-scoped flow: `start_flow` / `next_question` / `_next_in_section` / `_skip_answered` accept optional `regulation_id`. When set, root selection + linear fallback + `goto_question_code` resolution all scope to questions linked via the M:N junction; out-of-scope rule targets fall back to linear progression. `survey_flow.py::start_flow` + `submit_answer` accept `regulation_id` query param and emit `flow.started_for_regulation` audit when scoped. Wizard threads the param via `<SurveyWizard regulationId>`. | `backend/app/services/survey_question_service.py`, `backend/app/api/v1/survey_flow.py` |
| 2026-05-12 | F-106 | Branching validators: new `validate_branching(question_code)` detects forward-refs, archived targets, and cycles via DFS over `next_question_rules`. Returns soft-warns (never blocks). New schema `BranchingValidationWarning` + new endpoint `GET /admin/survey-questions/{code}/validate-flow`. `create_question` / `update_question` re-run on every write and stash results on the ORM instance for `_to_admin` to surface as `validation_warnings`. | `backend/app/services/survey_question_service.py`, `backend/app/api/v1/admin_survey_questions.py`, `backend/app/schemas/survey_question.py` |
| 2026-05-12 | F-110 (i18n helper) | Backend i18n fallback: new `app/services/i18n_utils.py` with `localised(record, field_base, locale)` returning `(value, fallback_used)` for missing SI/TA columns. Wired into `survey_flow._to_flow_question` and the new instrument-questions endpoint. `FlowQuestionOut` gains `prompt`, `prompt_locale`, `language_fallback_used`; `FlowStateOut` exposes the same flag at the response level. `Accept-Language` header overrides `user.preferred_language`. | `backend/app/services/i18n_utils.py`, `backend/app/api/v1/survey_flow.py`, `backend/app/schemas/survey_flow.py` |
| 2026-05-12 | F-109 (M:N) | M:N junction CRUD: new endpoints `POST/DELETE /admin/survey-questions/{code}/regulations[...]` + `/regulations/{id}/primary`. Service `link_regulation` / `unlink_regulation` / `set_primary_regulation` / `regulations_for_question` / `_sync_primary_junction` keep the cached `linked_regulation_id` in sync (auto-set on create/update of `linked_regulation_id`). `SurveyQuestionAdminOut` extended with `linked_regulations: list[LinkedRegulationRef]` for full M:N hydration. New audit events `survey_question.{linked_to_regulation, unlinked_from_regulation, primary_regulation_changed}`. Seeds (`seed_awareness_questions.py`, `seed_m23_questions.py`) write junction rows when stamping `linked_regulation_id` so fresh seeds populate the M:N table. | `backend/app/services/survey_question_service.py`, `backend/app/api/v1/admin_survey_questions.py`, `backend/app/schemas/survey_question.py`, `backend/app/scripts/seed_{awareness_questions,m23_questions}.py` |
| 2026-05-12 | F-108 (junction migration) | Two new migrations: (1) `202605120001_question_regulations_junction.py` creates `survey_question_regulations` (composite PK `(question_code, regulation_id)`, `weight smallint`, `is_primary bool`, partial-unique idx on `(question_code) WHERE is_primary=true`). Backfills from existing `survey_questions.linked_regulation_id IS NOT NULL` and drops the FK on the cached column. (2) `202605120002_survey_questions_is_baseline.py` adds `is_baseline boolean NOT NULL default false` + idx `(module_number, is_baseline)`. The 12 awareness questions in `seed_awareness_questions.py` flagged `is_baseline=True`. | `backend/alembic/versions/202605120001_question_regulations_junction.py`, `backend/alembic/versions/202605120002_survey_questions_is_baseline.py`, `backend/app/models/{survey_question,__init__}.py`, `backend/app/scripts/seed_awareness_questions.py` |
| 2026-05-10 | F-100 | Backend admin survey-questions CRUD. New `app/schemas/survey_question.py` with 8 DTOs (incl. `BranchingPredicate` + `BranchingRule`). 10 admin functions added to `survey_question_service` (list/get/create/update/archive/restore/duplicate/verify/bulk-verify/by-regulation), all audit-logged with `survey_question.*` event types. New router `app/api/v1/admin_survey_questions.py` mounted at `/api/v1/admin/survey-questions`. Legacy `/m2/questions` keeps a `Deprecation: true` + successor-version Link header. | `backend/app/schemas/survey_question.py`, `backend/app/services/survey_question_service.py`, `backend/app/api/v1/admin_survey_questions.py`, `backend/app/api/v1/router.py`, `backend/app/api/v1/m2.py` |
| 2026-05-10 | F-101 | `<QuestionForm>` + `/admin/questions/{list,new,[code]/edit}` admin pages. Five-card form (Identity / Linkage / Localised / Answers / Branching) mirroring `<RegulationForm>` polish — sticky save bar, EN/SI/TA Tabs, regulation Combobox, format-adaptive Answers card via `<OptionsBuilder>`. List page mirrors `/admin/regulations` styling: filters, search, bulk-verify, row actions, URL pagination. | `frontend/lib/api/admin-survey-questions.ts`, `frontend/lib/validators/survey-question.ts`, `frontend/lib/types/index.ts`, `frontend/components/forms/question-form.tsx`, `frontend/components/forms/options-builder.tsx`, `frontend/app/(admin)/admin/questions/page.tsx`, `frontend/app/(admin)/admin/questions/new/page.tsx`, `frontend/app/(admin)/admin/questions/[code]/edit/page.tsx` |
| 2026-05-10 | F-102 | `<BranchingRulesEditor>` (Visual + JSON tabs). Single source of truth on the form's `next_question_rules` array. Visual: predicate Select + format-adaptive value input + goto-question Combobox scoped to linked regulation. JSON: prettified textarea with apply / reset buttons and inline error surface. | `frontend/components/forms/branching-rules-editor.tsx` |
| 2026-05-10 | F-103 | Regulation-anchored authoring wizard + LinkedQuestionsPanel. New route `/admin/regulations/[id]/authoring` walks Step 1 (M1 awareness root) → Step 2 (M2 yes/no follow-ups) → Step 3 (M3 vulnerability for M1=No / M2-wrong). Regulation edit page now carries `<LinkedQuestionsPanel>` Card grouping linked questions by module. | `frontend/components/forms/authoring-wizard.tsx`, `frontend/components/forms/linked-questions-panel.tsx`, `frontend/app/(admin)/admin/regulations/[id]/authoring/page.tsx`, `frontend/app/(admin)/admin/regulations/[id]/edit/page.tsx` |
| 2026-05-10 | F-104 | i18n: `nav.adminQuestions`, `common.previous`, `questions.*`, `regulations.linkedQuestionsPanel.*`, `regulations.authoring.*` namespaces added to en/si/ta (parity preserved at 411 keys per locale). | `frontend/lib/i18n/messages/en.json`, `frontend/lib/i18n/messages/si.json`, `frontend/lib/i18n/messages/ta.json` |
| 2026-05-10 | F-105 | Sidebar entry for the unified question bank. New `<ListChecks>` icon entry between `/admin/regulations` and the legacy `/admin/m2/questions`. The legacy admin M2 list stays live for one slice; OQ12 (the hard redirect) deferred. | `frontend/components/layout/sidebar.tsx` |
| 2026-05-09 | F-99 | Per-instrument SurveyForm dotted-id leak — the F-89 fix re-applied to the older `<SurveyForm>` code path. Awareness `q.id` values are dotted (`awareness.v1.q01`); RHF interprets `.` as object-path so flat `values[q.id]` returned undefined in three call sites (progress counter, submit serialiser, regulation-context lookup). Fixed in one file by sanitising at the SurveyForm boundary: `safeQuestions` (rewrites `id` + `dependsOn.questionId`), `idMap` for submit-time translation back to the canonical `question_id` format, and `safeContextById` for the regulation card lookup. No backend / DB changes. | `frontend/components/forms/survey-form.tsx` |
| 2026-05-09 | F-95 | Two runtime bug fixes. (A) Radix Select empty-value crash on `/admin/regulations/new` and (latent) `/admin/m2/questions` — replaced empty-string item values with `__none__` / `__all__` sentinels translated at the form-state boundary. (B) `/surveys/knowledge` 500 ("Something went wrong") — `m2_service.questions_for_sme` and the admin `/m2/questions` list query were not filtering by `module_number`, so awareness/M3 rows leaked into the M2 bank and failed `M2QuestionOut` validation (their `domain_code` / `knowledge_type` are NULL post-Session-6). Added `module_number == 2` to both queries. | `frontend/components/forms/regulation-form.tsx`, `frontend/app/(admin)/admin/m2/questions/page.tsx`, `backend/app/services/m2_service.py`, `backend/app/api/v1/m2.py` |
| 2026-05-09 | F-96 | Backend full CRUD for users — admin-only PATCH + activate + deactivate + reset-password + DELETE. New schemas `AdminUpdateUserIn` / `AdminResetPasswordIn`. New `auth_service` functions: `update_user` (with last-active-admin demote-guard), `set_active` (toggles `is_active`, refuses to deactivate the last admin), `reset_password` (hash + audit; password never logged), `delete_user` (soft-delete with distinct `user.delete` audit event). Five new endpoints under `/api/v1/users`. | `backend/app/schemas/auth.py`, `backend/app/services/auth_service.py`, `backend/app/api/v1/users.py` |
| 2026-05-09 | F-97 | Backend full CRUD for regulations + soft-archive migration. New migration `202605110001` adds `m1_regulations.is_active` (default TRUE; index). Service: `archive_regulation`, `restore_regulation`, `bulk_verify` (atomic + idempotent + single audit row), `duplicate_regulation` (`secrets.token_hex(3)` suffix; strips prior `_COPY_*`; copies M2M sectors; resets verification). `list_regulations` accepts `include_archived`. SME public read 404s archived rows. New `RegulationBulkVerifyIn` schema + 4 endpoints (`DELETE /{id}`, `POST /{id}/restore`, `POST /{id}/duplicate`, `POST /bulk-verify`). | `backend/alembic/versions/202605110001_regulation_is_active.py`, `backend/app/models/regulation.py`, `backend/app/schemas/regulation.py`, `backend/app/services/m1_regulation_service.py`, `backend/app/api/v1/m1_regulations.py` |
| 2026-05-09 | F-98 | Frontend full CRUD UI. New primitives `confirm-dialog.tsx` (generic destructive-confirm with inline async-error) + `row-actions.tsx` (3-dot DropdownMenu with danger slot). New form components `edit-user-dialog.tsx` + `reset-password-dialog.tsx`. `/admin/users` gains a trailing actions column (Edit / Reset password / Activate-Deactivate / Delete) wired via TanStack mutations + `<ConfirmDialog>` for destructive actions. `/admin/regulations` gains a leading checkbox column + bulk-verify action bar (visible when ≥ 1 selected) + "Show archived" filter + per-row archived badge + row actions menu (Edit / Duplicate / Archive↔Restore). New `UsersApi.{update, activate, deactivate, resetPassword, delete}` + `RegulationsApi.{bulkVerify, archive, restore, duplicate}` + zod schemas + `RegulationAdmin.is_active` type. New i18n `users.actions.*` / `users.edit.*` / `users.resetPassword.*` / `regulations.actions.*` namespaces (en/si/ta parity at 294 keys; `common.confirm` added). | `frontend/components/ui/confirm-dialog.tsx`, `frontend/components/admin/row-actions.tsx`, `frontend/components/forms/{edit-user-dialog,reset-password-dialog}.tsx`, `frontend/app/(admin)/admin/{users,regulations}/page.tsx`, `frontend/lib/api/{users,regulations}.ts`, `frontend/lib/validators/user.ts`, `frontend/lib/types/index.ts`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-09 | F-92 | **`/admin/users` redesign.** Filter rail with 4 searchable multi-select filters (Role / Language / Status / Sector), client-side text search, 4 stat pills with tonal icon badges, polished table with avatar+email cells, role badges, sector capitalisation, region pin icon, joined date, active dot, responsive column hiding, `<PageHeader>` with breadcrumb + Create-user CTA. Mirrors the `/admin/regulations` style. | `frontend/app/(admin)/admin/users/page.tsx`, `frontend/lib/api/users.ts` |
| 2026-05-09 | F-93 | **Searchable multi-select Combobox primitive** (`components/ui/combobox.tsx`). Single + multi modes; chips for selected values; type-to-search; click-to-toggle; Esc + click-outside close. No new deps. | `frontend/components/ui/combobox.tsx` |
| 2026-05-09 | F-94 | **Admin-create-user.** Backend: new `AdminCreateUserIn` schema, `auth_service.admin_create_user`, `POST /api/v1/users` (admin-only, 201). Audit log captures acting admin + assigned role. Frontend: new `<Dialog>` primitive (Radix wrapper), `<CreateUserDialog>` form with zod validation + TanStack mutation, new `lib/validators/user.ts`, `UsersApi.create`. New i18n `users.*` namespace (en/si/ta), parity at 252 keys. | `backend/app/schemas/auth.py`, `backend/app/services/auth_service.py`, `backend/app/api/v1/users.py`, `frontend/components/ui/dialog.tsx`, `frontend/components/forms/create-user-dialog.tsx`, `frontend/lib/validators/user.ts`, `frontend/lib/api/users.ts`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-09 | F-91 | **Sticky desktop sidebar + mobile drawer with hamburger trigger.** Desktop `<aside>` gains `md:sticky md:top-0 md:h-screen md:self-start` so it stays pinned during page scroll. Mobile (< md): new `<Sheet>` primitive (Radix Dialog wrapper) + new `<MobileSidebar>` opens a 288 px slide-in drawer via a hamburger button at the topbar's left edge. `<SidebarContent>` extracted from `<Sidebar>` for reuse; `onItemClick` callback auto-closes the drawer on nav. New i18n key `topbar.menu` (en/si/ta). Zero new top-level deps. | `frontend/components/ui/sheet.tsx` (new), `frontend/components/layout/mobile-sidebar.tsx` (new), `frontend/components/layout/{sidebar,topbar}.tsx`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-09 | F-89 | **Survey-wizard dotted-field-name bug fix.** Awareness `question_code` values are dotted (`awareness.v1.q01`); RHF read the dots as object-path notation, so values lived nested while the wizard read flat → answers sent as null, backend rejected, no DB row, no progress update. Fixed by sanitising the field id (`.` → `__DOT__`) at the form layer. Backend untouched. | `frontend/lib/surveys/safe-field-id.ts` (new), `frontend/lib/surveys/flow-question-to-ui.ts`, `frontend/components/forms/survey-wizard.tsx` |
| 2026-05-09 | F-90 | **Module 4 data-collection scaffolding.** Three new research docs covering methodology (volumes, language balance, annotation workforce + budget, dedup, time-normalised virality, PII, ethics), a 7-task Perplexity research prompt for Sri Lanka source intelligence, and the source-registry skeleton (FB, YouTube, X, fact-check, Reddit, SMS scams, EN/SI/TA keywords). Pre-fills FactCheck.lk + 4 canonical subreddits; remaining sections marked `TODO — populate via Perplexity prompt`. Resolves architecture-doc §7 open questions. | `docs/research/module_4_data_collection.md` (new), `docs/research/module_4_perplexity_prompt.md` (new), `docs/research/module_4_sri_lankan_sources.md` (new) |
| 2026-05-09 | F-82 | **Hotfix: parallel-pages route collision.** All eight `(admin)/*` pages moved into `(admin)/admin/*` so URLs match the sidebar (`/admin/m2/questions`, `/admin/regulations`, `/admin/users`, etc.) and the SME hub keeps `/regulations`. Same fix-pattern as F-63, this time across every admin page (resolves a latent broken-link bug across the entire admin nav). | `frontend/app/(admin)/admin/{m2,m3,users,surveys,regulations}/**/*` (8 page files moved) |
| 2026-05-09 | F-83 | App-shell redesign: floating-card layout (sidebar + main content as separate rounded cards on a `bg-muted/30` background); collapsible sidebar with brand mark + gradient logo + tooltips on icon-only mode; topbar gains breadcrumb display, sidebar-collapse trigger, refresh button, avatar dropdown menu. | `frontend/components/layout/{sidebar,topbar}.tsx`, `frontend/app/(app)/layout.tsx`, `frontend/app/(admin)/layout.tsx` |
| 2026-05-09 | F-84 | New shell primitives + glue components. Avatar (initials fallback), Breadcrumb, Tooltip primitives in `components/ui/`; `BreadcrumbContext` + `SidebarStateProvider` + `<PageHeader>` + `<AvatarMenu>` in `components/layout/`. Zero new top-level deps (`@radix-ui/react-tooltip` already in `package.json`). | `frontend/components/ui/{avatar,breadcrumb,tooltip}.tsx`, `frontend/components/layout/{breadcrumb-context,sidebar-state,page-header,avatar-menu}.tsx` |
| 2026-05-09 | F-85 | Dashboard upgrade: gradient welcome banner with three CTAs, four stat cards (Knowledge / Risk signals / Survey progress / Regulations or Last update) with circular tonal icon badges, two-column "Pending tasks" + "Recent activity" panels. Server component with parallel `Promise.all` fetches and graceful fallback when `sme_id` is absent. | `frontend/app/(app)/dashboard/page.tsx` |
| 2026-05-09 | F-86 | List-page polish. `/admin/regulations` gets a 208px vertical filter rail (Verification / Domain / Sector groups, click-to-toggle), search input, polished table (avatar+code cells, severity dots, responsive column hiding), URL-driven pagination. `/admin/m2/questions` gets `<PageHeader>` + breadcrumb + tinted header row + hover-state rows. | `frontend/app/(admin)/admin/regulations/page.tsx`, `frontend/app/(admin)/admin/m2/questions/page.tsx` |
| 2026-05-09 | F-87 | i18n: new `dashboard.*` (welcome + stats + tasks + activity) and `topbar.*` (refresh / avatarLabel / signOut / sidebarToggle) keys added across en/si/ta. Parity preserved at 211 deep keys per file. | `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-09 | F-88 | Form-page polish on the regulation form. Numbered section chips ("01" – "04") on each Card header via a `SectionStep` helper. Sticky save bar at the bottom (`sticky bottom-0 backdrop-blur`). New + edit pages adopt `<PageHeader>` with full breadcrumb and lift action buttons / verified badge into the actions slot. | `frontend/components/forms/regulation-form.tsx`, `frontend/app/(admin)/admin/regulations/{new/page,[id]/edit/page}.tsx` |
| 2026-05-09 | F-74 (Phase A) | Admin regulations CRUD: list page (filters, pagination, inline Verify), new + edit pages, RegulationForm client component (Cards: identity, dates, sectors, EN/SI/TA Tabs) with live "Preview as SME" via `useWatch` + `RegulationContextCard`. Edit mode disables `regulation_short_code` (immutable). Save/create routes via `RegulationsApi`; toasts; `router.refresh()`. | `frontend/app/(admin)/regulations/{page,new/page,[id]/edit/page}.tsx`, `frontend/components/forms/regulation-form.tsx`, `frontend/lib/validators/regulation.ts` |
| 2026-05-09 | F-78 | New `Tabs` primitive (`components/ui/tabs.tsx`) on `@radix-ui/react-tabs` (already in deps). Reused by the regulation form's EN/SI/TA panels. | `frontend/components/ui/tabs.tsx` |
| 2026-05-09 | F-79 | Per-module accent extended into admin shells: `module-m2` wraps `/admin/m2/questions` and `/admin/m2/scores`; `module-m3` wraps `/admin/m3/risk-signals`. Visual coherence with the unified `/surveys` wizard. | `frontend/app/(admin)/m2/{questions,scores}/page.tsx`, `frontend/app/(admin)/m3/risk-signals/page.tsx` |
| 2026-05-09 | F-80 | i18n: new `nav.adminRegulations` entry + full `regulations.*` namespace (sections, filters, columns, tabs, fields, documentTypes, errors, toasts). JSON-key parity preserved at 185 deep keys across en/si/ta. | `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-09 | F-81 | Constants extracted to single source of truth: `lib/constants/domains.ts` (DOMAINS) and `lib/constants/sectors.ts` (SECTORS + DOCUMENT_TYPES). Older `/admin/m2/questions` page kept its inline copy to avoid risking regressions in working code. | `frontend/lib/constants/{domains,sectors}.ts` |
| 2026-05-09 | F-74 (Phase A) | Sidebar nav gains an "Regulations" admin entry with `BookOpen` icon, slotted between Survey responses and the M2 question bank. | `frontend/components/layout/sidebar.tsx` |
| 2026-05-09 | F-74 (Phase A) | Playwright smoke spec for the new admin pages: login as admin → see seeded codes → create + edit a regulation → list reflects the edit. | `frontend/tests/e2e/admin_regulations.spec.ts` |
| 2026-05-09 | F-73 | Follow-up migration relaxes `NOT NULL` on `survey_questions.domain_code` and `knowledge_type` so M0 (awareness) and M3 (vulnerability) rows can insert. The original `202605100001` only relaxed `correct_answer_json`; first seed run crashed on `M3_HIST_001` with `NotNullViolationError: null value in column "domain_code"`. | `backend/alembic/versions/202605100002_relax_survey_question_columns.py` |
| 2026-05-09 | F-72 | New i18n keys for the unified wizard: `surveys.flowTitle / flowSubtitle / questionOfTotal / crossModuleHint`. Added to en/si/ta with full parity. | `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-09 | F-71 | Unified `/surveys` page (server) + `SurveyWizard` client component + `flow-question-to-ui.ts` adapter (FlowQuestion → UI Question; covers single/multi/likert/date/numeric/short_text/ordered_steps). Per-question `useForm` reset; sticky progress; inline regulation-context card; cross-module hint; module-accent class swap. New typed clients `SurveyFlowApi` + `RegulationsApi`; `client.ts` extended with `patch` + `delete`. New types in `lib/types/index.ts` (RegulationPublic, RegulationAdmin, FlowQuestion, FlowState, FlowAnswer, ModuleNumber, …). | `frontend/app/(app)/surveys/page.tsx`, `frontend/components/forms/survey-wizard.tsx`, `frontend/lib/surveys/flow-question-to-ui.ts`, `frontend/lib/api/{survey-flow,regulations,client}.ts`, `frontend/lib/types/index.ts` |
| 2026-05-09 | F-70 | M3 projection on `survey_service.submit`: `_project_m3_snapshots` writes `module_number = 3` answers into `m3_compliance_history` / `m3_behavioural_signals` via `M3_HISTORY_FIELDS` + `M3_BEHAVIOURAL_FIELDS` field maps. `survey_responses` remains canonical. Scoring fan-out now keys on `module_number == 2` so unified-flow answers still score. | `backend/app/services/survey_service.py` |
| 2026-05-09 | F-69 | New backend routers + schemas: `/api/v1/survey-flow/{start,answer}` (SME) and `/api/v1/m1/regulations/*` (admin CRUD + `/{id}/public` SME-safe read). Mounted in `api/v1/router.py`. New schemas `RegulationPublicOut`, `RegulationAdminOut`, `RegulationsPage`, `FlowQuestionOut`, `FlowAnswerIn`, `FlowProgressOut`, `FlowStateOut`. | `backend/app/api/v1/{survey_flow,m1_regulations,router}.py`, `backend/app/schemas/{survey_flow,regulation}.py` |
| 2026-05-09 | F-68 | New `m1_regulation_service` with full CRUD + verify; every mutation writes a row to `audit_log` (`regulation_create` / `regulation_update` / `regulation_verify`). | `backend/app/services/m1_regulation_service.py` |
| 2026-05-09 | F-67 | Flow engine in new `survey_question_service`: `start_flow`, `next_question`, `_resolve_rule` (predicates `answer_eq` / `answer_in` / `answer_lt` / `answer_gt`), `_skip_answered`, `_question_id`. Resume by replaying `survey_responses` rows — no separate progress table. | `backend/app/services/survey_question_service.py` |
| 2026-05-09 | F-66 | Idempotent seed scripts wired into `seed_dev.py` in dependency order: `seed_regulations` (5 canonical regs) → `seed_vulnerability_questions` (M3 with rule-encoded `dependsOn`, plus new `M3_VAT_NOAWARE_PENALTY`) → `seed_m23_questions` (updated for new fields, with `CROSS_MODULE_LINKAGE` constant routing `VAT_FACT_002 → M3_HIST_004` and `VAT_FACT_001 → M3_VAT_NOAWARE_PENALTY`; S7500 lint fix `dict(result)`) → `seed_awareness_questions` (12 M0; Q4/Q5 carry `next_question_rules`). | `backend/app/scripts/{seed_regulations,seed_awareness_questions,seed_vulnerability_questions,seed_m23_questions,seed_dev}.py` |
| 2026-05-09 | F-65 | Schema generalisation: rename `m2_questions` → `survey_questions` preserving 40 rows; new columns `module_number`, `linked_regulation_id` (FK SET NULL → `m1_regulations`), `next_question_rules` (JSONB), `is_required`, `instrument_section`, `is_branching_root`. Relaxed `correct_answer_json` to NULL. New tables `m1_regulations` (admin-managed regulation bank, EN/SI/TA fields, dates, severity, sectors M2M) and `m1_regulation_sectors`. New `survey_responses.linked_regulation_id` with FK SET NULL. ORM updates: new `SurveyQuestion` + back-compat `M2Question` alias; new `M1Regulation` + `M1RegulationSector`; `survey.py` extended; `__init__.py` re-exports. | `backend/alembic/versions/202605100001_unified_survey_questions.py`, `backend/app/models/{regulation,survey_question,survey,__init__,m2_question}.py` |
| 2026-05-09 | F-64 | `useAutosave` no longer calls `useFormContext()` (which returned null because the hook ran before the enclosing `<FormProvider>` mounted, crashing every survey page). Hook now takes `methods: UseFormReturn<…>` explicitly; `SurveyForm` passes `methods` through. New troubleshooting entry #23 documents the symptom + the "pass methods, don't context-fetch" pattern for future survey-side hooks. | `frontend/components/forms/survey-autosave.tsx`, `frontend/components/forms/survey-form.tsx`, `docs/SETUP/09_Troubleshooting.md` |
| 2026-05-09 | F-63 | App-Router parallel-pages collision fix: moved `app/(admin)/surveys/awareness/page.tsx` → `app/(admin)/surveys/awareness/responses/page.tsx` so the SME survey at `/surveys/awareness` and the admin response list at `/admin/surveys/awareness/responses` no longer collide on the same URL. Updated sidebar nav, Playwright spec, and four SETUP docs. New troubleshooting entry #22. | `frontend/app/(admin)/surveys/awareness/responses/page.tsx`, `frontend/components/layout/sidebar.tsx`, `frontend/tests/e2e/auth_survey_admin.spec.ts`, `docs/SETUP/{00_INDEX,08_Testing,10_Next_Steps,09_Troubleshooting}.md` |
| 2026-05-09 | F-62 | Auth + DB diagnostic toolkit. New `app/scripts/doctor.py` runs three checks in one command (bcrypt version, password round-trip, users-table sanity). Makefile gains `doctor`, `db-shell`, `db-users`, `reseed-users` targets. New troubleshooting entry #13b documents the `Invalid credentials` symptom after a CORS-only fix as the venv-not-resynced case. | `Makefile`, `backend/app/scripts/doctor.py`, `docs/SETUP/09_Troubleshooting.md` |
| 2026-05-09 | F-61 | CORS preflight 400 fix: strip trailing slash from `CORS_ORIGINS` entries before passing to `CORSMiddleware` (Pydantic v2's `AnyHttpUrl.__str__` adds one; browser `Origin` headers never do). Backend now logs `cors_origins_configured` with the resolved allowlist. Frontend login + register pages now append the underlying `Error.message` to the on-screen error so CORS / network problems are debuggable from the UI. New troubleshooting entry #26. | `backend/app/main.py`, `frontend/app/(auth)/{login,register}/page.tsx`, `docs/SETUP/09_Troubleshooting.md` |
| 2026-05-09 | F-60 | Troubleshooting docs updated: entry 9 rewritten with the real bcrypt root cause + actual fix; new 9b (long unicode passwords), 25b (missing `enigmatrix` Postgres role / Homebrew collision / stale Docker volume), 25c (`CORS_ORIGINS` JSON-array requirement). | `docs/SETUP/09_Troubleshooting.md` |
| 2026-05-09 | F-59 | `.env.example` ships `CORS_ORIGINS=["http://localhost:3000"]` (JSON-array form) with an inline comment explaining the Pydantic Settings v2.1+ JSON-decode rule and showing a multi-origin example. | `.env.example` |
| 2026-05-09 | F-58 | `app/core/security.py.hash_password()` + `verify_password()` now route every secret through `_truncate()` — encode → slice 72 UTF-8 bytes → decode with `errors="ignore"` — so unicode passwords > 72 bytes no longer crash the auth path. Trade-off documented inline (cryptographically equivalent passwords past 72 bytes is an old well-known bcrypt limit). | `backend/app/core/security.py` |
| 2026-05-09 | F-57 | Pinned `bcrypt>=4.0,<4.1` in `pyproject.toml` to stop the recurring passlib `__about__` AttributeError. The pin sits next to `passlib[bcrypt]` with an inline comment so a future "tidy up" doesn't drop it. | `backend/pyproject.toml` |
| 2026-05-09 | F-43 | New SETUP file `11_Survey_System.md` — cross-module-linkage walkthrough; updated `00_INDEX.md` to link it. | `docs/SETUP/{00_INDEX,11_Survey_System}.md` |
| 2026-05-09 | F-56 | Tests: M2 scoring unit (12 cases), M2 + M3 integration (full flow + auth boundaries), Playwright extension. | `backend/app/tests/unit/test_m2_scoring.py`, `backend/app/tests/integration/{test_m2_flow,test_m3_flow}.py`, `frontend/tests/e2e/surveys_knowledge_vulnerability.spec.ts` |
| 2026-05-09 | F-51, F-52, F-53, F-54, F-55 | Frontend: shared `types.ts` for surveys, `dependsOn`-aware `QuestionRenderer`, rebuilt `SurveyForm` with progress + autosave + error-summary + regulation-context + customSubmit, M2/M3 question banks, six SME pages (hub, knowledge + thank-you, vulnerability + thank-you, /risk), three admin pages (M2 questions, M2 scores, M3 risk-signals), per-module accent CSS, sidebar nav, en/si/ta i18n parity. | `frontend/lib/surveys/{types,m3-vulnerability}.ts`, `frontend/lib/api/{m2,m3}.ts`, `frontend/lib/types/index.ts`, `frontend/components/forms/{question-renderer,survey-form,survey-progress,survey-autosave,survey-error-summary,regulation-context-card}.tsx`, `frontend/app/(app)/{regulations/page,surveys/{knowledge,vulnerability}/{page,thank-you/page},risk/page}.tsx`, `frontend/app/(admin)/{m2/{questions,scores},m3/risk-signals}/page.tsx`, `frontend/app/globals.css`, `frontend/components/layout/sidebar.tsx`, `frontend/lib/i18n/messages/{en,si,ta}.json` |
| 2026-05-09 | F-44, F-45, F-46, F-47, F-48, F-49, F-50 | Backend: 6 new tables + 7 columns on `survey_responses`; M2 scoring engine with five formats; cross-module linkage rules; M2/M3 services; routers under `/api/v1/m2/*` and `/api/v1/m3/*`; `SupportedInstrument` extended; `survey_service.submit()` auto-scores + recomputes cache; `seed_m23_questions.py` executes the architecture doc's PART A. | `backend/alembic/versions/202605090001_module23_schema.py`, `backend/app/models/{regulatory_domain,sector,m2_question,m2_knowledge_score,m3_compliance_history,m3_behavioural_signals,survey,__init__}.py`, `backend/app/schemas/{m2,m3,survey}.py`, `backend/app/services/{m2_scoring,m2_linkage_rules,m2_service,m3_service,survey_service}.py`, `backend/app/api/v1/{m2,m3,router}.py`, `backend/app/scripts/{seed_m23_questions,seed_dev}.py` |
| 2026-05-08 | F-43 | Setup & developer documentation: 11-file `docs/SETUP/` track (00 INDEX through 10 Next Steps); root `README.md` slimmed to one-screen front door; back-references added in `BUILD_00` and `research/00`. | `docs/SETUP/{00_INDEX,01_Prerequisites,02_Quickstart,03_Architecture,04_Backend_Development,05_Frontend_Development,06_Database_and_Migrations,07_Auth_and_Roles,08_Testing,09_Troubleshooting,10_Next_Steps}.md`, `README.md`, `docs/BUILD_PLAN/BUILD_00_INDEX.md`, `docs/research/00_INDEX.md` |
| 2026-05-08 | F-40, F-41, F-42 | Smoke tests: backend integration (testcontainers Postgres + httpx ASGI client) covering register → login → survey submit → admin list + 501-stubs. Backend unit test for bcrypt + JWT round-trip. Playwright E2E spec exercising the full UI flow. | `backend/app/tests/conftest.py`, `backend/app/tests/integration/test_survey_flow.py`, `backend/app/tests/unit/test_security.py`, `frontend/tests/e2e/auth_survey_admin.spec.ts`, `frontend/playwright.config.ts`, `frontend/vitest.config.ts` |
| 2026-05-08 | F-37, F-38, F-39 | Admin pages (response list + user list) and four "Coming soon" stub pages with shared `<ComingSoon>` component. | `frontend/components/coming-soon.tsx`, `frontend/app/(admin)/surveys/awareness/page.tsx`, `frontend/app/(admin)/users/page.tsx`, `frontend/app/(app)/{regulations,qa,risk,verify}/page.tsx` |
| 2026-05-08 | F-33, F-34, F-35, F-36 | Awareness survey end-to-end: 12-question bank with 6 question kinds, reusable `SurveyForm` + `QuestionRenderer`, survey page, thank-you page. | `frontend/lib/surveys/awareness.ts`, `frontend/components/forms/{survey-form,question-renderer}.tsx`, `frontend/app/(app)/surveys/awareness/{page,thank-you/page}.tsx` |
| 2026-05-08 | F-28, F-29, F-30, F-31, F-32 | Auth flow: login + register pages, cookie route handlers, layouts (`(app)` auth-guarded, `(admin)` admin-guarded), middleware fast-path, topbar with logout/theme/locale. | `frontend/app/(auth)/{layout,login/page,register/page}.tsx`, `frontend/app/api/auth/{establish,token,logout}/route.ts`, `frontend/app/(app)/{layout,dashboard/page}.tsx`, `frontend/app/(admin)/layout.tsx`, `frontend/middleware.ts`, `frontend/components/layout/{topbar,sidebar,theme-toggle,locale-switcher}.tsx` |
| 2026-05-08 | F-19, F-20, F-21, F-22, F-23, F-24, F-25, F-26, F-27 | Frontend foundation: Next.js 14 + Tailwind + shadcn-pat